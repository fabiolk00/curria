---
phase: 26-agent-runtime-simplification-and-budget-optimization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/agent-loop.ts
  - src/lib/agent
autonomous: true
requirements:
  - PERF-03
user_setup: []
must_haves:
  truths:
    - "The core runtime should have clearer latency-oriented responsibility boundaries."
    - "Simple deterministic chat intents should not pay a full model round-trip when a safe fast path already exists."
    - "Behavior must remain safe for billing, generation, and canonical state."
  artifacts:
    - path: ".planning/phases/CURRIA-26-agent-runtime-simplification-and-budget-optimization/26-CONTEXT.md"
      provides: "Phase 26 runtime and latency guardrails"
      contains: "Main focus is agent response time improvement first"
    - path: ".planning/phases/CURRIA-25-chat-and-ats-enhancement-request-path-reduction/25-VERIFICATION.md"
      provides: "Proof that request-path reductions are already in place"
      contains: "request-path cost is lower"
---

<objective>
Break down the agent runtime into clearer latency-oriented slices and add deterministic fast paths for simple chat intents that do not need a model round-trip.
</objective>

<context>
@.planning/phases/CURRIA-26-agent-runtime-simplification-and-budget-optimization/26-CONTEXT.md
@.planning/phases/CURRIA-25-chat-and-ats-enhancement-request-path-reduction/25-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Separate runtime intent detection from the main loop</name>
  <files>src/lib/agent/agent-loop.ts, src/lib/agent</files>
  <read_first>src/lib/agent/agent-loop.ts</read_first>
  <action>Move intent-detection and shortcut classification helpers into clearer runtime slices so the main loop is easier to optimize and reason about.</action>
  <verify>rg "isGenerationApproval|isGenerationRequest|isDialogRewriteRequest|looksLikeJobDescription" src/lib/agent</verify>
  <done>Intent detection no longer lives as an undifferentiated block inside the core loop file.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic fast handling for simple continuation turns</name>
  <files>src/lib/agent/agent-loop.ts, src/lib/agent</files>
  <read_first>src/lib/agent/agent-loop.ts</read_first>
  <action>Handle short continuation approvals and similar safe dialog turns through deterministic runtime behavior instead of waiting for the model and fallback path.</action>
  <verify>npm test -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.sse.test.ts</verify>
  <done>Simple continuation turns respond through a fast path and avoid unnecessary model retries.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] intent detection is isolated into clearer runtime slices
- [ ] simple continuation chat turns can complete without unnecessary model work
- [ ] regression coverage protects rewrite and generation-sensitive behavior
</verification>

<output>
After completion, the runtime has clearer responsibility boundaries and shorter deterministic chat paths for simple intents.
</output>

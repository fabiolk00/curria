---
phase: 26-agent-runtime-simplification-and-budget-optimization
plan: "02"
type: execute
wave: 1
depends_on:
  - 26-01
files_modified:
  - src/lib/agent/config.ts
  - src/lib/agent/context-builder.ts
  - src/lib/agent/agent-loop.ts
autonomous: true
requirements:
  - PERF-04
user_setup: []
must_haves:
  truths:
    - "Prompt, history, and tool-loop budgets should be tighter by phase, not only globally."
    - "Budget reductions should favor chat and ATS-visible latency without harming correctness."
    - "The runtime should log and enforce the phase-specific budgets it actually uses."
  artifacts:
    - path: ".planning/phases/CURRIA-26-agent-runtime-simplification-and-budget-optimization/26-CONTEXT.md"
      provides: "Budget-optimization guardrails"
      contains: "prompt and history cost per turn"
---

<objective>
Reduce prompt, history, and tool-loop cost by phase so shorter chat turns and ATS-related turns carry less runtime overhead.
</objective>

<context>
@.planning/phases/CURRIA-26-agent-runtime-simplification-and-budget-optimization/26-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Introduce phase-specific runtime budgets</name>
  <files>src/lib/agent/config.ts, src/lib/agent/agent-loop.ts</files>
  <read_first>src/lib/agent/config.ts, src/lib/agent/agent-loop.ts</read_first>
  <action>Add per-phase helpers for history window, tool-loop limits, and output token budgets so the runtime stops using one broad budget for every turn.</action>
  <verify>npm test -- src/lib/agent/config.test.ts src/lib/agent/streaming-loop.test.ts</verify>
  <done>The runtime resolves smaller budgets by phase and uses them in execution and observability.</done>
</task>

<task type="auto">
  <name>Task 2: Keep prompt shaping aligned with the tighter budgets</name>
  <files>src/lib/agent/context-builder.ts, src/lib/agent/config.ts</files>
  <read_first>src/lib/agent/context-builder.ts</read_first>
  <action>Ensure prompt construction and message trimming stay aligned with the new phase-specific budgets so chat turns do not carry unnecessary history/context weight.</action>
  <verify>npm test -- src/lib/agent/context-builder.test.ts src/lib/agent/__tests__/streaming-prompt-regression.test.ts</verify>
  <done>Prompt and history shaping prove they still preserve the needed context under the tighter runtime budgets.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] phase-specific budgets exist for history, tool-loop limit, and output budget
- [ ] the main loop uses those budgets instead of only the global defaults
- [ ] prompt regression coverage still passes under the tighter budgets
</verification>

<output>
After completion, chat and ATS-visible turns pay lower prompt/history/tool cost with explicit phase-level control.
</output>

---
phase: 25-chat-and-ats-enhancement-request-path-reduction
plan: "02"
type: execute
wave: 1
depends_on:
  - 25-01
files_modified:
  - src/app/api/agent/route.ts
  - src/lib/agent/agent-loop.ts
  - src/lib/agent/tools/pipeline.test.ts
  - src/lib/agent/tools
autonomous: true
requirements:
  - PERF-03
user_setup: []
must_haves:
  truths:
    - "ATS enhancement latency should improve by removing or deferring non-essential inline work."
    - "Deferred ATS work must not corrupt canonical resume state or create billing regressions."
    - "This phase should prefer explicit pending or follow-up behavior over hidden synchronous blocking."
  artifacts:
    - path: ".planning/phases/CURRIA-25-chat-and-ats-enhancement-request-path-reduction/25-CONTEXT.md"
      provides: "ATS-latency planning guardrails"
      contains: "Preserve `cvState` as canonical truth"
    - path: ".planning/phases/CURRIA-24-agent-response-baseline-and-chat-ats-latency-instrumentation/24-01-SUMMARY.md"
      provides: "Evidence that ATS and chat still share a heavy synchronous path"
      contains: "The current bottlenecks appear concentrated"
---

<objective>
Decouple or defer ATS enhancement work that does not need to block the user's visible response, while preserving `cvState`, billing safety, and existing route-level guarantees.
</objective>

<context>
@.planning/phases/CURRIA-25-chat-and-ats-enhancement-request-path-reduction/25-CONTEXT.md
@.planning/phases/CURRIA-25-chat-and-ats-enhancement-request-path-reduction/25-01-PLAN.md
@.planning/phases/CURRIA-24-agent-response-baseline-and-chat-ats-latency-instrumentation/24-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Separate mandatory ATS work from deferable ATS work</name>
  <files>src/app/api/agent/route.ts, src/lib/agent/agent-loop.ts, src/lib/agent/tools</files>
  <read_first>src/app/api/agent/route.ts, src/lib/agent/agent-loop.ts</read_first>
  <action>Identify every ATS-related inline step on the request path and classify it as mandatory-before-visible-response, safe-to-defer-until-after-first-response, or safe-to-skip-when-state-is-already-sufficient.</action>
  <verify>rg "ATS|ats|enhance|score|target" src/app/api/agent/route.ts src/lib/agent/agent-loop.ts src/lib/agent/tools</verify>
  <done>The implementation has an explicit execution boundary for ATS work instead of implicit blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce inline ATS blocking with explicit state-safe behavior</name>
  <files>src/app/api/agent/route.ts, src/lib/agent/agent-loop.ts, src/lib/agent/tools</files>
  <read_first>src/lib/agent/agent-loop.ts</read_first>
  <action>Adjust ATS orchestration so non-essential enhancement work no longer blocks the user's visible response when the response can begin safely, using explicit pending/follow-up handling where needed instead of hidden synchronous waits.</action>
  <verify>npm test -- src/lib/agent/tools/pipeline.test.ts</verify>
  <done>ATS enhancement no longer performs avoidable inline work before user-facing value is shown.</done>
</task>

<task type="auto">
  <name>Task 3: Prove state and billing safety around deferred ATS behavior</name>
  <files>src/lib/agent/tools/pipeline.test.ts, src/app/api/agent/route.ts</files>
  <read_first>src/lib/agent/tools/pipeline.test.ts</read_first>
  <action>Add focused regression proof that deferred ATS handling does not mutate canonical resume state incorrectly, does not bypass existing route guarantees, and does not create unintended billing-sensitive side effects.</action>
  <verify>npm run typecheck && npx eslint src/app/api/agent/route.ts src/lib/agent/agent-loop.ts src/lib/agent/tools/pipeline.test.ts</verify>
  <done>The reduced ATS request path is proven safe for canonical state and billing boundaries.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Non-essential ATS work is classified and reduced on the synchronous path
- [ ] The user gets visible response or progress before deferred ATS work when correctness allows it
- [ ] Tests prove `cvState`, route guarantees, and billing-sensitive behavior remain safe
</verification>

<output>
After completion, ATS enhancement no longer performs avoidable blocking work before user-visible value, and the repo is ready for consolidated latency proof in `25-03`.
</output>

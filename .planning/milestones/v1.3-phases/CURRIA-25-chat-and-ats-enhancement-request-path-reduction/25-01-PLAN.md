---
phase: 25-chat-and-ats-enhancement-request-path-reduction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/agent/route.ts
  - src/app/api/agent/route.sse.test.ts
  - src/lib/agent/agent-loop.ts
  - src/lib/agent/streaming-loop.test.ts
autonomous: true
requirements:
  - PERF-02
user_setup: []
must_haves:
  truths:
    - "Main focus is agent response time improvement first."
    - "The first visible chat response should arrive earlier without weakening route safety."
    - "Blocking work before first SSE or first useful assistant output should only remain when it is required for correctness."
  artifacts:
    - path: ".planning/phases/CURRIA-24-agent-response-baseline-and-chat-ats-latency-instrumentation/24-VERIFICATION.md"
      provides: "Baseline proof for first-response timing and request-stage latency"
      contains: "first SSE plus first assistant-text timing"
    - path: ".planning/phases/CURRIA-25-chat-and-ats-enhancement-request-path-reduction/25-CONTEXT.md"
      provides: "Brownfield-safe performance guardrails for Phase 25"
      contains: "Main focus is agent response time improvement first"
---

<objective>
Reduce blocking work before the first visible chat response so the user sees stream progress or useful assistant output earlier, while preserving auth, ownership, validation, and canonical state guarantees.
</objective>

<context>
@.planning/phases/CURRIA-25-chat-and-ats-enhancement-request-path-reduction/25-CONTEXT.md
@.planning/phases/CURRIA-24-agent-response-baseline-and-chat-ats-latency-instrumentation/24-01-SUMMARY.md
@.planning/phases/CURRIA-24-agent-response-baseline-and-chat-ats-latency-instrumentation/24-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Classify what currently blocks first chat output</name>
  <files>src/app/api/agent/route.ts, src/lib/agent/agent-loop.ts</files>
  <read_first>src/app/api/agent/route.ts, src/lib/observability/request-timing.ts</read_first>
  <action>Use the Phase 24 timing markers to identify which synchronous steps still happen before the first SSE chunk or first useful assistant text, then classify each step as required-now, safe-to-defer, or removable duplication.</action>
  <verify>rg "markStage|markFirst" src/app/api/agent/route.ts</verify>
  <done>A small, explicit inventory exists for what blocks visible chat output and what can move later safely.</done>
</task>

<task type="auto">
  <name>Task 2: Emit visible chat progress sooner without breaking request guarantees</name>
  <files>src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/streaming-loop.test.ts</files>
  <read_first>src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts</read_first>
  <action>Move or defer non-essential pre-stream work so the route can emit stream progress or the first useful assistant output earlier, while keeping auth checks, ownership checks, validation, and required session setup in place.</action>
  <verify>npm test -- src/app/api/agent/route.sse.test.ts src/lib/agent/streaming-loop.test.ts</verify>
  <done>The chat path delivers visible output earlier and regression coverage proves the stream contract still holds.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the reduction brownfield-safe with focused proof</name>
  <files>src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/streaming-loop.test.ts</files>
  <read_first>src/app/api/agent/route.sse.test.ts</read_first>
  <action>Add or update focused tests so the reduced blocking path proves no regression in first-response logging, stream structure, or canonical request invariants.</action>
  <verify>npm run typecheck && npx eslint src/app/api/agent/route.ts src/app/api/agent/route.sse.test.ts src/lib/agent/streaming-loop.test.ts</verify>
  <done>The repo has targeted regression proof for the faster first-response chat path.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] The route still enforces auth, input validation, and ownership before user-visible output
- [ ] First SSE or first useful assistant output happens earlier in the chat path
- [ ] Focused route/stream tests prove the stream contract and timing instrumentation still work
</verification>

<output>
After completion, chat-first latency is reduced without broad architectural churn, and the repo is ready to decouple ATS-specific inline work in `25-02`.
</output>

---
phase: 08-ats-enhancement-rewrite-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/lib/agent/tools/ats-analysis.ts
  - src/lib/agent/tools/rewrite-resume-full.ts
  - src/lib/agent/tools/validate-rewrite.ts
  - src/lib/agent/tools/index.ts
  - src/app/api/agent/route.ts
  - src/app/api/profile/ats-enhancement/route.ts
  - src/app/api/profile/ats-enhancement/route.test.ts
  - src/lib/agent/tools/pipeline.test.ts
autonomous: true
requirements:
  - ATS-02
user_setup: []
must_haves:
  truths:
    - "ATS enhancement runs as an imperative backend pipeline when a session has resume context and no target vacancy."
    - "The optimized resume is a fully structured `CVState` rewritten section-by-section, not an assistant-text suggestion."
    - "Post-rewrite validation blocks hallucinated resume facts before optimized state is persisted."
  artifacts:
    - path: "src/lib/agent/tools/ats-analysis.ts"
      provides: "General ATS analysis independent of vacancy-gap analysis"
      contains: "analyzeAtsGeneral"
    - path: "src/lib/agent/tools/rewrite-resume-full.ts"
      provides: "Full ATS-enhancement rewrite orchestration that returns structured CV state and change summary"
      contains: "rewriteResumeFull"
    - path: "src/lib/agent/tools/validate-rewrite.ts"
      provides: "Fact-preserving validation gate between original and optimized resume snapshots"
      contains: "validateRewrite"
  key_links:
    - from: "src/app/api/agent/route.ts"
      to: "src/lib/agent/tools/rewrite-resume-full.ts"
      via: "pre-loop ATS-enhancement orchestration must invoke the shared pipeline instead of leaving rewrite optional to the agent loop"
      pattern: "ats_enhancement"
    - from: "src/app/api/profile/ats-enhancement/route.ts"
      to: "src/lib/agent/tools/ats-analysis.ts"
      via: "the standalone ATS route should reuse the shared ATS-enhancement analysis/rewrite pipeline to avoid divergent behavior"
      pattern: "ATS_ENHANCEMENT"
---

<objective>
Implement the shared ATS-enhancement pipeline that analyzes, rewrites, validates, and persists an optimized resume snapshot.

Purpose: The repo already has ATS-oriented primitives and a standalone ATS route, but it lacks a first-class service that guarantees full-resume transformation when no target vacancy is provided.
Output: Reusable ATS-analysis, full-rewrite, and validation modules wired into both `/api/agent` and the dedicated profile ATS route.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/CURRIA-08-ats-enhancement-rewrite-pipeline/08-CONTEXT.md
@AGENTS.md
@src/app/api/agent/route.ts
@src/app/api/profile/ats-enhancement/route.ts
@src/app/api/profile/ats-enhancement/route.test.ts
@src/lib/agent/tools/index.ts
@src/lib/agent/tools/rewrite-section.ts
@src/lib/agent/tools/pipeline.test.ts
@src/lib/profile/ats-enhancement.ts
@src/types/agent.ts
</context>

<threat_model>
<trust_boundaries>
- Canonical base `cvState` -> ATS analysis and rewrite prompts
- Section-level rewrite outputs -> validated optimized `CVState`
- Shared ATS pipeline -> `/api/agent` pre-loop execution and standalone profile ATS route
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-08-03</threat_id>
  <category>Tampering</category>
  <component>Optimized resume snapshot</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Rewrite section-by-section using structured outputs and block persistence when the optimized snapshot introduces unsupported facts or malformed structure.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-08-04</threat_id>
  <category>Information Disclosure</category>
  <component>Prompted ATS analysis</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep the pipeline grounded in canonical resume data already held by the session and avoid introducing external personal data or unsupported vacancy context.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement general ATS analysis with explicit scoring and issue taxonomy</name>
  <files>src/lib/agent/tools/ats-analysis.ts, src/lib/agent/tools/pipeline.test.ts</files>
  <read_first>src/lib/profile/ats-enhancement.ts, src/lib/agent/tools/rewrite-section.ts, src/lib/agent/tools/pipeline.test.ts, src/types/agent.ts</read_first>
  <action>Create `src/lib/agent/tools/ats-analysis.ts` with `analyzeAtsGeneral(cvState, userId, sessionId)` returning the agreed `AtsAnalysisResult` shape: overall and dimension scores, typed issues, and recommendations. Reuse existing ATS-readiness helpers where appropriate, but keep this service independent from vacancy gap analysis and make it produce deterministic, structured output suitable for persistence and test assertions.</action>
  <verify>npm test -- src/lib/agent/tools/pipeline.test.ts</verify>
  <acceptance_criteria>
    - ATS-general analysis runs without a target vacancy and returns the persisted result shape expected by `AgentState`
    - issues can reference `summary`, `experience`, `skills`, `education`, or `certifications`
    - tests prove the analysis evaluates structure/readability concerns separately from target-vacancy gap logic
  </acceptance_criteria>
  <done>The codebase has a first-class ATS-general analysis primitive instead of overloading `score_ats` or vacancy-gap analysis.</done>
</task>

<task type="auto">
  <name>Task 2: Build a full structured rewrite service that rewrites all supported sections</name>
  <files>src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/tools/index.ts, src/lib/agent/tools/pipeline.test.ts</files>
  <read_first>src/lib/agent/tools/rewrite-section.ts, src/lib/agent/tools/index.ts, src/app/api/profile/ats-enhancement/route.ts, src/types/agent.ts</read_first>
  <action>Create `src/lib/agent/tools/rewrite-resume-full.ts` with a shared `rewriteResumeFull(...)` orchestration that performs ATS-enhancement rewriting section-by-section for `summary`, `experience`, `skills`, `education`, and `certifications`. Use rewrite planning inputs derived from ATS analysis, preserve factual truth, return a structured `optimizedCvState`, and emit a summary with changed sections plus operator-facing notes. Refactor any duplicated ATS-enhancement prompt logic from the standalone profile route into the shared service instead of maintaining parallel rewrite recipes.</action>
  <verify>npm test -- src/lib/agent/tools/pipeline.test.ts src/app/api/profile/ats-enhancement/route.test.ts</verify>
  <acceptance_criteria>
    - the full ATS-enhancement rewrite service rewrites every supported section into a structured `CVState`
    - malformed section rewrite payloads degrade safely according to the chosen policy without corrupting unrelated sections
    - the dedicated profile ATS route reuses the shared full-rewrite orchestration rather than duplicating section-specific rewrite logic
  </acceptance_criteria>
  <done>ATS enhancement becomes a reusable, fully structured rewrite pipeline instead of a route-local sequence of partial rewrites.</done>
</task>

<task type="auto">
  <name>Task 3: Validate optimized snapshots and run the ATS pipeline imperatively from backend entry points</name>
  <files>src/lib/agent/tools/validate-rewrite.ts, src/app/api/agent/route.ts, src/app/api/profile/ats-enhancement/route.ts, src/app/api/profile/ats-enhancement/route.test.ts</files>
  <read_first>src/app/api/agent/route.ts, src/app/api/profile/ats-enhancement/route.ts, src/lib/db/sessions.ts, src/types/agent.ts</read_first>
  <action>Create `src/lib/agent/tools/validate-rewrite.ts` to compare original and optimized resume snapshots and reject invented companies, dates, certifications, unsupported skills/tools, or unsupported numeric claims. Then wire the shared ATS pipeline into `/api/agent` so the resume-present/no-vacancy path imperatively runs ATS analysis -> full rewrite -> validation before the normal loop can treat the result as optional. Persist `atsAnalysis`, `rewriteStatus`, `optimizedCvState`, `optimizedAt`, `optimizationSummary`, and failure state transitions on the session, and update the standalone profile ATS route to follow the same validation gate.</action>
  <verify>npm test -- src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/tools/pipeline.test.ts</verify>
  <acceptance_criteria>
    - `/api/agent` no longer depends on optional agent-loop tool choice to produce the ATS-enhanced resume snapshot for resume-only sessions
    - invalid optimized snapshots do not persist as `optimizedCvState` and leave a clear failed rewrite status plus validation issues
    - successful ATS-enhancement runs persist analysis, optimized snapshot, timestamps, and change summary in session state
  </acceptance_criteria>
  <done>The ATS-enhancement path is imperative, shared, and guarded by a factual-validation layer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/agent/tools/pipeline.test.ts src/app/api/profile/ats-enhancement/route.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- ATS enhancement now guarantees analysis, full structured rewrite, and validation before optimized state is persisted
  </success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-08-ats-enhancement-rewrite-pipeline/08-02-SUMMARY.md`
</output>

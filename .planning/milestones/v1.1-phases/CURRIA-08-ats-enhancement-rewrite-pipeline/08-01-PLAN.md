---
phase: 08-ats-enhancement-rewrite-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/agent.ts
  - src/lib/db/sessions.ts
  - src/app/api/agent/route.ts
  - src/app/api/session/[id]/route.ts
  - src/app/api/agent/route.sse.test.ts
  - src/lib/db/sessions.test.ts
autonomous: true
requirements:
  - ATS-01
user_setup: []
must_haves:
  truths:
    - "The backend resolves one deterministic workflow mode before the agent loop starts."
    - "Resume-present requests without a saved target vacancy become `ats_enhancement` sessions instead of ambiguous intake or dialog flows."
    - "Session state can persist ATS analysis and optimized resume metadata without losing compatibility with existing session JSON rows."
  artifacts:
    - path: "src/types/agent.ts"
      provides: "Formal workflow, ATS analysis, rewrite status, and optimized snapshot contracts"
      contains: "workflowMode"
    - path: "src/app/api/agent/route.ts"
      provides: "Pre-loop workflow resolution and ATS-enhancement orchestration seam"
      contains: "runAgentLoop"
    - path: "src/app/api/session/[id]/route.ts"
      provides: "Workspace session snapshot that exposes optimized ATS state to consumers"
      contains: "agentState"
  key_links:
    - from: "src/app/api/agent/route.ts"
      to: "src/types/agent.ts"
      via: "route-owned workflow resolution must persist fields that the session contract can normalize and expose"
      pattern: "workflowMode"
    - from: "src/lib/db/sessions.ts"
      to: "src/app/api/session/[id]/route.ts"
      via: "normalized session state must survive persistence and be readable by the dashboard workspace snapshot"
      pattern: "agentState"
---

<objective>
Formalize the ATS-enhancement workflow contract before implementation logic lands.

Purpose: The current brownfield agent stack already distinguishes some ATS and targeting behaviors, but workflow mode, optimized snapshot state, and rewrite lifecycle are not explicit or durable enough for deterministic orchestration.
Output: A stable session/type contract plus route-level workflow resolution that can drive the later ATS pipeline without guessing from incidental phase state.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-08-ats-enhancement-rewrite-pipeline/08-CONTEXT.md
@AGENTS.md
@src/types/agent.ts
@src/lib/db/sessions.ts
@src/app/api/agent/route.ts
@src/app/api/session/[id]/route.ts
@src/lib/agent/agent-loop.ts
@src/app/api/profile/ats-enhancement/route.ts
</context>

<threat_model>
<trust_boundaries>
- User request payload plus persisted resume context -> workflow mode resolution before `runAgentLoop(...)`
- Legacy session JSON rows -> normalized runtime `AgentState` with new ATS-enhancement fields
- Session snapshot API -> dashboard/workspace clients that will consume optimized resume state
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-08-01</threat_id>
  <category>Tampering</category>
  <component>Workflow mode resolution</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Resolve workflow mode from explicit resume/target context with deterministic helpers and persist the result before any optional agent branching begins.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-08-02</threat_id>
  <category>Repudiation</category>
  <component>Session state exposure</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Normalize and expose ATS-enhancement state consistently so workspace and tests can prove which optimized snapshot and rewrite status the session currently holds.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit ATS-enhancement state contracts and backward-compatible session normalization</name>
  <files>src/types/agent.ts, src/lib/db/sessions.ts, src/lib/db/sessions.test.ts</files>
  <read_first>src/types/agent.ts, src/lib/db/sessions.ts, src/lib/db/sessions.test.ts, prisma/schema.prisma</read_first>
  <action>Extend `src/types/agent.ts` with formal `WorkflowMode`, `RewriteStatus`, `AtsAnalysisResult`, and `RewriteValidationResult` contracts plus the new `agentState` fields for `workflowMode`, `atsAnalysis`, `rewriteStatus`, `optimizedCvState`, `optimizedAt`, `optimizationSummary`, and `lastRewriteMode`. Update `src/lib/db/sessions.ts` so legacy rows still normalize correctly, nested patch merging preserves the new structures, and the session state version is bumped only if the new normalization semantics require it.</action>
  <verify>npm test -- src/lib/db/sessions.test.ts</verify>
  <acceptance_criteria>
    - legacy session rows still normalize without throwing and default ATS-enhancement fields remain optional
    - the new `AgentState` contract can store ATS analysis results, optimized CV snapshots, and rewrite status transitions
    - tool/session patch merging does not erase nested ATS-enhancement metadata when unrelated fields update
  </acceptance_criteria>
  <done>Session types and persistence utilities can carry the explicit ATS-enhancement lifecycle safely.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve workflow mode before the agent loop and persist it on the session</name>
  <files>src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts</files>
  <read_first>src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/agent-loop.ts, src/app/api/profile/ats-enhancement/route.ts</read_first>
  <action>Introduce a deterministic workflow resolver in `src/app/api/agent/route.ts` that classifies requests into `resume_review`, `ats_enhancement`, or `job_targeting` using persisted resume context and target-vacancy state before `runAgentLoop(...)` is invoked. Persist the resolved mode into `session.agentState.workflowMode`, and create the pre-loop orchestration seam that later plans can use to run ATS enhancement imperatively for the resume-present/no-vacancy case.</action>
  <verify>npm test -- src/app/api/agent/route.sse.test.ts</verify>
  <acceptance_criteria>
    - resume-present requests without a target vacancy persist `workflowMode: "ats_enhancement"` before streaming starts
    - resume-present requests with a target vacancy persist `workflowMode: "job_targeting"` without regressing current target detection behavior
    - requests without resume context remain classified as `resume_review`
  </acceptance_criteria>
  <done>The route owns workflow resolution explicitly instead of inferring it later from agent behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Expose optimized ATS state through the session snapshot used by the dashboard</name>
  <files>src/app/api/session/[id]/route.ts, src/app/api/session/[id]/route.test.ts</files>
  <read_first>src/app/api/session/[id]/route.ts, src/components/dashboard/chat-interface.tsx, src/lib/dashboard/workspace-client.ts</read_first>
  <action>Update the session snapshot API so it returns the ATS-enhancement fields the workspace will need next: resolved `workflowMode`, `rewriteStatus`, `atsAnalysis`, `optimizedCvState`, `optimizedAt`, and `optimizationSummary`. Keep the response backward compatible for existing consumers and add snapshot tests that prove optimized ATS state is serialized without hiding the canonical base `cvState`.</action>
  <verify>npm test -- src/app/api/session/[id]/route.test.ts</verify>
  <acceptance_criteria>
    - the session snapshot includes optimized ATS state when present and omits it cleanly when absent
    - existing fields such as `cvState`, `generatedOutput`, `phase`, and `atsScore` remain intact
    - dashboard consumers can distinguish canonical base resume state from optimized ATS session state without extra DB calls
  </acceptance_criteria>
  <done>The workspace snapshot can render and reason about optimized ATS state deterministically.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/db/sessions.test.ts src/app/api/agent/route.sse.test.ts src/app/api/session/[id]/route.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Workflow mode and optimized ATS state are explicit, persisted, and visible to downstream runtime consumers
  </success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-08-ats-enhancement-rewrite-pipeline/08-01-SUMMARY.md`
</output>

---
phase: 08-ats-enhancement-rewrite-pipeline
plan: "03"
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260414_ats_enhancement_versioning.sql
  - src/lib/db/cv-versions.ts
  - src/lib/resume-generation/generate-billable-resume.ts
  - src/lib/agent/tools/generate-file.ts
  - src/app/api/file/[sessionId]/route.ts
  - src/app/api/session/[id]/route.ts
  - src/app/api/session/[id]/compare/route.ts
  - src/lib/dashboard/workspace-client.ts
  - src/lib/db/cv-versions.test.ts
  - src/lib/resume-generation/generate-billable-resume.test.ts
  - src/app/api/file/[sessionId]/route.test.ts
  - src/app/api/session/[id]/compare/route.test.ts
autonomous: true
requirements:
  - ATS-03
user_setup: []
must_haves:
  truths:
    - "A successful ATS-enhancement rewrite persists a formal version snapshot that export and history flows can recognize."
    - "Preview/download flows use the optimized ATS snapshot when it exists and is valid, but safely fall back to the canonical base resume otherwise."
    - "Timeline, compare, and generation behavior remain correct for both ATS-enhancement and target-derived resume variants."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Explicit persisted versioning contract for ATS-enhancement snapshots"
      contains: "CVVersionSource"
    - path: "src/lib/db/cv-versions.ts"
      provides: "Timeline labels and lookup semantics for ATS-enhancement versions"
      contains: "getCVVersionLabel"
    - path: "src/lib/resume-generation/generate-billable-resume.ts"
      provides: "Generation eligibility for ATS-enhancement versions without breaking billing/idempotency"
      contains: "BILLABLE_CV_VERSION_SOURCES"
  key_links:
    - from: "src/lib/db/cv-versions.ts"
      to: "src/lib/resume-generation/generate-billable-resume.ts"
      via: "version labels and source eligibility must stay aligned so optimized ATS snapshots can be exported"
      pattern: "CVVersionSource"
    - from: "src/app/api/session/[id]/route.ts"
      to: "src/app/api/file/[sessionId]/route.ts"
      via: "workspace snapshot and download endpoints must agree on which optimized artifact/state is the effective base resume"
      pattern: "generatedOutput"
---

<objective>
Persist ATS-enhancement versions formally and switch downstream consumers to the optimized snapshot where appropriate.

Purpose: Analysis and rewrite are not enough unless version history, compare flows, preview/export selection, and billing-aware generation all recognize the optimized ATS resume as a first-class artifact.
Output: ATS-enhancement snapshots that participate in version history and drive effective preview/export behavior without breaking target-derived or billing flows.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/CURRIA-08-ats-enhancement-rewrite-pipeline/08-CONTEXT.md
@AGENTS.md
@prisma/schema.prisma
@src/lib/db/cv-versions.ts
@src/lib/resume-generation/generate-billable-resume.ts
@src/lib/agent/tools/generate-file.ts
@src/app/api/file/[sessionId]/route.ts
@src/app/api/file/[sessionId]/route.test.ts
@src/app/api/session/[id]/route.ts
@src/app/api/session/[id]/compare/route.ts
@src/lib/dashboard/workspace-client.ts
</context>

<threat_model>
<trust_boundaries>
- Persisted ATS-enhancement version snapshots -> generation/billing allowlist
- Session snapshot and file-download APIs -> dashboard preview/download consumers
- Version timeline and compare routes -> rollback and evidence workflows
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-08-05</threat_id>
  <category>Tampering</category>
  <component>Export source selection</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Centralize which snapshot is the effective ATS source for base preview/export so stale canonical `cvState` cannot silently override a validated optimized snapshot.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-08-06</threat_id>
  <category>Denial of Service</category>
  <component>Generation reuse and billing</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep idempotency, completed-generation reuse, and CV-version eligibility aligned when ATS-enhancement versions are introduced.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend versioning so ATS-enhancement snapshots are first-class persisted resume versions</name>
  <files>prisma/schema.prisma, prisma/migrations/20260414_ats_enhancement_versioning.sql, src/lib/db/cv-versions.ts, src/lib/db/cv-versions.test.ts</files>
  <read_first>prisma/schema.prisma, prisma/migrations/cv_versioning_and_targets.sql, src/lib/db/cv-versions.ts, src/lib/db/cv-versions.test.ts</read_first>
  <action>Add a dedicated ATS-enhancement version source to the persisted versioning contract and implement the corresponding Prisma/SQL changes needed to load, label, and compare those snapshots cleanly. Update `src/lib/db/cv-versions.ts` so ATS-enhancement versions receive user-meaningful labels, remain in the base comparison scope, and continue to dedupe correctly against the most recent relevant base snapshot.</action>
  <verify>npm test -- src/lib/db/cv-versions.test.ts</verify>
  <acceptance_criteria>
    - ATS-enhancement snapshots persist as a distinct version source without breaking existing `rewrite` or `target-derived` history
    - timeline labels make ATS-enhancement versions distinguishable from manual edits and target resumes
    - version dedupe/comparison logic still behaves correctly for the latest relevant base snapshot
  </acceptance_criteria>
  <done>Optimized ATS resumes exist as formal version-history artifacts instead of session-only JSON state.</done>
</task>

<task type="auto">
  <name>Task 2: Make export and generation flows honor ATS-enhancement versions and effective optimized state</name>
  <files>src/lib/resume-generation/generate-billable-resume.ts, src/lib/agent/tools/generate-file.ts, src/app/api/file/[sessionId]/route.ts, src/app/api/file/[sessionId]/route.test.ts, src/lib/resume-generation/generate-billable-resume.test.ts</files>
  <read_first>src/lib/resume-generation/generate-billable-resume.ts, src/lib/agent/tools/index.ts, src/lib/agent/tools/generate-file.ts, src/app/api/file/[sessionId]/route.ts</read_first>
  <action>Update generation eligibility and artifact-selection logic so validated ATS-enhancement versions can be exported through the existing billing-aware flow. Centralize how the backend resolves the effective base resume source: prefer `optimizedCvState` and its ATS-enhancement version/artifact metadata when present, otherwise fall back to canonical `cvState`. Preserve target-resume behavior, idempotency, and completed-generation reuse.</action>
  <verify>npm test -- src/lib/resume-generation/generate-billable-resume.test.ts src/app/api/file/[sessionId]/route.test.ts</verify>
  <acceptance_criteria>
    - ATS-enhancement versions are eligible for billable generation without bypassing current quota/idempotency checks
    - base download URLs and generation reuse can resolve from the optimized ATS snapshot when it is the latest valid source
    - target-derived resume generation and download flows keep their current behavior
  </acceptance_criteria>
  <done>Export/download behavior is aligned with the optimized ATS resume instead of relying only on canonical base state.</done>
</task>

<task type="auto">
  <name>Task 3: Surface ATS-enhancement versions and optimized state in workspace compare and snapshot consumers</name>
  <files>src/app/api/session/[id]/route.ts, src/app/api/session/[id]/compare/route.ts, src/app/api/session/[id]/compare/route.test.ts, src/lib/dashboard/workspace-client.ts</files>
  <read_first>src/app/api/session/[id]/route.ts, src/app/api/session/[id]/compare/route.ts, src/lib/dashboard/workspace-client.ts, src/types/dashboard.ts</read_first>
  <action>Update workspace-facing APIs and client helpers so the dashboard can distinguish canonical base state, optimized ATS state, and target-derived variants. Ensure compare/timeline consumers can inspect ATS-enhancement versions, and expose any additional effective-state metadata the preview UI needs without forcing extra bespoke API calls.</action>
  <verify>npm test -- src/app/api/session/[id]/compare/route.test.ts</verify>
  <acceptance_criteria>
    - session/workspace consumers can identify and render optimized ATS state when it exists
    - compare flows can target ATS-enhancement versions without regressing base or target comparisons
    - client helpers remain backward compatible for consumers that only need basic generated-output state
  </acceptance_criteria>
  <done>The workspace and compare surfaces can observe and use ATS-enhancement artifacts end-to-end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/db/cv-versions.test.ts src/lib/resume-generation/generate-billable-resume.test.ts src/app/api/file/[sessionId]/route.test.ts src/app/api/session/[id]/compare/route.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- ATS-enhancement snapshots participate in version history and drive effective preview/export behavior across the workspace
  </success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-08-ats-enhancement-rewrite-pipeline/08-03-SUMMARY.md`
</output>

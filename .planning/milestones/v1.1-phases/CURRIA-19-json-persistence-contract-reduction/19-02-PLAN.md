---
phase: 19-json-persistence-contract-reduction
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/profile/user-profiles.ts
  - src/lib/profile/user-profiles.test.ts
  - src/types/dashboard.ts
  - src/types/agent.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "At least one high-value JSON seam no longer returns raw `unknown` for stable product data."
    - "Profile-facing CV persistence uses an explicit typed adapter instead of leaking raw JSON rows."
    - "Callers can rely on a narrower contract for imported resume state."
  artifacts:
    - path: "src/lib/profile/user-profiles.ts"
      provides: "Typed repository contract for imported user profile state"
      contains: "CVState"
    - path: "src/lib/profile/user-profiles.test.ts"
      provides: "Proof that imported profile JSON is validated and normalized"
      contains: "cvState"
---

<objective>
Narrow one high-value JSON persistence seam so stable product data stops leaking out of the repository as raw unknown-shaped payloads.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-19-json-persistence-contract-reduction/19-CONTEXT.md
@AGENTS.md
@src/lib/profile/user-profiles.ts
@src/app/api/profile/route.ts
@src/app/api/profile/upload/route.ts
@src/lib/profile/pdf-import.ts
@src/types/agent.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace raw imported-profile JSON rows with a typed repository contract</name>
  <files>src/lib/profile/user-profiles.ts, src/lib/profile/user-profiles.test.ts, src/types/agent.ts, src/types/dashboard.ts</files>
  <read_first>src/lib/profile/user-profiles.ts, src/app/api/profile/route.ts, src/app/api/profile/upload/route.ts, src/lib/profile/pdf-import.ts</read_first>
  <action>Refactor the user-profile repository seam so imported profile state uses a validated, typed shape instead of exposing `cv_state: unknown` to callers. Keep the change narrow and preserve existing external behavior while making the contract explicit and test-backed.</action>
  <verify>pnpm vitest run src/lib/profile/user-profiles.test.ts src/app/api/profile/upload/route.test.ts</verify>
  <done>The imported profile persistence seam is narrowed and no longer relies on raw unknown JSON for stable resume data.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm tsc --noEmit`
- [ ] `pnpm vitest run src/lib/profile/user-profiles.test.ts src/app/api/profile/upload/route.test.ts`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-19-json-persistence-contract-reduction/19-02-SUMMARY.md`
</output>

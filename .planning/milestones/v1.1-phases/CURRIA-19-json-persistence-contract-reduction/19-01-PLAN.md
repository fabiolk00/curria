---
phase: 19-json-persistence-contract-reduction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/state-model.md
  - docs/operations/json-persistence-contracts.md
  - prisma/schema.prisma
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "Every major JSON seam is classified by stability, owner, and whether it should remain opaque or become strongly typed."
    - "The repo documents which JSON blobs are canonical product contracts and which are intentionally event-log or operational payloads."
    - "Future migrations have a concrete starting matrix instead of ad hoc intuition."
  artifacts:
    - path: "docs/operations/json-persistence-contracts.md"
      provides: "Inventory and ownership matrix for JSON-backed persistence"
      contains: "Contract Class"
    - path: "docs/state-model.md"
      provides: "Aligned product-state language for stable JSON seams"
      contains: "cvState"
---

<objective>
Inventory and classify the JSON-backed persistence seams so the implementation work can tighten the right contracts without over-constraining intentionally flexible data.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-19-json-persistence-contract-reduction/19-CONTEXT.md
@AGENTS.md
@prisma/schema.prisma
@docs/state-model.md
@src/lib/db/session-normalization.ts
@src/lib/db/resume-targets.ts
@src/lib/profile/user-profiles.ts
@src/lib/db/resume-generations.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Publish the JSON persistence inventory and contract classes</name>
  <files>docs/operations/json-persistence-contracts.md, docs/state-model.md, prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma, docs/state-model.md, .planning/phases/CURRIA-19-json-persistence-contract-reduction/19-CONTEXT.md</read_first>
  <action>Create a committed inventory of the major JSON columns and classify each seam as canonical product contract, operational contract, or intentionally opaque event payload. Update the state-model docs only where needed so the language matches the current schema and future contract-reduction plan.</action>
  <verify>rg -n "Contract Class|canonical|opaque|event payload|generated_output|agent_state|cv_state" docs/operations/json-persistence-contracts.md docs/state-model.md prisma/schema.prisma</verify>
  <done>The project has a usable inventory and classification matrix for its JSON persistence seams.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Contract Class|canonical|opaque|event payload|generated_output|agent_state|cv_state" docs/operations/json-persistence-contracts.md docs/state-model.md prisma/schema.prisma`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-19-json-persistence-contract-reduction/19-01-SUMMARY.md`
</output>

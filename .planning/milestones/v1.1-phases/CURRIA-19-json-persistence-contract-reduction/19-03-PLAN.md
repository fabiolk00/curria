---
phase: 19-json-persistence-contract-reduction
plan: "03"
type: execute
wave: 2
depends_on:
  - 19-01
  - 19-02
files_modified:
  - src/lib/db/session-normalization.ts
  - src/lib/db/resume-targets.ts
  - src/lib/db/resume-generations.ts
  - src/lib/db/sessions.test.ts
  - src/lib/db/resume-targets.test.ts
  - src/lib/db/resume-generations.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "Session, targeting, and generation repositories expose stronger normalization boundaries for mature JSON seams."
    - "Intentionally opaque blobs remain explicit exceptions instead of accidental weak spots."
    - "The final code and docs distinguish stable product JSON from operational/event payload JSON."
  artifacts:
    - path: "src/lib/db/session-normalization.ts"
      provides: "Centralized normalization boundary for session JSON"
      contains: "normalize"
    - path: "src/lib/db/resume-targets.ts"
      provides: "Validated targeting JSON contract"
      contains: "parse"
    - path: "src/lib/db/resume-generations.ts"
      provides: "Typed snapshot contract for billable generation state"
      contains: "structuredClone"
---

<objective>
Consolidate the contract-reduction work across the mature session, targeting, and generation adapters so the remaining opaque JSON seams are deliberate and documented.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-19-json-persistence-contract-reduction/19-CONTEXT.md
@AGENTS.md
@src/lib/db/session-normalization.ts
@src/lib/db/resume-targets.ts
@src/lib/db/resume-generations.ts
@src/lib/db/sessions.test.ts
@src/lib/db/resume-targets.test.ts
@src/lib/db/resume-generations.test.ts
@docs/operations/json-persistence-contracts.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Tighten mature JSON adapters and mark opaque exceptions explicitly</name>
  <files>src/lib/db/session-normalization.ts, src/lib/db/resume-targets.ts, src/lib/db/resume-generations.ts, src/lib/db/sessions.test.ts, src/lib/db/resume-targets.test.ts, src/lib/db/resume-generations.test.ts</files>
  <read_first>src/lib/db/session-normalization.ts, src/lib/db/resume-targets.ts, src/lib/db/resume-generations.ts, docs/operations/json-persistence-contracts.md</read_first>
  <action>Review the mature repository adapters and tighten normalization or validation only where needed so stable session, target, and generation JSON seams are explicit. Keep intentionally opaque event-log style payloads out of scope, but document them as deliberate exceptions rather than unresolved ambiguity.</action>
  <verify>pnpm vitest run src/lib/db/sessions.test.ts src/lib/db/resume-targets.test.ts src/lib/db/resume-generations.test.ts</verify>
  <done>The mature JSON seams are more explicit, more test-backed, and easier to evolve toward future schema narrowing.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm tsc --noEmit`
- [ ] `pnpm vitest run src/lib/db/sessions.test.ts src/lib/db/resume-targets.test.ts src/lib/db/resume-generations.test.ts`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-19-json-persistence-contract-reduction/19-03-SUMMARY.md`
</output>

# Plan 13-03

## Goal

Lock down the E2E auth bypass and prove it cannot drift into unintended environments.

## Tasks

1. Audit the current `E2E_AUTH_ENABLED` and `E2E_AUTH_BYPASS_SECRET` flow end to end.
2. Add explicit environment gating and failure behavior for unsupported environments.
3. Add tests that prove bypass requests fail when the required test context is absent.
4. Document the safe operational contract for local and CI usage.

## Verification

- Route-level tests for allowed and disallowed bypass environments
- Documentation for safe usage in local and CI contexts
- No implicit production-compatible fallback remains

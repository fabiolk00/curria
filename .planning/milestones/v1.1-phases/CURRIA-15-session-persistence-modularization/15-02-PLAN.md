# Plan 15-02

## Goal

Extract quota and message persistence concerns behind stable internal interfaces.

## Tasks

1. Move quota-specific behavior into a narrower persistence module without changing callers.
2. Move message append or read behavior into its own unit with explicit transaction expectations.
3. Keep external route and pipeline signatures stable while internals are decomposed.
4. Add unit-level tests around the extracted behavior.

## Verification

- Callers continue to pass unchanged
- New modules have targeted tests
- No quota or message regression appears in existing focused suites

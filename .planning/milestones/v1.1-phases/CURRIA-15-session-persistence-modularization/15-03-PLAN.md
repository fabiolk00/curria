# Plan 15-03

## Goal

Extract session patch and lifecycle orchestration from the remaining dense module.

## Tasks

1. Move session lifecycle and patch-merge behavior into narrower modules or helpers with stable contracts.
2. Preserve existing transaction and fallback semantics while reducing file-level responsibility concentration.
3. Update imports and tests incrementally rather than in one broad rewrite.
4. Close the phase only when the new boundaries are easier to test and reason about than the starting point.

## Verification

- Focused regression suites still pass
- Extracted modules are materially narrower than the original file
- Remaining `sessions.ts` surface is simpler and more cohesive

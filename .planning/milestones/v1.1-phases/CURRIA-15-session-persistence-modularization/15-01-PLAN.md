# Plan 15-01

## Goal

Map the current `sessions.ts` responsibilities and create safe extraction seams.

## Tasks

1. Inventory the current responsibilities in `sessions.ts`:
   - lifecycle and create
   - patch and merge behavior
   - quota checks
   - message appends
   - fallback transaction handling
2. Define target module boundaries and invariants before moving code.
3. Add baseline tests around the current behavior where coverage is thin.

## Verification

- Responsibility map is explicit and committed
- Extraction order is justified by risk and coupling
- Baseline tests protect the current behavior

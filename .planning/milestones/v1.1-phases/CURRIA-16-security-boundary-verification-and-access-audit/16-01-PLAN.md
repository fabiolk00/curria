---
phase: 16-security-boundary-verification-and-access-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/lib/auth/app-user.ts
  - src/lib/auth/e2e-auth.ts
  - src/app/api/e2e/auth/route.ts
  - src/app/(auth)/layout.test.tsx
  - src/app/api/e2e/auth/route.test.ts
autonomous: true
requirements:
  - SEC-03
  - SEC-04
user_setup: []
must_haves:
  truths:
    - "Middleware and test-only auth bypass behavior are proven with committed negative-path evidence."
    - "Unsupported runtimes and invalid bypass credentials fail closed instead of silently degrading open."
    - "Protected authenticated surfaces keep enforcing the expected boundary when bypass mode is absent or invalid."
  artifacts:
    - path: "src/middleware.ts"
      provides: "Explicit boundary behavior for webhook/public routes and E2E bypass-cookie trust"
      contains: "hasValidE2EAuthCookie"
    - path: "src/app/api/e2e/auth/route.test.ts"
      provides: "Fail-closed verification for the signed test-bypass bootstrap route"
      contains: "rejects requests"
    - path: "src/app/(auth)/layout.test.tsx"
      provides: "Protected-surface proof for bypass and non-bypass auth resolution"
      contains: "E2E"
---

<objective>
Prove and harden middleware plus test-auth boundaries so protected surfaces only open under the documented runtime and signed-cookie contract.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-16-security-boundary-verification-and-access-audit/16-CONTEXT.md
@AGENTS.md
@src/middleware.ts
@src/lib/auth/e2e-auth.ts
@src/lib/auth/app-user.ts
@src/app/api/e2e/auth/route.ts
@src/app/(auth)/layout.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Audit middleware and app-user auth resolution against the documented bypass contract</name>
  <files>src/middleware.ts, src/lib/auth/app-user.ts, src/lib/auth/e2e-auth.ts</files>
  <read_first>src/middleware.ts, src/lib/auth/app-user.ts, src/lib/auth/e2e-auth.ts, docs/operations/secret-boundaries-and-e2e-auth.md</read_first>
  <action>Trace the effective auth decision flow for public paths, webhook paths, normal authenticated paths, and E2E bypass-cookie handling. Make only the narrow code changes needed so unsupported-runtime, malformed-cookie, or invalid-signature cases fail closed consistently at the middleware plus auth-resolution seam, without weakening the documented bypass allowlist.</action>
  <verify>pnpm vitest run src/lib/auth/e2e-auth.test.ts</verify>
  <done>The runtime and cookie contract is explicit in code and fails closed when conditions are not satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Add route and layout verification for protected surfaces under valid and invalid bypass conditions</name>
  <files>src/app/api/e2e/auth/route.ts, src/app/api/e2e/auth/route.test.ts, src/app/(auth)/layout.test.tsx</files>
  <read_first>src/app/api/e2e/auth/route.ts, src/app/api/e2e/auth/route.test.ts, src/app/(auth)/layout.test.tsx, src/middleware.ts</read_first>
  <action>Strengthen tests so the signed cookie bootstrap route and at least one protected authenticated surface prove the real boundary behavior: valid bypass works only in allowed runtimes, invalid or absent bypass stays blocked, and unsupported-runtime requests do not accidentally authenticate. Keep assertions tied to user-visible or route-visible behavior rather than helper-only implementation details.</action>
  <verify>pnpm vitest run src/app/api/e2e/auth/route.test.ts src/app/(auth)/layout.test.tsx</verify>
  <done>Protected-surface behavior is covered for both success and fail-closed bypass scenarios.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm tsc --noEmit`
- [ ] `pnpm vitest run src/lib/auth/e2e-auth.test.ts src/app/api/e2e/auth/route.test.ts src/app/(auth)/layout.test.tsx`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-16-security-boundary-verification-and-access-audit/16-01-SUMMARY.md`
</output>

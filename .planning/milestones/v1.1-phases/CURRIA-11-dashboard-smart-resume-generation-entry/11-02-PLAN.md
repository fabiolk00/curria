---
phase: 11-dashboard-smart-resume-generation-entry
plan: "02"
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/app/api/profile/smart-generation/route.ts
  - src/app/api/profile/smart-generation/route.test.ts
  - src/lib/profile/ats-enhancement.ts
  - src/lib/agent/job-targeting-pipeline.ts
  - src/lib/agent/ats-enhancement-pipeline.ts
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "The setup page uses one backend entrypoint that branches by context."
    - "ATS enhancement and job targeting both reuse the deterministic pipelines rather than reimplementing rewrite logic."
    - "Target-job generation from setup persists a session ready for dashboard workspace use."
  artifacts:
    - path: "src/app/api/profile/smart-generation/route.ts"
      provides: "Single setup-page smart generation API"
      contains: "generationType"
    - path: "src/app/api/profile/smart-generation/route.test.ts"
      provides: "Deterministic branching coverage for ATS-only and target-job setup requests"
      contains: "JOB_TARGETING"
---

<objective>
Create one setup-page generation backend that chooses ATS enhancement or job targeting based on the provided context.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/CURRIA-11-dashboard-smart-resume-generation-entry/11-CONTEXT.md
@AGENTS.md
@src/app/api/profile/ats-enhancement/route.ts
@src/app/api/profile/ats-enhancement/route.test.ts
@src/lib/agent/ats-enhancement-pipeline.ts
@src/lib/agent/job-targeting-pipeline.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create a smart setup generation route that branches by target-job context</name>
  <files>src/app/api/profile/smart-generation/route.ts, src/lib/profile/ats-enhancement.ts</files>
  <read_first>src/app/api/profile/ats-enhancement/route.ts, src/lib/profile/ats-enhancement.ts, src/lib/agent/job-targeting-pipeline.ts</read_first>
  <action>Create a new setup-page route that accepts base CV data plus an optional target-job description, validates readiness, creates a session, seeds the right agent state, runs `runAtsEnhancementPipeline(...)` or `runJobTargetingPipeline(...)`, and then generates artifacts from the resulting optimized CV.</action>
  <verify>npm test -- src/app/api/profile/smart-generation/route.test.ts</verify>
  <done>The setup page has a single smart backend entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add route tests for ATS-only and target-job setup generation</name>
  <files>src/app/api/profile/smart-generation/route.test.ts</files>
  <read_first>src/app/api/profile/ats-enhancement/route.test.ts, src/app/api/profile/smart-generation/route.ts</read_first>
  <action>Add focused route tests proving ATS-only requests stay on `ATS_ENHANCEMENT`, target-job requests run `JOB_TARGETING`, the correct pipeline is called, and the returned payload is consistent for dashboard redirects.</action>
  <verify>npm test -- src/app/api/profile/smart-generation/route.test.ts</verify>
  <done>Smart setup branching is protected at the HTTP contract level.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/app/api/profile/smart-generation/route.test.ts`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-11-dashboard-smart-resume-generation-entry/11-02-SUMMARY.md`
</output>

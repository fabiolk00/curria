---
phase: 09-ats-enhancement-reliability-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/ats-enhancement-pipeline.ts
  - src/lib/agent/ats-enhancement-retry.ts
  - src/lib/agent/tools/rewrite-resume-full.ts
  - src/types/agent.ts
  - src/app/api/agent/route.ts
  - src/app/api/profile/ats-enhancement/route.ts
  - src/app/api/agent/route.sse.test.ts
  - src/app/api/profile/ats-enhancement/route.test.ts
  - src/lib/agent/tools/pipeline.test.ts
autonomous: true
requirements:
  - REL-01
user_setup: []
must_haves:
  truths:
    - "Sending only a resume still deterministically triggers `ats_enhancement`."
    - "ATS-enhancement runs persist stage-aware progress and do not collapse into one opaque failure mode."
    - "Large or partially failing rewrites retry by stage or section instead of rerunning the whole workflow blindly."
    - "Retry behavior is bounded by an explicit ceiling and large-resume stress coverage proves the ceiling is respected."
  artifacts:
    - path: "src/lib/agent/ats-enhancement-pipeline.ts"
      provides: "Stage-aware ATS-enhancement orchestration with retries and structured state transitions"
      contains: "runAtsEnhancementPipeline"
    - path: "src/lib/agent/ats-enhancement-retry.ts"
      provides: "Explicit retry ceilings, payload-shaping policy, and failure-budget helpers for ATS stages"
      contains: "MAX_ATS_STAGE_RETRIES"
    - path: "src/lib/agent/tools/rewrite-resume-full.ts"
      provides: "Section-level retry-safe full rewrite flow"
      contains: "rewriteResumeFull"
    - path: "src/types/agent.ts"
      provides: "Operational ATS-enhancement state for stages, retries, and failure reasons"
      contains: "rewriteStatus"
---

<objective>
Harden ATS-enhancement execution so large resumes and transient step failures do not turn the whole pipeline into an opaque all-or-nothing operation.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-09-ats-enhancement-reliability-hardening/09-CONTEXT.md
@AGENTS.md
@src/lib/agent/ats-enhancement-pipeline.ts
@src/lib/agent/tools/rewrite-resume-full.ts
@src/types/agent.ts
@src/app/api/agent/route.ts
@src/app/api/profile/ats-enhancement/route.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add ATS-enhancement stage metadata and retry-aware state transitions</name>
  <files>src/types/agent.ts, src/lib/agent/ats-enhancement-pipeline.ts, src/lib/agent/ats-enhancement-retry.ts</files>
  <read_first>src/types/agent.ts, src/lib/agent/ats-enhancement-pipeline.ts, src/lib/db/sessions.ts, src/lib/openai/chat.ts</read_first>
  <action>Extend the ATS-enhancement operational state with stage-aware metadata that can represent where a run is currently executing, which stage failed, and whether a retry happened. Introduce an explicit ATS retry helper or equivalent seam that defines retry ceilings, safe payload-shaping rules for oversized sections, and a failure budget the pipeline must honor. Update the pipeline so ATS analysis, rewrite, validation, and version persistence each leave deterministic state transitions rather than one generic `running` or `failed` marker.</action>
  <verify>npm test -- src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/tools/pipeline.test.ts</verify>
  <done>The ATS pipeline exposes stage-level progress and failure reasons instead of one opaque status.</done>
</task>

<task type="auto">
  <name>Task 2: Add section-level retry behavior to large or partially failing rewrites</name>
  <files>src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/ats-enhancement-pipeline.ts</files>
  <read_first>src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/tools/rewrite-section.ts, src/lib/agent/ats-enhancement-pipeline.ts</read_first>
  <action>Harden `rewriteResumeFull(...)` so a single section failure or malformed section output can trigger a scoped retry policy instead of forcing the whole ATS run to restart. Apply the explicit retry ceiling and payload-shaping policy from Task 1, and add at least one large-resume stress fixture proving the rewrite path stays section-based, bounded, and fact-preserving under heavy input.</action>
  <verify>npm test -- src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/tools/pipeline.test.ts</verify>
  <done>Rewrite reliability improves without sacrificing deterministic section-based behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Lock resume-only ATS triggering against the hardened pipeline contract</name>
  <files>src/app/api/agent/route.ts, src/app/api/profile/ats-enhancement/route.ts, src/app/api/agent/route.sse.test.ts, src/app/api/profile/ats-enhancement/route.test.ts</files>
  <read_first>src/app/api/agent/route.ts, src/app/api/profile/ats-enhancement/route.ts, src/app/api/agent/route.sse.test.ts, src/app/api/profile/ats-enhancement/route.test.ts</read_first>
  <action>Keep the existing deterministic `ats_enhancement` trigger for resume-only flows while updating the route tests to prove the hardened pipeline semantics: stage-aware persistence, retry-safe failures, and continued use of the shared ATS path from both backend entry points.</action>
  <verify>npm test -- src/app/api/agent/route.sse.test.ts src/app/api/profile/ats-enhancement/route.test.ts</verify>
  <done>ATS-enhancement triggering remains deterministic while the reliability contract gets stricter.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/app/api/agent/route.sse.test.ts src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/tools/pipeline.test.ts`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-09-ats-enhancement-reliability-hardening/09-01-SUMMARY.md`
</output>

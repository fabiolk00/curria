---
phase: 09-ats-enhancement-reliability-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/lib/agent/tools/build-rewrite-plan.ts
  - src/lib/agent/tools/validate-rewrite.ts
  - src/lib/agent/tools/rewrite-resume-full.ts
  - src/lib/agent/ats-enhancement-pipeline.ts
  - src/types/agent.ts
  - src/lib/agent/tools/index.test.ts
  - src/app/api/profile/ats-enhancement/route.test.ts
autonomous: true
requirements:
  - REL-02
user_setup: []
must_haves:
  truths:
    - "Optimized resumes are rejected when they introduce factual inventions or cross-section inconsistencies."
    - "Summary, experience, and skills remain mutually consistent after ATS-enhancement rewrites."
    - "Validation failures never persist a misleading optimized snapshot as the effective export source."
  artifacts:
    - path: "src/lib/agent/tools/build-rewrite-plan.ts"
      provides: "Explicit intermediate rewrite plan that aligns section scope, factual anchors, and keyword intent before rewriting"
      contains: "buildRewritePlan"
    - path: "src/lib/agent/tools/validate-rewrite.ts"
      provides: "Factual and cross-section consistency validation for ATS rewrites"
      contains: "validateRewrite"
    - path: "src/lib/agent/tools/rewrite-resume-full.ts"
      provides: "Rewrite assembly that preserves consistency signals across sections"
      contains: "changedSections"
---

<objective>
Strengthen ATS-enhancement safety so rewrites stay factually grounded and internally consistent across sections.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/CURRIA-09-ats-enhancement-reliability-hardening/09-CONTEXT.md
@AGENTS.md
@src/lib/agent/tools/validate-rewrite.ts
@src/lib/agent/tools/rewrite-resume-full.ts
@src/lib/agent/ats-enhancement-pipeline.ts
@src/types/agent.ts
@src/lib/agent/tools/index.test.ts
@src/app/api/profile/ats-enhancement/route.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expand validation from fact invention checks to cross-section consistency checks</name>
  <files>src/lib/agent/tools/validate-rewrite.ts, src/types/agent.ts</files>
  <read_first>src/lib/agent/tools/validate-rewrite.ts, src/types/agent.ts, src/lib/agent/tools/rewrite-resume-full.ts</read_first>
  <action>Strengthen `validateRewrite(...)` so it not only blocks invented employers, dates, certifications, tools, and unsupported numbers, but also flags inconsistencies between summary, experience, and skills. Focus on drift that could create fake seniority signals, unsupported tool emphasis, or misleading capability summaries.</action>
  <verify>npm test -- src/app/api/profile/ats-enhancement/route.test.ts</verify>
  <done>The ATS safety gate catches misleading internal inconsistencies, not just obvious hallucinated facts.</done>
</task>

<task type="auto">
  <name>Task 2: Add an explicit intermediate rewrite-plan artifact and use it to align sections</name>
  <files>src/lib/agent/tools/build-rewrite-plan.ts, src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/ats-enhancement-pipeline.ts</files>
  <read_first>src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/tools/ats-analysis.ts, src/lib/agent/ats-enhancement-pipeline.ts</read_first>
  <action>Create an explicit `buildRewritePlan(...)` seam or equivalent artifact that derives shared factual anchors, section goals, keyword focus, and do-not-invent constraints from the original resume plus ATS analysis before rewriting starts. Update the full rewrite service to execute against that shared plan so summary, experience, and skills reinforce the same factual story instead of drifting independently.</action>
  <verify>npm test -- src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/tools/index.test.ts</verify>
  <done>The rewrite flow produces a more coherent optimized resume across sections.</done>
</task>

<task type="auto">
  <name>Task 3: Prove validation failures never become effective optimized sources</name>
  <files>src/lib/agent/ats-enhancement-pipeline.ts, src/app/api/profile/ats-enhancement/route.test.ts, src/lib/agent/tools/index.test.ts</files>
  <read_first>src/lib/agent/ats-enhancement-pipeline.ts, src/lib/agent/tools/index.ts, src/app/api/profile/ats-enhancement/route.test.ts, src/lib/agent/tools/index.test.ts</read_first>
  <action>Add regression coverage proving that validation failures or cross-section inconsistency failures do not leave behind a persisted `optimizedCvState` that downstream generation can accidentally use. Keep the last valid source-selection behavior intact.</action>
  <verify>npm test -- src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/tools/index.test.ts</verify>
  <done>Only validated ATS rewrites can drive downstream preview and export behavior.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/tools/index.test.ts`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-09-ats-enhancement-reliability-hardening/09-02-SUMMARY.md`
</output>

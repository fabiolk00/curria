---
phase: 05-deployed-agent-parity-and-evidence
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/lib/runtime/release-metadata.test.ts
  - src/app/api/agent/route.test.ts
  - src/lib/agent/streaming-loop.test.ts
  - scripts/check-agent-runtime-parity.test.ts
autonomous: true
requirements:
  - OPS-04
  - OPS-05
  - OPS-06
user_setup: []
must_haves:
  truths:
    - "Regression coverage proves `/api/agent` provenance headers exist on safe non-stream and stream responses."
    - "Completed-turn and fallback log tests lock the release, model, assistant text length, and fallback fields Phase 5 depends on."
    - "The parity CLI is tested for both success and mismatch cases so operator docs cannot silently drift from actual behavior."
  artifacts:
    - path: "src/lib/runtime/release-metadata.test.ts"
      provides: "Helper-level proof for release metadata fallbacks and normalization"
      contains: "getAgentReleaseMetadata"
    - path: "src/app/api/agent/route.test.ts"
      provides: "Route-level proof for provenance headers and safe parity request paths"
      contains: "X-Agent-Release"
    - path: "src/lib/agent/streaming-loop.test.ts"
      provides: "Log-schema proof for completed turns and fallback diagnostics"
      contains: "assistantTextChars"
    - path: "scripts/check-agent-runtime-parity.test.ts"
      provides: "CLI-level proof for parity success and mismatch handling"
      contains: "expected-release"
  key_links:
    - from: "src/lib/runtime/release-metadata.test.ts"
      to: "src/lib/runtime/release-metadata.ts"
      via: "helper tests lock the server-derived release contract"
      pattern: "releaseSource"
    - from: "src/app/api/agent/route.test.ts"
      to: "src/app/api/agent/route.ts"
      via: "route tests prove provenance headers are emitted on the intended response paths"
      pattern: "X-Agent-Release"
    - from: "scripts/check-agent-runtime-parity.test.ts"
      to: "scripts/check-agent-runtime-parity.ts"
      via: "CLI tests prove mismatch reporting stays aligned with the operator runbook"
      pattern: "expected-release"
---

<objective>
Lock the Phase 5 provenance and parity contract in with targeted regression coverage.

Purpose: After the runtime surface and operator workflow exist, the final step is to make sure headers, log schema, and parity CLI behavior cannot drift silently.
Output: Helper, route, loop, and CLI tests that prove the Phase 5 provenance contract holds.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployed-agent-parity-and-evidence/05-RESEARCH.md
@.planning/phases/05-deployed-agent-parity-and-evidence/05-VALIDATION.md
@AGENTS.md
@src/lib/runtime/release-metadata.ts
@src/app/api/agent/route.ts
@src/lib/agent/agent-loop.ts
@scripts/check-agent-runtime-parity.ts
@src/app/api/agent/route.test.ts
@src/app/api/agent/route.model-selection.test.ts
@src/app/api/agent/route.sse.test.ts
@src/lib/agent/streaming-loop.test.ts
</context>

<threat_model>
<trust_boundaries>
- Test fixtures -> release metadata helper and parity CLI
- Route tests -> safe provenance response paths
- Loop tests -> structured log contract
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-05-01</threat_id>
  <category>Repudiation</category>
  <component>Release provenance contract</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Route and helper tests must prove the emitted release identifier is stable and present on the intended responses.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-05-03</threat_id>
  <category>Information Disclosure</category>
  <component>Structured log schema</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Loop tests must assert the new provenance fields stay flat and safe alongside the required model and fallback fields.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-05-04</threat_id>
  <category>Denial of Service</category>
  <component>Parity CLI</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>CLI tests must prove the safe parity request path and mismatch handling work without requiring a mutating live session.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add helper and route coverage for provenance headers</name>
  <files>src/lib/runtime/release-metadata.test.ts, src/app/api/agent/route.test.ts</files>
  <read_first>src/lib/runtime/release-metadata.ts, src/app/api/agent/route.ts, src/app/api/agent/route.test.ts, src/app/api/agent/route.model-selection.test.ts, src/app/api/agent/route.sse.test.ts</read_first>
  <action>Create `src/lib/runtime/release-metadata.test.ts` to lock the helper contract for commit-based and local-dev fallbacks. Extend `src/app/api/agent/route.test.ts` so unauthenticated, invalid-body, and successful SSE responses all assert the new provenance headers, and add a safe parity-request regression that proves the chosen request shape does not create a session or increment a message.</action>
  <verify>npm test -- src/lib/runtime/release-metadata.test.ts src/app/api/agent/route.test.ts</verify>
  <acceptance_criteria>
    - helper tests cover explicit commit metadata and deterministic local fallback behavior
    - route tests assert `X-Agent-Release` and related headers on both stream and non-stream responses
    - route parity tests prove the safe operator check path does not create a session or increment message count
  </acceptance_criteria>
  <done>Release provenance is protected by helper- and route-level tests instead of manual spot checks.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the completed-turn log schema and parity CLI behavior</name>
  <files>src/lib/agent/streaming-loop.test.ts, scripts/check-agent-runtime-parity.test.ts</files>
  <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.test.ts, scripts/check-agent-runtime-parity.ts, docs/agent-runtime-parity.md</read_first>
  <action>Extend `src/lib/agent/streaming-loop.test.ts` to assert that completed-turn, truncated-response, and empty-fallback log events include the required provenance and runtime fields: `releaseId`, `releaseSource`, `model`, `assistantTextChars`, `usedLengthRecovery`, `usedConciseRecovery`, and `fallbackKind` where applicable. Add `scripts/check-agent-runtime-parity.test.ts` to cover success, mismatch, and missing-header behavior for the CLI so the operator runbook stays aligned with actual script output and exit codes.</action>
  <verify>npm test -- src/lib/agent/streaming-loop.test.ts scripts/check-agent-runtime-parity.test.ts</verify>
  <acceptance_criteria>
    - loop tests assert the completed-turn and fallback log field contract Phase 5 depends on
    - parity CLI tests cover pass, mismatch, and missing-header outcomes
    - the new tests remain focused on provenance and parity rather than transcript UX, which belongs to Phase 7
  </acceptance_criteria>
  <done>Phase 5 finishes with stable automated proof for both the runtime evidence and the operator parity workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/runtime/release-metadata.test.ts src/app/api/agent/route.test.ts src/lib/agent/streaming-loop.test.ts scripts/check-agent-runtime-parity.test.ts`
- [ ] `node .codex/get-shit-done/bin/gsd-tools.cjs state validate`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 provenance headers, log schema, and parity CLI behavior are protected by committed regression coverage
  </success_criteria>

<output>
After completion, create `.planning/phases/05-deployed-agent-parity-and-evidence/05-03-SUMMARY.md`
</output>

---
phase: 05-deployed-agent-parity-and-evidence
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/check-agent-runtime-parity.ts
  - package.json
  - docs/agent-runtime-parity.md
  - docs/architecture-overview.md
  - docs/openai/README.md
  - docs/INDEX.md
autonomous: true
requirements:
  - OPS-04
  - OPS-06
user_setup: []
must_haves:
  truths:
    - "Operators can run one committed parity check against a deployed `/api/agent` route without creating a session or consuming credits."
    - "The parity check compares live provenance headers against explicit expected release and model values and fails loudly on mismatch."
    - "Repo docs explain which headers matter, how to run the parity check, and why the check is safe."
  artifacts:
    - path: "scripts/check-agent-runtime-parity.ts"
      provides: "Non-mutating parity check for deployed `/api/agent` provenance"
      contains: "expected-release"
    - path: "package.json"
      provides: "Top-level parity command for operators"
      contains: "agent:parity"
    - path: "docs/agent-runtime-parity.md"
      provides: "Operator runbook for verifying live agent provenance"
      contains: "X-Agent-Release"
  key_links:
    - from: "scripts/check-agent-runtime-parity.ts"
      to: "src/app/api/agent/route.ts"
      via: "script reads the route-level provenance headers without entering the SSE loop"
      pattern: "X-Agent-Release"
    - from: "docs/agent-runtime-parity.md"
      to: "docs/architecture-overview.md"
      via: "operator runbook is linked from the existing architecture and docs index entry points"
      pattern: "agent runtime parity"
    - from: "docs/openai/README.md"
      to: "src/lib/agent/config.ts"
      via: "docs explain how expected model values map to the runtime contract being checked"
      pattern: "OPENAI_DIALOG_MODEL"
---

<objective>
Add a safe post-deploy parity workflow for `/api/agent` and document how operators should use it.

Purpose: Once the route exposes provenance, the team needs one committed, non-mutating way to confirm a deployment is serving the intended release and model contract.
Output: A parity CLI script, a package command, and docs that explain the header contract and operator workflow.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployed-agent-parity-and-evidence/05-RESEARCH.md
@.planning/phases/05-deployed-agent-parity-and-evidence/05-VALIDATION.md
@AGENTS.md
@package.json
@src/app/api/agent/route.ts
@src/lib/runtime/release-metadata.ts
@src/lib/agent/config.ts
@docs/architecture-overview.md
@docs/openai/README.md
@docs/INDEX.md
</context>

<threat_model>
<trust_boundaries>
- Operator parity CLI -> deployed `/api/agent` route
- Operator documentation -> release verification procedure
- Release and model expectations -> CLI mismatch reporting
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-05-04</threat_id>
  <category>Denial of Service</category>
  <component>Post-deploy parity check</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Use a safe request shape that returns provenance headers without creating a session, incrementing message count, or burning credits.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-05-05</threat_id>
  <category>Tampering</category>
  <component>Operator expectations</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Make the script compare explicit expected release and model values to the live header contract and fail with clear mismatches instead of printing ambiguous output.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build a safe `/api/agent` parity checker CLI</name>
  <files>scripts/check-agent-runtime-parity.ts, package.json</files>
  <read_first>package.json, src/app/api/agent/route.ts, src/lib/runtime/release-metadata.ts, src/lib/agent/config.ts</read_first>
  <action>Implement `scripts/check-agent-runtime-parity.ts` as a non-mutating CLI that accepts concrete flags such as `--url`, `--expected-release`, `--expected-agent-model`, and `--expected-dialog-model`. The script should call the deployed `/api/agent` route using a safe request shape that returns provenance headers without creating a session, then compare the live `X-Agent-Release`, `X-Agent-Release-Source`, `X-Agent-Resolved-Agent-Model`, and `X-Agent-Resolved-Dialog-Model` values to the explicit expectations. Add a top-level `agent:parity` script in `package.json` that runs this file through `tsx`.</action>
  <verify>npx tsx scripts/check-agent-runtime-parity.ts --help</verify>
  <acceptance_criteria>
    - the CLI exposes explicit expectation flags for release and model values
    - the CLI prints a clear pass or mismatch summary and exits non-zero on mismatch
    - the parity check uses a request shape that does not create sessions or consume credits
    - `package.json` includes a stable `agent:parity` command for operators
  </acceptance_criteria>
  <done>Operators have one committed CLI for verifying deployed agent provenance safely.</done>
</task>

<task type="auto">
  <name>Task 2: Document the parity contract and rollout procedure</name>
  <files>docs/agent-runtime-parity.md, docs/architecture-overview.md, docs/openai/README.md, docs/INDEX.md</files>
  <read_first>docs/architecture-overview.md, docs/openai/README.md, docs/INDEX.md, scripts/check-agent-runtime-parity.ts, src/app/api/agent/route.ts, src/lib/agent/config.ts</read_first>
  <action>Create `docs/agent-runtime-parity.md` with a concrete operator workflow: expected headers, the `npm run agent:parity -- --url ... --expected-release ...` command shape, why the request is safe, and how to read mismatches. Update `docs/architecture-overview.md`, `docs/openai/README.md`, and `docs/INDEX.md` so the runtime provenance and parity workflow are discoverable from the existing agent architecture and model-selection docs.</action>
  <verify>powershell -NoProfile -Command \"$help = npx tsx scripts/check-agent-runtime-parity.ts --help 2>&1 | Out-String; if ($help -notmatch 'expected-release') { exit 1 }; $files = 'docs/agent-runtime-parity.md','docs/architecture-overview.md','docs/openai/README.md','docs/INDEX.md'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \\\"`n\\\"; foreach ($target in @('X-Agent-Release','agent:parity','/api/agent','OPENAI_DIALOG_MODEL')) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0\"</verify>
  <acceptance_criteria>
    - a dedicated parity runbook exists and names the exact header contract
    - the docs explain how expected model values relate to the runtime config contract
    - the architecture and docs index pages link to the new parity workflow
    - operators can copy one documented command to confirm a deployment after rollout
  </acceptance_criteria>
  <done>The deployed agent parity contract is documented, discoverable, and runnable by operators.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx tsx scripts/check-agent-runtime-parity.ts --help`
- [ ] `powershell -NoProfile -Command \"$help = npx tsx scripts/check-agent-runtime-parity.ts --help 2>&1 | Out-String; if ($help -notmatch 'expected-release') { exit 1 }; $files = 'docs/agent-runtime-parity.md','docs/architecture-overview.md','docs/openai/README.md','docs/INDEX.md'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \\\"`n\\\"; foreach ($target in @('X-Agent-Release','agent:parity','/api/agent','OPENAI_DIALOG_MODEL')) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0\"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The team now has a safe documented way to prove a deployed `/api/agent` route matches the intended release and model contract
  </success_criteria>

<output>
After completion, create `.planning/phases/05-deployed-agent-parity-and-evidence/05-02-SUMMARY.md`
</output>

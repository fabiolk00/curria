---
phase: 05-deployed-agent-parity-and-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/runtime/release-metadata.ts
  - src/app/api/agent/route.ts
  - src/lib/agent/agent-loop.ts
autonomous: true
requirements:
  - OPS-04
  - OPS-05
user_setup: []
must_haves:
  truths:
    - "A live `/api/agent` response exposes safe server-derived provenance even when the request fails before the SSE loop starts."
    - "Route and completed-turn logs carry release provenance keyed by `requestId`, so operators can tie a production incident to a specific deploy."
    - "Release provenance is derived from explicit server metadata fields and never echoes secrets, raw env blobs, cookies, or arbitrary request content."
  artifacts:
    - path: "src/lib/runtime/release-metadata.ts"
      provides: "Canonical safe release metadata for route headers and structured logs"
      contains: "getAgentReleaseMetadata"
    - path: "src/app/api/agent/route.ts"
      provides: "Release-aware `/api/agent` headers and request lifecycle logging"
      contains: "X-Agent-Release"
    - path: "src/lib/agent/agent-loop.ts"
      provides: "Completed-turn logs that include release provenance alongside model and recovery data"
      contains: "releaseId"
  key_links:
    - from: "src/lib/runtime/release-metadata.ts"
      to: "src/app/api/agent/route.ts"
      via: "shared provenance helper for every response path"
      pattern: "X-Agent-Release"
    - from: "src/lib/runtime/release-metadata.ts"
      to: "src/lib/agent/agent-loop.ts"
      via: "shared release metadata on request-correlated logs"
      pattern: "releaseSource"
    - from: "src/app/api/agent/route.ts"
      to: "src/lib/agent/agent-loop.ts"
      via: "requestId joins response provenance with turn-completion logs"
      pattern: "requestId"
---

<objective>
Add canonical server-derived provenance to the live `/api/agent` route and its request-correlated logs.

Purpose: Phase 5 starts by making every important `/api/agent` request self-identifying so production incidents can be tied to a real deploy before deeper behavior fixes land.
Output: A shared release-metadata helper, provenance headers on `/api/agent`, and request or turn logs that carry the same release identity alongside model and recovery fields.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployed-agent-parity-and-evidence/05-RESEARCH.md
@.planning/phases/05-deployed-agent-parity-and-evidence/05-VALIDATION.md
@.planning/research/SUMMARY.md
@AGENTS.md
@next.config.js
@package.json
@src/app/api/agent/route.ts
@src/lib/agent/agent-loop.ts
@src/lib/agent/config.ts
@src/lib/observability/structured-log.ts
@src/app/api/agent/route.test.ts
@src/app/api/agent/route.model-selection.test.ts
@src/app/api/agent/route.sse.test.ts
</context>

<threat_model>
<trust_boundaries>
- Server-side build and env metadata -> public route headers and structured logs
- Unauthenticated or validation-fail `/api/agent` requests -> safe provenance surface
- Request-correlated logs -> operator incident analysis
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-05-01</threat_id>
  <category>Repudiation</category>
  <component>Agent runtime provenance</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Emit a stable server-derived release identifier and source on route responses and request-correlated logs so operators can prove which deploy served a request.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-05-02</threat_id>
  <category>Tampering</category>
  <component>Release metadata</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Derive release metadata only from trusted server env or build fields and never from request payloads, cookies, or user-controlled headers.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-05-03</threat_id>
  <category>Information Disclosure</category>
  <component>Parity headers and logs</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Expose only safe scalars such as short release id, source label, and resolved model names; do not serialize raw env values or secrets.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create a canonical safe release-metadata helper</name>
  <files>src/lib/runtime/release-metadata.ts</files>
  <read_first>next.config.js, package.json, src/lib/agent/config.ts, src/lib/observability/structured-log.ts</read_first>
  <action>Create `src/lib/runtime/release-metadata.ts` with a single server-side helper such as `getAgentReleaseMetadata()` that resolves a safe release identity from explicit env or build sources. Support concrete inputs like `VERCEL_GIT_COMMIT_SHA`, `VERCEL_GIT_COMMIT_REF`, `VERCEL_ENV`, and a local-dev fallback. Return only flat scalar fields such as `releaseId`, `releaseSource`, `commitShortSha`, `resolvedAgentModel`, and `resolvedDialogModel` so the result can be reused in both headers and structured logs without leaking secrets.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `getAgentReleaseMetadata()` returns only safe scalar fields
    - release identity prefers explicit server metadata and falls back deterministically for local development
    - no raw env object, cookie, token, or full secret value is returned
  </acceptance_criteria>
  <done>There is one canonical source of safe release metadata for the agent runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Attach provenance to `/api/agent` responses and request-correlated logs</name>
  <files>src/app/api/agent/route.ts, src/lib/agent/agent-loop.ts</files>
  <read_first>src/app/api/agent/route.ts, src/lib/agent/agent-loop.ts, src/lib/runtime/release-metadata.ts, src/lib/observability/structured-log.ts, src/app/api/agent/route.test.ts, src/app/api/agent/route.model-selection.test.ts, src/app/api/agent/route.sse.test.ts</read_first>
  <action>Update `/api/agent` to reuse the new release-metadata helper and attach stable headers such as `X-Agent-Release`, `X-Agent-Release-Source`, `X-Agent-Resolved-Agent-Model`, and `X-Agent-Resolved-Dialog-Model` on every safe response path, including 401, 400, 402, 404, 429, and SSE success responses. Extend the route and agent-loop structured logs so `agent.request.received`, `agent.session.loaded`, `agent.session.created`, `agent.turn.completed`, `agent.response.truncated_after_recovery`, `agent.response.empty_fallback`, and `agent.stream.completed` all carry the same release identity plus the existing model or recovery fields via `requestId` correlation.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `/api/agent` returns the same provenance header contract on both stream and non-stream responses
    - completed-turn and fallback logs include release provenance in addition to the existing model and recovery fields
    - the response and log contract is derived only from the shared helper
    - no auth, billing, or session behavior regresses while adding provenance
  </acceptance_criteria>
  <done>Operators can identify which deployed agent runtime served a request by inspecting the response or correlated logs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `/api/agent` responses and request-correlated logs now expose safe deploy provenance that can be tied to a specific runtime build
  </success_criteria>

<output>
After completion, create `.planning/phases/05-deployed-agent-parity-and-evidence/05-01-SUMMARY.md`
</output>

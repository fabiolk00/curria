---
phase: 06-dialog-continuity-and-model-routing-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/agent-loop.ts
  - src/lib/agent/streaming-loop.ts
autonomous: true
requirements:
  - AGNT-01
  - AGNT-03
user_setup: []
must_haves:
  truths:
    - "A terse `dialog` follow-up such as `reescreva`, `continue`, or `pode fazer` returns either concrete rewrite text or a short non-repetitive continuation response."
    - "Recovery and deterministic fallback prefer the latest rewrite intent and the latest pasted vacancy over stale bootstrap copy."
    - "Repeated truncation or empty-response recovery preserves the best visible text whenever that is more useful than falling back."
  artifacts:
    - path: "src/lib/agent/agent-loop.ts"
      provides: "Continuity-aware recovery, fallback selection, and latest-intent preservation for dialog turns"
      contains: "buildDialogFallback"
    - path: "src/lib/agent/streaming-loop.ts"
      provides: "Stable public export surface for the hardened loop"
      contains: "runAgentLoop"
  key_links:
    - from: "src/lib/agent/agent-loop.ts"
      to: "src/app/api/agent/route.ts"
      via: "the route streams whichever recovery or fallback text the loop finally selects"
      pattern: "runAgentLoop"
    - from: "src/lib/agent/agent-loop.ts"
      to: "src/lib/agent/config.ts"
      via: "continuity changes must preserve the existing per-phase model-selection hook"
      pattern: "resolveAgentModelForPhase"
---

<objective>
Tighten dialog continuity so rewrite follow-ups continue usefully instead of repeating the earlier vacancy bootstrap.

Purpose: The live `reescreva` incident is a dialog-turn continuity failure after truncation and concise recovery. Phase 6 starts by fixing the loop behavior before transcript-level verification in Phase 7.
Output: A hardened dialog recovery path in `agent-loop.ts` that preserves the latest rewrite intent, the latest target-job context, and the best visible text.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-RESEARCH.md
@.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-VALIDATION.md
@.planning/phases/05-deployed-agent-parity-and-evidence/05-VERIFICATION.md
@AGENTS.md
@src/lib/agent/agent-loop.ts
@src/lib/agent/streaming-loop.ts
@src/lib/agent/config.ts
@src/app/api/agent/route.ts
@src/lib/agent/streaming-loop.test.ts
@src/app/api/agent/route.sse.test.ts
@src/lib/agent/__tests__/streaming-prompt-regression.test.ts
</context>

<threat_model>
<trust_boundaries>
- Persisted session state and saved target-job context -> recovery prompts and deterministic fallbacks
- Latest user message -> continuity classifier for terse rewrite requests and newly pasted vacancies
- Truncated or empty model output -> final visible assistant text
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-06-01</threat_id>
  <category>Tampering</category>
  <component>Dialog continuity classifier</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Favor the latest rewrite intent and latest pasted vacancy over stale saved bootstrap assumptions when the turn degrades.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-06-02</threat_id>
  <category>Denial of Service</category>
  <component>Truncation and empty-response recovery</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Preserve the strongest visible text and only emit deterministic fallback when repeated recovery attempts still cannot produce a useful response.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-06-03</threat_id>
  <category>Repudiation</category>
  <component>Fallback observability</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep the existing fallback-kind and recovery logging contract intact while refining the text-selection logic.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Preserve latest rewrite intent in dialog recovery and fallback selection</name>
  <files>src/lib/agent/agent-loop.ts</files>
  <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/config.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/streaming-loop.test.ts</read_first>
  <action>Refine the dialog continuity logic in `src/lib/agent/agent-loop.ts` so terse imperatives like `reescreva`, `continue`, `pode fazer`, and section-specific follow-ups are treated as explicit rewrite continuation intent when resume context is already loaded. Keep the latest pasted vacancy higher priority than saved target-job context, and make the continuation text explicitly move the user toward a concrete rewrite target instead of repeating the saved-vacancy bootstrap copy.</action>
  <verify>npm test -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.sse.test.ts</verify>
  <acceptance_criteria>
    - terse dialog follow-ups no longer resolve to the earlier vacancy bootstrap text
    - the latest pasted vacancy still wins over stale saved target context
    - continuation text tells the user what will be rewritten next or which section can be rewritten now
  </acceptance_criteria>
  <done>Dialog recovery preserves what the user just asked for instead of falling back to stale bootstrap messaging.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the best visible text through repeated degradation</name>
  <files>src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.ts</files>
  <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.ts, src/lib/agent/streaming-loop.test.ts, src/lib/agent/__tests__/streaming-prompt-regression.test.ts</read_first>
  <action>Tighten the merge rules around `recoverTruncatedTurn(...)`, `recoverConciseTurn(...)`, and the final deterministic fallback so the loop keeps the strongest visible rewrite text when possible, avoids appending or replacing it with duplicate bootstrap copy, and only chooses deterministic fallback when the visible text is absent or clearly less useful than the phase-aware continuation. Preserve the existing Phase 5 observability fields while making this selection logic explicit.</action>
  <verify>npm test -- src/lib/agent/streaming-loop.test.ts src/lib/agent/__tests__/streaming-prompt-regression.test.ts</verify>
  <acceptance_criteria>
    - repeated truncation or empty-response recovery keeps the best visible text when it is still useful
    - deterministic fallback no longer overwrites useful partial rewrite text with stale vacancy copy
    - `model`, `assistantTextChars`, `usedLengthRecovery`, `usedConciseRecovery`, and `fallbackKind` remain available on the affected log events
  </acceptance_criteria>
  <done>The loop degrades gracefully under truncation instead of regressing into repeated or stale assistant text.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.sse.test.ts src/lib/agent/__tests__/streaming-prompt-regression.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dialog follow-ups now continue usefully under degraded paths instead of repeating stale vacancy bootstrap messaging
  </success_criteria>

<output>
After completion, create `.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-01-SUMMARY.md`
</output>

---
phase: 06-dialog-continuity-and-model-routing-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/lib/agent/config.test.ts
  - src/lib/agent/streaming-loop.test.ts
  - src/lib/agent/__tests__/streaming-prompt-regression.test.ts
  - src/app/api/agent/route.model-selection.test.ts
  - src/app/api/agent/route.sse.test.ts
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
user_setup: []
must_haves:
  truths:
    - "Automated coverage proves terse dialog follow-ups no longer repeat the earlier vacancy bootstrap."
    - "Loop and route tests prove the latest pasted vacancy and the latest rewrite intent survive degraded recovery paths."
    - "Fresh-import route tests prove both override and no-override model selection for dialog and confirm."
  artifacts:
    - path: "src/lib/agent/streaming-loop.test.ts"
      provides: "Loop-level regressions for truncation, empty-response recovery, fallbackKind, and latest-intent preservation"
      contains: "dialog"
    - path: "src/app/api/agent/route.sse.test.ts"
      provides: "Real route SSE proof for final visible assistant text under degraded dialog flows"
      contains: "reescreva"
    - path: "src/app/api/agent/route.model-selection.test.ts"
      provides: "Fresh-import route proof for dialog and confirm model selection with and without overrides"
      contains: "OPENAI_DIALOG_MODEL"
    - path: "src/lib/agent/config.test.ts"
      provides: "Env-import proof for the resolver contract the route depends on"
      contains: "MODEL_CONFIG.dialogModel"
  key_links:
    - from: "src/lib/agent/streaming-loop.test.ts"
      to: "src/lib/agent/agent-loop.ts"
      via: "loop regressions lock the final continuity behavior selected by recovery and fallback code"
      pattern: "fallbackKind"
    - from: "src/app/api/agent/route.sse.test.ts"
      to: "src/app/api/agent/route.ts"
      via: "route tests prove the final streamed assistant output is coherent for one request"
      pattern: "text/event-stream"
    - from: "src/app/api/agent/route.model-selection.test.ts"
      to: "src/lib/agent/config.ts"
      via: "fresh-import route tests prove the config contract reaches OpenAI"
      pattern: "resolveAgentModelForPhase"
---

<objective>
Lock the Phase 6 continuity and model-routing behavior in with targeted regressions.

Purpose: After the loop and config contracts are corrected, the final step is to make sure terse rewrite follow-ups, degraded recovery, and dialog or confirm model selection cannot silently drift back.
Output: Focused loop, route SSE, route model-selection, and config regressions for Phase 6.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-RESEARCH.md
@.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-VALIDATION.md
@AGENTS.md
@src/lib/agent/agent-loop.ts
@src/lib/agent/config.ts
@src/lib/agent/config.test.ts
@src/lib/agent/streaming-loop.test.ts
@src/lib/agent/__tests__/streaming-prompt-regression.test.ts
@src/app/api/agent/route.ts
@src/app/api/agent/route.model-selection.test.ts
@src/app/api/agent/route.sse.test.ts
</context>

<threat_model>
<trust_boundaries>
- Regression fixtures -> real loop and real route behavior
- Fresh env imports -> model-selection contract
- Degraded model output -> final route SSE transcript
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-06-01</threat_id>
  <category>Tampering</category>
  <component>Dialog continuity regressions</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Lock the latest-intent and latest-vacancy behavior with loop and route tests so stale bootstrap copy cannot silently return.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-06-03</threat_id>
  <category>Repudiation</category>
  <component>Recovery diagnostics</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Assert the fallback-kind and degraded-path behavior in tests so operators can trust the logged and visible outcome.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-06-05</threat_id>
  <category>Repudiation</category>
  <component>Model-routing regressions</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Use fresh-import route and config tests to prove both override and no-override model selection on the real route seam.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend continuity regressions for terse rewrite follow-ups and degraded recovery</name>
  <files>src/lib/agent/streaming-loop.test.ts, src/lib/agent/__tests__/streaming-prompt-regression.test.ts, src/app/api/agent/route.sse.test.ts</files>
  <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.test.ts, src/lib/agent/__tests__/streaming-prompt-regression.test.ts, src/app/api/agent/route.sse.test.ts</read_first>
  <action>Extend the loop and real-route SSE suites to cover the Phase 6 incident shape directly: terse rewrite follow-ups such as `reescreva`, continuation approvals, latest pasted vacancy replacement during `dialog`, and repeated truncation or empty-response recovery. Assert both the final visible assistant text and the fallback or recovery metadata needed to explain that outcome.</action>
  <verify>npm test -- src/lib/agent/streaming-loop.test.ts src/lib/agent/__tests__/streaming-prompt-regression.test.ts src/app/api/agent/route.sse.test.ts</verify>
  <acceptance_criteria>
    - route or loop tests explicitly cover the `reescreva` class of follow-up
    - latest pasted vacancy context is proven to replace stale saved target context during degraded dialog turns
    - degraded recovery tests assert both the final assistant text and the relevant fallback or recovery fields
  </acceptance_criteria>
  <done>Phase 6 continuity behavior is protected by committed route and loop regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Lock route-level model-selection proof for both override and fallback paths</name>
  <files>src/lib/agent/config.test.ts, src/app/api/agent/route.model-selection.test.ts</files>
  <read_first>src/lib/agent/config.ts, src/lib/agent/config.test.ts, src/app/api/agent/route.model-selection.test.ts, src/app/api/agent/route.ts</read_first>
  <action>Extend the config and route model-selection suites so they prove the remaining Phase 6 seams end to end: fresh-import route behavior when `OPENAI_DIALOG_MODEL` is present, fresh-import route behavior when it is absent and dialog or confirm should inherit the resolved agent model, and helper-level env import coverage that matches the route contract exactly.</action>
  <verify>npm test -- src/lib/agent/config.test.ts src/app/api/agent/route.model-selection.test.ts</verify>
  <acceptance_criteria>
    - fresh-import route tests cover dialog and confirm with explicit override
    - fresh-import route tests cover dialog and confirm inheriting the resolved agent model when the override is absent
    - config tests prove the same contract as the route-level assertions instead of a looser helper-only assumption
  </acceptance_criteria>
  <done>Phase 6 finishes with explicit automated proof for both the continuity behavior and the model-routing contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/agent/config.test.ts src/lib/agent/streaming-loop.test.ts src/lib/agent/__tests__/streaming-prompt-regression.test.ts src/app/api/agent/route.model-selection.test.ts src/app/api/agent/route.sse.test.ts`
- [ ] `node .codex/get-shit-done/bin/gsd-tools.cjs state validate`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 continuity and model-routing behavior is protected by focused automated regressions before transcript-level work begins in Phase 7
  </success_criteria>

<output>
After completion, create `.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-03-SUMMARY.md`
</output>

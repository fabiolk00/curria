---
phase: 18-file-access-storage-ownership-and-rls-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/file/[sessionId]/route.ts
  - src/app/api/file/[sessionId]/route.test.ts
  - src/app/api/session/[id]/route.ts
  - src/app/api/session/[id]/route.test.ts
autonomous: true
requirements:
  - SEC-03
  - SEC-04
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "Session and file access routes prove cross-user and cross-target requests fail closed."
    - "Signed URL delivery remains downstream of ownership checks instead of acting as the authorization layer."
    - "Route-level proofs cover both base artifacts and target artifacts."
  artifacts:
    - path: "src/app/api/file/[sessionId]/route.test.ts"
      provides: "Route-level proof for owned vs foreign artifact access"
      contains: "rejects non-owners"
    - path: "src/app/api/session/[id]/route.test.ts"
      provides: "Workspace snapshot proof that session reads stay ownership-aware"
      contains: "Not found"
---

<objective>
Strengthen and re-prove the route-level ownership contract for session and file access before reasoning about storage or RLS below the app layer.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-18-file-access-storage-ownership-and-rls-proof/18-CONTEXT.md
@AGENTS.md
@src/app/api/file/[sessionId]/route.ts
@src/app/api/file/[sessionId]/route.test.ts
@src/app/api/session/[id]/route.ts
@src/app/api/session/[id]/route.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Re-prove file and target artifact ownership boundaries</name>
  <files>src/app/api/file/[sessionId]/route.ts, src/app/api/file/[sessionId]/route.test.ts</files>
  <read_first>src/app/api/file/[sessionId]/route.ts, src/app/api/file/[sessionId]/route.test.ts, docs/operations/security-boundary-audit.md</read_first>
  <action>Audit the file-download route and tighten only the seams needed so owner, non-owner, target mismatch, and stale artifact scenarios are explicitly proven. Add or refine regressions only where the current route proof leaves ambiguity about whether signed URLs can be requested without prior ownership.</action>
  <verify>pnpm vitest run "src/app/api/file/[sessionId]/route.test.ts"</verify>
  <done>The file-download route has committed negative-path proof for ownership and target-bound access.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfirm session snapshot reads as ownership-gated API state</name>
  <files>src/app/api/session/[id]/route.ts, src/app/api/session/[id]/route.test.ts</files>
  <read_first>src/app/api/session/[id]/route.ts, src/app/api/session/[id]/route.test.ts, docs/state-model.md</read_first>
  <action>Review the session snapshot route so it stays clearly fail-closed for unauthorized or foreign-session access. Add focused regression proof only if the current suite does not fully show the route boundary for session-plus-target workspace data.</action>
  <verify>pnpm vitest run "src/app/api/session/[id]/route.test.ts"</verify>
  <done>The workspace snapshot path remains explicitly ownership-aware and does not imply storage-level guarantees.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm tsc --noEmit`
- [ ] `pnpm vitest run "src/app/api/file/[sessionId]/route.test.ts" "src/app/api/session/[id]/route.test.ts"`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-18-file-access-storage-ownership-and-rls-proof/18-01-SUMMARY.md`
</output>

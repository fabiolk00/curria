---
phase: 10-target-job-rewrite-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/agent/route.ts
  - src/types/agent.ts
  - src/lib/db/sessions.ts
  - src/lib/agent/job-targeting-retry.ts
  - src/app/api/agent/route.sse.test.ts
  - src/app/api/session/[id]/route.ts
  - src/app/api/session/[id]/route.test.ts
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "Resume plus target-job context deterministically resolves `workflowMode = job_targeting`."
    - "The backend never stops at gap analysis when the user goal is target-job adaptation."
    - "Target-job rewrite state is explicit in session state and visible to workspace consumers."
    - "Detected target-job persistence is proven to survive the insertion of the deterministic rewrite pipeline."
  artifacts:
    - path: "src/app/api/agent/route.ts"
      provides: "Deterministic job-targeting orchestration seam after gap analysis"
      contains: "job_targeting"
    - path: "src/lib/agent/job-targeting-retry.ts"
      provides: "Explicit retry ceilings and failure-budget helpers for target-job pipeline stages"
      contains: "MAX_JOB_TARGETING_STAGE_RETRIES"
    - path: "src/types/agent.ts"
      provides: "Explicit target-job planning and rewrite state contracts"
      contains: "targetingPlan"
    - path: "src/app/api/session/[id]/route.ts"
      provides: "Workspace snapshot exposure for target-job rewrite state"
      contains: "agentState"
---

<objective>
Formalize the target-job rewrite workflow so resume-plus-vacancy sessions always move beyond fit analysis into deterministic rewrite orchestration.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-10-target-job-rewrite-pipeline/10-CONTEXT.md
@AGENTS.md
@src/app/api/agent/route.ts
@src/types/agent.ts
@src/lib/db/sessions.ts
@src/app/api/session/[id]/route.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Make job_targeting an explicit workflow contract with targeting state</name>
  <files>src/types/agent.ts, src/lib/db/sessions.ts, src/app/api/session/[id]/route.ts</files>
  <read_first>src/types/agent.ts, src/lib/db/sessions.ts, src/app/api/session/[id]/route.ts</read_first>
  <action>Extend session state for the target-job rewrite lifecycle: explicit `targetingPlan`, rewrite metadata, and any target-job optimization summary fields needed for persistence and snapshot exposure. Keep the current small-phase approach based on `workflowMode` and rewrite state.</action>
  <verify>npm test -- src/app/api/session/[id]/route.test.ts</verify>
  <done>Target-job rewrite state is explicit, persisted, and visible through the session contract.</done>
</task>

<task type="auto">
  <name>Task 2: Make gap analysis an intermediate step in the automatic job-targeting flow</name>
  <files>src/app/api/agent/route.ts, src/lib/agent/job-targeting-retry.ts, src/app/api/agent/route.sse.test.ts</files>
  <read_first>src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/agent-loop.ts</read_first>
  <action>Update the route so resume-plus-target-job requests always continue from target detection and gap analysis into the job-targeting rewrite pipeline. Preserve the existing target detection and gap-analysis behavior, but remove any path where the backend treats gap analysis as the terminal result when the user goal is adaptation. Introduce an explicit target-job retry helper or equivalent seam that defines retry ceilings and a small failure budget for imperative target-job stages, and add a regression proving detected target-job data is still persisted while the new rewrite pipeline runs.</action>
  <verify>npm test -- src/app/api/agent/route.sse.test.ts</verify>
  <done>The target-job path is deterministic and no longer stops at fit analysis alone.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/app/api/agent/route.sse.test.ts src/app/api/session/[id]/route.test.ts`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-10-target-job-rewrite-pipeline/10-01-SUMMARY.md`
</output>

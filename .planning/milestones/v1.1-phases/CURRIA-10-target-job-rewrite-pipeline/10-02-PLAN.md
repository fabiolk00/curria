---
phase: 10-target-job-rewrite-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/lib/agent/tools/build-targeting-plan.ts
  - src/lib/agent/tools/rewrite-resume-full.ts
  - src/lib/agent/tools/validate-rewrite.ts
  - src/lib/agent/job-targeting-retry.ts
  - src/lib/agent/tools/pipeline.test.ts
  - src/app/api/profile/ats-enhancement/route.test.ts
  - src/types/agent.ts
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "Gap analysis feeds an explicit targeting plan before the rewrite starts."
    - "The target-job rewrite rewrites the full supported resume structure, not only summary or bullets."
    - "Validation blocks fake alignment and invented fit while preserving facts."
    - "Target-job rewrite cost is bounded by explicit retry ceilings, payload shaping, and at least one long-input stress fixture."
  artifacts:
    - path: "src/lib/agent/tools/build-targeting-plan.ts"
      provides: "Explicit intermediate targeting plan derived from cvState, target job, and gap analysis"
      contains: "buildTargetingPlan"
    - path: "src/lib/agent/tools/rewrite-resume-full.ts"
      provides: "Shared full rewrite service extended for mode job_targeting"
      contains: "job_targeting"
    - path: "src/lib/agent/tools/validate-rewrite.ts"
      provides: "Validation that rejects fabricated experience or target-fit inflation"
      contains: "validateRewrite"
---

<objective>
Implement the target-job-specific rewrite stack: targeting plan, deterministic full rewrite, and factual validation.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/CURRIA-10-target-job-rewrite-pipeline/10-CONTEXT.md
@AGENTS.md
@src/lib/agent/tools/rewrite-resume-full.ts
@src/lib/agent/tools/build-rewrite-plan.ts
@src/lib/agent/tools/validate-rewrite.ts
@src/lib/agent/tools/pipeline.test.ts
@src/types/agent.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add a targeting-plan artifact that turns gap analysis into rewrite input</name>
  <files>src/lib/agent/tools/build-targeting-plan.ts, src/types/agent.ts</files>
  <read_first>src/types/agent.ts, src/lib/agent/tools/rewrite-resume-full.ts, src/app/api/agent/route.ts</read_first>
  <action>Create `buildTargetingPlan(...)` and a `TargetingPlan` contract that derives target-role emphasis, de-emphasis, missing-but-cannot-invent gaps, and section strategy from the original resume plus `targetJobDescription` and `gapAnalysis`.</action>
  <verify>npm test -- src/lib/agent/tools/pipeline.test.ts</verify>
  <done>The job-targeting flow has an auditable intermediate plan instead of using gap analysis alone.</done>
</task>

<task type="auto">
  <name>Task 2: Extend full-resume rewriting for mode job_targeting</name>
  <files>src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/job-targeting-retry.ts, src/lib/agent/tools/pipeline.test.ts</files>
  <read_first>src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/tools/build-rewrite-plan.ts, src/lib/agent/tools/rewrite-section.ts</read_first>
  <action>Extend the shared full rewrite service so `mode: 'job_targeting'` uses the targeting plan to rewrite summary, experience, skills, education, and certifications with target-role-specific guidance. Reprioritize and reword based only on facts already present in the resume. Apply explicit retry ceilings plus payload-shaping rules for oversized job descriptions or long experience sections, and add at least one stress fixture that proves the target-job rewrite stays bounded under long-input conditions.</action>
  <verify>npm test -- src/lib/agent/tools/pipeline.test.ts</verify>
  <done>The shared rewrite engine can produce a deterministic full target-job resume adaptation.</done>
</task>

<task type="auto">
  <name>Task 3: Expand validation to block fake target-role alignment</name>
  <files>src/lib/agent/tools/validate-rewrite.ts, src/lib/agent/tools/pipeline.test.ts</files>
  <read_first>src/lib/agent/tools/validate-rewrite.ts, src/lib/agent/tools/pipeline.test.ts, src/lib/agent/tools/rewrite-resume-full.ts</read_first>
  <action>Strengthen validation so it rejects not only invented facts, but also exaggerated target-role alignment unsupported by experience, skills, or certifications. Ensure gaps can still remain visible instead of being artificially erased by the rewrite.</action>
  <verify>npm test -- src/lib/agent/tools/pipeline.test.ts</verify>
  <done>The target-job rewrite cannot fake a match by inventing or overstating fit.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/agent/tools/pipeline.test.ts`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-10-target-job-rewrite-pipeline/10-02-SUMMARY.md`
</output>

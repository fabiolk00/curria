---
phase: 10-target-job-rewrite-pipeline
plan: "03"
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/app/api/agent/route.ts
  - src/lib/agent/job-targeting-observability.ts
  - src/lib/db/cv-versions.ts
  - src/lib/agent/tools/index.ts
  - src/app/api/file/[sessionId]/route.ts
  - src/app/api/file/[sessionId]/route.test.ts
  - src/app/api/session/[id]/route.ts
  - src/app/api/session/[id]/route.test.ts
  - src/lib/dashboard/workspace-client.ts
  - src/lib/agent/tools/pipeline.test.ts
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "Successful job-targeting rewrites persist a distinct targeted version linked to the target job."
    - "Preview and export use the targeted optimized source when present."
    - "Workspace and download consumers can distinguish targeted optimized state from generic ATS enhancement."
    - "Job-targeting runs emit structured logs keyed by workflow and stage so operators can diagnose cost and failure points."
  artifacts:
    - path: "src/lib/agent/job-targeting-observability.ts"
      provides: "Stable structured log payload helpers for deterministic job-targeting runs"
      contains: "job_targeting"
    - path: "src/lib/db/cv-versions.ts"
      provides: "Target-job version metadata and labels"
      contains: "job_targeting"
    - path: "src/lib/agent/tools/index.ts"
      provides: "Effective source selection preferring target-job optimized state when appropriate"
      contains: "optimizedCvState"
    - path: "src/app/api/file/[sessionId]/route.test.ts"
      provides: "Real export-edge regression for targeted optimized versions"
      contains: "target"
---

<objective>
Persist target-job rewrites as first-class versions and route preview or export through the targeted optimized source.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/CURRIA-10-target-job-rewrite-pipeline/10-CONTEXT.md
@AGENTS.md
@src/lib/db/cv-versions.ts
@src/lib/agent/tools/index.ts
@src/app/api/file/[sessionId]/route.ts
@src/app/api/session/[id]/route.ts
@src/lib/dashboard/workspace-client.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Persist job-targeting versions with target-linked metadata</name>
  <files>src/lib/db/cv-versions.ts, src/app/api/agent/route.ts</files>
  <read_first>src/lib/db/cv-versions.ts, src/app/api/agent/route.ts, prisma/schema.prisma</read_first>
  <action>Persist successful target-job rewrites as first-class versions with metadata that distinguishes them from generic ATS enhancement, including target-linked identifiers or hashes where the current persistence seam allows it.</action>
  <verify>npm test -- src/lib/agent/tools/pipeline.test.ts</verify>
  <done>Target-job rewrites exist as versioned, auditable artifacts linked to their target context.</done>
</task>

<task type="auto">
  <name>Task 2: Add structured job_targeting observability keyed by workflow and stage</name>
  <files>src/lib/agent/job-targeting-observability.ts, src/app/api/agent/route.ts</files>
  <read_first>src/app/api/agent/route.ts, src/lib/observability/structured-log.ts, src/lib/agent/ats-enhancement-pipeline.ts</read_first>
  <action>Introduce structured `job_targeting` logs around target detection persistence, gap analysis completion, targeting-plan generation, full rewrite, validation, and target-linked version persistence. Reuse the same logging discipline established for ATS enhancement so operators can trace stage, retry behavior, and terminal outcome by `workflowMode`.</action>
  <verify>npm test -- src/lib/agent/tools/pipeline.test.ts</verify>
  <done>Job-targeting runs are observable and diagnosable with stable workflow-stage logs.</done>
</task>

<task type="auto">
  <name>Task 3: Make preview and export prefer targeted optimized state</name>
  <files>src/lib/agent/tools/index.ts, src/app/api/file/[sessionId]/route.ts, src/app/api/file/[sessionId]/route.test.ts</files>
  <read_first>src/lib/agent/tools/index.ts, src/app/api/file/[sessionId]/route.ts, src/app/api/file/[sessionId]/route.test.ts</read_first>
  <action>Update source selection and the real download edge so targeted optimized resumes become the effective preview/export source when they exist and pass validation, while preserving current target-derived and ATS-enhancement behavior.</action>
  <verify>npm test -- src/app/api/file/[sessionId]/route.test.ts</verify>
  <done>Preview and export resolve from the correct target-job optimized version.</done>
</task>

<task type="auto">
  <name>Task 4: Expose target-job optimized state through workspace consumers</name>
  <files>src/app/api/session/[id]/route.ts, src/app/api/session/[id]/route.test.ts, src/lib/dashboard/workspace-client.ts</files>
  <read_first>src/app/api/session/[id]/route.ts, src/app/api/session/[id]/route.test.ts, src/lib/dashboard/workspace-client.ts, src/types/dashboard.ts</read_first>
  <action>Expose the target-job rewrite state needed by workspace consumers so the session snapshot clearly communicates when a targeted optimized version exists and should drive preview or export. Include at least one regression that proves target detection or persisted target-job state remains intact alongside the new deterministic rewrite state.</action>
  <verify>npm test -- src/app/api/session/[id]/route.test.ts</verify>
  <done>The workspace can reason about target-job optimized state without bespoke extra calls.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/app/api/file/[sessionId]/route.test.ts src/app/api/session/[id]/route.test.ts src/lib/agent/tools/pipeline.test.ts`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-10-target-job-rewrite-pipeline/10-03-SUMMARY.md`
</output>

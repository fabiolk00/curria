--- phase: 28-agent-input-and-setup-service-extraction plan: "02" type: execute wave: 2 depends_on:   - 28-01 files_modified:   - src/app/api/agent/route.ts   - src/lib/agent/pre-loop-setup.ts   - src/lib/agent/agent-loop.ts autonomous: true requirements:   - AGENT-01 user_setup: [] must_haves:   truths:     - "Pre-loop setup becomes an explicit service boundary instead of route-local orchestration glue."     - "The route still emits `sessionCreated` before slow setup work for new sessions."     - "The setup service keeps `cvState` canonical and `agentState` operational while preserving current workflow-mode and target-context behavior."   artifacts:     - path: "src/lib/agent/pre-loop-setup.ts"       provides: "Explicit pre-loop setup orchestration service used by the route"       contains: "runPreLoopSetup"     - path: "src/app/api/agent/route.ts"       provides: "Thin brownfield route that delegates setup work before calling `runAgentLoop(...)`"       contains: "runAgentLoop"   key_links:     - from: "src/app/api/agent/route.ts"       to: "src/lib/agent/pre-loop-setup.ts"       via: "the route delegates session-aware setup before loop execution"       pattern: "runPreLoopSetup"     - from: "src/lib/agent/pre-loop-setup.ts"       to: "src/lib/agent/agent-loop.ts"       via: "setup hands prepared message and session context into the existing loop contract"       pattern: "runAgentLoop" ---  <objective> Extract and narrow pre-loop setup into an explicit service that keeps the current route-facing contract intact.  Purpose: After input and vacancy analysis are shared, the route can delegate new-session and existing-session setup through one clearer service boundary, making Phase 29's deeper runtime decomposition safer. Output: A dedicated pre-loop setup module used by the route before `runAgentLoop(...)`, with current SSE ordering and session behavior preserved. Dependency note: This plan follows `28-01` because both plans materially reshape `src/app/api/agent/route.ts`; keeping them parallel would create file-ownership conflict and risk drift between the shared vacancy-analysis contract and the extracted setup seam. </objective>  <execution_context> @C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md @C:/CurrIA/.codex/get-shit-done/templates/summary.md </execution_context>  <context> @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/REQUIREMENTS.md @.planning/phases/28-agent-input-and-setup-service-extraction/28-RESEARCH.md @.planning/phases/28-agent-input-and-setup-service-extraction/28-VALIDATION.md @AGENTS.md @src/app/api/agent/route.ts @src/lib/agent/agent-loop.ts @src/lib/agent/vacancy-analysis.ts @src/app/api/agent/route.test.ts @src/app/api/agent/route.sse.test.ts </context>  <threat_model> <trust_boundaries> - Session bootstrap state -> pre-loop setup orchestration for new and existing sessions - Prepared user message and detected target-job context -> route-to-loop handoff - Early SSE events -> user-visible ordering for `sessionCreated` and downstream streaming </trust_boundaries>  <stride_register> <threat>   <threat_id>T-28-04</threat_id>   <category>Tampering</category>   <component>Pre-loop setup extraction</component>   <disposition>mitigate</disposition>   <mitigation_plan>Keep the extracted setup contract semantically identical to the current helper, including how it mutates message, session context, and workflow decisions before the loop starts.</mitigation_plan> </threat> <threat>   <threat_id>T-28-05</threat_id>   <category>Denial of Service</category>   <component>Early SSE ordering</component>   <disposition>mitigate</disposition>   <mitigation_plan>Preserve the current route rule that `sessionCreated` is emitted before slow setup work on new sessions and do not insert extra blocking work before streaming begins.</mitigation_plan> </threat> <threat>   <threat_id>T-28-06</threat_id>   <category>Information Disclosure</category>   <component>Session-state boundaries</component>   <disposition>mitigate</disposition>   <mitigation_plan>Keep canonical resume truth in `cvState` and prevent the new setup service from creating duplicate durable state outside existing persistence seams.</mitigation_plan> </threat> </stride_register> </threat_model>  <tasks>  <task type="auto">   <name>Task 1: Move `runPreLoopSetup(...)` into a dedicated agent setup service</name>   <files>src/app/api/agent/route.ts, src/lib/agent/pre-loop-setup.ts</files>   <read_first>src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts, src/app/api/agent/route.test.ts, AGENTS.md</read_first>   <action>Extract the current `runPreLoopSetup(...)` helper out of `src/app/api/agent/route.ts` into a dedicated `src/lib/agent/pre-loop-setup.ts` module. Keep the existing inputs explicit, preserve current mutation points for new-session and existing-session setup, and ensure the route continues to delegate setup without re-implementing branching logic inline.</action>   <verify>npm run typecheck && npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts</verify>   <acceptance_criteria>     - `runPreLoopSetup(...)` no longer lives as a large route-local helper in `src/app/api/agent/route.ts`     - the route still delegates setup with explicit inputs instead of recreating the orchestration inline     - existing route tests covering pre-loop abort or setup-sensitive behavior remain green   </acceptance_criteria>   <done>The route calls an explicit setup service rather than owning the full pre-loop orchestration body.</done> </task>  <task type="auto">   <name>Task 2: Tighten the route-to-loop handoff contract after setup extraction</name>   <files>src/app/api/agent/route.ts, src/lib/agent/pre-loop-setup.ts, src/lib/agent/agent-loop.ts</files>   <read_first>src/app/api/agent/route.ts, src/lib/agent/agent-loop.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/streaming-loop.test.ts</read_first>   <action>Make the handoff from route to loop explicit after setup extraction: the route should pass the prepared message, session, request metadata, and signal into `runAgentLoop(...)` without hidden coupling. Keep `sessionCreated` timing, SSE headers, workflow prediction, and setup-driven message changes behaviorally identical to the current flow.</action>   <verify>npm run typecheck && npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts src/lib/agent/streaming-loop.test.ts</verify>   <acceptance_criteria>     - the route-to-loop call site remains easy to read and clearly separates route setup from loop execution     - new sessions still emit `sessionCreated` before slow setup work begins     - existing-session and new-session setup both preserve the current loop inputs and SSE startup behavior   </acceptance_criteria>   <done>The route hands off to `runAgentLoop(...)` through a stable and explicit post-setup contract.</done> </task>  </tasks>  <verification> Before declaring plan complete: - [ ] `npm run typecheck` - [ ] `npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts src/lib/agent/streaming-loop.test.ts` </verification>  <success_criteria>  - All tasks completed - All verification checks pass - No errors or warnings introduced - Pre-loop setup now lives behind a dedicated service and the route still preserves current early-streaming and session behavior </success_criteria>  <output> After completion, create `.planning/phases/28-agent-input-and-setup-service-extraction/28-02-SUMMARY.md` </output>
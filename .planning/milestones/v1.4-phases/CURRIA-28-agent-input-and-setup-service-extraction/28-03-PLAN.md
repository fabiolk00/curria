--- phase: 28-agent-input-and-setup-service-extraction plan: "03" type: execute wave: 3 depends_on:   - 28-01   - 28-02 files_modified:   - src/app/api/agent/route.test.ts   - src/app/api/agent/route.sse.test.ts   - src/lib/agent/streaming-loop.test.ts   - src/lib/agent/vacancy-analysis.test.ts   - src/lib/agent/pre-loop-setup.test.ts autonomous: true requirements:   - AGENT-01 user_setup: [] must_haves:   truths:     - "Phase 28 closes with committed regression proof around the extracted entry flow, not only green production code."     - "Tests lock the route-to-loop contract, shared vacancy heuristics, and setup extraction against brownfield regressions."     - "Later runtime decomposition can rely on these tests as the safety net for the front-half contract."   artifacts:     - path: "src/lib/agent/vacancy-analysis.test.ts"       provides: "Unit-level proof for the shared vacancy-analysis contract"       contains: "detectTargetJobDescription"     - path: "src/lib/agent/pre-loop-setup.test.ts"       provides: "Direct proof for the extracted pre-loop setup contract"       contains: "runPreLoopSetup"     - path: "src/app/api/agent/route.sse.test.ts"       provides: "Route-level proof that setup extraction preserves early streaming and SSE behavior"       contains: "sessionCreated"   key_links:     - from: "src/lib/agent/vacancy-analysis.test.ts"       to: "src/app/api/agent/route.ts"       via: "shared vacancy-analysis behavior is asserted independently from route wiring"       pattern: "detectTargetJobDescription"     - from: "src/app/api/agent/route.sse.test.ts"       to: "src/lib/agent/pre-loop-setup.ts"       via: "route-level tests prove setup delegation preserves stream startup behavior"       pattern: "sessionCreated" ---  <objective> Add regression proof for the extracted entry flow so Phase 28 ends with explicit protection around route input handling, shared vacancy analysis, and pre-loop setup behavior.  Purpose: The extraction work is only safe if the repo can prove that current `/api/agent` startup behavior and vacancy heuristics still hold after the code moves. Output: Focused unit and route-level regression coverage for the new front-half agent service boundaries, including direct proof for the extracted pre-loop setup service. </objective>  <execution_context> @C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md @C:/CurrIA/.codex/get-shit-done/templates/summary.md </execution_context>  <context> @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/REQUIREMENTS.md @.planning/phases/28-agent-input-and-setup-service-extraction/28-RESEARCH.md @.planning/phases/28-agent-input-and-setup-service-extraction/28-VALIDATION.md @AGENTS.md @src/app/api/agent/route.ts @src/lib/agent/pre-loop-setup.ts @src/lib/agent/vacancy-analysis.ts @src/lib/agent/pre-loop-setup.test.ts @src/lib/agent/streaming-loop.test.ts @src/app/api/agent/route.test.ts @src/app/api/agent/route.sse.test.ts </context>  <threat_model> <trust_boundaries> - Shared vacancy-analysis helpers -> route workflow and target-job behavior - Extracted pre-loop setup service -> early SSE and loop-entry behavior - Route-to-loop contract -> persisted session state and initial assistant streaming </trust_boundaries>  <stride_register> <threat>   <threat_id>T-28-07</threat_id>   <category>Repudiation</category>   <component>Regression coverage</component>   <disposition>mitigate</disposition>   <mitigation_plan>Commit targeted tests that prove the extracted boundaries preserve current behavior, so future runtime refactors can be audited through stable evidence.</mitigation_plan> </threat> <threat>   <threat_id>T-28-08</threat_id>   <category>Tampering</category>   <component>Vacancy-analysis contract</component>   <disposition>mitigate</disposition>   <mitigation_plan>Lock the shared detection behavior with unit tests for summarized vacancy, structured-role, and resume-only inputs before later phases reuse the helper more widely.</mitigation_plan> </threat> <threat>   <threat_id>T-28-09</threat_id>   <category>Denial of Service</category>   <component>Route startup and SSE ordering</component>   <disposition>mitigate</disposition>   <mitigation_plan>Keep route-level SSE coverage around `sessionCreated`, setup abort handling, and loop invocation so extractions do not silently degrade startup behavior.</mitigation_plan> </threat> </stride_register> </threat_model>  <tasks>  <task type="auto">   <name>Task 1: Add direct seam coverage for shared vacancy analysis and extracted pre-loop setup</name>   <files>src/lib/agent/vacancy-analysis.ts, src/lib/agent/vacancy-analysis.test.ts, src/lib/agent/pre-loop-setup.test.ts</files>   <read_first>src/lib/agent/vacancy-analysis.ts, src/lib/agent/pre-loop-setup.ts, src/lib/agent/agent-intents.ts, src/app/api/agent/route.test.ts, .planning/phases/28-agent-input-and-setup-service-extraction/28-RESEARCH.md</read_first>   <action>Create focused direct tests for the extracted front-half services. Keep unit coverage for the shared vacancy-analysis service around summarized vacancy detection, structured role descriptions, confidence preservation, and non-vacancy resume-only text. Add `src/lib/agent/pre-loop-setup.test.ts` to prove the extracted setup service preserves message-accounting behavior, keeps `cvState` canonical, keeps `agentState` operational, and does not reorder the existing setup decisions.</action>   <verify>npm run typecheck && npm test -- src/lib/agent/vacancy-analysis.test.ts src/lib/agent/pre-loop-setup.test.ts</verify>   <acceptance_criteria>     - the shared vacancy-analysis module has direct unit coverage for vacancy-shaped and non-vacancy-shaped inputs     - tests assert the same brownfield confidence or detection behavior expected by the route     - `src/lib/agent/pre-loop-setup.test.ts` directly proves the extracted setup contract without relying only on route wiring   </acceptance_criteria>   <done>The extracted vacancy-analysis and pre-loop setup services are protected by direct unit tests.</done> </task>  <task type="auto">   <name>Task 2: Extend route and loop regression tests around extracted setup flow</name>   <files>src/app/api/agent/route.test.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/streaming-loop.test.ts</files>   <read_first>src/app/api/agent/route.test.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/streaming-loop.test.ts, src/lib/agent/pre-loop-setup.ts, src/lib/agent/pre-loop-setup.test.ts</read_first>   <action>Extend the existing route and loop suites to prove the extracted entry flow still works end to end: new sessions emit `sessionCreated` before slow setup, setup delegation preserves abort handling and message transformation behavior, and the route-to-loop handoff still produces the same startup semantics after the service extractions from plans 28-01 and 28-02.</action>   <verify>npm run typecheck && npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts src/lib/agent/streaming-loop.test.ts src/lib/agent/vacancy-analysis.test.ts src/lib/agent/pre-loop-setup.test.ts</verify>   <acceptance_criteria>     - route-level tests explicitly cover setup delegation and early `sessionCreated` behavior after extraction     - loop-adjacent tests still validate the handoff assumptions relied on by the route entrypoint     - the final targeted phase suite is green with the new service boundaries in place, including direct proof for the extracted pre-loop setup service   </acceptance_criteria>   <done>Phase 28 closes with committed regression proof for the extracted front-half runtime contracts.</done> </task>  </tasks>  <verification> Before declaring plan complete: - [ ] `npm run typecheck` - [ ] `npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts src/lib/agent/streaming-loop.test.ts src/lib/agent/vacancy-analysis.test.ts src/lib/agent/pre-loop-setup.test.ts` </verification>  <success_criteria>  - All tasks completed - All verification checks pass - No errors or warnings introduced - Phase 28 ends with explicit tests for shared vacancy analysis and extracted setup behavior </success_criteria>  <output> After completion, create `.planning/phases/28-agent-input-and-setup-service-extraction/28-03-SUMMARY.md` </output>
--- phase: 30-authenticated-route-and-billing-boundary-hardening plan: "02" type: summary status: completed ---  # Plan 30-02 Summary  ## What changed  - Added `src/lib/security/request-trust.ts` with `validateTrustedMutationRequest(...)` so authenticated browser mutations now share one fail-closed trust-boundary check based on trusted `Origin` or `Referer` context. - Applied the shared trust helper to the high-value authenticated mutations that persist canonical profile or session state or trigger paid generation work: profile save, ATS enhancement, smart generation, manual edit, gap action, target creation, session generation, and checkout. - Extended the affected route suites to prove both accepted and rejected trust paths, and added billing-boundary proof that checkout rejects hostile browser origins while the Asaas webhook still follows its intended token-based server-to-server trust contract even if an `Origin` header is present.  ## Verification  - `npm run typecheck` - `npm test -- src/app/api/checkout/route.test.ts src/app/api/webhook/asaas/route.test.ts src/app/api/profile/ats-enhancement/route.test.ts src/app/api/profile/smart-generation/route.test.ts src/app/api/session/[id]/manual-edit/route.test.ts src/app/api/session/[id]/gap-action/route.test.ts src/app/api/session/[id]/targets/route.test.ts src/app/api/session/[id]/generate/route.test.ts`  ## Outcome  - Sensitive authenticated mutations now fail closed on missing or invalid browser trust signals, checkout no longer accepts hostile cross-origin mutation attempts, and the billing webhook boundary remains explicitly verified as non-browser and token-authenticated.
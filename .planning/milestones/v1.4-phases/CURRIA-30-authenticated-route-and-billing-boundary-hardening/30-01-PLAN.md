--- phase: 30-authenticated-route-and-billing-boundary-hardening plan: "01" type: execute wave: 1 depends_on:   - 28-01 requirements:   - SEC-01 files_modified:   - src/app/api/checkout/route.ts   - src/lib/config/app-url.ts   - src/lib/asaas/checkout.ts   - src/app/api/checkout/route.test.ts   - src/app/api/webhook/asaas/route.test.ts autonomous: true user_setup: [] must_haves:   truths:     - "Sensitive checkout and external return URLs derive from canonical app configuration instead of raw caller-controlled request origin headers."     - "Unsafe or malformed return-path inputs are rejected or normalized through one explicit trust boundary."     - "Billing redirect behavior keeps structured evidence in tests around the intended host policy."   artifacts:     - path: "src/app/api/checkout/route.ts"       provides: "Checkout route that resolves hosted callback URLs from trusted canonical app configuration"       contains: "POST"     - path: "src/lib/config/app-url.ts"       provides: "Canonical app URL helpers suitable for billing redirect and trust-boundary decisions"       contains: "getAppOrigin"   key_links:     - from: "src/app/api/checkout/route.ts"       to: "src/lib/config/app-url.ts"       via: "checkout route resolves success and cancel URLs from canonical app configuration"       pattern: "getAppOrigin" ---  <objective> Remove raw request-origin trust from checkout and related external return-path handling so billing redirects always resolve against the canonical app URL contract.  Purpose: Phase 30 starts by closing the most direct billing trust gap before broader authenticated mutation hardening changes land. Output: Canonical-host-aware checkout return handling plus regression proof for accepted and rejected redirect behavior. </objective>  <execution_context> @C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/fabio/.codex/get-shit-done/templates/summary.md </execution_context>  <context> @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/REQUIREMENTS.md @.planning/phases/30-authenticated-route-and-billing-boundary-hardening/30-RESEARCH.md @.planning/phases/30-authenticated-route-and-billing-boundary-hardening/30-VALIDATION.md @AGENTS.md @src/app/api/checkout/route.ts @src/lib/config/app-url.ts @src/lib/asaas/checkout.ts @src/app/api/checkout/route.test.ts @src/app/api/webhook/asaas/route.test.ts </context>  <threat_model> <trust_boundaries> - Browser request metadata -> hosted billing callback URLs - Canonical app configuration -> return destination used by external billing flows - External redirect and callback tests -> operator confidence in billing-boundary behavior </trust_boundaries>  <stride_register> <threat>   <threat_id>T-30-01</threat_id>   <category>Spoofing</category>   <component>Checkout return URLs</component>   <disposition>mitigate</disposition>   <mitigation_plan>Resolve hosted checkout success and cancel URLs from canonical configuration instead of trusting raw request origin headers.</mitigation_plan> </threat> <threat>   <threat_id>T-30-02</threat_id>   <category>Open Redirect</category>   <component>External return-path handling</component>   <disposition>mitigate</disposition>   <mitigation_plan>Reject or normalize malformed return-path inputs through one explicit helper and lock the intended behavior in route tests.</mitigation_plan> </threat> </stride_register> </threat_model>  <tasks>  <task type="auto">   <name>Task 1: Move checkout callback URL derivation behind canonical app configuration</name>   <files>src/app/api/checkout/route.ts, src/lib/config/app-url.ts, src/lib/asaas/checkout.ts</files>   <read_first>src/app/api/checkout/route.ts, src/lib/config/app-url.ts, src/lib/asaas/checkout.ts, AGENTS.md</read_first>   <action>Replace the checkout route's direct `Origin` header trust with one explicit canonical-origin helper from `src/lib/config/app-url.ts`. Keep localhost or preview-safe behavior only where intentionally supported, and make the route hand the resulting success or cancel URLs into the existing Asaas checkout flow without changing the brownfield billing contract otherwise.</action>   <verify>npm run typecheck && npm test -- src/app/api/checkout/route.test.ts</verify>   <acceptance_criteria>     - checkout no longer builds hosted callback URLs from raw `req.headers.origin`     - canonical app URL helpers are the single source of truth for checkout return destinations     - existing checkout behavior still succeeds for normal trusted environments   </acceptance_criteria>   <done>Hosted billing callbacks resolve from canonical configuration instead of caller-controlled request metadata.</done> </task>  <task type="auto">   <name>Task 2: Add regression proof for canonical-host and return-path trust decisions</name>   <files>src/app/api/checkout/route.test.ts, src/app/api/webhook/asaas/route.test.ts</files>   <read_first>src/app/api/checkout/route.test.ts, src/app/api/webhook/asaas/route.test.ts, src/app/api/checkout/route.ts</read_first>   <action>Extend the checkout and billing callback tests to prove trusted callback URLs are derived from canonical configuration, unsafe request metadata no longer controls redirects, and the broader billing boundary still documents accepted and rejected trust paths through route-level proof.</action>   <verify>npm run typecheck && npm test -- src/app/api/checkout/route.test.ts src/app/api/webhook/asaas/route.test.ts</verify>   <acceptance_criteria>     - route tests assert canonical-host callback URL derivation explicitly     - negative-path proof exists for unsafe or malformed request-controlled return signals     - billing callback tests still protect the intended accepted and rejected trust boundary   </acceptance_criteria>   <done>Canonical-host and billing-return trust behavior is protected by committed regression tests.</done> </task>  </tasks>  <verification> Before declaring plan complete: - [ ] `npm run typecheck` - [ ] `npm test -- src/app/api/checkout/route.test.ts src/app/api/webhook/asaas/route.test.ts` </verification>  <success_criteria>  - All tasks completed - All verification checks pass - No errors or warnings introduced - Checkout and external return-path trust decisions now derive from canonical configuration instead of raw request origin </success_criteria>  <output> After completion, create `.planning/phases/30-authenticated-route-and-billing-boundary-hardening/30-01-SUMMARY.md` </output>
--- phase: 30-authenticated-route-and-billing-boundary-hardening plan: "01" type: summary status: completed ---  # Plan 30-01 Summary  ## What changed  - Added `buildAppUrl(...)` to `src/lib/config/app-url.ts` so route code can derive canonical app URLs from configured host policy instead of raw request metadata. - Updated `src/app/api/checkout/route.ts` to resolve Asaas `successUrl`, `cancelUrl`, and `expiredUrl` from canonical configuration rather than trusting `req.headers.origin`. - Extended `src/app/api/checkout/route.test.ts` to prove checkout callbacks use canonical URLs and ignore spoofed `Origin` headers while preserving the existing billing lifecycle behavior.  ## Verification  - `npm run typecheck` - `npm test -- src/app/api/checkout/route.test.ts src/app/api/webhook/asaas/route.test.ts`  ## Outcome  - The most direct billing redirect trust gap is closed: hosted checkout callback URLs now come from canonical app configuration, not caller-controlled request headers.
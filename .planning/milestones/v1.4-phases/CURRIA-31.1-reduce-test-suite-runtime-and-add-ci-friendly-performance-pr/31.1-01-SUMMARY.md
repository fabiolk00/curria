--- phase: 31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr plan: "01" type: summary status: completed ---  # Plan 31.1-01 Summary  ## What changed  - Kept `vitest.config.ts` on a node-first default and scoped `jsdom` to `*.test.tsx`, formalizing the lower-cost environment split for non-E2E runs. - Preserved the test-only retry-delay override in `src/lib/agent/agent-recovery.ts`, `src/lib/agent/streaming-loop.test.ts`, and `src/app/api/agent/route.sse.test.ts` so recovery assertions no longer sleep through production backoff. - Added the reusable runtime-proof entrypoint `test:profile:non-e2e` in `package.json` so profiling or budget checks can be rerun from one named command instead of ad-hoc shell history.  ## Verification  - `npm run typecheck` - `npm run test:profile:non-e2e -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.sse.test.ts`  ## Outcome  - The largest artificial runtime costs from test environment bootstrap and retry sleeping are now codified behind repo-native configuration, and the repo has a canonical profiling command for the remaining non-E2E outliers.
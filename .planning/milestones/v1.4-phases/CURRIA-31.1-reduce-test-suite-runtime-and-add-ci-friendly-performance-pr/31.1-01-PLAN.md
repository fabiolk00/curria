--- phase: 31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr plan: "01" type: execute wave: 1 depends_on:   - 31-02 files_modified:   - vitest.config.ts   - package.json   - src/lib/agent/agent-recovery.ts   - src/lib/agent/streaming-loop.test.ts   - src/app/api/agent/route.sse.test.ts autonomous: true requirements:   - PERF-01 user_setup: [] must_haves:   truths:     - "Non-E2E tests default to the lightest safe runtime environment instead of paying browser setup by default."     - "Retry-oriented tests must not sleep for real production backoff when the assertion is about control flow rather than elapsed time."     - "The repo exposes a repeatable path to measure remaining non-E2E runtime outliers before broader optimization work continues."   artifacts:     - path: "vitest.config.ts"       provides: "Runtime environment split between node-first tests and jsdom-only UI suites"       contains: "environmentMatchGlobs"     - path: "src/lib/agent/agent-recovery.ts"       provides: "Configurable retry-delay base for test-safe recovery timing"       contains: "AGENT_RECOVERY_RETRY_DELAY_BASE_MS"     - path: "package.json"       provides: "Repeatable profiling or budget command for non-E2E runtime evidence"       contains: "test:"   key_links:     - from: "src/lib/agent/agent-recovery.ts"       to: "src/lib/agent/streaming-loop.test.ts"       via: "test-only delay override removes artificial sleep from retry assertions"       pattern: "AGENT_RECOVERY_RETRY_DELAY_BASE_MS" ---  <objective> Lock the non-E2E runtime baseline and the lightest safe test environment so the suite stops paying obvious structural overhead before deeper suite-specific optimization.  Purpose: Phase 31.1 starts by formalizing the already-proven infra wins and adding a repeatable runtime-measurement path for the remaining outliers. Output: Node-first Vitest setup, configurable retry-delay control for tests, and a repo-native profiling or budget command for non-E2E runtime. </objective>  <execution_context> @C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/fabio/.codex/get-shit-done/templates/summary.md </execution_context>  <context> @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/REQUIREMENTS.md @.planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-RESEARCH.md @.planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-VALIDATION.md @AGENTS.md @vitest.config.ts @package.json @src/lib/agent/agent-recovery.ts @src/lib/agent/streaming-loop.test.ts @src/app/api/agent/route.sse.test.ts </context>  <threat_model> <trust_boundaries> - Test runtime configuration -> correctness of backend versus UI test execution - Retry timing controls -> preservation of real recovery behavior assertions - Runtime measurement command -> credibility of reported suite performance </trust_boundaries>  <stride_register> <threat>   <threat_id>T-31.1-01</threat_id>   <category>Tampering</category>   <component>Vitest environment split</component>   <disposition>mitigate</disposition>   <mitigation_plan>Keep jsdom coverage on UI suites while moving non-UI tests to node-first defaults, and re-run representative backend plus UI-adjacent suites after the change.</mitigation_plan> </threat> <threat>   <threat_id>T-31.1-02</threat_id>   <category>Repudiation</category>   <component>Runtime proof</component>   <disposition>mitigate</disposition>   <mitigation_plan>Add one explicit profiling or budget command in the repo so performance claims are reproducible instead of conversational.</mitigation_plan> </threat> </stride_register> </threat_model>  <tasks>  <task type="auto">   <name>Task 1: Formalize node-first test environment defaults and keep jsdom scoped to UI suites</name>   <files>vitest.config.ts</files>   <read_first>vitest.config.ts, AGENTS.md, .planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-RESEARCH.md</read_first>   <action>Update `vitest.config.ts` so non-UI tests default to `node` and only the browser-dependent suites explicitly opt into `jsdom`. Preserve existing setup files and coverage behavior, and verify the configuration still matches the repo's current `*.test.tsx` UI convention.</action>   <verify>npm run typecheck && npm test -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.sse.test.ts</verify>   <acceptance_criteria>     - backend and library tests do not inherit jsdom globally anymore     - UI suites still resolve jsdom through explicit matching rather than accidental fallback     - targeted agent and route suites remain green under the updated config   </acceptance_criteria>   <done>Vitest uses the lightest safe environment by default and the config makes that intent explicit.</done> </task>  <task type="auto">   <name>Task 2: Remove real retry sleeping from agent recovery tests without changing production semantics</name>   <files>src/lib/agent/agent-recovery.ts, src/lib/agent/streaming-loop.test.ts, src/app/api/agent/route.sse.test.ts</files>   <read_first>src/lib/agent/agent-recovery.ts, src/lib/agent/streaming-loop.test.ts, src/app/api/agent/route.sse.test.ts</read_first>   <action>Make the recovery retry delay base configurable for tests and wire the affected suites to override it to zero during execution. Keep the production default unchanged, and preserve the assertions around fallback or recovery behavior rather than time spent waiting.</action>   <verify>npm run typecheck && npm test -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.sse.test.ts</verify>   <acceptance_criteria>     - production recovery still defaults to the current retry timing when no override is set     - retry-oriented test suites can run without real backoff sleeps     - targeted route and streaming suites remain green and materially faster   </acceptance_criteria>   <done>Artificial retry sleep is removed from the known agent-runtime hotspots while production semantics stay intact.</done> </task>  <task type="auto">   <name>Task 3: Add a repeatable profiling or budget command for non-E2E runtime</name>   <files>package.json</files>   <read_first>package.json, vitest.config.ts, .planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-RESEARCH.md</read_first>   <action>Add or update a package script that makes runtime measurement repeatable for the non-E2E suite or the intended budget slice. The command should be stable enough to reuse during this phase and understandable enough to promote into CI or milestone proof in the final wave.</action>   <verify>npm run typecheck</verify>   <acceptance_criteria>     - the repo has one named command for profiling or budgeted non-E2E runtime checks     - the command targets the relevant non-E2E slice rather than E2E coverage     - future execution can use the command as the canonical runtime-proof entrypoint   </acceptance_criteria>   <done>The repo has a reusable command for runtime measurement instead of relying on ad-hoc shell history.</done> </task>  </tasks>  <verification> Before declaring plan complete: - [ ] `npm run typecheck` - [ ] `npm test -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.sse.test.ts` </verification>  <success_criteria>  - All tasks completed - All verification checks pass - No errors or warnings introduced - The suite no longer pays the most obvious global environment or retry-delay overhead in non-E2E runs </success_criteria>  <output> After completion, create `.planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-01-SUMMARY.md` </output>
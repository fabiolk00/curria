--- phase: 31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr plan: "03" type: execute wave: 3 depends_on:   - 31.1-02 files_modified:   - src/components/resume/resume-builder.test.tsx   - package.json   - .github/workflows/ci.yml autonomous: true requirements:   - PERF-03   - PERF-02 user_setup: [] must_haves:   truths:     - "The phase closes with a repeatable runtime-proof path, not only anecdotal timing wins."     - "Any remaining major outlier is either reduced or explicitly documented with committed rationale."     - "Milestone closure is re-enabled only after the test-runtime bottleneck has measurable evidence behind it."   artifacts:     - path: "src/components/resume/resume-builder.test.tsx"       provides: "Next known large non-E2E outlier addressed or explicitly documented through retained behavior-sensitive coverage"       contains: "test"     - path: "package.json"       provides: "Canonical runtime-proof command for local and CI-style verification"       contains: "test:"     - path: ".github/workflows/ci.yml"       provides: "Optional CI-visible exposure for runtime proof or profiling budget if appropriate"       contains: "test"   key_links:     - from: "package.json"       to: ".github/workflows/ci.yml"       via: "runtime-proof command can be reused in CI-visible checks without inventing a second path"       pattern: "test:" ---  <objective> Finish Phase 31.1 with repeatable runtime proof: address the next remaining major outlier, remeasure the broader non-E2E suite, and expose a durable verification path for future regressions.  Purpose: The phase should end with measurable confidence, not just a handful of faster local suites. Output: Remaining major outlier work landed or explicitly documented, plus a canonical runtime-proof command and optional CI visibility for the resulting budget. </objective>  <execution_context> @C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/fabio/.codex/get-shit-done/templates/summary.md </execution_context>  <context> @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/REQUIREMENTS.md @.planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-RESEARCH.md @.planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-VALIDATION.md @AGENTS.md @src/components/resume/resume-builder.test.tsx @package.json @.github/workflows/ci.yml </context>  <threat_model> <trust_boundaries> - Final runtime budget claims -> credibility of milestone closure - Remaining UI outlier optimization -> preservation of coverage in behavior-sensitive tests - CI-visible proof -> operator confidence that drift will be caught early </trust_boundaries>  <stride_register> <threat>   <threat_id>T-31.1-05</threat_id>   <category>Repudiation</category>   <component>Final runtime claim</component>   <disposition>mitigate</disposition>   <mitigation_plan>Use one canonical runtime-proof command and record the measured outcome in the phase summary instead of relying on informal reports.</mitigation_plan> </threat> <threat>   <threat_id>T-31.1-06</threat_id>   <category>Tampering</category>   <component>Remaining outlier optimization</component>   <disposition>mitigate</disposition>   <mitigation_plan>If the remaining outlier cannot be sped up safely, keep the higher-fidelity test behavior and document the residual cost with explicit rationale rather than weakening coverage.</mitigation_plan> </threat> </stride_register> </threat_model>  <tasks>  <task type="auto">   <name>Task 1: Re-profile the remaining largest non-E2E outlier and reduce it only if the speedup is behavior-safe</name>   <files>src/components/resume/resume-builder.test.tsx</files>   <read_first>src/components/resume/resume-builder.test.tsx, .planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-RESEARCH.md</read_first>   <action>Re-measure `src/components/resume/resume-builder.test.tsx` and any newly surfaced large outlier from the profiling command introduced earlier. Attempt only behavior-safe optimizations. If a promising shortcut changes semantics or causes flakiness, keep the higher-fidelity interaction and document the residual cost instead of forcing a risky speedup.</action>   <verify>npm run typecheck && npm test -- src/components/resume/resume-builder.test.tsx</verify>   <acceptance_criteria>     - the next largest known suite has fresh timing evidence     - any landed optimization preserves green behavior     - if no safe optimization exists, the residual outlier is explicitly documented for closure evidence   </acceptance_criteria>   <done>The next largest remaining outlier is either reduced safely or documented as an intentional residual cost.</done> </task>  <task type="auto">   <name>Task 2: Promote runtime proof into a durable local or CI-visible verification path</name>   <files>package.json, .github/workflows/ci.yml</files>   <read_first>package.json, .github/workflows/ci.yml, .planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-VALIDATION.md</read_first>   <action>Finalize the profiling or budget command so it becomes the canonical proof path for non-E2E runtime, and wire it into CI only if the signal is stable enough to be actionable. Prefer one shared command reused in both local verification and CI over multiple ad-hoc variants.</action>   <verify>npm run typecheck</verify>   <acceptance_criteria>     - the repo has one durable runtime-proof path for non-E2E checks     - CI visibility, if added, reuses the same command instead of inventing a second verification path     - the phase can report final runtime evidence from a repeatable command   </acceptance_criteria>   <done>Runtime proof is durable and reusable for future regression checks.</done> </task>  <task type="auto">   <name>Task 3: Re-measure the broader non-E2E suite and record closure evidence</name>   <files>package.json, .github/workflows/ci.yml</files>   <read_first>package.json, .github/workflows/ci.yml</read_first>   <action>Run the canonical runtime-proof command with a bounded timeout or budget-aware strategy, capture the resulting timing evidence, and use that result to decide whether Phase 31.1 fully closes the bottleneck or needs an explicit residual-risk note. Record the outcome in the phase summary so milestone audit can rely on concrete evidence.</action>   <verify>npm run typecheck</verify>   <acceptance_criteria>     - a bounded broader-suite measurement exists at the end of the phase     - the final phase summary records the measured result and any remaining caveat     - milestone closure can reference committed runtime evidence instead of conversational timing notes   </acceptance_criteria>   <done>Phase 31.1 ends with concrete runtime evidence and an explicit closure posture.</done> </task>  </tasks>  <verification> Before declaring plan complete: - [ ] `npm run typecheck` - [ ] `npm test -- src/components/resume/resume-builder.test.tsx` - [ ] run the canonical runtime-proof command introduced in this phase </verification>  <success_criteria>  - All tasks completed - All verification checks pass - No errors or warnings introduced - The milestone now has repeatable runtime-proof evidence for the non-E2E suite </success_criteria>  <output> After completion, create `.planning/phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-03-SUMMARY.md` </output>
--- phase: 31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr plan: "03" type: summary status: completed ---  # Plan 31.1-03 Summary  ## What changed  - Added polling-interval override props to `src/components/resume/resume-builder.tsx` so the `ImportResumeModal` can be profiled and adjusted in tests without changing production timing defaults. - Re-profiled `src/components/resume/resume-builder.test.tsx` with the canonical runtime command and confirmed the file still concentrates most of its cost in two async polling scenarios, even after the safe test-only interval seam was introduced. - Promoted the runtime-proof path into CI by updating `.github/workflows/ci.yml` to run `npm run test:profile:non-e2e`, reusing the same named command added in Wave 1 instead of inventing a second verification path.  ## Verification  - `npm run typecheck` - `npm run test:profile:non-e2e -- src/components/resume/resume-builder.test.tsx` - `npm run test:profile:non-e2e` with a 2-minute local ceiling, which still timed out after about `129s`  ## Outcome  - Phase 31.1 now leaves a durable runtime-proof workflow in both local and CI contexts, and it narrows the remaining bottleneck to residual medium-cost suites plus `resume-builder` polling behavior rather than the earlier structural waste from global jsdom and retry sleeping.
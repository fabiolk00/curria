--- phase: 29-agent-recovery-streaming-and-persistence-decomposition plan: "01" type: execute wave: 1 depends_on:   - 28-02 files_modified:   - src/lib/agent/agent-loop.ts   - src/lib/agent/agent-recovery.ts   - src/lib/agent/streaming-loop.test.ts autonomous: true requirements:   - AGENT-02 user_setup: [] must_haves:   truths:     - "Retry and degraded-recovery policy move out of the oversized loop body into dedicated runtime helpers."     - "Length, zero-text, and concise recovery keep the same user-visible fallback class and usage tracking behavior."     - "The loop still orchestrates recovery through explicit contracts instead of open-coded policy branches."   artifacts:     - path: "src/lib/agent/agent-recovery.ts"       provides: "Explicit recovery-policy helpers for truncated, empty, and concise runtime fallback handling"       contains: "recoverTruncatedTurn"     - path: "src/lib/agent/agent-loop.ts"       provides: "Thinner loop orchestration that delegates recovery policy instead of owning all recovery branches inline"       contains: "runAgentLoop"   key_links:     - from: "src/lib/agent/agent-loop.ts"       to: "src/lib/agent/agent-recovery.ts"       via: "loop delegates retry and degraded-recovery policy to dedicated helpers"       pattern: "recoverTruncatedTurn" ---  <objective> Extract retry and degraded-recovery policy into explicit runtime helpers so the main loop stops owning scattered recovery branches inline.  Purpose: Phase 29 begins by isolating the most behavior-sensitive but naturally clustered logic in `agent-loop.ts`, reducing branching complexity before streaming and persistence seams move. Output: Dedicated recovery helpers used by `runAgentLoop(...)`, with current fallback semantics preserved. </objective>  <execution_context> @C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/fabio/.codex/get-shit-done/templates/summary.md </execution_context>  <context> @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/REQUIREMENTS.md @.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-RESEARCH.md @.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-VALIDATION.md @AGENTS.md @src/lib/agent/agent-loop.ts @src/lib/agent/streaming-loop.test.ts </context>  <threat_model> <trust_boundaries> - Runtime turn result -> retry and recovery policy - Recovery prompt policy -> user-visible degraded response behavior - Usage tracking and observability -> recovery-attempt attribution </trust_boundaries>  <stride_register> <threat>   <threat_id>T-29-01</threat_id>   <category>Tampering</category>   <component>Recovery extraction</component>   <disposition>mitigate</disposition>   <mitigation_plan>Move recovery orchestration into dedicated helpers without changing prompt policy, retry counts, or fallback routing in the same plan.</mitigation_plan> </threat> <threat>   <threat_id>T-29-02</threat_id>   <category>Denial of Service</category>   <component>Degraded-response handling</component>   <disposition>mitigate</disposition>   <mitigation_plan>Preserve current recovery stop conditions, abort handling, and exponential-backoff behavior so extraction does not introduce looping or silent failures.</mitigation_plan> </threat> </stride_register> </threat_model>  <tasks>  <task type="auto">   <name>Task 1: Move length, zero-text, and concise recovery helpers into a dedicated recovery module</name>   <files>src/lib/agent/agent-loop.ts, src/lib/agent/agent-recovery.ts</files>   <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.test.ts, AGENTS.md</read_first>   <action>Extract `recoverTruncatedTurn(...)`, `recoverZeroTextTurn(...)`, `recoverConciseTurn(...)`, and closely related recovery helpers into `src/lib/agent/agent-recovery.ts`. Keep the current retry counts, prompt selection, and return shapes intact so `runAgentLoop(...)` can call one explicit recovery boundary instead of owning the full branch logic inline.</action>   <verify>npm run typecheck && npm test -- src/lib/agent/streaming-loop.test.ts</verify>   <acceptance_criteria>     - the main loop no longer owns all recovery helper implementations inline     - extracted recovery helpers preserve the current recovery return contract and prompt behavior     - targeted streaming-loop tests covering truncation and empty-response recovery remain green   </acceptance_criteria>   <done>Recovery helpers live in a dedicated module and the loop delegates to them without behavior drift.</done> </task>  <task type="auto">   <name>Task 2: Narrow the final fallback retry block into one explicit recovery-policy path</name>   <files>src/lib/agent/agent-loop.ts, src/lib/agent/agent-recovery.ts, src/lib/agent/streaming-loop.test.ts</files>   <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/agent-recovery.ts, src/lib/agent/streaming-loop.test.ts</read_first>   <action>Refactor the late `!assistantResponded` fallback branch so retry attempts, exponential backoff, and deterministic fallback selection are orchestrated through an explicit recovery-policy helper rather than embedded directly in `runAgentLoop(...)`. Preserve usage tracking, fallback logging, and assistant transcript persistence semantics.</action>   <verify>npm run typecheck && npm test -- src/lib/agent/streaming-loop.test.ts</verify>   <acceptance_criteria>     - the loop does not inline the full final recovery retry block anymore     - deterministic fallback kind and assistant transcript persistence stay behaviorally consistent     - streaming-loop tests covering empty-response fallback and degraded recovery remain green   </acceptance_criteria>   <done>The loop uses one explicit recovery-policy seam for degraded-response handling.</done> </task>  </tasks>  <verification> Before declaring plan complete: - [ ] `npm run typecheck` - [ ] `npm test -- src/lib/agent/streaming-loop.test.ts` </verification>  <success_criteria>  - All tasks completed - All verification checks pass - No errors or warnings introduced - Retry and degraded-recovery policy now live behind explicit runtime helpers instead of scattered loop-local branches </success_criteria>  <output> After completion, create `.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-01-SUMMARY.md` </output>
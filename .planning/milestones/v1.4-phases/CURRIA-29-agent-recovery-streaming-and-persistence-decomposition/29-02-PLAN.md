--- phase: 29-agent-recovery-streaming-and-persistence-decomposition plan: "02" type: execute wave: 2 depends_on:   - 29-01 files_modified:   - src/lib/agent/agent-loop.ts   - src/lib/agent/agent-streaming.ts   - src/lib/agent/agent-persistence.ts   - src/lib/agent/streaming-loop.test.ts   - src/app/api/agent/route.sse.test.ts autonomous: true requirements:   - AGENT-02 user_setup: [] must_haves:   truths:     - "Streaming transport and persistence handoffs become explicit runtime boundaries instead of line-by-line loop-local responsibilities."     - "Transcript appends, patch application, usage tracking, and done-chunk semantics stay aligned with the current brownfield contract."     - "The loop remains the orchestrator, not the owner of every transport and persistence detail."   artifacts:     - path: "src/lib/agent/agent-streaming.ts"       provides: "Explicit streaming transport helpers used by the loop"       contains: "streamAssistantTurn"     - path: "src/lib/agent/agent-persistence.ts"       provides: "Transcript, patch, and completion-persistence helpers for runtime handoffs"       contains: "appendAssistantTurn"     - path: "src/lib/agent/agent-loop.ts"       provides: "Thinner orchestration over extracted streaming and persistence services"       contains: "runAgentLoop"   key_links:     - from: "src/lib/agent/agent-loop.ts"       to: "src/lib/agent/agent-streaming.ts"       via: "loop delegates raw model streaming transport to a dedicated helper"       pattern: "streamAssistantTurn"     - from: "src/lib/agent/agent-loop.ts"       to: "src/lib/agent/agent-persistence.ts"       via: "loop delegates transcript and completion handoffs to a dedicated helper"       pattern: "appendAssistantTurn" ---  <objective> Separate streaming transport and persistence responsibilities from the main loop body so runtime orchestration becomes clearer and easier to test.  Purpose: After recovery policy is isolated, the next highest-value cut is to extract transport and persistence seams that still crowd `runAgentLoop(...)` and make behavior changes risky. Output: Dedicated streaming and persistence helpers used by `runAgentLoop(...)`, with transcript and SSE-adjacent behavior preserved. </objective>  <execution_context> @C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/fabio/.codex/get-shit-done/templates/summary.md </execution_context>  <context> @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/REQUIREMENTS.md @.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-RESEARCH.md @.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-VALIDATION.md @AGENTS.md @src/lib/agent/agent-loop.ts @src/lib/agent/streaming-loop.test.ts @src/app/api/agent/route.sse.test.ts </context>  <threat_model> <trust_boundaries> - OpenAI stream transport -> emitted text and tool-call accumulation - Runtime turn result -> persisted transcript and patch application - Loop completion -> final `done` chunk and structured logs </trust_boundaries>  <stride_register> <threat>   <threat_id>T-29-03</threat_id>   <category>Tampering</category>   <component>Streaming extraction</component>   <disposition>mitigate</disposition>   <mitigation_plan>Keep streaming request assembly, first-token logging, and tool-call accumulation behaviorally identical while moving transport code into a dedicated helper.</mitigation_plan> </threat> <threat>   <threat_id>T-29-04</threat_id>   <category>Information Disclosure</category>   <component>Persistence extraction</component>   <disposition>mitigate</disposition>   <mitigation_plan>Keep transcript appends and tool-patch persistence routed through existing session APIs so `cvState` and `agentState` boundaries do not drift.</mitigation_plan> </threat> </stride_register> </threat_model>  <tasks>  <task type="auto">   <name>Task 1: Move streaming transport helpers behind an explicit runtime module</name>   <files>src/lib/agent/agent-loop.ts, src/lib/agent/agent-streaming.ts</files>   <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.test.ts, AGENTS.md</read_first>   <action>Extract `streamAssistantTurn(...)` and closely related transport-only helpers into `src/lib/agent/agent-streaming.ts`. Preserve model selection, request assembly, first-token logging, text chunk emission, and tool-call accumulation so the loop consumes the same streamed turn contract through a clearer service boundary.</action>   <verify>npm run typecheck && npm test -- src/lib/agent/streaming-loop.test.ts</verify>   <acceptance_criteria>     - `agent-loop.ts` no longer owns the full raw streaming transport implementation inline     - the extracted transport helper preserves the current streamed turn contract and logging semantics     - streaming-loop tests covering normal streamed output and tool-call accumulation remain green   </acceptance_criteria>   <done>Streaming transport lives behind a dedicated helper instead of an oversized loop-local implementation.</done> </task>  <task type="auto">   <name>Task 2: Extract transcript, patch, and completion handoffs into a dedicated persistence module</name>   <files>src/lib/agent/agent-loop.ts, src/lib/agent/agent-persistence.ts, src/lib/agent/streaming-loop.test.ts, src/app/api/agent/route.sse.test.ts</files>   <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.test.ts, src/app/api/agent/route.sse.test.ts</read_first>   <action>Move repeated transcript and patch-adjacent responsibilities into `src/lib/agent/agent-persistence.ts`: loading or trimming history if needed, appending assistant output, applying persisted patches, and packaging completion metadata. Keep durable writes routed through existing session APIs and preserve route-visible SSE completion behavior through the existing loop contract.</action>   <verify>npm run typecheck && npm test -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts</verify>   <acceptance_criteria>     - repeated append or patch or completion logic is no longer scattered throughout `runAgentLoop(...)`     - extracted persistence helpers preserve canonical state boundaries and current transcript ordering     - route and loop tests covering stream completion and transcript persistence remain green   </acceptance_criteria>   <done>The loop delegates transcript and completion handoffs through explicit persistence helpers.</done> </task>  </tasks>  <verification> Before declaring plan complete: - [ ] `npm run typecheck` - [ ] `npm test -- src/lib/agent/streaming-loop.test.ts src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts` </verification>  <success_criteria>  - All tasks completed - All verification checks pass - No errors or warnings introduced - Streaming and persistence responsibilities now live behind explicit runtime boundaries while the loop preserves current observable behavior </success_criteria>  <output> After completion, create `.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-02-SUMMARY.md` </output>
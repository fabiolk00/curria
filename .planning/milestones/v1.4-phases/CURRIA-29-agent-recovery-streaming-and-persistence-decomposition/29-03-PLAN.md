--- phase: 29-agent-recovery-streaming-and-persistence-decomposition plan: "03" type: execute wave: 3 depends_on:   - 29-01   - 29-02 files_modified:   - src/lib/agent/streaming-loop.test.ts   - src/app/api/agent/route.test.ts   - src/app/api/agent/route.sse.test.ts   - src/lib/agent/agent-recovery.test.ts   - src/lib/agent/agent-persistence.test.ts autonomous: true requirements:   - AGENT-03 user_setup: [] must_haves:   truths:     - "Phase 29 ends with committed regression proof for extracted recovery, streaming, and persistence seams."     - "Tests explicitly protect canonical `cvState`, operational `agentState`, and deterministic generation persistence contracts."     - "Future stability work can rely on direct seam coverage instead of only the oversized loop integration suite."   artifacts:     - path: "src/lib/agent/agent-recovery.test.ts"       provides: "Direct proof for extracted retry and degraded-recovery policy"       contains: "recoverTruncatedTurn"     - path: "src/lib/agent/agent-persistence.test.ts"       provides: "Direct proof for transcript, patch, and completion persistence helpers"       contains: "appendAssistantTurn"     - path: "src/lib/agent/streaming-loop.test.ts"       provides: "Loop-level regression proof that extracted runtime seams preserve brownfield behavior"       contains: "runAgentLoop"   key_links:     - from: "src/lib/agent/agent-recovery.test.ts"       to: "src/lib/agent/agent-loop.ts"       via: "direct seam tests prove extracted recovery preserves the runtime contract consumed by the loop"       pattern: "recoverTruncatedTurn"     - from: "src/lib/agent/agent-persistence.test.ts"       to: "src/lib/agent/agent-loop.ts"       via: "direct seam tests prove transcript and patch persistence preserve canonical state handoffs"       pattern: "appendAssistantTurn" ---  <objective> Add regression proof for the extracted back-half runtime seams so Phase 29 closes with explicit protection around recovery, streaming, and persistence behavior.  Purpose: The decomposition only becomes safe long-term if the repo proves the new seams preserve canonical state, transcript ordering, and degraded-response behavior after the code moves. Output: Direct seam tests plus updated loop and route regressions for the extracted back-half runtime contracts. </objective>  <execution_context> @C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/fabio/.codex/get-shit-done/templates/summary.md </execution_context>  <context> @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/REQUIREMENTS.md @.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-RESEARCH.md @.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-VALIDATION.md @AGENTS.md @src/lib/agent/agent-loop.ts @src/lib/agent/streaming-loop.test.ts @src/app/api/agent/route.test.ts @src/app/api/agent/route.sse.test.ts </context>  <threat_model> <trust_boundaries> - Extracted recovery helpers -> degraded-response and fallback behavior - Extracted persistence helpers -> canonical state and transcript ordering - Route-to-loop contract -> visible SSE completion and transcript continuity </trust_boundaries>  <stride_register> <threat>   <threat_id>T-29-05</threat_id>   <category>Repudiation</category>   <component>Regression coverage</component>   <disposition>mitigate</disposition>   <mitigation_plan>Commit direct seam tests and loop-level regressions so future refactors can prove the extracted back-half runtime contract still holds.</mitigation_plan> </threat> <threat>   <threat_id>T-29-06</threat_id>   <category>Tampering</category>   <component>Canonical state contract</component>   <disposition>mitigate</disposition>   <mitigation_plan>Assert that extracted persistence seams preserve transcript ordering, patch handling, and `cvState` or `agentState` boundaries before later stability work depends on them.</mitigation_plan> </threat> </stride_register> </threat_model>  <tasks>  <task type="auto">   <name>Task 1: Add direct seam coverage for extracted recovery and persistence services</name>   <files>src/lib/agent/agent-recovery.test.ts, src/lib/agent/agent-persistence.test.ts</files>   <read_first>src/lib/agent/agent-recovery.ts, src/lib/agent/agent-persistence.ts, src/lib/agent/streaming-loop.test.ts, AGENTS.md</read_first>   <action>Create focused direct tests for the extracted back-half services. Prove retry counts, degraded fallback selection, and abort-aware recovery behavior in the recovery seam, and prove transcript append ordering, patch propagation, and canonical state handling in the persistence seam.</action>   <verify>npm run typecheck && npm test -- src/lib/agent/agent-recovery.test.ts src/lib/agent/agent-persistence.test.ts</verify>   <acceptance_criteria>     - extracted recovery policy has direct tests for truncation, empty-response, and fallback behavior     - extracted persistence helpers have direct tests for transcript, patch, and completion handoffs     - the new seam tests assert canonical state boundaries rather than only happy-path output   </acceptance_criteria>   <done>The extracted back-half services are protected by direct seam tests.</done> </task>  <task type="auto">   <name>Task 2: Extend loop and route regressions around extracted back-half runtime behavior</name>   <files>src/lib/agent/streaming-loop.test.ts, src/app/api/agent/route.test.ts, src/app/api/agent/route.sse.test.ts</files>   <read_first>src/lib/agent/streaming-loop.test.ts, src/app/api/agent/route.test.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/agent-recovery.test.ts, src/lib/agent/agent-persistence.test.ts</read_first>   <action>Extend the existing runtime suites to prove the loop still delivers the same observable behavior after decomposition: degraded-response recovery still yields a user-visible answer, assistant transcript persistence still matches the visible output, and route-visible SSE startup or completion semantics still hold after the loop delegates to extracted back-half helpers.</action>   <verify>npm run typecheck && npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts src/lib/agent/streaming-loop.test.ts src/lib/agent/agent-recovery.test.ts src/lib/agent/agent-persistence.test.ts</verify>   <acceptance_criteria>     - loop-level tests explicitly cover extracted recovery and persistence handoff behavior     - route-level proof still protects the loop contract where user-visible SSE behavior depends on it     - the final targeted phase suite is green with direct seam proof for the extracted back-half services   </acceptance_criteria>   <done>Phase 29 closes with committed regression proof for extracted recovery, streaming, and persistence boundaries.</done> </task>  </tasks>  <verification> Before declaring plan complete: - [ ] `npm run typecheck` - [ ] `npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts src/lib/agent/streaming-loop.test.ts src/lib/agent/agent-recovery.test.ts src/lib/agent/agent-persistence.test.ts` </verification>  <success_criteria>  - All tasks completed - All verification checks pass - No errors or warnings introduced - Phase 29 ends with explicit tests for extracted recovery, streaming, and persistence seams </success_criteria>  <output> After completion, create `.planning/phases/29-agent-recovery-streaming-and-persistence-decomposition/29-03-SUMMARY.md` </output>
--- phase: 31-long-vacancy-stability-and-release-hygiene-gates plan: "02" type: summary status: completed ---  # Plan 31-02 Summary  ## What changed  - Added the named script `test:e2e:release-critical` in `package.json` so the milestone-critical browser gate can be run locally and in CI without reading workflow internals. - Updated `.github/workflows/ci.yml` to execute that script in an explicit `Browser E2E - Release Critical Stability` step, making the core funnel and repeated long-vacancy seam a visible merge blocker. - Revalidated the narrowed browser gate against both `tests/e2e/core-funnel.spec.ts` and `tests/e2e/long-vacancy-generation.spec.ts` so the release contract now covers both baseline workspace readiness and the repaired long-vacancy stress path.  ## Verification  - `npm run typecheck` - `npm run test:e2e:release-critical`  ## Outcome  - Phase 31 now ends with an explicit release-critical browser gate that blocks merges when the core funnel or long-vacancy generation stability regresses again.
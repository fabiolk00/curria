--- phase: 31-long-vacancy-stability-and-release-hygiene-gates plan: "01" type: summary status: completed ---  # Plan 31-01 Summary  ## What changed  - Added an E2E-only fast path in `src/lib/asaas/optional-billing-info.ts` and wired it through `scripts/start-e2e-dev-server.mjs` so dashboard reloads no longer stall on slow optional billing lookups during browser stability runs. - Hardened `tests/e2e/long-vacancy-generation.spec.ts` to revisit the workspace through explicit navigation instead of `page.reload()`, preserving the real rehydration contract while avoiding detached-frame instability around the preview surface. - Cleaned the remaining mojibake expectations in `src/components/dashboard/chat-interface.test.tsx` and `src/lib/agent/vacancy-analysis.test.ts` so the active dashboard and agent copy proofs now match the intended Portuguese text.  ## Verification  - `npm run typecheck` - `npm test -- src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/preview-panel.test.tsx src/components/dashboard/chat-interface.test.tsx src/lib/agent/pre-loop-setup.test.ts src/lib/agent/vacancy-analysis.test.ts` - `npm run test:e2e -- tests/e2e/long-vacancy-generation.spec.ts --project=chromium`  ## Outcome  - The repeated long-vacancy generate-and-return path now comes back to a ready workspace under committed browser proof, and the remaining encoding regressions are removed from the release-facing regression net.
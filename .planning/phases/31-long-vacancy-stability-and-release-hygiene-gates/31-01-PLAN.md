---
phase: 31-long-vacancy-stability-and-release-hygiene-gates
plan: "01"
type: execute
wave: 1
depends_on:
  - 29-03
  - 30-02
requirements:
  - REL-01
  - REL-02
files_modified:
  - tests/e2e/fixtures/api-mocks.ts
  - tests/e2e/long-vacancy-generation.spec.ts
  - src/components/dashboard/resume-workspace.tsx
  - src/components/dashboard/chat-interface.tsx
  - src/lib/agent/pre-loop-setup.ts
  - src/lib/agent/message-preparation.ts
  - src/lib/agent/vacancy-analysis.ts
  - src/components/dashboard/resume-workspace.test.tsx
  - src/components/dashboard/preview-panel.test.tsx
  - src/components/dashboard/chat-interface.test.tsx
  - src/lib/agent/pre-loop-setup.test.ts
  - src/lib/agent/vacancy-analysis.test.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The long-vacancy generate-and-reload path returns to a ready workspace and ready preview state instead of getting stuck busy or losing generated output."
    - "Broken encoding artifacts are removed from active user-visible and agent-visible copy rather than being left as committed mojibake."
    - "Focused unit and browser proof exist for both the repeated-generation seam and the encoding cleanup."
  artifacts:
    - path: "tests/e2e/long-vacancy-generation.spec.ts"
      provides: "Browser proof for repeated long-vacancy generation and workspace stability"
      contains: "handles a near-limit vacancy, repeated generation, and template rendering stability"
    - path: "src/components/dashboard/chat-interface.tsx"
      provides: "Workspace chat copy without broken encoding artifacts"
      contains: "createWelcomeMessages"
  key_links:
    - from: "src/components/dashboard/resume-workspace.tsx"
      to: "tests/e2e/fixtures/api-mocks.ts"
      via: "workspace rehydration and mock generated-output state stay aligned across repeated reloads"
      pattern: "data-base-output-ready"
    - from: "src/lib/agent/pre-loop-setup.ts"
      to: "src/lib/agent/vacancy-analysis.ts"
      via: "agent-side vacancy and attachment notes use consistently encoded copy"
      pattern: "Nota do sistema"
---

<objective>
Fix the known long-vacancy instability and remove broken encoding artifacts from the active product surface before tightening release automation.

Purpose: Phase 31 starts by resolving the concrete reliability and copy defects that currently undermine workspace confidence.
Output: Stable repeated-generation workspace behavior, cleaned user-visible text, and committed regression proof around both seams.
</objective>

<execution_context>
@C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fabio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-long-vacancy-stability-and-release-hygiene-gates/31-RESEARCH.md
@.planning/phases/31-long-vacancy-stability-and-release-hygiene-gates/31-VALIDATION.md
@AGENTS.md
@tests/e2e/fixtures/api-mocks.ts
@tests/e2e/long-vacancy-generation.spec.ts
@src/components/dashboard/resume-workspace.tsx
@src/components/dashboard/chat-interface.tsx
@src/lib/agent/pre-loop-setup.ts
@src/lib/agent/message-preparation.ts
@src/lib/agent/vacancy-analysis.ts
</context>

<threat_model>
<trust_boundaries>
- Repeated browser generation cycles -> workspace rehydration and preview readiness
- Active source strings -> rendered dashboard and agent-facing copy
- E2E mock contract -> confidence in long-vacancy release behavior
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-31-01</threat_id>
  <category>Reliability</category>
  <component>Long-vacancy generation and workspace reload</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Stabilize the repeated generate-and-reload seam and lock it with focused browser and component tests.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-31-02</threat_id>
  <category>Integrity</category>
  <component>User-visible and agent-visible text surfaces</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Remove mojibake from active source files and tests so broken encoding cannot silently re-enter the release surface.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize repeated long-vacancy generation across workspace reloads</name>
  <files>tests/e2e/fixtures/api-mocks.ts, tests/e2e/long-vacancy-generation.spec.ts, src/components/dashboard/resume-workspace.tsx, src/components/dashboard/resume-workspace.test.tsx, src/components/dashboard/preview-panel.test.tsx</files>
  <read_first>tests/e2e/fixtures/api-mocks.ts, tests/e2e/long-vacancy-generation.spec.ts, src/components/dashboard/resume-workspace.tsx, src/lib/dashboard/workspace-client.ts, AGENTS.md</read_first>
  <action>Fix the seam that leaves `data-base-output-ready` false or the workspace stuck busy after repeated generate-and-reload cycles. Prefer correcting workspace or mock state handoff and preview readiness semantics over simply increasing timeouts. Extend component and browser proof so the known long-vacancy failure is locked down explicitly.</action>
  <verify>npm run typecheck && npm test -- src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/preview-panel.test.tsx && npm run test:e2e -- tests/e2e/long-vacancy-generation.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - repeated base generation keeps `data-base-output-ready` true after reload once generation succeeds
    - preview and document panel stay ready across repeated long-vacancy cycles
    - the fix addresses the actual state handoff seam rather than only relaxing timing assertions
  </acceptance_criteria>
  <done>The long-vacancy stress path becomes stable under committed browser proof.</done>
</task>

<task type="auto">
  <name>Task 2: Remove broken encoding artifacts from active dashboard and agent copy</name>
  <files>src/components/dashboard/chat-interface.tsx, src/lib/agent/pre-loop-setup.ts, src/lib/agent/message-preparation.ts, src/lib/agent/vacancy-analysis.ts, src/components/dashboard/chat-interface.test.tsx, src/lib/agent/pre-loop-setup.test.ts, src/lib/agent/vacancy-analysis.test.ts</files>
  <read_first>src/components/dashboard/chat-interface.tsx, src/lib/agent/pre-loop-setup.ts, src/lib/agent/message-preparation.ts, src/lib/agent/vacancy-analysis.ts, src/components/dashboard/chat-interface.test.tsx</read_first>
  <action>Clean broken encoding artifacts from active dashboard and agent strings, aligning source and test expectations so visible copy and system notes no longer contain mojibake. Prefer consistent ASCII or correctly encoded UTF-8 strings according to local file conventions, and add focused test proof where the cleanup changes behaviorally significant text.</action>
  <verify>npm run typecheck && npm test -- src/components/dashboard/chat-interface.test.tsx src/lib/agent/pre-loop-setup.test.ts src/lib/agent/vacancy-analysis.test.ts</verify>
  <acceptance_criteria>
    - active user-visible workspace strings no longer contain broken encoding artifacts
    - agent-side vacancy and attachment notes no longer contain committed mojibake
    - tests assert the cleaned copy so easy regressions are caught quickly
  </acceptance_criteria>
  <done>Phase 31 removes the current encoding defects from the release surface and protects the cleanup with tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/preview-panel.test.tsx src/components/dashboard/chat-interface.test.tsx src/lib/agent/pre-loop-setup.test.ts src/lib/agent/vacancy-analysis.test.ts`
- [ ] `npm run test:e2e -- tests/e2e/long-vacancy-generation.spec.ts --project=chromium`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Long-vacancy generation and visible copy defects are fixed with committed regression proof
</success_criteria>

<output>
After completion, create `.planning/phases/31-long-vacancy-stability-and-release-hygiene-gates/31-01-SUMMARY.md`
</output>

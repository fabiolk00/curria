---
phase: 31-long-vacancy-stability-and-release-hygiene-gates
plan: "02"
type: execute
wave: 2
depends_on:
  - 31-01
requirements:
  - REL-03
files_modified:
  - .github/workflows/ci.yml
  - package.json
  - tests/e2e/core-funnel.spec.ts
  - tests/e2e/long-vacancy-generation.spec.ts
  - src/components/dashboard/resume-workspace.test.tsx
  - src/components/dashboard/preview-panel.test.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - "CI names and executes the release-critical workspace and generation stability checks explicitly instead of relying only on broad aggregate jobs."
    - "Merge-blocking automation covers both the core funnel and the long-vacancy browser path that previously regressed."
    - "The new gate stays focused on workspace, preview, and generation-state confidence rather than turning into a broad all-repo slowdown."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Explicit merge-blocking gate for release-critical workspace and browser stability checks"
      contains: "Browser E2E"
    - path: "package.json"
      provides: "Named script entrypoints for release-critical stability verification"
      contains: "agent:stress-long-vacancy"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "CI executes named release-critical scripts instead of hiding the gate inside one generic browser command"
      pattern: "test:e2e"
---

<objective>
Promote the repaired long-vacancy and workspace stability proof into an explicit release gate so merges fail fast when the critical funnel regresses again.

Purpose: After the product seam is fixed, Phase 31 ends by making the protection durable in CI and release automation.
Output: Focused CI gate for release-critical workspace, preview, and generation-state regressions.
</objective>

<execution_context>
@C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fabio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-long-vacancy-stability-and-release-hygiene-gates/31-RESEARCH.md
@.planning/phases/31-long-vacancy-stability-and-release-hygiene-gates/31-VALIDATION.md
@AGENTS.md
@.github/workflows/ci.yml
@package.json
@tests/e2e/core-funnel.spec.ts
@tests/e2e/long-vacancy-generation.spec.ts
</context>

<threat_model>
<trust_boundaries>
- Local passing fixes -> merge-blocking CI confidence
- Release-critical browser coverage -> confidence in preview and generation-state behavior
- Generic CI jobs -> operator visibility into what actually protects the milestone
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-31-03</threat_id>
  <category>Regression</category>
  <component>Workspace and generation release path</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Elevate the repaired core funnel and long-vacancy browser proof into explicit CI gates that block merges when release-critical state breaks again.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create explicit release-critical browser and workspace gate entrypoints</name>
  <files>package.json, tests/e2e/core-funnel.spec.ts, tests/e2e/long-vacancy-generation.spec.ts, src/components/dashboard/resume-workspace.test.tsx, src/components/dashboard/preview-panel.test.tsx</files>
  <read_first>package.json, tests/e2e/core-funnel.spec.ts, tests/e2e/long-vacancy-generation.spec.ts, src/components/dashboard/resume-workspace.test.tsx, src/components/dashboard/preview-panel.test.tsx</read_first>
  <action>Define or refine named scripts and any supporting focused suites so the release-critical workspace, preview, and generation-state checks are runnable as one explicit gate. Keep the gate narrow to the milestone-critical surface: core funnel browser flow, long-vacancy stress flow, and the key workspace or preview unit contracts.</action>
  <verify>npm run typecheck && npm run test:e2e -- tests/e2e/core-funnel.spec.ts tests/e2e/long-vacancy-generation.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - release-critical verification can be invoked by a clear named command or small set of named commands
    - the gate covers both core funnel readiness and long-vacancy repeated-generation stability
    - supporting focused unit or component proof still runs as part of the release-critical contract where useful
  </acceptance_criteria>
  <done>The release-critical stability net is explicit and runnable without reading CI internals.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the release-critical stability net into CI as a merge blocker</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <read_first>.github/workflows/ci.yml, package.json, .planning/phases/31-long-vacancy-stability-and-release-hygiene-gates/31-VALIDATION.md</read_first>
  <action>Update CI so merge-blocking automation runs the explicit release-critical workspace and browser stability checks with readable step names. Preserve the existing brownfield CI shape where possible, but make it obvious that preview and generation-state regressions block release rather than being buried in one generic browser job.</action>
  <verify>npm run typecheck && npm run test:e2e -- tests/e2e/core-funnel.spec.ts tests/e2e/long-vacancy-generation.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - CI has an explicit step or job for release-critical workspace and generation stability
    - the gate fails on core funnel or long-vacancy browser regressions
    - the resulting CI changes are scoped to release-critical confidence instead of broad unrelated workflow churn
  </acceptance_criteria>
  <done>Phase 31 ends with focused merge-blocking automation for the repaired workspace and generation seams.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm run test:e2e -- tests/e2e/core-funnel.spec.ts tests/e2e/long-vacancy-generation.spec.ts --project=chromium`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- CI explicitly blocks merges on release-critical workspace or generation regressions
</success_criteria>

<output>
After completion, create `.planning/phases/31-long-vacancy-stability-and-release-hygiene-gates/31-02-SUMMARY.md`
</output>

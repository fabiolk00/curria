# 56-01 Plan

## Objective

Align CI, audit scope, proof-pack membership, and governance documentation with the documented critical-route architecture without changing runtime behavior.

## Tasks

1. Update `.github/workflows/ci.yml` to run `npm run test:architecture-proof-pack`.
2. Extend `scripts/audit-route-architecture.mjs` so `compare`, `comparison`, and `versions` are enforced critical route families with expected extracted modules.
3. Add the `comparison` route and extracted seam tests to `scripts/run-architecture-proof-pack.mjs`.
4. Replace non-portable architecture doc links and refresh governance docs to describe the enforced critical-route surface.
5. Run `npm run audit:route-architecture`, `npm run test:architecture-proof-pack`, and `npm run typecheck`.

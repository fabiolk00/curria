---
phase: 45-improve-billing-transparency-alerts-and-concurrency-proof
plan: "02"
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - src/app/api/billing/history/route.ts
  - src/app/api/billing/history/route.test.ts
  - src/components/dashboard/billing-activity-card.tsx
  - src/components/dashboard/billing-activity-card.test.tsx
  - src/app/(auth)/settings/page.tsx
  - src/lib/dashboard/workspace-client.ts
autonomous: true
requirements:
  - BILL-UX-01
user_setup: []
must_haves:
  truths:
    - "An authenticated user can inspect recent export credit activity without leaving the existing authenticated product surface."
    - "Billing history stays derived from ledger and reservation evidence, with localized copy that explains reserve, finalize, release, and reconciliation outcomes."
    - "The new history surface remains read-only and secure: it never exposes another user's activity, does not mutate billing state, and degrades without breaking the settings page."
  artifacts:
    - path: "src/app/api/billing/history/route.ts"
      provides: "Authenticated read-only billing history endpoint"
      contains: "GET"
    - path: "src/components/dashboard/billing-activity-card.tsx"
      provides: "Settings-page billing transparency UI"
      contains: "recent export credit activity"
    - path: "src/app/(auth)/settings/page.tsx"
      provides: "Integrated authenticated billing history section on the existing settings page"
      contains: "Billing activity"
    - path: "src/app/api/billing/history/route.test.ts"
      provides: "Ownership and response-shape proof for the new history endpoint"
      contains: "Unauthorized"
    - path: "src/components/dashboard/billing-activity-card.test.tsx"
      provides: "UI proof for labels, empty state, and reconciliation messaging"
      contains: "reconciliacao"
  key_links:
    - from: "src/app/api/billing/history/route.ts"
      to: "src/lib/billing/credit-activity.ts"
      via: "authenticated history query"
      pattern: "listBillingHistoryForUser"
    - from: "src/components/dashboard/billing-activity-card.tsx"
      to: "src/lib/dashboard/workspace-client.ts"
      via: "client-side history fetch that keeps the server-rendered settings page additive and non-blocking"
      pattern: "getBillingHistory"
    - from: "src/lib/dashboard/workspace-client.ts"
      to: "src/app/api/billing/history/route.ts"
      via: "typed request helper for the additive card"
      pattern: "/api/billing/history"
    - from: "src/app/(auth)/settings/page.tsx"
      to: "src/components/dashboard/billing-activity-card.tsx"
      via: "existing authenticated settings surface"
      pattern: "BillingActivityCard"
---

<objective>
Expose the first authenticated user-facing transparency surface for reservation-backed export billing inside the existing settings flow.

Purpose: Extend the brownfield settings experience with clear export credit history instead of introducing a new product area or second billing truth.
Output: One authenticated read-only history API plus a settings-page card that renders recent export credit activity and reconciliation outcomes safely.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/CURRIA-45-improve-billing-transparency-alerts-and-concurrency-proof/45-CONTEXT.md
@.planning/phases/CURRIA-45-improve-billing-transparency-alerts-and-concurrency-proof/45-RESEARCH.md
@.planning/phases/CURRIA-45-improve-billing-transparency-alerts-and-concurrency-proof/45-01-PLAN.md
@src/app/(auth)/settings/page.tsx
@src/lib/asaas/optional-billing-info.ts
@src/lib/dashboard/workspace-client.ts
@src/types/dashboard.ts

<interfaces>
From `src/types/dashboard.ts`:
```ts
export type DownloadUrlsResponse = {
  docxUrl: string | null
  pdfUrl: string | null
  available: boolean
  generationStatus: ArtifactStatusSummary['generationStatus']
}
```

From `src/lib/asaas/optional-billing-info.ts`:
```ts
export async function loadOptionalBillingInfo(
  appUserId: string,
  surface: 'auth_layout' | 'dashboard_page' | 'settings_page',
): Promise<{
  billingInfo: Awaited<ReturnType<typeof getUserBillingInfo>>
  billingNotice: string | null
}>
```

Existing settings surface to preserve:
```tsx
export default async function SettingsPage() {
  const [appUser, clerkUser] = await Promise.all([getCurrentAppUser(), currentUser()])
}
```

Contracts from Plan 45-01 to use directly:
- `BillingHistoryEntry`
- `BillingHistoryResponse`
- `listBillingHistoryForUser(...)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an authenticated read-only billing history endpoint</name>
  <files>src/app/api/billing/history/route.ts, src/app/api/billing/history/route.test.ts, src/lib/dashboard/workspace-client.ts</files>
  <behavior>
    - Test 1: unauthenticated requests return 401 before any billing-history lookup.
    - Test 2: authenticated requests return only the current user's export credit activity and respect a small, validated page-size limit.
    - Test 3: response rows preserve the Plan 45-01 DTO contract instead of leaking raw ledger table names directly.
  </behavior>
  <action>Add a thin authenticated route under `src/app/api/billing/history/route.ts` that validates query params with `zod`, resolves the current app user, and returns recent export credit activity through the Plan 45-01 contract. Keep the handler thin and read-only. Add a typed `getBillingHistory(...)` helper to `src/lib/dashboard/workspace-client.ts` so the additive settings card can fetch the new route without changing `loadOptionalBillingInfo(...)`. Keep plan and balance metadata separate from the new timeline payload so the brownfield auth layout remains stable.</action>
  <verify>
    <automated>npx vitest run src/app/api/billing/history/route.test.ts</automated>
  </verify>
  <done>An authenticated billing-history route exists with focused ownership tests and a stable DTO shape suitable for the settings page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the billing activity card to the existing settings page</name>
  <files>src/components/dashboard/billing-activity-card.tsx, src/components/dashboard/billing-activity-card.test.tsx, src/app/(auth)/settings/page.tsx, src/lib/dashboard/workspace-client.ts</files>
  <behavior>
    - Test 1: the settings page shows recent export credit activity with localized labels for reserve, finalize, release, and reconciliation states.
    - Test 2: empty history renders an explicit no-activity state instead of a blank card.
    - Test 3: history-load failures stay non-blocking for the settings page and reconciliation-required rows remain understandable without implying that credits were double-charged.
  </behavior>
  <action>Add a new `BillingActivityCard` to the existing settings page rather than creating a standalone billing area. Implement the card as an additive client component that calls `getBillingHistory(...)`, so the existing server-rendered settings page keeps its current load path and remains usable even if billing history lookup fails. Render the history as a read-only section alongside the current account summary cards, keep the wording explicit that this feed represents recent export credit activity derived from current reservation and ledger evidence, and show a lightweight degraded-state notice instead of blocking the page. Keep copy in PT-BR, avoid raw enum names, and do not introduce new mutation controls, retry buttons, or billing-model redesign in this phase.</action>
  <verify>
    <automated>npx vitest run src/components/dashboard/billing-activity-card.test.tsx</automated>
  </verify>
  <done>The authenticated settings page contains a clear, localized export credit history card backed by the new endpoint and covered by focused UI tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> billing history route | User identity gates access to financial activity data |
| billing history DTO -> settings UI | Internal audit states are translated into user-facing explanations |
| settings page -> new history card | New transparency UI must not destabilize existing account and plan surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | I | `/api/billing/history` | mitigate | Resolve `getCurrentAppUser()` first and scope all activity reads by the returned app user id. |
| T-45-06 | T | settings page timeline copy | mitigate | Use typed DTOs from Plan 45-01 and explicit PT-BR presentation mapping instead of rendering raw ledger enum strings. |
| T-45-07 | D | settings page rendering | mitigate | Keep the history section additive and read-only so billing transparency cannot block the rest of the settings surface. |
| T-45-08 | R | user understanding of reconciliation | mitigate | Show reconciliation states with explicit explanatory labels rather than ambiguous generic failures. |
</threat_model>

<verification>
- `npx vitest run src/app/api/billing/history/route.test.ts`
- `npx vitest run src/components/dashboard/billing-activity-card.test.tsx`
- `npm run typecheck`
</verification>

<success_criteria>
- Users can inspect recent export credit activity inside the existing authenticated settings page.
- The history feed is authenticated, localized, and clearly derived from reservation and ledger truth.
- No new billing mutation surface or parallel billing source is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-45-improve-billing-transparency-alerts-and-concurrency-proof/45-02-SUMMARY.md`
</output>

---
phase: 88-harden-experience-entry-highlight-surfacing-with-explicit-po
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resume/optimized-preview-highlights.ts
  - src/lib/resume/optimized-preview-highlights.test.ts
  - src/lib/resume/optimized-preview-contracts.test.ts
  - .planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-VALIDATION.md
autonomous: true
requirements:
  - EXP-HILITE-SURFACING-HARDEN-01
  - EXP-HILITE-SURFACING-HARDEN-TEST-01
  - EXP-HILITE-SURFACING-HARDEN-OBS-01
user_setup: []
must_haves:
  truths:
    - "The same-entry editorial priority order is an exported policy constant with an explicit warning that changing it alters visible editorial behavior under cap pressure."
    - "Cap ownership remains explicit at the Layer 3 selector boundary rather than hidden indirectly in callers or inline comparator logic."
    - "Layer 3 now has direct edge-case coverage and a lightweight debug-only trace that explains selected versus suppressed bullets without changing normal product behavior."
  artifacts:
    - path: "src/lib/resume/optimized-preview-highlights.ts"
      provides: "Explicit exported Layer 3 editorial policy constant, cap contract clarification, and debug-only surfacing observability"
      contains: "EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY"
    - path: "src/lib/resume/optimized-preview-highlights.test.ts"
      provides: "Direct pure-unit coverage for no-highlight entries, deterministic same-category ties, and cap enforcement"
      contains: "returns no surfaced highlights when an entry has no eligible visible bullets"
    - path: "src/lib/resume/optimized-preview-contracts.test.ts"
      provides: "Contract proof that surfacing hardening preserves existing entry/bullet cap behavior"
      contains: "Contract 5"
    - path: ".planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-VALIDATION.md"
      provides: "Short hardening note covering policy constant location, cap ownership, debug trace, and hardening-only scope"
      contains: "hardening pass only"
  key_links:
    - from: "src/lib/resume/optimized-preview-highlights.ts"
      to: "src/lib/resume/optimized-preview-highlights.ts"
      via: "policy constant and explicit selector contract"
      pattern: "(EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY|selectVisibleExperienceHighlightsForEntry|maxVisibleHighlights)"
    - from: "src/lib/resume/optimized-preview-highlights.ts"
      to: "src/lib/resume/optimized-preview-highlights.test.ts"
      via: "pure selector unit scenarios"
      pattern: "selectVisibleExperienceHighlightsForEntry"
    - from: "src/lib/resume/optimized-preview-highlights.ts"
      to: "debug trace"
      via: "development-only diagnostic logging"
      pattern: "(console\\.debug|__CURRIA_DEBUG_EXPERIENCE_HIGHLIGHT_SURFACING__)"
---

<objective>
Implement Phase 88 as a strict hardening pass over the Phase 87 Layer 3 selector.

Purpose: make the same-entry surfacing policy more explicit, easier to test, and easier to debug without reopening selector ranking, completion, render tiers, ATS gates, summary behavior, or export behavior.
Output: explicit exported policy constant, clarified cap contract, direct edge-case tests, lightweight debug-only observability, and a short hardening note.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-CONTEXT.md
@.planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-RESEARCH.md
@.planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-CONTEXT.md
@.planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-VALIDATION.md
@src/lib/resume/optimized-preview-highlights.ts
@src/lib/resume/optimized-preview-highlights.test.ts
@src/lib/resume/optimized-preview-contracts.test.ts

<scope_rules>
- Do not change bullet-level candidate extraction or winner selection.
- Do not change span completion rules.
- Do not change evidence tier semantics or styling.
- Do not change ATS readiness, summary, rewrite, or export behavior.
- Keep the implementation local to the Layer 3 selector seam and immediate tests/docs.
</scope_rules>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Externalize the Layer 3 editorial policy and clarify cap ownership</name>
  <files>src/lib/resume/optimized-preview-highlights.ts, src/lib/resume/optimized-preview-highlights.test.ts</files>
  <behavior>
    - Test 1: the surfacing seam still selects Tier 1 bullets ahead of Tier 2 after the editorial priority map becomes an exported constant.
    - Test 2: the selector remains callable with an explicit cap parameter and preserves deterministic tie behavior under that explicit contract.
  </behavior>
  <action>Export `EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY` from `optimized-preview-highlights.ts` and add a clear comment warning that this ordering directly controls visible same-entry editorial behavior under cap pressure. Preserve the preferred explicit-cap design by documenting `maxVisibleHighlights` at `selectVisibleExperienceHighlightsForEntry(...)` rather than moving cap ownership back into hidden caller state. Keep the comparator logic behaviorally identical; this task is making policy explicit, not changing policy.</action>
  <verify>
    <automated>npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"</automated>
  </verify>
  <done>The editorial order is an exported, documented policy constant, the selector's cap contract is explicit at the function boundary, and existing happy-path ordering still passes without changing the underlying Layer 3 policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add direct Layer 3 edge-case coverage and debug-only surfacing observability</name>
  <files>src/lib/resume/optimized-preview-highlights.ts, src/lib/resume/optimized-preview-highlights.test.ts, src/lib/resume/optimized-preview-contracts.test.ts</files>
  <behavior>
    - Test 1: an entry with bullets but no eligible/renderable highlight returns zero surfaced bullets.
    - Test 2: same tier, same category, same `winnerScore`, and same `improvementScore` stay deterministic by stable bullet order.
    - Test 3: the selector never exceeds the entry cap and lower-priority bullets do not leak in after stronger bullets fill that cap.
    - Test 4: the debug trace only emits in non-production mode when `globalThis.__CURRIA_DEBUG_EXPERIENCE_HIGHLIGHT_SURFACING__ === true`.
    - Test 5: the debug payload is compact and summarizes eligible, selected, and suppressed bullets with tier/category/score metadata rather than dumping raw resume content.
  </behavior>
  <action>Add explicit pure-unit tests around the Layer 3 selector for the missing edge cases. Then add a tiny debug-only observability helper local to `optimized-preview-highlights.ts` that emits a compact `console.debug` trace only in non-production builds when a dedicated dev flag on `globalThis` is enabled. The payload should summarize eligibility, category, tier, scores, and selected/suppressed status without turning this into product analytics or changing normal behavior.</action>
  <verify>
    <automated>npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts"</automated>
    <automated>npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" -t "debug trace"</automated>
  </verify>
  <done>The Layer 3 selector has direct edge-case tests for empty eligibility, deterministic ties, explicit cap behavior, and debug-trace gating/payload shape, and a debug-only trace exists that explains surfacing outcomes without affecting normal product behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record the hardening note and hardening-only validation statement</name>
  <files>.planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-VALIDATION.md</files>
  <behavior>
    - Validation note states where the priority map now lives.
    - Validation note states how cap ownership is handled and confirms the preferred explicit-cap option was used.
    - Validation note states what debug observability was added and confirms no selector logic or UI-tier policy was changed.
  </behavior>
  <action>Write `88-VALIDATION.md` with a short hardening note covering the exported policy constant, cap ownership decision, debug observability hook, and a clear statement that this was a hardening pass only with no reopened selector/completion/render-tier behavior.</action>
  <verify>
    <automated>rg -n "(EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY|maxVisibleHighlights|debug-only|hardening pass only)" ".planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-VALIDATION.md"</automated>
  </verify>
  <done>The phase directory contains a concise hardening note that documents the policy constant location, the explicit cap contract, the debug-only trace, and the fact that no selector or UI-tier behavior was reopened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editorial policy constant -> Layer 3 comparator | Future edits must understand they are changing visible product behavior, not incidental code style |
| explicit cap parameter -> selector output | Cap changes alter visible editorial suppression and must remain explicit at the API boundary |
| debug trace -> developer diagnostics | Observability must stay diagnostic-only and must not affect user-visible behavior or production noise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | T | policy ordering | mitigate | Export the category-priority constant with an intent comment so future edits are explicit editorial changes. |
| T-88-02 | D | cap behavior | mitigate | Keep `maxVisibleHighlights` explicit in the selector interface and prove cap enforcement directly in unit tests. |
| T-88-03 | I | debug trace | mitigate | Guard `console.debug` behind non-production mode and an opt-in global debug flag with compact summarized payloads only. |
</threat_model>

<verification>
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"`
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"`
- `npm run typecheck`
</verification>

<success_criteria>
- The Layer 3 editorial ordering lives in an exported, explicitly documented policy constant.
- Cap ownership is explicit in the selector API and remains easy to reason about in isolation.
- Missing Layer 3 edge cases are covered directly by pure-unit tests.
- A debug-only surfacing trace exists and stays local, low-risk, and behaviorally inert when disabled.
- The hardening note confirms this phase did not reopen selector logic, UI-tier policy, or adjacent layers.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-01-SUMMARY.md`
</output>

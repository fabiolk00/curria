# 72-01 Plan

1. Add centralized helpers to normalize filename segments and build the final resume export filename.
2. Teach the file-download response to emit canonical filename metadata for the current export.
3. Update preview and session-document consumers to use the canonical filename instead of local hardcoded names.
4. Add regression coverage for accent stripping, ATS-vs-targeted naming, fallback behavior, and route propagation.

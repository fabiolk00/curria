---
phase: 38-refactor-api-agent-into-a-lightweight-orchestrator
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/agent/route.ts
  - src/lib/agent/request-orchestrator.ts
  - src/lib/agent/action-classification.ts
  - src/lib/agent/async-dispatch.ts
  - src/lib/agent/pre-loop-setup.ts
  - src/lib/agent/agent-loop.ts
  - src/lib/agent/agent-persistence.ts
  - src/lib/agent/action-classification.test.ts
  - src/lib/agent/request-orchestrator.test.ts
  - src/lib/agent/pre-loop-setup.test.ts
  - src/app/api/agent/route.test.ts
  - src/app/api/agent/route.sse.test.ts
  - .planning/phases/38-refactor-api-agent-into-a-lightweight-orchestrator/**
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
user_setup: []
must_haves:
  truths:
    - "Phase 38 keeps `/api/agent` as the public entry point and preserves lightweight chat as a synchronous streaming path."
    - "Heavy ATS enhancement, job targeting, and artifact generation no longer execute inline in the request path; they are classified explicitly and handed off through the Phase 37 durable-job foundation."
    - "Session load/create, target detection, `X-Session-Id`, `sessionCreated`, and transcript ordering must stay behaviorally consistent with the current brownfield client contract."
    - "Async acknowledgement paths must persist user and assistant turns outside `runAgentLoop(...)` so history and `messageCount` do not drift."
    - "`cvState` remains canonical truth, `agentState` remains operational context, and async dispatch must use source-of-truth refs plus internal app user IDs."
    - "Phase 38 changes request-boundary routing only; worker execution logic and rich status UX remain deferred to Phases 39 and 40."
  artifacts:
    - path: "src/app/api/agent/route.ts"
      provides: "Thin POST wrapper that delegates orchestration instead of owning heavy execution logic"
      contains: "handleAgentPost"
    - path: "src/lib/agent/request-orchestrator.ts"
      provides: "Route-boundary orchestration for auth, session continuity, target detection, streaming shell, and sync-vs-async branching"
      contains: "export async function handleAgentPost"
    - path: "src/lib/agent/action-classification.ts"
      provides: "Pure mapping from request/session context into `AgentActionType` and execution mode"
      contains: "export function classifyAgentAction"
    - path: "src/lib/agent/async-dispatch.ts"
      provides: "Durable heavy-action dispatch helper using Phase 37 jobs plus acknowledgement payload construction"
      contains: "export async function dispatchAsyncAction"
    - path: "src/lib/agent/agent-persistence.ts"
      provides: "Shared transcript helpers reused by sync loop and async acknowledgement paths"
      contains: "appendAssistantTurn"
    - path: "src/lib/agent/pre-loop-setup.ts"
      provides: "Lightweight pre-loop preparation only after inline ATS and target-job work is removed"
      contains: "export async function runPreLoopSetup"
  key_links:
    - from: "src/lib/agent/action-classification.ts"
      to: "src/types/jobs.ts"
      via: "classification must terminate in the frozen Phase 37 action and execution-mode contract"
      pattern: "resolveExecutionMode"
    - from: "src/lib/agent/async-dispatch.ts"
      to: "src/lib/jobs/repository.ts"
      via: "heavy request handoff must create or reuse durable jobs through the shared repository"
      pattern: "createJob"
    - from: "src/lib/agent/async-dispatch.ts"
      to: "src/lib/jobs/source-of-truth.ts"
      via: "dispatch input refs must resolve the correct effective resume snapshot"
      pattern: "resolveEffectiveResumeSource"
    - from: "src/lib/agent/request-orchestrator.ts"
      to: "src/lib/agent/agent-persistence.ts"
      via: "async acknowledgement branches must persist transcript turns outside the sync loop"
      pattern: "appendUserTurn"
    - from: "src/lib/agent/request-orchestrator.ts"
      to: "src/app/api/agent/route.ts"
      via: "the route stays public but delegates orchestration to one extracted boundary module"
      pattern: "handleAgentPost"
---

<objective>
Refactor `/api/agent` into a lightweight orchestrator that preserves synchronous chat behavior while classifying and dispatching heavy ATS, targeting, and artifact work asynchronously through the Phase 37 job contracts.

Purpose: Phase 38 is the request-boundary split. It must keep the current public route and UX contract intact while removing inline heavy execution from the synchronous request path.
Output: One thin route wrapper, one extracted orchestrator, one explicit classification and async-dispatch seam, lighter pre-loop and agent-loop modules, and focused regression proof for sync chat parity plus async handoff behavior.
</objective>

<execution_context>
@C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fabio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-refactor-api-agent-into-a-lightweight-orchestrator/38-RESEARCH.md
@.planning/phases/38-refactor-api-agent-into-a-lightweight-orchestrator/38-VALIDATION.md
@AGENTS.md
@CLAUDE.md
@src/app/api/agent/route.ts
@src/app/api/agent/route.test.ts
@src/app/api/agent/route.sse.test.ts
@src/lib/agent/agent-loop.ts
@src/lib/agent/pre-loop-setup.ts
@src/lib/agent/agent-persistence.ts
@src/types/agent.ts
@src/types/jobs.ts
@src/lib/jobs/repository.ts
@src/lib/jobs/source-of-truth.ts
@src/components/dashboard/chat-interface.tsx
</context>

<threat_model>
<trust_boundaries>
- Public `/api/agent` request body -> authenticated app-user-scoped orchestration
- Orchestrator classification -> synchronous loop versus durable async dispatch
- Async acknowledgement path -> persisted transcript and session continuity state
- Durable job creation -> source-of-truth snapshot refs and duplicate-dispatch protection
</trust_boundaries>
<stride_register>
<threat>
  <threat_id>T-38-01</threat_id>
  <category>Elevation of Privilege</category>
  <component>Route and durable-job ownership</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep orchestration and dispatch scoped to internal app user IDs so session and job reads never cross user boundaries.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-38-02</threat_id>
  <category>Tampering</category>
  <component>Heavy-action dispatch and transcript persistence</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Use deterministic job idempotency plus shared transcript helpers so retries or repeated clicks do not duplicate jobs or silently lose user-visible turns.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-38-03</threat_id>
  <category>Repudiation</category>
  <component>Session continuity and acknowledgement ordering</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Preserve `X-Session-Id`, `sessionCreated`, target detection, and message-ordering behavior so the client can still prove what was accepted and when.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-38-04</threat_id>
  <category>Denial of Service</category>
  <component>Inline heavy execution remaining in request path</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Remove ATS, target-job, and artifact heavy branches from the synchronous request path instead of only moving code into smaller helper files.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit action classification and durable async dispatch seams</name>
  <files>src/lib/agent/action-classification.ts, src/lib/agent/async-dispatch.ts, src/lib/agent/action-classification.test.ts</files>
  <read_first>src/types/jobs.ts, src/lib/jobs/repository.ts, src/lib/jobs/source-of-truth.ts, src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts, .planning/phases/38-refactor-api-agent-into-a-lightweight-orchestrator/38-RESEARCH.md</read_first>
  <action>Create `src/lib/agent/action-classification.ts` as one pure brownfield-to-contract mapping layer that inspects current request plus session context and returns an `AgentActionType`, `ExecutionMode`, and any branch metadata needed by the orchestrator. Cover the known heavy entry points that already exist in the route today: target-job rewrite continuation, ATS enhancement continuation, and `Aceito` or generation-confirm flows that should become `artifact_generation`, while preserving ordinary lightweight chat and deterministic dialog-recovery paths as synchronous `chat`. Create `src/lib/agent/async-dispatch.ts` to build deterministic idempotency keys, resolve the effective resume source via `resolveEffectiveResumeSource(...)`, create or reuse jobs through `createJob(...)`, and return a text-only acknowledgement payload plus canonical job snapshot data for the orchestrator. Do not invent a new status vocabulary, do not add a second job persistence path, and do not emit a new SSE chunk type in this phase. Add `src/lib/agent/action-classification.test.ts` to prove the main brownfield inputs map to the expected `AgentActionType` and `ExecutionMode` outputs.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/action-classification.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/agent/action-classification.ts` contains `export function classifyAgentAction(`
    - `src/lib/agent/action-classification.ts` contains `resolveExecutionMode(`
    - `src/lib/agent/action-classification.ts` contains `'artifact_generation'`
    - `src/lib/agent/action-classification.ts` contains `'job_targeting'`
    - `src/lib/agent/action-classification.ts` contains `'ats_enhancement'`
    - `src/lib/agent/async-dispatch.ts` contains `export async function dispatchAsyncAction(`
    - `src/lib/agent/async-dispatch.ts` contains `createJob(`
    - `src/lib/agent/async-dispatch.ts` contains `resolveEffectiveResumeSource(`
    - `src/lib/agent/async-dispatch.ts` contains `idempotencyKey`
    - `src/lib/agent/action-classification.test.ts` contains `describe('classifyAgentAction'`
  </acceptance_criteria>
  <done>The repo has one explicit sync-vs-async decision seam and one durable dispatch seam that already speak the Phase 37 contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Extract request-boundary orchestration and keep the route thin</name>
  <files>src/app/api/agent/route.ts, src/lib/agent/request-orchestrator.ts, src/lib/agent/request-orchestrator.test.ts, src/app/api/agent/route.test.ts</files>
  <read_first>src/app/api/agent/route.ts, src/app/api/agent/route.test.ts, src/components/dashboard/chat-interface.tsx, src/lib/auth/app-user.ts, src/lib/rate-limit.ts, .planning/phases/38-refactor-api-agent-into-a-lightweight-orchestrator/38-RESEARCH.md</read_first>
  <action>Extract the current route-boundary responsibilities into `src/lib/agent/request-orchestrator.ts`: auth, rate limiting, Zod body parsing, message preparation, session load-or-create, target-job detection, session updates, timing and structured logs, stream shell creation, and the branch point between sync chat and async acknowledgement or dispatch. Leave `src/app/api/agent/route.ts` as a thin wrapper that delegates to the orchestrator instead of directly owning `runAgentLoop(...)`, `runPreLoopSetup(...)`, or durable dispatch details. Preserve `X-Session-Id` and `sessionCreated` behavior for new sessions, keep target detection ahead of the sync-versus-async branch, and keep async acknowledgements text-only so the current frontend chunk handling continues to work unchanged. Add `src/lib/agent/request-orchestrator.test.ts` and update `src/app/api/agent/route.test.ts` to prove auth, session continuity, target detection, header ordering, and new-session event ordering remain intact after extraction.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/request-orchestrator.test.ts src/app/api/agent/route.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/agent/request-orchestrator.ts` contains `export async function handleAgentPost(`
    - `src/lib/agent/request-orchestrator.ts` contains `X-Session-Id`
    - `src/lib/agent/request-orchestrator.ts` contains `sessionCreated`
    - `src/lib/agent/request-orchestrator.ts` contains `classifyAgentAction(`
    - `src/app/api/agent/route.ts` contains `handleAgentPost(`
    - `src/app/api/agent/route.ts` does not contain `runAgentLoop(`
    - `src/app/api/agent/route.ts` does not contain `runPreLoopSetup(`
    - `src/lib/agent/request-orchestrator.test.ts` contains `describe('handleAgentPost'`
  </acceptance_criteria>
  <done>`/api/agent` stays public but becomes a thin route wrapper over one extracted orchestration boundary that preserves current session and SSE contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Remove inline heavy execution from pre-loop and sync-loop paths</name>
  <files>src/lib/agent/pre-loop-setup.ts, src/lib/agent/agent-loop.ts, src/lib/agent/agent-persistence.ts, src/lib/agent/pre-loop-setup.test.ts, src/lib/agent/agent-persistence.test.ts</files>
  <read_first>src/lib/agent/pre-loop-setup.ts, src/lib/agent/agent-loop.ts, src/lib/agent/agent-persistence.ts, src/lib/agent/pre-loop-setup.test.ts, src/lib/agent/agent-persistence.test.ts, .planning/phases/38-refactor-api-agent-into-a-lightweight-orchestrator/38-RESEARCH.md</read_first>
  <action>Refactor `src/lib/agent/pre-loop-setup.ts` so it only does lightweight preparation work such as file attachment parsing, workflow-mode persistence, and message-count handling. Remove inline `runAtsEnhancementPipeline(...)` and `runJobTargetingPipeline(...)` execution from the request path, because those actions now classify and dispatch asynchronously before entering the sync loop. Refactor `src/lib/agent/agent-loop.ts` so lightweight chat behavior stays intact, but confirmed heavy artifact-generation paths no longer execute `create_target_resume` or `generate_file` inline. Where a request is classified async and short-circuits before `runAgentLoop(...)`, use shared helpers in `src/lib/agent/agent-persistence.ts` to persist the user turn plus assistant acknowledgement before returning a `done`-compatible response. Update `src/lib/agent/pre-loop-setup.test.ts` and `src/lib/agent/agent-persistence.test.ts` so they prove heavy pipelines are no longer invoked inline and transcript persistence stays shared across sync and async branches.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/pre-loop-setup.test.ts src/lib/agent/agent-persistence.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/agent/pre-loop-setup.ts` does not contain `runAtsEnhancementPipeline(`
    - `src/lib/agent/pre-loop-setup.ts` does not contain `runJobTargetingPipeline(`
    - `src/lib/agent/agent-loop.ts` does not contain `generate_file`
    - `src/lib/agent/agent-loop.ts` does not contain `create_target_resume`
    - `src/lib/agent/agent-persistence.ts` contains `appendUserTurn(`
    - `src/lib/agent/agent-persistence.ts` contains `appendAssistantTurn(`
    - `src/lib/agent/pre-loop-setup.test.ts` contains `not.toHaveBeenCalled()`
  </acceptance_criteria>
  <done>The request path no longer executes ATS, target-job, or artifact heavy work inline, and async acknowledgement branches reuse the shared transcript seam.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused route and SSE regressions for sync chat parity and async handoff</name>
  <files>src/app/api/agent/route.test.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/action-classification.test.ts, src/lib/agent/request-orchestrator.test.ts</files>
  <read_first>src/app/api/agent/route.test.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/action-classification.test.ts, src/lib/agent/request-orchestrator.test.ts, .planning/phases/38-refactor-api-agent-into-a-lightweight-orchestrator/38-VALIDATION.md</read_first>
  <action>Extend the route and SSE test suites so they prove both halves of the contract in one place: lightweight prompts still stream synchronously, while heavy ATS enhancement, job-targeting, and artifact-generation triggers acknowledge and dispatch through the durable-job seam instead of invoking inline pipelines. Cover both new-session and existing-session flows, preserve target-detection updates, preserve `X-Session-Id` and `sessionCreated` ordering, prove async acknowledgements persist transcript turns, and assert that route code now delegates to the orchestrator and dispatch helper rather than owning heavy branches itself. Keep the regression suite focused and mock-driven so it can run after every task commit.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/action-classification.test.ts src/lib/agent/request-orchestrator.test.ts src/lib/agent/pre-loop-setup.test.ts src/lib/agent/agent-persistence.test.ts src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts</verify>
  <acceptance_criteria>
    - `src/app/api/agent/route.test.ts` contains `sessionCreated`
    - `src/app/api/agent/route.test.ts` contains `X-Session-Id`
    - `src/app/api/agent/route.sse.test.ts` contains `createJob`
    - `src/app/api/agent/route.sse.test.ts` contains `appendAssistantTurn`
    - `src/app/api/agent/route.sse.test.ts` contains `runAgentLoop`
    - `src/lib/agent/request-orchestrator.test.ts` contains `sessionCreated`
    - `npm run typecheck && npx vitest run src/lib/agent/action-classification.test.ts src/lib/agent/request-orchestrator.test.ts src/lib/agent/pre-loop-setup.test.ts src/lib/agent/agent-persistence.test.ts src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts` exits 0
  </acceptance_criteria>
  <done>Phase 38 closes with committed proof that sync chat parity and async durable-job handoff both work through the unchanged `/api/agent` surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/lib/agent/action-classification.test.ts src/lib/agent/request-orchestrator.test.ts src/lib/agent/pre-loop-setup.test.ts src/lib/agent/agent-persistence.test.ts src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts`
- [ ] confirm `/api/agent` still returns `X-Session-Id` for new sessions and omits it for existing-session requests in focused tests
- [ ] confirm no inline `runAtsEnhancementPipeline`, `runJobTargetingPipeline`, `create_target_resume`, or `generate_file` calls remain in the synchronous request path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 38 leaves the repo with one thin route wrapper, one extracted orchestrator, one explicit sync-vs-async classification seam, one durable heavy-dispatch handoff layer, and focused proof that sync chat parity plus async acknowledgement behavior are both preserved
</success_criteria>

<output>
After completion, create `.planning/phases/38-refactor-api-agent-into-a-lightweight-orchestrator/38-01-SUMMARY.md`
</output>

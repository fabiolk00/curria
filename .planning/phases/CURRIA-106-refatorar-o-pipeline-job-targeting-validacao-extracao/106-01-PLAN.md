---
phase: 106-refatorar-o-pipeline-job-targeting-validacao-extracao
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/agent.ts
  - src/types/dashboard.ts
  - src/lib/agent/tools/validate-rewrite.ts
  - src/lib/agent/tools/validate-rewrite.test.ts
  - src/lib/agent/job-targeting-pipeline.ts
  - src/lib/agent/tools/build-targeting-plan.ts
  - src/lib/agent/tools/build-targeting-plan.test.ts
  - src/lib/agent/tools/rewrite-resume-full.ts
  - src/lib/routes/smart-generation/result-normalization.ts
  - src/lib/routes/smart-generation/types.ts
  - src/lib/agent/tools/pipeline.test.ts
autonomous: true
requirements:
  - JOB-TARGET-VAL-01
  - JOB-TARGET-ROLE-01
  - JOB-TARGET-ATS-ISO-01
  - JOB-TARGET-TEST-01
---

## Objective

Corrigir a validacao e o save path de `job_targeting`, introduzir extracao semantica de target role em camadas, e provar por testes e artefatos que o `ats_enhancement` permanece isolado.

## Tasks

### Task 1: Refatorar o contrato compartilhado de validacao com isolamento ATS explicito

Arquivos:

- `src/types/agent.ts`
- `src/types/dashboard.ts`
- `src/lib/agent/tools/validate-rewrite.ts`
- `src/lib/agent/tools/validate-rewrite.test.ts`

Acoes:

- ampliar `RewriteValidationResult` com `blocked`, `hardIssues`, `softWarnings` e alias `issues`;
- reclassificar severidades conforme a tabela da task;
- corrigir a Regra 8 para usar `buildEvidenceText(originalCvState)` como ancora de evidencia do curriculo inteiro;
- manter `valid` como compatibilidade estrita para nao mudar o comportamento ATS nesta fase;
- expandir `buildEvidenceText` para cobrir tambem certificacoes e educacao, evitando falso negativo de evidencia.

Verificacao:

- `npx vitest run src/lib/agent/tools/validate-rewrite.test.ts`

Done:

- `validateRewrite` passa a diferenciar bloqueio duro de warning medio sem quebrar consumidores ATS existentes.

### Task 2: Migrar o pipeline `job_targeting` para bloquear apenas `high`

Arquivos:

- `src/lib/agent/job-targeting-pipeline.ts`
- `src/lib/agent/tools/pipeline.test.ts`

Acoes:

- trocar o criterio de bloqueio de `validation.valid` para `validation.blocked`;
- tratar `softWarnings` como sucesso persistivel;
- manter rollback apenas para `blocked === true` ou falha posterior de persistencia;
- persistir `rewriteValidation` mesmo quando o rewrite segue com warnings;
- persistir `extractionWarning` quando `targetRoleConfidence === 'low'`;
- adicionar warnings amigaveis ao retorno de smart-generation success, se necessario.

Verificacao:

- `npx vitest run src/lib/agent/tools/pipeline.test.ts src/app/api/profile/smart-generation/route.test.ts`

Done:

- `job_targeting` salva com warnings medios, mas continua bloqueando drift factual severo.

### Task 3: Tornar `buildTargetingPlan` async com extracao heuristica -> LLM -> fallback

Arquivos:

- `src/lib/agent/tools/build-targeting-plan.ts`
- `src/lib/agent/tools/build-targeting-plan.test.ts`
- `src/lib/agent/tools/rewrite-resume-full.ts`
- `src/lib/agent/tools/pipeline.test.ts`

Acoes:

- adicionar `targetRoleConfidence: 'medium'` e `targetRoleSource`;
- manter a heuristica atual intacta como camada zero;
- chamar LLM apenas quando a heuristica retornar `low`;
- usar fallback final para `Vaga Alvo` apenas quando heuristica e LLM falharem;
- atualizar callers para `await`, inclusive o fallback interno de `rewriteResumeFull`.

Verificacao:

- `npx vitest run src/lib/agent/tools/build-targeting-plan.test.ts src/lib/agent/tools/pipeline.test.ts`

Done:

- vagas com titulo implicito forte passam a produzir `source: 'llm'` e `confidence: 'medium'` sem ampliar regex hardcoded.

## Shared-File ATS Isolation Notes

- `src/lib/agent/tools/validate-rewrite.ts`: o ATS continua chamando sem `context`, entao as Regras 9 e 10 seguem inativas; `valid` e `issues` permanecem para preservar a semantica atual.
- `src/lib/agent/tools/rewrite-resume-full.ts`: o branch ATS nao avalia `buildTargetingPlan(...)`; a asyncficacao afeta apenas o branch `job_targeting`.
- `src/types/agent.ts`: `issues` continua presente; `extractionWarning` e aditivo; `TargetingPlan.targetRoleSource` nao e construido pelo ATS.

## Validation Gate

- `npx vitest run src/lib/agent/tools/validate-rewrite.test.ts src/lib/agent/tools/build-targeting-plan.test.ts src/lib/agent/tools/pipeline.test.ts src/app/api/profile/smart-generation/route.test.ts src/app/api/session/[id]/route.test.ts`
- `npm run typecheck`

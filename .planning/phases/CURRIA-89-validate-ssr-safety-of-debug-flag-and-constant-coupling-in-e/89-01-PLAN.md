---
phase: 89-validate-ssr-safety-of-debug-flag-and-constant-coupling-in-e
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resume/optimized-preview-highlights.ts
  - .planning/phases/CURRIA-89-validate-ssr-safety-of-debug-flag-and-constant-coupling-in-e/89-VALIDATION.md
autonomous: true
requirements:
  - EXP-HILITE-SAFETY-SSR-01
  - EXP-HILITE-SAFETY-COUPLING-01
  - EXP-HILITE-SAFETY-VALIDATION-01
user_setup: []
must_haves:
  truths:
    - "The real call chain into the same-entry surfacing layer is documented well enough to justify whether the debug flag is runtime-local, server-only, test-only, or mixed-context."
    - "No selector logic, editorial policy, ATS gates, span completion, or UI-tier behavior changes as part of this safety validation pass."
    - "Tests do not couple themselves to `EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY` as a fixture; if a coupling is discovered, it is corrected back to observable-behavior assertions."
  artifacts:
    - path: "src/lib/resume/optimized-preview-highlights.ts"
      provides: "Explicit mixed-context/runtime-local debug-flag semantics for the same-entry surfacing trace"
      contains: "shouldTraceExperienceHighlightSurfacing"
    - path: ".planning/phases/CURRIA-89-validate-ssr-safety-of-debug-flag-and-constant-coupling-in-e/89-VALIDATION.md"
      provides: "Short safety-validation note covering the real call chain, debug-flag semantics, and constant-coupling audit outcome"
      contains: "safety validation pass only"
  key_links:
    - from: "src/components/resume/resume-comparison-view.tsx"
      to: "src/lib/resume/optimized-preview-highlights.ts"
      via: "ResumeDocument useMemo"
      pattern: "buildOptimizedPreviewHighlights"
    - from: "src/lib/resume/optimized-preview-highlights.ts"
      to: "src/lib/resume/optimized-preview-highlights.test.ts"
      via: "guard behavior and same-entry selector proof"
      pattern: "selectVisibleExperienceHighlightsForEntry"
---

<objective>
Validate the residual Phase 88 SSR/debug-flag and exported-constant coupling risks, then apply only the smallest confirmed safety fix if one is needed.

Output: a documented call chain, explicit debug-flag semantics, a coupling audit result, and green verification without changing editorial behavior.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/CURRIA-89-validate-ssr-safety-of-debug-flag-and-constant-coupling-in-e/89-CONTEXT.md
@.planning/phases/CURRIA-89-validate-ssr-safety-of-debug-flag-and-constant-coupling-in-e/89-RESEARCH.md
@.planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-VALIDATION.md
@src/app/(auth)/dashboard/resumes/compare/[sessionId]/page.tsx
@src/components/resume/resume-comparison-page.tsx
@src/components/resume/resume-comparison-view.tsx
@src/lib/dashboard/workspace-client.ts
@src/app/api/session/[id]/comparison/route.ts
@src/lib/routes/session-comparison/context.ts
@src/lib/routes/session-comparison/decision.ts
@src/lib/resume/optimized-preview-highlights.ts
@src/lib/resume/optimized-preview-highlights.test.ts
@src/lib/resume/optimized-preview-contracts.test.ts
@src/components/resume/resume-comparison-view.test.tsx

<scope_rules>
- Do not change editorial ordering or selector logic.
- Do not change span completion, ATS gates, summary behavior, or UI-tier rendering.
- Do not add a user-facing debug control.
- If the constant-coupling audit finds no issue, document that outcome instead of manufacturing test changes.
</scope_rules>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make the debug-flag semantics explicit for the confirmed mixed render context</name>
  <files>src/lib/resume/optimized-preview-highlights.ts</files>
  <behavior>
    - The code makes clear that the same-entry surfacing trace can run in a mixed render context because the call chain enters through a Client Component in App Router.
    - The flag is documented as runtime-local and non-production only, not as a browser-driven cross-SSR switch.
    - Any guard change remains minimal and does not alter selector results or editorial ranking.
  </behavior>
  <action>Update `shouldTraceExperienceHighlightSurfacing()` only if needed to make the mixed-context/runtime-local assumption explicit. Prefer a minimal comment plus a tiny defensive guard over any architectural change. Keep the trace non-production only and local to the current runtime.</action>
  <verify>
    <automated>npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"</automated>
  </verify>
  <done>The debug trace semantics are explicit enough that future maintainers will not infer unsupported browser-to-SSR control behavior, and Layer 3 behavior remains unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Audit constant coupling and record the validation outcome</name>
  <files>.planning/phases/CURRIA-89-validate-ssr-safety-of-debug-flag-and-constant-coupling-in-e/89-VALIDATION.md</files>
  <behavior>
    - Validation notes document the exact call chain from route entrypoint to surfacing layer.
    - Validation notes document whether any test or helper imported `EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY`.
    - Validation notes explicitly state whether the outcome was a narrow fix or validation-only.
  </behavior>
  <action>Audit direct and indirect test usage of `EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY`. If a fixture coupling is found, replace it with observable-behavior assertions. If none are found, document that explicitly. Write `89-VALIDATION.md` summarizing the call chain, debug-flag semantics, constant-coupling outcome, and the fact that no editorial policy or selector logic was reopened.</action>
  <verify>
    <automated>npx vitest run "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The phase has an explicit safety-validation note and the repo remains free of constant-internals fixture assertions for the exported policy constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App Router render context -> Layer 3 debug trace | Client-component render work may execute during server prerender and client hydration, so debug semantics must be runtime-local and explicit |
| exported policy constant -> tests | Tests must not mirror constant internals instead of asserting observable selector behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | T | debug semantics | mitigate | Document and, if needed, minimally guard `shouldTraceExperienceHighlightSurfacing()` so mixed-context runtime-local behavior is explicit. |
| T-89-02 | T | test coupling | mitigate | Audit test imports/helpers for `EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY` and keep assertions behavioral rather than constant-internal. |
</threat_model>

<verification>
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"`
- `npx vitest run "src/lib/resume/optimized-preview-contracts.test.ts"`
- `npx vitest run "src/components/resume/resume-comparison-view.test.tsx"`
- `npm run typecheck`
</verification>

<success_criteria>
- The real execution context into Layer 3 is documented, not guessed.
- Debug-flag semantics are explicit enough to avoid unsupported cross-SSR assumptions.
- No tests use `EXPERIENCE_HIGHLIGHT_CATEGORY_PRIORITY` as a fixture.
- No editorial policy or selector behavior changes land in this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-89-validate-ssr-safety-of-debug-flag-and-constant-coupling-in-e/89-01-SUMMARY.md`
</output>

# Phase 107 Plan

## Goal

Add traceable origin metadata to the shared highlight artifact, harden `job_targeting` keyword hygiene and gate observability, and prove the current session/compare readers serve the correct highlight without changing ATS editorial behavior.

## Steps

1. Extend `CvHighlightState` with `highlightSource` and `highlightGeneratedAt`, deriving the source inside `generateCvHighlightState(...)`.
2. Update ATS and job-targeting highlight persistence expectations to the new additive artifact shape.
3. Harden `extractJobKeywords(...)`:
   - exclude placeholder target-role signals when confidence is low
   - dedupe case-insensitively
   - drop trivial short tokens
   - preserve the current first-non-empty-source selection unless audit proves it unsafe
4. Enrich `job_targeting` highlight gate logs with `targetRoleConfidence` and `targetRoleSource`.
5. Document that direct smart-generation success still omits `highlightState`, while session/compare surfaces serve it from persisted session state.
6. Add focused tests for:
   - normal job-targeting highlight generation
   - rollback on blocked validation
   - repeated identical runs skipping regeneration
   - low-confidence target-role exclusion from keywords
   - ATS highlight source continuity

## Outcome

- Implemented additive origin metadata on the shared highlight artifact via `highlightSource` and `highlightGeneratedAt`
- Preserved ATS behavior by deriving origin centrally from `workflowMode` inside the shared generator instead of branching ATS logic
- Hardened `job_targeting` keyword sanitation and excluded low-confidence placeholder roles from highlight keyword inputs
- Changed the unchanged gate to compare against the previous optimized CV when available and preserve the previous highlight artifact on idempotent reruns
- Verified the shared readers keep serving the persisted artifact, while direct smart-generation success intentionally still omits `highlightState`

---
phase: 28-agent-input-and-setup-service-extraction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/agent/route.ts
  - src/lib/agent/agent-intents.ts
  - src/lib/agent/message-preparation.ts
  - src/lib/agent/vacancy-analysis.ts
autonomous: true
requirements:
  - AGENT-01
user_setup: []
must_haves:
  truths:
    - "Route-level message preparation and vacancy analysis live behind explicit shared helpers instead of ad hoc route-local functions."
    - "The brownfield `/api/agent` entrypoint keeps the same request semantics and continues to decide workflow mode from canonical session context."
    - "Vacancy heuristics are unified instead of duplicated between `route.ts` and `agent-loop.ts` adjacent helpers."
  artifacts:
    - path: "src/lib/agent/message-preparation.ts"
      provides: "Shared request-message sanitization and preparation helpers for the route entrypoint"
      contains: "prepareUserMessage"
    - path: "src/lib/agent/vacancy-analysis.ts"
      provides: "Shared vacancy and target-job detection contract used by route and loop-facing callers"
      contains: "detectTargetJobDescription"
  key_links:
    - from: "src/app/api/agent/route.ts"
      to: "src/lib/agent/message-preparation.ts"
      via: "route input preparation delegates to a dedicated helper instead of inline sanitization logic"
      pattern: "prepareUserMessage"
    - from: "src/app/api/agent/route.ts"
      to: "src/lib/agent/vacancy-analysis.ts"
      via: "route target-job detection uses the shared vacancy-analysis contract"
      pattern: "detectTargetJobDescription"
---

<objective>
Extract route-level message preparation and shared vacancy-analysis logic into explicit services without changing the visible `/api/agent` contract.

Purpose: Phase 28 starts by removing the easiest-to-isolate duplication in the brownfield entrypoint, so later setup extraction can reuse one shared input-analysis contract instead of copying heuristics again.
Output: Dedicated message-preparation and vacancy-analysis services used by `src/app/api/agent/route.ts`, with the current route behavior preserved.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-agent-input-and-setup-service-extraction/28-RESEARCH.md
@.planning/phases/28-agent-input-and-setup-service-extraction/28-VALIDATION.md
@AGENTS.md
@src/app/api/agent/route.ts
@src/lib/agent/agent-intents.ts
@src/lib/agent/agent-loop.ts
@src/app/api/agent/route.test.ts
@src/app/api/agent/route.sse.test.ts
</context>

<threat_model>
<trust_boundaries>
- Raw user message and scraped job text -> route-level sanitization and target-job analysis
- Shared vacancy heuristics -> workflow-mode prediction and downstream agent setup decisions
- Canonical session state -> route branching and persisted target-job context
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-28-01</threat_id>
  <category>Tampering</category>
  <component>Message-preparation extraction</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Move sanitization and vacancy analysis into explicit helpers without changing thresholds, ordering, or route branching semantics in the same plan.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-28-02</threat_id>
  <category>Denial of Service</category>
  <component>Route input analysis</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep the extracted helpers pure and lightweight so route entry performance and early SSE behavior do not regress.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-28-03</threat_id>
  <category>Repudiation</category>
  <component>Vacancy detection contract</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Unify the route-facing target-job detection contract in one module so later debugging can point to one authoritative heuristic implementation.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract message-preparation helpers from the route entrypoint</name>
  <files>src/app/api/agent/route.ts, src/lib/agent/message-preparation.ts</files>
  <read_first>src/app/api/agent/route.ts, src/app/api/agent/route.test.ts, AGENTS.md</read_first>
  <action>Create a dedicated agent message-preparation module that owns the current route sanitization behavior for raw user input and scraped job text. Replace the inline `sanitizeUserInput(...)` usage in `src/app/api/agent/route.ts` with explicit helper calls, keeping the same ordering and output shape so the route still prepares messages exactly once before workflow prediction and pre-loop setup.</action>
  <verify>npm run typecheck && npm test -- src/app/api/agent/route.test.ts</verify>
  <acceptance_criteria>
    - `src/app/api/agent/route.ts` no longer owns the full inline user-message sanitization implementation
    - the extracted helper preserves the current tag stripping and trimmed output behavior
    - route tests covering request validation and message handling still pass without changing user-visible response expectations
  </acceptance_criteria>
  <done>The route delegates message preparation to a dedicated helper without behavior drift.</done>
</task>

<task type="auto">
  <name>Task 2: Unify route-level vacancy and target-job detection behind one shared contract</name>
  <files>src/app/api/agent/route.ts, src/lib/agent/agent-intents.ts, src/lib/agent/vacancy-analysis.ts</files>
  <read_first>src/app/api/agent/route.ts, src/lib/agent/agent-intents.ts, src/lib/agent/agent-loop.ts, src/app/api/agent/route.sse.test.ts</read_first>
  <action>Extract the route's `detectTargetJobDescription(...)` behavior into a shared vacancy-analysis module and align it with the boolean heuristic exposed through `looksLikeJobDescription(...)`. Keep the same brownfield thresholds and confidence semantics, but remove duplicated route-local logic so route and loop-adjacent callers can share one authoritative vacancy-analysis contract.</action>
  <verify>npm run typecheck && npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts</verify>
  <acceptance_criteria>
    - vacancy-analysis logic is no longer duplicated only inside `src/app/api/agent/route.ts`
    - the route still produces the same target-job detection outcomes for summarized vacancy and structured-role inputs covered by existing tests
    - `looksLikeJobDescription(...)` and the richer route-facing detection contract remain explicitly related instead of drifting independently
  </acceptance_criteria>
  <done>The route and loop-facing code share one vacancy-analysis contract instead of maintaining parallel heuristics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/app/api/agent/route.test.ts src/app/api/agent/route.sse.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Message preparation and vacancy detection now live behind explicit shared helpers while `/api/agent` preserves current behavior
</success_criteria>

<output>
After completion, create `.planning/phases/28-agent-input-and-setup-service-extraction/28-01-SUMMARY.md`
</output>

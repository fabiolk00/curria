---
phase: 02-core-funnel-browser-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - playwright.config.ts
  - tests/e2e/auth.guard.spec.ts
  - tests/e2e/fixtures/auth-session.ts
  - tests/e2e/fixtures/api-mocks.ts
  - tests/e2e/helpers/sse.ts
  - src/lib/auth/e2e-auth.ts
  - src/lib/auth/app-user.ts
  - src/middleware.ts
  - src/app/api/e2e/auth/route.ts
  - src/lib/auth/e2e-auth.test.ts
  - src/app/api/e2e/auth/route.test.ts
autonomous: true
requirements:
  - QA-01
  - QA-03
user_setup: []
must_haves:
  truths:
    - "Protected UI routes can be entered in browser tests without live Clerk, but only when an explicit E2E auth flag and secret are present."
    - "Playwright has one repo-local command, one Chromium project, and shared helpers for auth bootstrap plus deterministic API mocking."
    - "The new browser harness does not require live Supabase, OpenAI, Asaas, or storage credentials to run locally or in CI."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Committed Playwright browser-test configuration"
      contains: "chromium"
    - path: "package.json"
      provides: "Repo-local browser test command"
      contains: "test:e2e"
    - path: "src/lib/auth/e2e-auth.ts"
      provides: "Signed E2E auth-cookie helper"
      contains: "E2E_AUTH_BYPASS_SECRET"
    - path: "src/app/api/e2e/auth/route.ts"
      provides: "Bootstrap and clear route for test auth"
      contains: "E2E_AUTH_ENABLED"
    - path: "tests/e2e/fixtures/api-mocks.ts"
      provides: "Shared route-mocking helpers for profile, session, agent, and file APIs"
      min_lines: 40
    - path: "tests/e2e/auth.guard.spec.ts"
      provides: "Browser proof that guest redirect and authenticated entry both work"
      min_lines: 20
  key_links:
    - from: "tests/e2e/auth.guard.spec.ts"
      to: "src/app/api/e2e/auth/route.ts"
      via: "auth bootstrap helper"
      pattern: "api/e2e/auth"
    - from: "src/lib/auth/app-user.ts"
      to: "src/lib/auth/e2e-auth.ts"
      via: "test-only synthetic app-user resolution"
      pattern: "E2E_AUTH"
    - from: "tests/e2e/fixtures/api-mocks.ts"
      to: "src/app/api/agent/route.ts"
      via: "mocked SSE payload shape"
      pattern: "sessionCreated|done|text"
---

<objective>
Install the browser-test foundation for Phase 2 and make protected pages reachable without live Clerk.

Purpose: Browser specs cannot provide reliable launch gating until the repo has a committed Playwright stack, deterministic route-mocking helpers, and a tightly gated auth seam for protected pages.
Output: Playwright config, reusable E2E helpers, auth bootstrap flow, and one browser smoke spec proving the harness works.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-funnel-browser-verification/02-RESEARCH.md
@.planning/phases/02-core-funnel-browser-verification/02-VALIDATION.md
@AGENTS.md
@package.json
@vitest.config.ts
@.github/workflows/ci.yml
@docs/developer-rules/TESTING.md
@src/middleware.ts
@src/lib/auth/app-user.ts
@src/app/(auth)/layout.tsx
@src/app/(auth)/dashboard/page.tsx
@src/app/api/profile/route.ts
@src/app/api/agent/route.ts
@src/app/api/file/[sessionId]/route.ts
</context>

<threat_model>
<trust_boundaries>
- Browser-test runtime -> protected Next.js routes and route handlers
- Test-only auth bootstrap -> synthetic authenticated session state
- Playwright fixtures -> mocked provider-backed APIs
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-02-01</threat_id>
  <category>Elevation of Privilege</category>
  <component>E2E auth bypass</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Require `E2E_AUTH_ENABLED` and `E2E_AUTH_BYPASS_SECRET`, sign the cookie, expose the bootstrap route only in E2E mode, and preserve normal Clerk behavior everywhere else.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-02-02</threat_id>
  <category>Denial of Service</category>
  <component>Browser-test infrastructure</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Commit a single Chromium-first Playwright config, one repo-local command, and deterministic fixtures so the first browser lane is simple enough to stay green.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-02-03</threat_id>
  <category>Spoofing</category>
  <component>Shared E2E fixtures</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Bootstrap auth only through same-origin helper calls and keep synthetic session data isolated to test fixtures instead of ad hoc page scripts.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Playwright scripts, config, and reusable browser helpers</name>
  <files>package.json, playwright.config.ts, tests/e2e/auth.guard.spec.ts, tests/e2e/fixtures/auth-session.ts, tests/e2e/fixtures/api-mocks.ts, tests/e2e/helpers/sse.ts</files>
  <read_first>package.json, vitest.config.ts, .github/workflows/ci.yml, docs/developer-rules/TESTING.md, src/app/api/profile/route.ts, src/app/api/session/[id]/route.ts, src/app/api/agent/route.ts, src/app/api/file/[sessionId]/route.ts</read_first>
  <action>Add `@playwright/test` as a direct dev dependency and create a single repo command `npm run test:e2e`. Commit `playwright.config.ts` with one required Chromium project, base URL `http://127.0.0.1:3000`, and a local web server strategy that works in both developer machines and CI. Under `tests/e2e/`, add shared helpers for signed-auth bootstrap, deterministic route mocks for `/api/profile`, `/api/session/[id]`, `/api/session/[id]/messages`, `/api/agent`, and `/api/file/[sessionId]`, plus an SSE payload builder so chat tests can emulate `text`, `sessionCreated`, and `done` events. Add one smoke spec `tests/e2e/auth.guard.spec.ts` that proves guest access to `/dashboard` redirects to `/login`, then uses the shared auth helper to enter an authenticated route successfully.</action>
  <verify>npm run test:e2e -- tests/e2e/auth.guard.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - `package.json` contains a direct `test:e2e` script and `@playwright/test`
    - `playwright.config.ts` contains a Chromium project and a base URL
    - `tests/e2e/fixtures/api-mocks.ts` contains route helpers for profile, session, agent, and file APIs
    - `tests/e2e/helpers/sse.ts` can emit a valid `text/event-stream` body for `/api/agent`
    - `tests/e2e/auth.guard.spec.ts` covers guest redirect plus authenticated entry
  </acceptance_criteria>
  <done>The repo has a runnable browser harness with one command and shared fixture helpers instead of ad hoc test setup.</done>
</task>

<task type="auto">
  <name>Task 2: Add a tightly gated E2E auth seam for protected routes</name>
  <files>src/lib/auth/e2e-auth.ts, src/lib/auth/app-user.ts, src/middleware.ts, src/app/api/e2e/auth/route.ts, src/lib/auth/e2e-auth.test.ts, src/app/api/e2e/auth/route.test.ts</files>
  <read_first>src/middleware.ts, src/lib/auth/app-user.ts, src/app/(auth)/layout.tsx, src/app/(auth)/dashboard/page.tsx, src/components/auth/login-form.tsx</read_first>
  <action>Create a small auth helper module that signs and verifies an E2E auth cookie using `E2E_AUTH_BYPASS_SECRET`. Add a same-origin bootstrap route such as `POST /api/e2e/auth` and `DELETE /api/e2e/auth` that only works when `E2E_AUTH_ENABLED=true`; otherwise it must fail closed. Update `src/middleware.ts` so protected UI routes skip `auth().protect()` only when a valid E2E cookie is present in E2E mode. Update `src/lib/auth/app-user.ts` so `getCurrentAppUser()` can synthesize a minimal active app user from the signed cookie in E2E mode while keeping Clerk-based resolution unchanged for all normal environments. Add focused Vitest coverage for cookie signing, invalid-secret rejection, disabled-mode rejection, and route behavior.</action>
  <verify>npm test -- src/lib/auth/e2e-auth.test.ts src/app/api/e2e/auth/route.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/auth/e2e-auth.ts` references `E2E_AUTH_ENABLED` and `E2E_AUTH_BYPASS_SECRET`
    - `src/app/api/e2e/auth/route.ts` rejects requests when E2E mode is disabled
    - `src/middleware.ts` preserves Clerk protection when no valid E2E cookie exists
    - `src/lib/auth/app-user.ts` keeps the Clerk path intact and only returns synthetic auth in E2E mode
    - Targeted tests cover happy path, disabled mode, and invalid-cookie cases
  </acceptance_criteria>
  <done>Browser tests can reach protected pages deterministically without introducing a production auth bypass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/auth/e2e-auth.test.ts src/app/api/e2e/auth/route.test.ts`
- [ ] `npm run test:e2e -- tests/e2e/auth.guard.spec.ts --project=chromium`
- [ ] `powershell -NoProfile -Command "if (rg -n 'E2E_AUTH_BYPASS_SECRET|E2E_AUTH_ENABLED' src/lib/auth/e2e-auth.ts src/app/api/e2e/auth/route.ts src/middleware.ts src/lib/auth/app-user.ts) { exit 0 } else { exit 1 }"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a committed browser harness and protected pages can be entered in tests without live Clerk
  </success_criteria>

<output>
After completion, create `.planning/phases/02-core-funnel-browser-verification/02-01-SUMMARY.md`
</output>

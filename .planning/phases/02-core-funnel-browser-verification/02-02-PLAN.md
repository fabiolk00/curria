---
phase: 02-core-funnel-browser-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tests/e2e/profile-setup.spec.ts
  - tests/e2e/core-funnel.spec.ts
  - tests/e2e/fixtures/api-mocks.ts
  - src/components/resume/user-data-page.tsx
  - src/components/dashboard/resume-workspace.tsx
  - src/components/dashboard/chat-interface.tsx
  - src/components/dashboard/session-documents-panel.tsx
  - src/components/dashboard/preview-panel.tsx
  - src/components/dashboard/resume-workspace.test.tsx
  - src/components/dashboard/chat-interface.test.tsx
  - src/components/dashboard/preview-panel.test.tsx
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03
user_setup: []
must_haves:
  truths:
    - "The browser suite covers guest redirect, manual profile save, session creation, agent interaction, target outcome, and artifact delivery."
    - "Stable browser assertions come from live component state and deterministic API fixtures, not brittle marketing copy or timing guesses."
    - "The core funnel specs stay staging-safe by mocking provider-backed APIs instead of calling live Clerk, Supabase, OpenAI, or storage services."
  artifacts:
    - path: "tests/e2e/profile-setup.spec.ts"
      provides: "Authenticated browser coverage for manual profile setup or edit"
      min_lines: 30
    - path: "tests/e2e/core-funnel.spec.ts"
      provides: "Browser coverage for chat, target outcome, and artifact delivery"
      min_lines: 40
    - path: "src/components/dashboard/resume-workspace.tsx"
      provides: "Stable workspace state hooks for session, target, and generation assertions"
      contains: "data-testid"
    - path: "src/components/dashboard/preview-panel.tsx"
      provides: "Stable preview and download hooks for artifact assertions"
      contains: "data-testid"
    - path: "src/components/dashboard/session-documents-panel.tsx"
      provides: "Stable document-panel hooks for browser download assertions"
      contains: "data-testid"
  key_links:
    - from: "tests/e2e/profile-setup.spec.ts"
      to: "src/components/resume/user-data-page.tsx"
      via: "manual profile save flow"
      pattern: "Salvar"
    - from: "tests/e2e/core-funnel.spec.ts"
      to: "src/components/dashboard/resume-workspace.tsx"
      via: "workspace state assertion"
      pattern: "data-testid"
    - from: "tests/e2e/core-funnel.spec.ts"
      to: "src/components/dashboard/preview-panel.tsx"
      via: "artifact preview and download assertion"
      pattern: "Download|data-testid"
---

<objective>
Implement the launch-critical browser specs and the small UI hooks they need to assert real workspace state.

Purpose: Infrastructure alone does not create confidence. Phase 2 needs executable browser journeys for the profile and dashboard funnel, including the mocked SSE and artifact-delivery behaviors that matter most to launch.
Output: Stable browser selectors plus deterministic specs for profile setup and the core dashboard funnel.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-funnel-browser-verification/02-RESEARCH.md
@.planning/phases/02-core-funnel-browser-verification/02-VALIDATION.md
@AGENTS.md
@playwright.config.ts
@tests/e2e/fixtures/auth-session.ts
@tests/e2e/fixtures/api-mocks.ts
@tests/e2e/helpers/sse.ts
@src/components/resume/user-data-page.tsx
@src/components/dashboard/resume-workspace.tsx
@src/components/dashboard/chat-interface.tsx
@src/components/dashboard/session-documents-panel.tsx
@src/components/dashboard/preview-panel.tsx
@src/lib/dashboard/workspace-client.ts
@src/types/dashboard.ts
@src/app/api/profile/route.ts
@src/app/api/session/[id]/route.ts
@src/app/api/session/[id]/messages/route.ts
@src/app/api/agent/route.ts
@src/app/api/file/[sessionId]/route.ts
</context>

<threat_model>
<trust_boundaries>
- Browser specs -> mocked profile, session, agent, and file APIs
- Test selectors -> live component state inside the authenticated funnel
- Download assertions -> synthetic signed URLs and browser download events
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-02-04</threat_id>
  <category>Tampering</category>
  <component>Playwright route mocks</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep fixture payloads aligned with the actual route contracts and drive assertions through browser-observable state instead of only inspecting mocked internals.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-02-05</threat_id>
  <category>Repudiation</category>
  <component>Browser selectors and state assertions</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Add small stable hooks sourced from live component state so test failures map back to real funnel behavior rather than fragile text matching.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-02-06</threat_id>
  <category>Integrity</category>
  <component>Artifact-delivery coverage</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Assert both artifact availability and an actual browser download event so the suite proves more than just button rendering.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add stable browser hooks to the profile, workspace, and artifact surfaces</name>
  <files>src/components/resume/user-data-page.tsx, src/components/dashboard/resume-workspace.tsx, src/components/dashboard/chat-interface.tsx, src/components/dashboard/session-documents-panel.tsx, src/components/dashboard/preview-panel.tsx, src/components/dashboard/resume-workspace.test.tsx, src/components/dashboard/chat-interface.test.tsx, src/components/dashboard/preview-panel.test.tsx</files>
  <read_first>src/components/resume/user-data-page.tsx, src/components/dashboard/resume-workspace.tsx, src/components/dashboard/chat-interface.tsx, src/components/dashboard/session-documents-panel.tsx, src/components/dashboard/preview-panel.tsx, src/components/dashboard/resume-workspace.test.tsx, src/components/dashboard/chat-interface.test.tsx, src/components/dashboard/preview-panel.test.tsx</read_first>
  <action>Add minimal `data-testid` or `data-state-*` hooks that expose the real browser-relevant state without changing UX or product logic. At minimum, surface stable selectors for the profile save action, workspace session metadata, target count, generated-output readiness, document panel availability, preview readiness, and preview download controls. Update the touched component tests so these hooks are exercised and do not regress silently.</action>
  <verify>npm test -- src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/chat-interface.test.tsx src/components/dashboard/preview-panel.test.tsx</verify>
  <acceptance_criteria>
    - `src/components/resume/user-data-page.tsx` contains a stable selector for the save flow
    - `src/components/dashboard/resume-workspace.tsx` exposes session or target state through stable test hooks
    - `src/components/dashboard/session-documents-panel.tsx` exposes a stable documents-ready surface
    - `src/components/dashboard/preview-panel.tsx` exposes a stable preview-ready or download control surface
    - Touched component tests assert the new hooks rather than leaving them unverified
  </acceptance_criteria>
  <done>Browser specs can assert funnel outcomes through stable component state instead of fragile copy or timing assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Cover guest redirect and manual profile setup in Playwright</name>
  <files>tests/e2e/profile-setup.spec.ts, tests/e2e/fixtures/api-mocks.ts</files>
  <read_first>tests/e2e/auth.guard.spec.ts, tests/e2e/fixtures/auth-session.ts, tests/e2e/fixtures/api-mocks.ts, src/components/resume/user-data-page.tsx, src/app/api/profile/route.ts</read_first>
  <action>Create `tests/e2e/profile-setup.spec.ts` using the shared auth bootstrap plus route mocks for `GET /api/profile` and `PUT /api/profile`. The spec must first prove that a guest visit to `/dashboard` lands on `/login`, then authenticate through the E2E helper, visit `/dashboard/resumes/new`, fill the manual profile form, save it, and assert that the outgoing profile payload matches the canonical `cvState` shape expected by `PUT /api/profile`. After save, assert the page transitions to `/dashboard` as the current UI intends.</action>
  <verify>npm run test:e2e -- tests/e2e/profile-setup.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - `tests/e2e/profile-setup.spec.ts` covers guest redirect to `/login`
    - The spec authenticates through the shared E2E helper instead of live Clerk UI
    - The spec intercepts `PUT /api/profile` and asserts the submitted payload contains canonical `cvState` fields
    - The spec asserts the post-save redirect to `/dashboard`
  </acceptance_criteria>
  <done>The browser suite proves the auth boundary and manual profile setup path without relying on external providers.</done>
</task>

<task type="auto">
  <name>Task 3: Cover session creation, target outcome, and artifact delivery in Playwright</name>
  <files>tests/e2e/core-funnel.spec.ts, tests/e2e/fixtures/api-mocks.ts</files>
  <read_first>tests/e2e/fixtures/api-mocks.ts, tests/e2e/helpers/sse.ts, src/components/dashboard/resume-workspace.tsx, src/components/dashboard/chat-interface.tsx, src/components/dashboard/session-documents-panel.tsx, src/components/dashboard/preview-panel.tsx, src/app/api/agent/route.ts, src/app/api/session/[id]/route.ts, src/app/api/file/[sessionId]/route.ts</read_first>
  <action>Create `tests/e2e/core-funnel.spec.ts` that authenticates through the E2E helper, opens `/dashboard`, and drives one launch-critical chat journey using mocked APIs only. Fulfill `POST /api/agent` with a deterministic SSE stream that emits `sessionCreated`, one or more `text` chunks, and `done`, then fulfill the subsequent session refresh so `workspace.targets.length` becomes `1` and generated output becomes ready. Assert that the workspace syncs to the new session, the target outcome is reflected through the stable workspace hook, the documents panel shows available artifacts, the preview panel loads a PDF URL, and at least one artifact download event succeeds in Chromium.</action>
  <verify>npm run test:e2e -- tests/e2e/core-funnel.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - `tests/e2e/core-funnel.spec.ts` fulfills `/api/agent` as `text/event-stream`
    - The spec asserts session creation outcome in the browser after the mocked SSE completes
    - The spec asserts target-count or equivalent target outcome state becomes `1`
    - The spec asserts artifact availability through the documents or preview surfaces
    - The spec captures at least one successful browser download event
  </acceptance_criteria>
  <done>The browser suite covers the highest-value dashboard funnel from first agent turn through artifact delivery.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/chat-interface.test.tsx src/components/dashboard/preview-panel.test.tsx`
- [ ] `npm run test:e2e -- tests/e2e/profile-setup.spec.ts tests/e2e/core-funnel.spec.ts --project=chromium`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The browser suite covers the launch-critical funnel with deterministic mocked providers and stable assertions
  </success_criteria>

<output>
After completion, create `.planning/phases/02-core-funnel-browser-verification/02-02-SUMMARY.md`
</output>

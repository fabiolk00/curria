---
phase: 02-core-funnel-browser-verification
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - .github/workflows/ci.yml
  - README.md
  - docs/developer-rules/TESTING.md
autonomous: true
requirements:
  - QA-03
user_setup: []
must_haves:
  truths:
    - "CI installs Chromium, starts the app in E2E mode, and fails clearly on core-funnel browser regressions."
    - "Contributor docs explain how to run the browser suite locally and make clear that it uses mocked providers plus a test-only auth seam."
    - "The browser lane does not require live Clerk, Supabase, OpenAI, Asaas, or storage credentials."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI lane for Chromium browser verification"
      contains: "test:e2e"
    - path: "README.md"
      provides: "Top-level local run instructions for browser verification"
      contains: "test:e2e"
    - path: "docs/developer-rules/TESTING.md"
      provides: "Contributor rules for maintaining the browser suite"
      contains: "Playwright"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "shared browser command"
      pattern: "test:e2e"
    - from: "README.md"
      to: "docs/developer-rules/TESTING.md"
      via: "local run and maintenance guidance"
      pattern: "Playwright|test:e2e"
---

<objective>
Wire the new browser suite into CI and document how contributors should run and extend it.

Purpose: Phase 2 is not complete until the new browser coverage becomes a repeatable launch gate instead of a local-only experiment.
Output: A CI browser lane plus updated top-level and testing docs for the mocked-provider E2E workflow.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-funnel-browser-verification/02-RESEARCH.md
@.planning/phases/02-core-funnel-browser-verification/02-VALIDATION.md
@AGENTS.md
@package.json
@playwright.config.ts
@.github/workflows/ci.yml
@README.md
@docs/developer-rules/TESTING.md
@tests/e2e/auth.guard.spec.ts
@tests/e2e/profile-setup.spec.ts
@tests/e2e/core-funnel.spec.ts
</context>

<threat_model>
<trust_boundaries>
- GitHub Actions runtime -> browser-test command, Next.js app server, and mocked-provider env
- Contributor docs -> local developer machines and future maintenance workflows
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-02-07</threat_id>
  <category>Denial of Service</category>
  <component>CI browser lane</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep CI to one Chromium project, install browsers explicitly, and use a clear job or step name so failures are diagnosable instead of silently timing out.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-02-08</threat_id>
  <category>Information Disclosure</category>
  <component>Documentation for E2E auth mode</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Document only the test-only env names and dummy-provider expectations; do not encourage use of real provider secrets for the browser suite.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-02-09</threat_id>
  <category>Tampering</category>
  <component>Contributor workflow</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Explain that Playwright specs should rely on the shared mock helpers and the E2E auth seam, not direct calls to live provider behavior.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add a CI browser-verification lane for the core funnel</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml, package.json, playwright.config.ts, tests/e2e/auth.guard.spec.ts, tests/e2e/profile-setup.spec.ts, tests/e2e/core-funnel.spec.ts</read_first>
  <action>Update `.github/workflows/ci.yml` so CI installs Playwright browsers explicitly, provides the dummy env needed for the app plus the E2E auth seam, and runs the Chromium browser suite through the committed `test:e2e` command. Keep the browser lane separate enough that failures are obvious in job output, and ensure the workflow still preserves the existing typecheck, lint, db-audit, and Vitest steps. Use explicit env names such as `E2E_AUTH_ENABLED` and `E2E_AUTH_BYPASS_SECRET`, and do not introduce any requirement for live Clerk, Supabase, OpenAI, Asaas, or storage credentials.</action>
  <verify>rg -n "test:e2e|playwright install --with-deps chromium|E2E_AUTH_ENABLED|E2E_AUTH_BYPASS_SECRET" .github/workflows/ci.yml</verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a browser step or job that runs `test:e2e`
    - `.github/workflows/ci.yml` installs Chromium explicitly
    - `.github/workflows/ci.yml` injects `E2E_AUTH_ENABLED` and `E2E_AUTH_BYPASS_SECRET`
    - The workflow still keeps the existing repo-local checks instead of replacing them
  </acceptance_criteria>
  <done>CI can fail clearly on core-funnel browser regressions instead of relying on local-only verification.</done>
</task>

<task type="auto">
  <name>Task 2: Document the local browser workflow and maintenance rules</name>
  <files>README.md, docs/developer-rules/TESTING.md</files>
  <read_first>README.md, docs/developer-rules/TESTING.md, package.json, playwright.config.ts, .github/workflows/ci.yml</read_first>
  <action>Update `README.md` so contributors can discover and run the browser suite from the top-level docs. Update `docs/developer-rules/TESTING.md` so it explains when to add or update Playwright coverage, which funnel behaviors are required by Phase 2, how the shared API mocks and E2E auth seam should be used, and why the suite must not depend on live provider credentials. Keep the documented commands copy-pastable and aligned to the committed script names.</action>
  <verify>rg -n "test:e2e|Playwright|E2E_AUTH_ENABLED|E2E_AUTH_BYPASS_SECRET|mocked API" README.md docs/developer-rules/TESTING.md</verify>
  <acceptance_criteria>
    - `README.md` contains `test:e2e`
    - `docs/developer-rules/TESTING.md` names Playwright as the browser framework
    - `docs/developer-rules/TESTING.md` explains the mocked-provider model and the E2E auth seam
    - Neither doc instructs contributors to use live provider credentials for the browser suite
  </acceptance_criteria>
  <done>The browser suite is discoverable, reproducible, and maintainable for the next contributor.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm run test:e2e -- --project=chromium`
- [ ] `rg -n "test:e2e|playwright install --with-deps chromium|E2E_AUTH_ENABLED|E2E_AUTH_BYPASS_SECRET" .github/workflows/ci.yml README.md docs/developer-rules/TESTING.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- CI and repo docs treat browser verification as a first-class launch gate for the core funnel
  </success_criteria>

<output>
After completion, create `.planning/phases/02-core-funnel-browser-verification/02-03-SUMMARY.md`
</output>

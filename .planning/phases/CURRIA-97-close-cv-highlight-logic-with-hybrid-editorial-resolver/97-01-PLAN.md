---
phase: 97-close-cv-highlight-logic-with-hybrid-editorial-resolver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/tools/detect-cv-highlights.ts
  - src/lib/agent/tools/detect-cv-highlights.test.ts
  - src/lib/resume/cv-highlight-artifact.ts
  - src/lib/resume/cv-highlight-artifact.test.ts
  - src/lib/agent/tools/pipeline.test.ts
  - src/lib/routes/session-comparison/decision.test.ts
  - src/components/resume/resume-comparison-view.test.tsx
autonomous: true
requirements:
  - CV-HILITE-EDITORIAL-01
  - CV-HILITE-RESOLVER-01
  - CV-HILITE-SHARED-SMOKE-01
must_haves:
  truths:
    - "Detector output prefers semantically closed highlight spans over machine-cut fragments."
    - "Metric/result bullets keep the metric together with its immediate complement when that context exists in the same bullet."
    - "Generic lead verbs do not survive as the final highlight start when a stronger local nucleus exists."
    - "Highlight behavior remains deterministic for the same bullet and does not reopen aggressive candidate scoring."
    - "Preview response and renderer behavior stay unchanged except for smoke-proof regressions on the shared persisted-highlight seam."
  artifacts:
    - path: "src/lib/agent/tools/detect-cv-highlights.ts"
      provides: "Prompt hardening and detector contract preservation"
      min_lines: 80
    - path: "src/lib/agent/tools/detect-cv-highlights.test.ts"
      provides: "Detector-focused editorial fixtures for weak starts and metric closure"
    - path: "src/lib/resume/cv-highlight-artifact.ts"
      provides: "Minimal local resolver only if prompt-only proof is insufficient"
      contains: "normalizeHighlightSpanBoundaries"
    - path: "src/lib/resume/cv-highlight-artifact.test.ts"
      provides: "Resolver regressions for trim-left, metric closure, and keep-base cases"
    - path: "src/lib/agent/tools/pipeline.test.ts"
      provides: "Shared ATS/job-targeting persisted-highlight smoke proof"
  key_links:
    - from: "src/lib/agent/tools/detect-cv-highlights.ts"
      to: "src/lib/resume/cv-highlight-artifact.ts"
      via: "validateAndResolveHighlights"
      pattern: "validateAndResolveHighlights"
    - from: "src/lib/agent/tools/pipeline.test.ts"
      to: "src/lib/agent/tools/detect-cv-highlights.ts"
      via: "generateCvHighlightState shared seam"
      pattern: "generateCvHighlightState"
    - from: "src/components/resume/resume-comparison-view.test.tsx"
      to: "src/lib/resume/cv-highlight-artifact.ts"
      via: "segmentTextByHighlightRanges smoke regression"
      pattern: "segmentTextByHighlightRanges"
---

<objective>
Close the remaining persisted CV highlight quality gap by hardening detector editorial guidance first, then adding only the smallest deterministic artifact-side resolver needed to finish the weak-start and incomplete-closure cases.

Purpose: Improve editorial finality of persisted highlight spans without changing the brownfield preview architecture or reintroducing unstable candidate scoring.
Output: One prompt-first implementation plan with detector fixtures, optional minimal resolver logic, and shared-smoke proof for ATS/job-targeting plus preview consumers.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-97-close-cv-highlight-logic-with-hybrid-editorial-resolver/97-CONTEXT.md
@.planning/phases/CURRIA-97-close-cv-highlight-logic-with-hybrid-editorial-resolver/97-RESEARCH.md
@src/lib/agent/tools/detect-cv-highlights.ts
@src/lib/resume/cv-highlight-artifact.ts
@src/lib/agent/tools/detect-cv-highlights.test.ts
@src/lib/resume/cv-highlight-artifact.test.ts
@src/lib/agent/tools/pipeline.test.ts
@src/lib/routes/session-comparison/decision.test.ts
@src/components/resume/resume-comparison-view.test.tsx

<interfaces>
From src/lib/agent/tools/detect-cv-highlights.ts:
```typescript
function buildHighlightSystemPrompt(): string
export async function detectCvHighlights(
  items: CvHighlightInputItem[],
  context?: CvHighlightDetectionContext,
): Promise<CvResolvedHighlight[]>
export async function generateCvHighlightState(
  cvState: CVState,
  context?: CvHighlightDetectionContext,
): Promise<CvHighlightState>
```

From src/lib/resume/cv-highlight-artifact.ts:
```typescript
export function normalizeHighlightSpanBoundaries(
  text: string,
  range: CvHighlightRange,
): CvHighlightRange | null

export function isEditoriallyAcceptableHighlightRange(
  text: string,
  range: CvHighlightRange,
  section: 'summary' | 'experience',
): boolean

export function validateAndResolveHighlights(
  items: CvHighlightInputItem[],
  raw: unknown,
): CvResolvedHighlight[]

export function segmentTextByHighlightRanges(
  text: string,
  ranges: CvHighlightRange[],
): CvHighlightTextSegment[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden detector prompt and add detector-first editorial fixtures</name>
  <files>src/lib/agent/tools/detect-cv-highlights.ts, src/lib/agent/tools/detect-cv-highlights.test.ts</files>
  <behavior>
    - Test 1: weak generic verb bullets resolve to a later stronger nucleus when the same bullet contains denser metric/scale/business context.
    - Test 2: metric bullets keep the metric plus immediate measured complement instead of stopping at the number or short verb phrase.
    - Test 3: short compact measurable bullets can still resolve to whole-bullet highlights when that is the natural semantic unit.
  </behavior>
  <action>Expand detector-focused fixtures first, then harden `buildHighlightSystemPrompt()` per the locked phase order: add explicit editorial rules for complete semantic units, metric-plus-context closure, invalid generic-verb starts when a stronger later nucleus exists, and slightly longer natural spans over machine-cut fragments. Add positive and negative examples for pt-BR/English cases already represented by the codebase tests. Preserve the JSON envelope, reason labels, single-call contract, and fail-closed parsing behavior. Do not touch artifact resolver code in this task unless a detector fixture proves the prompt contract itself cannot express the desired outcome.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/tools/detect-cv-highlights.test.ts src/lib/resume/cv-highlight-artifact.test.ts</automated>
  </verify>
  <done>Detector tests prove prompt-first improvements for weak-start and closure cases, while payload parsing, alias normalization, and observability contracts remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add minimal artifact-side resolver only for prompt-proven residual gaps</name>
  <files>src/lib/resume/cv-highlight-artifact.ts, src/lib/resume/cv-highlight-artifact.test.ts, src/lib/agent/tools/pipeline.test.ts</files>
  <behavior>
    - Test 1: when prompt-only still leaves a generic lead verb, normalization can conservatively trim left to the stronger local nucleus.
    - Test 2: when prompt-only still under-closes a metric/result phrase, normalization can promote a metric-closure alternative.
    - Test 3: ambiguous cases keep `base_range` and do not expand into broad tails or scoring-heavy arbitration.
    - Test 4: one explicit shared job-targeting persisted-highlight regression remains green alongside ATS-focused coverage.
  </behavior>
  <action>Run the prompt-focused suite after Task 1 and only implement resolver changes for remaining acceptance failures. Keep all work inside `normalizeHighlightSpanBoundaries(...)` and related local helpers. Priority order: add left-trim editorial logic first, then clarify full-bullet-versus-nucleus behavior for short measurable bullets, then at most three local candidates (`base_range`, `trim_left_range`, `metric_closure_range`) if still needed. Promote an alternative only when it is clearly better on at least two allowed dimensions from the phase context, otherwise keep `base_range`. Do not reintroduce aggressive candidate scoring, more than three candidates, or any relaxation of `isEditoriallyAcceptableHighlightRange(...)`. Add one mandatory shared pipeline regression that proves persisted highlight behavior remains correct across the shared ATS/job-targeting generator seam.</action>
  <verify>
    <automated>npx vitest run src/lib/resume/cv-highlight-artifact.test.ts src/lib/agent/tools/pipeline.test.ts -t highlight</automated>
  </verify>
  <done>Residual weak-start and under-closure cases are resolved deterministically with minimal local logic, and shared ATS/job-targeting highlight persistence remains proven without changing pipeline shape.</done>
</task>

<task type="auto">
  <name>Task 3: Prove unchanged preview and response seams with smoke-only regressions</name>
  <files>src/lib/routes/session-comparison/decision.test.ts, src/components/resume/resume-comparison-view.test.tsx, src/lib/agent/tools/detect-cv-highlights.test.ts, src/lib/resume/cv-highlight-artifact.test.ts, src/lib/agent/tools/pipeline.test.ts</files>
  <action>Run the focused shared-seam suite after Tasks 1-2 and update tests only where the persisted resolved-span expectations need to reflect the editorially improved artifact. Leave `src/lib/routes/session-comparison/decision.ts` and `src/components/resume/resume-comparison-view.tsx` production code unchanged unless a failing test proves a shared seam regression; if that happens, limit changes to smoke-level contract proof rather than behavior redesign. Verify locked-preview omission, renderer segmentation, and pipeline persistence still match the existing architecture.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/tools/detect-cv-highlights.test.ts src/lib/resume/cv-highlight-artifact.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/routes/session-comparison/decision.test.ts src/components/resume/resume-comparison-view.test.tsx</automated>
  </verify>
  <done>Focused regressions prove the prompt/resolver changes improve highlight spans while route and renderer behavior remain unchanged consumers of persisted highlight state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAI detector -> local parser | Untrusted model output crosses into persisted highlight-state resolution. |
| Artifact normalization -> persisted highlightState | Local resolver decides which spans survive into the server-side artifact consumed by multiple flows. |
| Persisted highlightState -> route/renderer consumers | Response and preview consumers trust resolved spans and must not reinterpret them unsafely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | T | `src/lib/agent/tools/detect-cv-highlights.ts` | mitigate | Preserve strict JSON envelope parsing, fixed reason labels, and fail-closed invalid-payload handling while tightening prompt rules. |
| T-97-02 | T | `src/lib/resume/cv-highlight-artifact.ts` | mitigate | Keep resolver local and deterministic, cap candidates at three, and retain `isEditoriallyAcceptableHighlightRange(...)` as the final acceptance gate. |
| T-97-03 | I | `src/lib/routes/session-comparison/decision.ts` | mitigate | Do not change locked-preview or response behavior; prove omission/regression safety through focused decision tests only. |
| T-97-04 | D | Shared ATS/job-targeting highlight generation | accept | The phase keeps the existing single detector call and shared generator seam; scope stays on span quality, not pipeline throughput changes. |
</threat_model>

<verification>
Run the task-level focused Vitest commands after each task. End the phase with:

`npx vitest run src/lib/agent/tools/detect-cv-highlights.test.ts src/lib/resume/cv-highlight-artifact.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/routes/session-comparison/decision.test.ts src/components/resume/resume-comparison-view.test.tsx`

If time remains and no unrelated failures are present, run `npm test` as the broader phase gate.
</verification>

<success_criteria>
- Detector prompt fixtures prove semantically closed spans and stronger local nuclei without changing the detector response contract.
- Artifact-side logic is added only for prompt-proven residual cases and never reopens aggressive candidate scoring.
- Metric bullets keep their immediate complement, short compact measurable bullets can remain whole, and generic verbs no longer anchor the final highlight when a better nucleus exists.
- ATS/job-targeting persistence, locked-preview omission, and renderer segmentation remain unchanged apart from updated smoke expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-97-close-cv-highlight-logic-with-hybrid-editorial-resolver/97-01-SUMMARY.md`
</output>

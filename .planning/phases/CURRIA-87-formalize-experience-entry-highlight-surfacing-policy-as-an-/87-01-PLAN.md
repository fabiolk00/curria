---
phase: 87-formalize-experience-entry-highlight-surfacing-policy-as-an-
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resume/optimized-preview-highlights.ts
  - src/lib/resume/optimized-preview-highlights.test.ts
  - src/lib/resume/optimized-preview-contracts.test.ts
  - src/components/resume/resume-comparison-view.test.tsx
  - .planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-VALIDATION.md
autonomous: true
requirements:
  - EXP-HILITE-SURFACING-01
  - EXP-HILITE-SURFACING-PRIORITY-01
  - EXP-HILITE-SURFACING-TEST-01
user_setup: []
must_haves:
  truths:
    - "A dedicated, pure entry-level selector exists downstream of bullet improvement gating and winner-span selection, and it alone decides which same-entry bullets consume the visible highlight slots."
    - "Within one experience entry, Tier 1 evidence (`metric`, `scope_scale`) dominates visible slots before Tier 2 evidence (`contextual_stack`, `anchored_leadership`, `anchored_outcome`) can consume remaining capacity."
    - "Existing caps and renderer contracts remain intact: max one highlight span per bullet, max two highlighted bullets per experience entry, zero-highlight safety remains allowed, and `highlightTier` plus `highlightCategory` still reach the renderer."
  artifacts:
    - path: "src/lib/resume/optimized-preview-highlights.ts"
      provides: "Explicit Layer 3 selector plus finalized per-bullet result wiring"
      contains: "selectVisibleExperienceHighlightsForEntry"
    - path: "src/lib/resume/optimized-preview-highlights.test.ts"
      provides: "Direct unit proof for same-entry editorial surfacing policy"
      contains: "Tier 1 dominates Tier 2 within the same experience entry"
    - path: "src/lib/resume/optimized-preview-contracts.test.ts"
      provides: "Regression proof that highlight caps and contract invariants remain unchanged"
      contains: "Contract 5"
    - path: "src/components/resume/resume-comparison-view.test.tsx"
      provides: "Renderer proof that surfaced bullets still carry highlight tier and category metadata"
      contains: "data-highlight-tier"
    - path: ".planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-VALIDATION.md"
      provides: "Short architecture note plus before/after validation note for the new Layer 3 policy"
      contains: "metric surfaces before contextual_stack"
  key_links:
    - from: "src/lib/resume/optimized-preview-highlights.ts"
      to: "src/lib/resume/optimized-preview-highlights.ts"
      via: "finalized bullet results flow into the new entry-level selector before renderer mapping"
      pattern: "(buildBulletHighlight|selectVisibleExperienceHighlightsForEntry|buildOptimizedPreviewHighlights)"
    - from: "src/lib/resume/optimized-preview-highlights.ts"
      to: "src/components/resume/resume-comparison-view.test.tsx"
      via: "highlight metadata propagation"
      pattern: "(highlightTier|highlightCategory)"
    - from: "src/lib/resume/optimized-preview-highlights.test.ts"
      to: "src/lib/resume/optimized-preview-highlights.ts"
      via: "selector-focused unit scenarios"
      pattern: "selectVisibleExperienceHighlightsForEntry"
---

<objective>
Implement Phase 87 as a local, reversible refactor that formalizes the experience-entry visible highlight policy as Layer 3 of the existing experience highlight pipeline.

Purpose: stop same-entry highlight surfacing from depending on score-only sorting or incidental bullet order.
Output: a dedicated pure selector, focused tests, and a short validation artifact documenting the new architecture and before/after behavior.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-CONTEXT.md
@.planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-RESEARCH.md
@.planning/phases/CURRIA-79-decouple-experience-rendered-highlight-from-diff-and-make-di/79-CONTEXT.md
@.planning/phases/CURRIA-81-calibrate-experience-span-candidate-taxonomy-and-ranking-aft/81-CONTEXT.md
@.planning/phases/CURRIA-86-introduce-evidence-tiered-presentation-for-experience-highl/86-CONTEXT.md
@src/lib/resume/optimized-preview-highlights.ts
@src/lib/resume/optimized-preview-highlights.test.ts
@src/lib/resume/optimized-preview-contracts.test.ts
@src/components/resume/resume-comparison-view.test.tsx

<scope_rules>
- Keep Layer 1 improvement gating unchanged in intent.
- Keep Layer 2 winner-span selection unchanged in intent.
- Do not reopen parsing, completion, ATS readiness gates, summary logic, rewrite logic, PDF/export, or CSS styling.
- Preserve existing caps and renderer contract.
</scope_rules>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the explicit same-entry surfacing selector and wire finalized bullet metadata into it</name>
  <files>src/lib/resume/optimized-preview-highlights.ts, src/lib/resume/optimized-preview-highlights.test.ts</files>
  <behavior>
    - Test 1: a same-entry `metric` bullet surfaces before a `contextual_stack` bullet even when the metric bullet has a lower raw improvement score.
    - Test 2: two valid Tier 1 bullets consume the entry cap of 2 and suppress a Tier 2 bullet from the same entry.
    - Test 3: same-rank ties stay deterministic by existing score metadata first and stable bullet order last.
  </behavior>
  <action>Refactor `buildBulletHighlight(...)` so it produces a finalized per-bullet result object carrying the existing render line, improvement score, renderability, highlight tier, highlight category, and stable bullet order. Extract the current same-entry visible-slot logic from `buildOptimizedPreviewHighlights(...)` into a pure helper such as `selectVisibleExperienceHighlightsForEntry(...)`. Implement explicit editorial ordering inside that helper: Tier 1 before Tier 2, `metric` before `scope_scale`, then `contextual_stack`, `anchored_leadership`, and `anchored_outcome`, while keeping the selector strictly downstream of existing gating and winner selection.</action>
  <verify>
    <automated>npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"</automated>
  </verify>
  <done>`optimized-preview-highlights.ts` contains an explicit same-entry selector that consumes finalized bullet metadata only, the old score-only `sort(...).slice(...)` path is gone from entry allocation, and focused selector tests prove Tier 1 dominance and deterministic tie-breaking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve caps, zero-highlight safety, and renderer contract while adding focused regressions</name>
  <files>src/lib/resume/optimized-preview-highlights.test.ts, src/lib/resume/optimized-preview-contracts.test.ts, src/components/resume/resume-comparison-view.test.tsx</files>
  <behavior>
    - Test 1: Tier 2 still surfaces when Tier 1 evidence is absent in the entry.
    - Test 2: weak or non-renderable secondary bullets are not forced into unused capacity.
    - Test 3: max one highlighted span per bullet and max two highlighted bullets per entry remain unchanged.
    - Test 4: surfaced bullets still preserve `highlightTier` and `highlightCategory` into the renderer contract.
  </behavior>
  <action>Add direct selector-oriented regressions in `optimized-preview-highlights.test.ts`, then update contract and component tests only where needed to prove the new Layer 3 selection still preserves zero-highlight safety, the existing caps, and the current renderer metadata flow. Keep the React comparison contract unchanged except for the improved surfaced subset chosen within the same experience entry.</action>
  <verify>
    <automated>npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"</automated>
  </verify>
  <done>The focused tests cover the required surfacing-policy scenarios, existing caps still hold, zero-highlight entries remain allowed, and renderer-facing metadata remains intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record the architecture seam and before/after validation note for Phase 87</name>
  <files>.planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-VALIDATION.md</files>
  <behavior>
    - Validation note names where the previous implicit same-entry surfacing lived.
    - Validation note names the new explicit selector and the metadata it consumes.
    - Validation note includes a realistic before/after example where Tier 1 now surfaces ahead of Tier 2 within the same entry.
  </behavior>
  <action>Write `87-VALIDATION.md` with a short architecture note and a before/after editorial validation example anchored on the new Layer 3 selector. Keep it specific to same-entry surfacing so the phase documents what changed without implying parsing, completion, tier rendering, or export behavior changed.</action>
  <verify>
    <automated>rg -n "(implicit same-entry surfacing|selectVisibleExperienceHighlightsForEntry|metric surfaces before contextual_stack)" ".planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-VALIDATION.md"</automated>
  </verify>
  <done>The phase directory contains a concise validation artifact that documents the old implicit seam, the new explicit selector, the metadata reuse boundary, and one concrete before/after editorial correction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finalized bullet highlight results -> entry-level selector | Layer 3 must trust upstream results without silently reinterpreting Layer 1 or Layer 2 responsibilities |
| entry-level selector -> preview renderer | Surfaced bullets must keep existing caps and renderer metadata contracts |
| tests/docs -> future highlight tuning phases | The new selector must remain auditable so future tuning work does not accidentally collapse the architecture back into score-only sorting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | T | same-entry selection logic | mitigate | Keep the new selector pure and downstream of finalized bullet metadata only; do not rerun parsing or winner selection inside Layer 3. |
| T-87-02 | D | visible highlight quality | mitigate | Use explicit editorial ordering plus deterministic ties so same-entry visible slots cannot flap by incidental array order or unstable scores. |
| T-87-03 | T | preview contract | mitigate | Preserve existing caps and metadata flow, and prove them with direct unit, contract, and renderer regressions. |
</threat_model>

<verification>
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"`
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"`
- `npm run typecheck`
</verification>

<success_criteria>
- A named pure selector formalizes same-entry visible highlight allocation using finalized bullet metadata only.
- Tier 1 evidence dominates same-entry visible slots before Tier 2 evidence can consume remaining capacity.
- Zero-highlight safety, per-bullet/per-entry caps, and renderer metadata contracts remain intact.
- The phase directory contains a short architecture plus validation note documenting the new Layer 3 seam and a before/after correction example.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-01-SUMMARY.md`
</output>

# 57-01 Plan

## Objective

Perform a narrow repository cleanup so temporary planning debris is removed or ignored, historical artifacts remain trustworthy, and current architecture docs are easier to navigate.

## Tasks

1. Inventory leftover `.planning/` debris and classify what is temporary versus canonical.
2. Remove or ignore clearly local-only files such as `.planning/.tmp-copy-audit.json` and `.planning/debug/`.
3. Preserve real historical planning artifacts, including the untracked Phase 48 directory, as canonical project memory.
4. Add lightweight doc entry points and hygiene guidance through `docs/architecture/README.md` and `.planning/README.md`.
5. Run `npm run audit:route-architecture`, `npm run test:architecture-proof-pack`, and `npm run typecheck`.

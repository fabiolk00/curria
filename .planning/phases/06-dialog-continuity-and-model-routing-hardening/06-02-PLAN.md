---
phase: 06-dialog-continuity-and-model-routing-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/config.ts
  - src/lib/agent/config.test.ts
  - src/app/api/agent/route.model-selection.test.ts
  - docs/openai/README.md
  - docs/ENVIRONMENT_SETUP.md
  - .env.example
autonomous: true
requirements:
  - AGNT-02
user_setup: []
must_haves:
  truths:
    - "Dialog and confirm turns follow one explicit resolved model contract."
    - "When `OPENAI_DIALOG_MODEL` is set, it overrides dialog and confirm turns; when it is absent, those phases inherit the resolved agent model."
    - "Docs and env examples describe the same contract the route and loop actually use."
  artifacts:
    - path: "src/lib/agent/config.ts"
      provides: "Canonical per-phase model resolver for agent, dialog, and confirm turns"
      contains: "resolveAgentModelForPhase"
    - path: "src/lib/agent/config.test.ts"
      provides: "Helper-level proof for override and no-override routing behavior"
      contains: "OPENAI_DIALOG_MODEL"
    - path: "src/app/api/agent/route.model-selection.test.ts"
      provides: "Fresh-import route proof that the resolved dialog model reaches OpenAI"
      contains: "model selection"
  key_links:
    - from: "src/lib/agent/config.ts"
      to: "src/app/api/agent/route.model-selection.test.ts"
      via: "route tests prove the resolved per-phase model reaches the real route seam"
      pattern: "OPENAI_DIALOG_MODEL"
    - from: "docs/openai/README.md"
      to: ".env.example"
      via: "docs and env examples must describe the same runtime contract"
      pattern: "OPENAI_AGENT_MODEL"
---

<objective>
Align the per-phase model-routing behavior with one explicit runtime and documentation contract.

Purpose: Dialog continuity is only trustworthy if operators and tests can prove which model dialog or confirm traffic actually uses. This plan hardens the resolver and removes hidden contract drift.
Output: A clear model-routing contract in `config.ts`, fresh-import route coverage, and docs or env examples that match the runtime behavior.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-RESEARCH.md
@.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-VALIDATION.md
@AGENTS.md
@src/lib/agent/config.ts
@src/lib/agent/config.test.ts
@src/lib/agent/agent-loop.ts
@src/app/api/agent/route.ts
@src/app/api/agent/route.model-selection.test.ts
@docs/openai/README.md
@docs/ENVIRONMENT_SETUP.md
@.env.example
</context>

<threat_model>
<trust_boundaries>
- Server env and combo configuration -> resolved agent and dialog model contract
- Config resolver -> route and loop model selection
- Operator docs and env examples -> deployment expectations
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-06-04</threat_id>
  <category>Tampering</category>
  <component>Per-phase model resolver</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep one explicit resolver where dialog and confirm either use `OPENAI_DIALOG_MODEL` or inherit the resolved agent model, with no hidden second default.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-06-05</threat_id>
  <category>Repudiation</category>
  <component>Operator model contract</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Make docs, env examples, helper tests, and fresh-import route tests all describe the same dialog-model override and fallback behavior.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the runtime dialog-model contract</name>
  <files>src/lib/agent/config.ts, src/lib/agent/config.test.ts, src/app/api/agent/route.model-selection.test.ts</files>
  <read_first>src/lib/agent/config.ts, src/lib/agent/config.test.ts, src/app/api/agent/route.model-selection.test.ts, src/lib/agent/agent-loop.ts, src/app/api/agent/route.ts</read_first>
  <action>Refine `src/lib/agent/config.ts` so the resolved dialog-model contract is explicit and deterministic: `dialog` and `confirm` use `OPENAI_DIALOG_MODEL` only when it is present and otherwise inherit the resolved agent model from the combo or agent-model env. Update helper and fresh-import route tests so both override and no-override cases are proven against the real route seam that reaches OpenAI.</action>
  <verify>npm test -- src/lib/agent/config.test.ts src/app/api/agent/route.model-selection.test.ts</verify>
  <acceptance_criteria>
    - `resolveAgentModelForPhase('dialog')` and `resolveAgentModelForPhase('confirm')` use the override only when configured
    - no hidden second default keeps dialog on a different model when the override is absent
    - route-level fresh-import tests prove the selected model reaches OpenAI for both dialog and confirm
  </acceptance_criteria>
  <done>The runtime model contract is explicit and protected by helper and route tests.</done>
</task>

<task type="auto">
  <name>Task 2: Align docs and env examples to the runtime contract</name>
  <files>docs/openai/README.md, docs/ENVIRONMENT_SETUP.md, .env.example</files>
  <read_first>docs/openai/README.md, docs/ENVIRONMENT_SETUP.md, .env.example, src/lib/agent/config.ts</read_first>
  <action>Update `docs/openai/README.md`, `docs/ENVIRONMENT_SETUP.md`, and `.env.example` so they describe the real routing contract in plain language: the combo resolves the baseline models, `OPENAI_AGENT_MODEL` can override the agent default, and `OPENAI_DIALOG_MODEL` is optional and affects both `dialog` and `confirm`. Remove or rewrite any wording that implies dialog permanently uses a hidden stronger model unless explicitly configured.</action>
  <verify>powershell -NoProfile -Command \"$files = 'docs/openai/README.md','docs/ENVIRONMENT_SETUP.md','.env.example'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \\\"`n\\\"; foreach ($target in @('OPENAI_AGENT_MODEL','OPENAI_DIALOG_MODEL','dialog','confirm')) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0\"</verify>
  <acceptance_criteria>
    - docs and env examples mention the optional dialog override explicitly
    - docs state that confirm shares the dialog-routing contract
    - no repo docs imply a hidden default dialog model that differs from runtime behavior
  </acceptance_criteria>
  <done>Operators and developers now read the same model-routing contract the runtime enforces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/agent/config.test.ts src/app/api/agent/route.model-selection.test.ts`
- [ ] `powershell -NoProfile -Command \"$files = 'docs/openai/README.md','docs/ENVIRONMENT_SETUP.md','.env.example'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \\\"`n\\\"; foreach ($target in @('OPENAI_AGENT_MODEL','OPENAI_DIALOG_MODEL','dialog','confirm')) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0\"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dialog and confirm requests now follow one explicit documented model-routing contract
  </success_criteria>

<output>
After completion, create `.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-02-SUMMARY.md`
</output>

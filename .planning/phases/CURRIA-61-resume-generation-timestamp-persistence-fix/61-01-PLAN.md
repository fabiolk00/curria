# 61-01 Plan

1. Confirm the `resume_generations.updated_at` contract in Prisma and the billing migration.
2. Align `createPendingResumeGeneration(...)` with that contract by persisting `updated_at` explicitly on create.
3. Keep the reuse/idempotency path unchanged.
4. Add a focused helper-level regression proving the create insert includes `updated_at`.
5. Re-run typecheck and the targeted resume-generation persistence tests.

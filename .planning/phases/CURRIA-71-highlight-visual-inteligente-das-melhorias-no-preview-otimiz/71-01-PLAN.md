# 71-01 Plan

1. Add a preview-scoped helper that compares original vs optimized resume text and emits selective highlight segments for meaningful additions only.
2. Integrate the helper into `resume-comparison-view` so the optimized summary and experience bullets render subtle green emphasis while the original column stays clean.
3. Add a lightweight toggle to hide/show highlights without changing any resume content.
4. Cover relevance-vs-noise behavior with unit and component tests, including premium metric bullet highlighting and “no whole screen green” guardrails.

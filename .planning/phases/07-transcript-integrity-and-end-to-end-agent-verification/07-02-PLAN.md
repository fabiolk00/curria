---
phase: 07-transcript-integrity-and-end-to-end-agent-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/dashboard/chat-interface.route-stream.test.tsx
  - src/components/dashboard/chat-interface.test.tsx
  - tests/e2e/chat-transcript.spec.ts
  - tests/e2e/fixtures/api-mocks.ts
  - tests/e2e/helpers/sse.ts
autonomous: true
requirements:
  - UX-01
  - QA-04
user_setup: []
must_haves:
  truths:
    - "The final visible chat transcript is proven against the real `/api/agent` stream seam, not only handcrafted UI payloads."
    - "Automated verification covers backend recovery, route model selection, and the final rendered assistant output for rewrite flows."
    - "Chromium regression coverage catches repeated bootstrap text, duplicate assistant bubbles, and transcript instability after hydration."
  artifacts:
    - path: "src/components/dashboard/chat-interface.route-stream.test.tsx"
      provides: "Real route-to-visible transcript integration proof"
      contains: "/api/agent"
    - path: "tests/e2e/chat-transcript.spec.ts"
      provides: "Browser-level transcript regression coverage for degraded rewrite flows"
      contains: "reescreva"
    - path: "tests/e2e/fixtures/api-mocks.ts"
      provides: "Deterministic SSE browser fixtures for transcript-specific degraded paths"
      contains: "streamChunks"
  key_links:
    - from: "src/components/dashboard/chat-interface.route-stream.test.tsx"
      to: "src/app/api/agent/route.ts"
      via: "the component must render the same stream the real route emits"
      pattern: "text/event-stream"
    - from: "tests/e2e/chat-transcript.spec.ts"
      to: "src/components/dashboard/chat-interface.tsx"
      via: "the browser transcript must stay coherent for the same degraded flows covered in the route tests"
      pattern: "chat-interface"
---

<objective>
Prove that the real `/api/agent` stream becomes the correct visible transcript in tests and in the browser.

Purpose: Phase 7 needs to close the seam between backend recovery proof and the exact assistant bubble the user sees, then lock that behavior in a focused Chromium regression.
Output: A real route-to-UI transcript integration suite plus a dedicated browser transcript spec for degraded rewrite flows.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-RESEARCH.md
@.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-VALIDATION.md
@AGENTS.md
@src/components/dashboard/chat-interface.tsx
@src/components/dashboard/chat-interface.test.tsx
@src/app/api/agent/route.ts
@src/app/api/agent/route.model-selection.test.ts
@src/app/api/agent/route.sse.test.ts
@tests/e2e/helpers/sse.ts
@tests/e2e/fixtures/api-mocks.ts
@tests/e2e/core-funnel.spec.ts
</context>

<threat_model>
<trust_boundaries>
- Real route SSE output -> component-rendered assistant transcript
- Mocked lower DB/OpenAI seams -> route-to-UI integration proof
- Browser fixture SSE payloads -> visible transcript, hydration, and final assistant bubble in Chromium
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-07-03</threat_id>
  <category>Repudiation</category>
  <component>Route-to-visible transcript proof</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Add a test seam where the rendered chat UI consumes a real `/api/agent` response stream, then mirror the same transcript risks in a focused browser regression.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add a real route-to-visible transcript integration seam</name>
  <files>src/components/dashboard/chat-interface.route-stream.test.tsx, src/components/dashboard/chat-interface.test.tsx</files>
  <read_first>src/components/dashboard/chat-interface.tsx, src/components/dashboard/chat-interface.test.tsx, src/app/api/agent/route.ts, src/app/api/agent/route.sse.test.ts, src/app/api/agent/route.model-selection.test.ts</read_first>
  <action>Create a dedicated component integration test that renders `ChatInterface`, routes `fetch('/api/agent')` through the real `src/app/api/agent/route.ts` response stream with mocked lower dependencies, and asserts the final visible assistant transcript for representative degraded flows such as `reescreva`, latest-vacancy replacement, and done-without-text fallback. Keep the existing route model-selection coverage in the focused verification bundle so `QA-04` spans backend selection plus rendered transcript output.</action>
  <verify>npm test -- src/components/dashboard/chat-interface.route-stream.test.tsx src/app/api/agent/route.sse.test.ts src/app/api/agent/route.model-selection.test.ts</verify>
  <acceptance_criteria>
    - at least one test renders `ChatInterface` from a real `/api/agent` response stream rather than a handcrafted SSE payload
    - the rendered transcript matches the expected final assistant text for degraded rewrite flows
    - the focused verification bundle still includes route-level model-selection proof from Phase 6
  </acceptance_criteria>
  <done>The repo can prove that the real route output becomes the expected visible assistant transcript.</done>
</task>

<task type="auto">
  <name>Task 2: Add a focused Chromium transcript regression for degraded rewrite flows</name>
  <files>tests/e2e/chat-transcript.spec.ts, tests/e2e/fixtures/api-mocks.ts, tests/e2e/helpers/sse.ts</files>
  <read_first>tests/e2e/core-funnel.spec.ts, tests/e2e/fixtures/api-mocks.ts, tests/e2e/helpers/sse.ts, src/components/dashboard/chat-interface.tsx</read_first>
  <action>Add a dedicated Chromium browser spec for transcript integrity using the existing mocked SSE infrastructure. Extend the fixture helpers only as needed so the browser lane can simulate degraded rewrite flows, latest-vacancy replacement, and hydration after `done`, then assert that the final visible transcript contains one coherent assistant message and does not regress into repeated vacancy bootstrap text.</action>
  <verify>npm run test:e2e -- tests/e2e/chat-transcript.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - the browser suite includes a `reescreva`-style degraded rewrite flow
    - the final rendered transcript contains one coherent assistant turn for the request
    - transcript stability is asserted after session hydration or refresh, not only during the live stream
  </acceptance_criteria>
  <done>The browser lane can catch transcript regressions that would still look correct in loop-only or route-only tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/components/dashboard/chat-interface.route-stream.test.tsx src/app/api/agent/route.sse.test.ts src/app/api/agent/route.model-selection.test.ts`
- [ ] `npm run test:e2e -- tests/e2e/chat-transcript.spec.ts --project=chromium`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Automated verification now spans route model selection, degraded dialog recovery, and the final visible transcript in both integration and browser seams
  </success_criteria>

<output>
After completion, create `.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-02-SUMMARY.md`
</output>

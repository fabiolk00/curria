---
phase: 07-transcript-integrity-and-end-to-end-agent-verification
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - scripts/replay-agent-dialog.ts
  - scripts/replay-agent-dialog.test.ts
  - package.json
  - docs/agent-transcript-repro.md
autonomous: true
requirements:
  - QA-04
  - QA-05
user_setup: []
must_haves:
  truths:
    - "Operators can replay a representative vacancy -> `reescreva` sequence and capture release headers, SSE events, and final assistant text."
    - "The transcript replay workflow composes with the existing Phase 5 parity check instead of inventing a separate deployment-diagnosis contract."
    - "Phase 7 closes with one focused verification bundle that ties route model selection, transcript rendering, browser proof, and replay evidence together."
  artifacts:
    - path: "scripts/replay-agent-dialog.ts"
      provides: "Operator replay CLI for transcript-specific incident capture"
      contains: "reescreva"
    - path: "scripts/replay-agent-dialog.test.ts"
      provides: "Automated proof for replay argument parsing and capture formatting"
      contains: "replay-agent-dialog"
    - path: "docs/agent-transcript-repro.md"
      provides: "Runbook for parity plus transcript replay evidence"
      contains: "agent:parity"
  key_links:
    - from: "scripts/replay-agent-dialog.ts"
      to: "scripts/check-agent-runtime-parity.ts"
      via: "replay evidence must reuse the same release and runtime provenance contract"
      pattern: "X-Agent-Release"
    - from: "docs/agent-transcript-repro.md"
      to: "src/components/dashboard/chat-interface.tsx"
      via: "operator guidance must describe the same visible transcript behavior the UI and browser tests prove"
      pattern: "chat-interface"
---

<objective>
Give operators a reproducible transcript replay path and capture the evidence needed to close the original `reescreva` incident.

Purpose: The milestone only closes cleanly when the team can replay and inspect the exact class of failure that motivated it, using the same provenance contract and visible transcript expectations already proven in code.
Output: A transcript replay CLI, docs that compose it with `agent:parity`, and a final verification bundle that ties the entire milestone together.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-RESEARCH.md
@.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-VALIDATION.md
@.planning/phases/05-deployed-agent-parity-and-evidence/05-VERIFICATION.md
@AGENTS.md
@scripts/check-agent-runtime-parity.ts
@package.json
@src/app/api/agent/route.ts
@src/components/dashboard/chat-interface.tsx
</context>

<threat_model>
<trust_boundaries>
- Live deployment headers and SSE events -> committed incident evidence
- Operator replay CLI -> route provenance and final assistant text capture
- Replay docs -> safe usage against dev or deployed environments
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-07-04</threat_id>
  <category>Repudiation</category>
  <component>Incident replay evidence</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Capture release headers, SSE event sequence, and final assistant text in one reproducible workflow that composes with the Phase 5 parity contract.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build operator replay tooling for the `reescreva` incident shape</name>
  <files>scripts/replay-agent-dialog.ts, scripts/replay-agent-dialog.test.ts, package.json</files>
  <read_first>scripts/check-agent-runtime-parity.ts, package.json, src/app/api/agent/route.ts, .planning/phases/05-deployed-agent-parity-and-evidence/05-VERIFICATION.md</read_first>
  <action>Create a transcript-focused replay CLI that posts a representative vacancy plus follow-up rewrite sequence to `/api/agent`, captures release headers, request metadata, SSE events, and final assistant text, and emits a deterministic JSON or markdown artifact suitable for incident evidence. Add a package script entry for the CLI and automated tests for argument parsing and output shaping. Reuse the Phase 5 provenance contract rather than inventing a second header or release scheme.</action>
  <verify>npm test -- scripts/replay-agent-dialog.test.ts && npx tsx scripts/replay-agent-dialog.ts --help</verify>
  <acceptance_criteria>
    - the replay CLI captures release headers, SSE sequence, and final assistant text in one run
    - the CLI can be inspected safely through `--help` and unit tests without requiring live credentials
    - the package scripts expose the replay flow in a discoverable way for operators
  </acceptance_criteria>
  <done>Operators can capture representative transcript evidence for the `reescreva` incident with one committed replay workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Document the replay workflow and lock the final Phase 7 verification bundle</name>
  <files>docs/agent-transcript-repro.md, package.json</files>
  <read_first>docs/openai/README.md, .planning/phases/05-deployed-agent-parity-and-evidence/05-VERIFICATION.md, .planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-VALIDATION.md, package.json</read_first>
  <action>Document the exact operator flow for closing the original `reescreva` incident: run the parity check, replay the representative dialog sequence, inspect the final visible transcript evidence, and compare it against the focused route, component, and browser suites from Phase 7. Update the final verification bundle and any supporting script entries so `QA-04` and `QA-05` can be closed with reproducible commands instead of loose notes.</action>
  <verify>npm run typecheck && npm test -- src/components/dashboard/chat-interface.test.tsx src/components/dashboard/chat-interface.route-stream.test.tsx src/app/api/agent/route.model-selection.test.ts src/app/api/agent/route.sse.test.ts scripts/replay-agent-dialog.test.ts && npm run test:e2e -- tests/e2e/chat-transcript.spec.ts --project=chromium && npx tsx scripts/replay-agent-dialog.ts --help && node .codex/get-shit-done/bin/gsd-tools.cjs state validate</verify>
  <acceptance_criteria>
    - docs describe the parity-plus-replay workflow in concrete operator steps
    - final verification includes route model-selection proof, visible-transcript proof, browser proof, and replay-tool proof
    - the phase can close with committed incident evidence instead of an unstructured anecdote
  </acceptance_criteria>
  <done>Phase 7 finishes with reproducible operator guidance and one verification bundle that closes the original transcript incident.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/components/dashboard/chat-interface.test.tsx src/components/dashboard/chat-interface.route-stream.test.tsx src/app/api/agent/route.model-selection.test.ts src/app/api/agent/route.sse.test.ts scripts/replay-agent-dialog.test.ts`
- [ ] `npm run test:e2e -- tests/e2e/chat-transcript.spec.ts --project=chromium`
- [ ] `npx tsx scripts/replay-agent-dialog.ts --help`
- [ ] `node .codex/get-shit-done/bin/gsd-tools.cjs state validate`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The original `reescreva` incident can now be replayed, inspected, and closed with committed route or transcript evidence
  </success_criteria>

<output>
After completion, create `.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-03-SUMMARY.md`
</output>

---
phase: 07-transcript-integrity-and-end-to-end-agent-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/chat-interface.tsx
  - src/components/dashboard/chat-interface.test.tsx
autonomous: true
requirements:
  - UX-01
user_setup: []
must_haves:
  truths:
    - "One chat request updates exactly one assistant bubble in the visible transcript."
    - "Recoverable stream errors and done-without-text paths do not create duplicate or competing assistant turns."
    - "Late history hydration or session snapshot refetches cannot replace a richer in-memory transcript with stale or shorter assistant text."
  artifacts:
    - path: "src/components/dashboard/chat-interface.tsx"
      provides: "Deterministic transcript assembly and reconciliation for one in-flight assistant turn"
      contains: "assistantMessageId"
    - path: "src/components/dashboard/chat-interface.test.tsx"
      provides: "Component proof for SSE chunk handling, optimistic transcript preservation, and history reconciliation"
      contains: "ChatInterface"
  key_links:
    - from: "src/components/dashboard/chat-interface.tsx"
      to: "src/app/api/agent/route.ts"
      via: "the chat UI renders the SSE chunk contract emitted by the route"
      pattern: "type"
    - from: "src/components/dashboard/chat-interface.tsx"
      to: "src/app/api/session/[id]/messages/route.ts"
      via: "history hydration must not overwrite or duplicate the live transcript"
      pattern: "messages"
---

<objective>
Harden the chat client so one request becomes one coherent visible assistant turn.

Purpose: Phase 6 stabilized the backend recovery contract, but the chat UI still owns the final transcript assembly across SSE chunks, history hydration, and post-done session refetches.
Output: A transcript-safe `ChatInterface` that preserves one assistant bubble per request and avoids stale or duplicated visible assistant text.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-RESEARCH.md
@.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-VALIDATION.md
@.planning/phases/06-dialog-continuity-and-model-routing-hardening/06-VERIFICATION.md
@AGENTS.md
@src/components/dashboard/chat-interface.tsx
@src/components/dashboard/chat-interface.test.tsx
@src/app/api/agent/route.ts
@src/app/api/session/[id]/messages/route.ts
@src/lib/db/sessions.ts
</context>

<threat_model>
<trust_boundaries>
- Real `/api/agent` SSE chunk sequence -> visible assistant bubble in `ChatInterface`
- Optimistic in-memory transcript -> `/api/session/:id/messages` hydration and `/api/session/:id` snapshot refetch
- Recoverable stream errors and empty `done` paths -> final rendered assistant copy
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-07-01</threat_id>
  <category>Tampering</category>
  <component>Visible transcript assembly</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Use one request-scoped assistant message lifecycle so text, recoverable error, and done events all converge on the same visible transcript row.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-07-02</threat_id>
  <category>Repudiation</category>
  <component>History and snapshot reconciliation</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Prevent shorter or stale server hydration payloads from overwriting a richer in-memory transcript for the active request while keeping legitimate server truth updates intact.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the one-request-one-assistant-bubble lifecycle</name>
  <files>src/components/dashboard/chat-interface.tsx, src/components/dashboard/chat-interface.test.tsx</files>
  <read_first>src/components/dashboard/chat-interface.tsx, src/components/dashboard/chat-interface.test.tsx, src/app/api/agent/route.ts, src/types/agent.ts</read_first>
  <action>Refactor the transcript update logic in `src/components/dashboard/chat-interface.tsx` so every user send creates exactly one request-scoped assistant bubble and all related SSE events update that same bubble. Make `text`, recoverable `error`, and `done` paths converge on one visible assistant transcript entry, avoid leaving `Pensando...` behind when useful assistant content exists, and do not surface recoverable stream errors as a second competing assistant turn.</action>
  <verify>npm test -- src/components/dashboard/chat-interface.test.tsx</verify>
  <acceptance_criteria>
    - a single send creates one assistant bubble that is incrementally updated through stream completion
    - recoverable stream errors do not create duplicate assistant or warning bubbles
    - empty `done` paths still replace the thinking bubble exactly once with the expected fallback text
  </acceptance_criteria>
  <done>The visible transcript stays scoped to one assistant turn per request while the stream is active and when it ends.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile optimistic transcript state with history hydration and done snapshot refresh</name>
  <files>src/components/dashboard/chat-interface.tsx, src/components/dashboard/chat-interface.test.tsx</files>
  <read_first>src/components/dashboard/chat-interface.tsx, src/components/dashboard/chat-interface.test.tsx, src/app/api/session/[id]/messages/route.ts, src/lib/db/sessions.ts</read_first>
  <action>Tighten the history and snapshot reconciliation rules in `src/components/dashboard/chat-interface.tsx` so the `/api/session/:id/messages` fetch and `/api/session/:id` refetch after `done` cannot replace a richer in-memory assistant transcript with shorter or stale server content for the active request. Preserve the current protections for empty first-history responses and session state header updates, and add explicit transcript-focused component tests for these races.</action>
  <verify>npm test -- src/components/dashboard/chat-interface.test.tsx</verify>
  <acceptance_criteria>
    - late history hydration does not replace a richer visible assistant transcript with shorter persisted content
    - done-triggered snapshot refresh updates phase, ATS, and message-count UI without duplicating assistant text
    - session reload or reset scenarios preserve a coherent transcript after the active request settles
  </acceptance_criteria>
  <done>The UI can reconcile optimistic and server truth updates without duplicating or degrading the visible assistant transcript.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/components/dashboard/chat-interface.test.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- One request now renders as one coherent visible assistant turn even when degraded transcript paths fire
  </success_criteria>

<output>
After completion, create `.planning/phases/07-transcript-integrity-and-end-to-end-agent-verification/07-01-SUMMARY.md`
</output>

---
phase: 35-harden-rewrite-state-coherence-between-ats-optimized-state-c
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/agent-loop.ts
  - src/lib/agent/tools/index.ts
  - .planning/phases/CURRIA-35-harden-rewrite-state-coherence-between-ats-optimized-state-c/**
autonomous: true
requirements:
  - COH-01
  - COH-02
user_setup: []
must_haves:
  truths:
    - "Phases 8, 9, and 10 already established `optimizedCvState` as the validated deterministic rewrite output."
    - "`cvState` must remain canonical persisted truth even while downstream consumers prefer an effective optimized source when one exists."
    - "The smallest safe change is to centralize effective-state selection instead of patching each rewrite call site independently."
  artifacts:
    - path: "src/lib/agent/agent-loop.ts"
      provides: "Chat follow-up rewrite sourcing and deterministic helper paths"
      contains: "buildRewriteCurrentContent"
    - path: "src/lib/agent/tools/index.ts"
      provides: "Tool-driven target resume creation path"
      contains: "create_target_resume"
    - path: ".planning/milestones/v1.1-phases/CURRIA-10-target-job-rewrite-pipeline/10-CONTEXT.md"
      provides: "Historical target-job optimized-state contract"
      contains: "optimizedCvState"
---

<objective>
Route chat rewrite sourcing and deterministic target derivation through one effective optimized resume state.

Purpose: Remove stale-source drift without changing the underlying ATS or job-targeting pipeline design.
Output: Chat and target-derivation consumers consistently prefer `agentState.optimizedCvState ?? cvState`.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/CURRIA-35-harden-rewrite-state-coherence-between-ats-optimized-state-c/35-CONTEXT.md
@.planning/milestones/v1.1-phases/06-dialog-continuity-and-model-routing-hardening/06-RESEARCH.md
@.planning/milestones/v1.1-phases/CURRIA-09-ats-enhancement-reliability-hardening/09-02-SUMMARY.md
@.planning/milestones/v1.1-phases/CURRIA-10-target-job-rewrite-pipeline/10-CONTEXT.md
@src/lib/agent/agent-loop.ts
@src/lib/agent/tools/index.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize effective resume-state selection in the chat rewrite path</name>
  <files>src/lib/agent/agent-loop.ts</files>
  <read_first>src/lib/agent/agent-loop.ts, .planning/milestones/v1.1-phases/06-dialog-continuity-and-model-routing-hardening/06-RESEARCH.md</read_first>
  <action>Add or use one effective state helper so `buildRewriteCurrentContent`, rewrite keyword sourcing, scoring text generation, and deterministic target derivation all prefer `agentState.optimizedCvState` when present. Preserve the existing canonical `cvState` rule and avoid broad loop rewrites.</action>
  <verify>npm test -- src/lib/agent/streaming-loop.test.ts</verify>
  <acceptance_criteria>
    - follow-up rewrite content can come from the effective optimized state
    - deterministic helper paths no longer depend on stale base `cvState` when an optimized snapshot exists
    - the change stays localized and brownfield-safe
  </acceptance_criteria>
  <done>Chat rewrite and deterministic helper paths share one effective optimized-state source.</done>
</task>

<task type="auto">
  <name>Task 2: Align target resume creation with the same effective optimized source</name>
  <files>src/lib/agent/tools/index.ts</files>
  <read_first>src/lib/agent/tools/index.ts, .planning/milestones/v1.1-phases/CURRIA-10-target-job-rewrite-pipeline/10-CONTEXT.md</read_first>
  <action>Update `create_target_resume` so it derives from the latest effective optimized resume state when an ATS rewrite already exists, instead of rebuilding from stale base `cvState`.</action>
  <verify>npm test -- src/lib/agent/tools/index.test.ts</verify>
  <acceptance_criteria>
    - tool-driven target resume creation starts from the latest effective optimized source
    - ATS enhancement and target resume generation stay coherent after earlier rewrites
    - no new alternate state model is introduced
  </acceptance_criteria>
  <done>Target resume creation uses the same effective optimized-state contract as chat follow-up rewrites.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/lib/agent/streaming-loop.test.ts`
- [ ] `npm test -- src/lib/agent/tools/index.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Chat rewrite sourcing and target resume derivation now agree on the same effective optimized resume state
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-35-harden-rewrite-state-coherence-between-ats-optimized-state-c/35-01-SUMMARY.md`
</output>

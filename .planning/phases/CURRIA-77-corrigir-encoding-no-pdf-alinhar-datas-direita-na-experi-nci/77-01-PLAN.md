# Phase 77 Plan

1. Replace the PDF font embedding with a broader Inter asset and sanitize PDF text deterministically before wrapping/drawing.
2. Refactor the PDF experience renderer so the role title stays on the left and the period is drawn right-aligned on the same header line, with company/location below.
3. Reduce the `Estimado` tooltip footprint while preserving accessibility and existing trigger behavior.
4. Add regression tests for accented PDF output, technical strings, experience header alignment, and the compact tooltip contract.

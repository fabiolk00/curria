---
phase: 48-route-policy-extraction-and-decision-normalization
plan: "01"
type: execute
wave: 1
autonomous: true
requirements:
  - ROUTE-POLICY-01
files_modified:
  - src/app/api/session/[id]/generate/route.ts
  - src/app/api/session/[id]/generate/route.test.ts
  - src/lib/routes/shared/types.ts
  - src/lib/routes/shared/response.ts
  - src/lib/routes/session-generate/context.ts
  - src/lib/routes/session-generate/policy.ts
  - src/lib/routes/session-generate/decision.ts
  - src/lib/routes/session-generate/response.ts
  - src/lib/routes/session-generate/types.ts
  - src/lib/routes/session-generate/policy.test.ts
  - src/lib/routes/session-generate/decision.test.ts
---

<objective>
Refactor the session generate route into explicit context, policy, decision, and response modules without changing idempotency, billing, active-export blocking, replay behavior, or durable job semantics.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-CONTEXT.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-RESEARCH.md
@src/app/api/session/[id]/generate/route.ts
@src/app/api/session/[id]/generate/route.test.ts
@src/lib/jobs/repository.ts
@src/lib/jobs/runtime.ts
@src/lib/jobs/source-of-truth.ts
@src/lib/db/sessions.ts
@src/lib/db/resume-targets.ts
@src/lib/security/request-trust.ts
@src/lib/agent/profile-review.ts

<must_preserve>
- `409 EXPORT_ALREADY_PROCESSING`
- `409 BILLING_RECONCILIATION_PENDING`
- `409 CAREER_FIT_CONFIRMATION_REQUIRED`
- existing durable job reuse and retry behavior
- existing degraded artifact-success fallback when completed job metadata is partial
- existing success payloads for completed and in-progress responses
</must_preserve>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract session generate request context and policy gates</name>
  <files>src/lib/routes/shared/types.ts, src/lib/routes/session-generate/context.ts, src/lib/routes/session-generate/policy.ts, src/lib/routes/session-generate/types.ts, src/app/api/session/[id]/generate/route.ts</files>
  <action>Move app-user lookup, session ownership resolution, trust validation, body parsing, target resolution, effective source resolution, and idempotency key construction into a typed context resolver. Move active export blocking, reconciliation pending detection, and reusable failed-job retry eligibility into a policy module that returns explicit internal decisions instead of inline route branches. Keep policy helpers pure where possible so route behavior remains transparent and easy to test.</action>
  <verify>
    <automated>npx vitest run "src/app/api/session/[id]/generate/route.test.ts"</automated>
  </verify>
  <done>The route no longer owns repeated policy branching for scope resolution, active export guards, or reconciliation gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize durable orchestration and HTTP response mapping</name>
  <files>src/lib/routes/shared/response.ts, src/lib/routes/session-generate/decision.ts, src/lib/routes/session-generate/response.ts, src/app/api/session/[id]/generate/route.ts</files>
  <action>Move durable job create-or-reuse, retry job creation, start attempt, completed-job normalization, failed-job normalization, and generating-state persistence into decision and response modules. Keep the route body to a thin adapter that resolves context, evaluates policy, executes or normalizes the job path, and maps the resulting decision to the current HTTP contract.</action>
  <verify>
    <automated>npx vitest run "src/app/api/session/[id]/generate/route.test.ts"</automated>
  </verify>
  <done>The route reads like a declarative adapter and still returns identical success, conflict, failed, and in-progress responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused unit proof for extracted generate decisions</name>
  <files>src/lib/routes/session-generate/policy.test.ts, src/lib/routes/session-generate/decision.test.ts, src/app/api/session/[id]/generate/route.test.ts</files>
  <action>Add small unit tests for active export blocking, reconciliation pending, retry-job reuse, and completed-job normalization while retaining the current route regression suite. Prefer direct policy and decision tests over new heavyweight integration setups.</action>
  <verify>
    <automated>npx vitest run src/lib/routes/session-generate/policy.test.ts src/lib/routes/session-generate/decision.test.ts "src/app/api/session/[id]/generate/route.test.ts"</automated>
  </verify>
  <done>The extraction is covered both by route regressions and by cheap decision-level proof.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/routes/session-generate/policy.test.ts src/lib/routes/session-generate/decision.test.ts "src/app/api/session/[id]/generate/route.test.ts"`
- `npm run typecheck`
</verification>

<success_criteria>
- The generate route becomes substantially smaller and easier to read.
- Product-policy logic for active export, reconciliation blocking, retry normalization, and durable result mapping no longer lives inline in the route.
- Public status codes and payloads remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-01-SUMMARY.md`
</output>

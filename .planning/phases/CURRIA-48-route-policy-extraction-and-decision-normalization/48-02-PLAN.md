---
phase: 48-route-policy-extraction-and-decision-normalization
plan: "02"
type: execute
wave: 2
depends_on:
  - 48-01
autonomous: true
requirements:
  - ROUTE-POLICY-01
  - ROUTE-POLICY-TEST-01
files_modified:
  - src/app/api/file/[sessionId]/route.ts
  - src/app/api/file/[sessionId]/route.test.ts
  - src/app/api/profile/smart-generation/route.ts
  - src/app/api/profile/smart-generation/route.test.ts
  - src/lib/routes/file-access/context.ts
  - src/lib/routes/file-access/decision.ts
  - src/lib/routes/file-access/response.ts
  - src/lib/routes/file-access/types.ts
  - src/lib/routes/file-access/decision.test.ts
  - src/lib/routes/smart-generation/context.ts
  - src/lib/routes/smart-generation/decision.ts
  - src/lib/routes/smart-generation/response.ts
  - src/lib/routes/smart-generation/types.ts
  - src/lib/routes/smart-generation/decision.test.ts
---

<objective>
Normalize file access and smart-generation decisions so preview lock, artifact availability, persisted generation output, and response mapping are explicit and centrally enforced instead of interpreted inline by the routes.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-CONTEXT.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-RESEARCH.md
@src/app/api/file/[sessionId]/route.ts
@src/app/api/file/[sessionId]/route.test.ts
@src/app/api/profile/smart-generation/route.ts
@src/app/api/profile/smart-generation/route.test.ts
@src/lib/generated-preview/locked-preview.ts
@src/lib/cv/preview-sanitization.ts
@src/lib/agent/tools/generate-file.ts
@src/lib/jobs/repository.ts
@src/lib/db/sessions.ts
@src/lib/db/resume-targets.ts

<must_preserve>
- locked preview URLs keep historical-lock precedence over current plan inference
- signed URLs can only be emitted after the decision layer allows real artifact access
- reconciliation and pending-generation payload semantics remain unchanged
- smart-generation returns the same workflow-mode, validation, credits, and preview-lock contracts
</must_preserve>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract file access context and artifact access decisions</name>
  <files>src/lib/routes/file-access/context.ts, src/lib/routes/file-access/decision.ts, src/lib/routes/file-access/response.ts, src/lib/routes/file-access/types.ts, src/app/api/file/[sessionId]/route.ts</files>
  <action>Move session or target lookup, artifact metadata resolution, latest artifact job lookup, lifecycle normalization, reconciliation detection, preview lock inspection, and signed-url eligibility into explicit file-access modules. Model decisions such as unavailable, locked preview, real artifact available, and failed or reconciling artifact so the route stops reconstructing policy inline.</action>
  <verify>
    <automated>npx vitest run "src/app/api/file/[sessionId]/route.test.ts"</automated>
  </verify>
  <done>The file route becomes viewer-aware but no longer policy-dense inline, and signed URL issuance is impossible outside the decision layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract smart-generation context and preview-aware generation decisions</name>
  <files>src/lib/routes/smart-generation/context.ts, src/lib/routes/smart-generation/decision.ts, src/lib/routes/smart-generation/response.ts, src/lib/routes/smart-generation/types.ts, src/app/api/profile/smart-generation/route.ts</files>
  <action>Move authenticated request validation, workflow-mode and copy resolution, readiness and quota gating, session bootstrap, pipeline execution, dispatch result normalization, persisted-patch or generated-output interpretation, and preview-lock response shaping into smart-generation modules. Add explicit consistency checks so generated output and persisted patch are interpreted centrally rather than ad hoc in the route.</action>
  <verify>
    <automated>npx vitest run "src/app/api/profile/smart-generation/route.test.ts"</automated>
  </verify>
  <done>The smart-generation route becomes a thin adapter and is no longer the place where preview-lock contract interpretation lives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused unit proof for file access and smart-generation decisions</name>
  <files>src/lib/routes/file-access/decision.test.ts, src/lib/routes/smart-generation/decision.test.ts, src/app/api/file/[sessionId]/route.test.ts, src/app/api/profile/smart-generation/route.test.ts</files>
  <action>Add cheap tests for file decision mapping, historical lock precedence, real artifact gating, smart-generation preview contract normalization, and generated-output versus persisted-patch consistency while preserving the current route regression coverage.</action>
  <verify>
    <automated>npx vitest run src/lib/routes/file-access/decision.test.ts src/lib/routes/smart-generation/decision.test.ts "src/app/api/file/[sessionId]/route.test.ts" "src/app/api/profile/smart-generation/route.test.ts"</automated>
  </verify>
  <done>The extracted helpers prove the sensitive preview-lock and artifact-access invariants without relying only on full route tests.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/routes/file-access/decision.test.ts src/lib/routes/smart-generation/decision.test.ts "src/app/api/file/[sessionId]/route.test.ts" "src/app/api/profile/smart-generation/route.test.ts"`
- `npm run typecheck`
</verification>

<success_criteria>
- File access and smart-generation routes are thin adapters over explicit decisions.
- Preview-lock, artifact access, reconciliation, and persisted generation-output semantics remain unchanged.
- The decision layer becomes the only place that authorizes real artifact URLs or locked-preview responses.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-02-SUMMARY.md`
</output>

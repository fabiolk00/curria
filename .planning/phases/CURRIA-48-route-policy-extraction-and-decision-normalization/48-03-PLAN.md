---
phase: 48-route-policy-extraction-and-decision-normalization
plan: "03"
type: execute
wave: 3
depends_on:
  - 48-01
  - 48-02
autonomous: true
requirements:
  - ROUTE-POLICY-TEST-01
  - ROUTE-POLICY-DOC-01
files_modified:
  - src/app/api/session/[id]/versions/route.ts
  - src/app/api/session/[id]/versions/route.test.ts
  - src/app/api/session/[id]/compare/route.ts
  - src/app/api/session/[id]/compare/route.test.ts
  - src/lib/routes/session-versions/context.ts
  - src/lib/routes/session-versions/decision.ts
  - src/lib/routes/session-versions/response.ts
  - src/lib/routes/session-versions/types.ts
  - src/lib/routes/session-versions/decision.test.ts
  - src/lib/routes/session-compare/context.ts
  - src/lib/routes/session-compare/decision.ts
  - src/lib/routes/session-compare/response.ts
  - src/lib/routes/session-compare/types.ts
  - src/lib/routes/session-compare/decision.test.ts
  - docs/architecture/route-policy-boundaries.md
---

<objective>
Thin the versions and compare routes, add route-decision regression proof, and document the route-policy boundary model so future routes keep policy logic out of handlers.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-CONTEXT.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-RESEARCH.md
@src/app/api/session/[id]/versions/route.ts
@src/app/api/session/[id]/versions/route.test.ts
@src/app/api/session/[id]/compare/route.ts
@src/app/api/session/[id]/compare/route.test.ts
@src/lib/cv/preview-sanitization.ts
@src/lib/cv/compare.ts
@src/lib/db/cv-versions.ts
@src/lib/db/resume-targets.ts
@src/lib/db/sessions.ts

<must_preserve>
- versions route still sanitizes locked timeline entries and never leaks real locked snapshots
- compare route still returns `locked: true` and omits `diff` when either side is preview-locked
- invalid refs, ownership checks, and response payloads remain unchanged
- cross-surface preview-lock proof stays intact and is not weakened
</must_preserve>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract versions route sanitization decisions</name>
  <files>src/lib/routes/session-versions/context.ts, src/lib/routes/session-versions/decision.ts, src/lib/routes/session-versions/response.ts, src/lib/routes/session-versions/types.ts, src/app/api/session/[id]/versions/route.ts</files>
  <action>Move scope parsing, timeline plus target loading, and preview-aware version sanitization into dedicated session-versions modules. Keep snapshot stripping and preview-lock metadata mapping centralized in the decision layer so the route no longer decides what to redact.</action>
  <verify>
    <automated>npx vitest run "src/app/api/session/[id]/versions/route.test.ts"</automated>
  </verify>
  <done>The versions route becomes a declarative adapter over a sanitized versions decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract compare route outcome decisions and preserve locked-compare semantics</name>
  <files>src/lib/routes/session-compare/context.ts, src/lib/routes/session-compare/decision.ts, src/lib/routes/session-compare/response.ts, src/lib/routes/session-compare/types.ts, src/app/api/session/[id]/compare/route.ts</files>
  <action>Move snapshot ref resolution, target and version lookup, preview-aware sanitization, locked-compare detection, and diff-or-lock normalization into session-compare modules. Ensure the decision layer owns the rule that any locked side suppresses real diff generation.</action>
  <verify>
    <automated>npx vitest run "src/app/api/session/[id]/compare/route.test.ts"</automated>
  </verify>
  <done>The compare route no longer interprets preview-lock semantics inline and returns the same locked or diff responses as before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add decision tests and document route policy boundaries</name>
  <files>src/lib/routes/session-versions/decision.test.ts, src/lib/routes/session-compare/decision.test.ts, src/app/api/session/[id]/versions/route.test.ts, src/app/api/session/[id]/compare/route.test.ts, docs/architecture/route-policy-boundaries.md</files>
  <action>Add unit tests for versions sanitization and compare lock decisions, keep the existing route regressions green, and write a short architecture note that states what routes may and may not do. Include one concrete generate-route before-versus-after example so future refactors follow the same boundary model.</action>
  <verify>
    <automated>npx vitest run src/lib/routes/session-versions/decision.test.ts src/lib/routes/session-compare/decision.test.ts "src/app/api/session/[id]/versions/route.test.ts" "src/app/api/session/[id]/compare/route.test.ts"</automated>
  </verify>
  <done>Future route authors have a documented pattern to follow, and the preview-aware decision helpers are proved independently of the route shell.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/routes/session-versions/decision.test.ts src/lib/routes/session-compare/decision.test.ts "src/app/api/session/[id]/versions/route.test.ts" "src/app/api/session/[id]/compare/route.test.ts"`
- `npx vitest run "src/app/api/session/[id]/generate/route.test.ts" "src/app/api/file/[sessionId]/route.test.ts" "src/app/api/profile/smart-generation/route.test.ts" "src/app/api/session/[id]/versions/route.test.ts" "src/app/api/session/[id]/compare/route.test.ts"`
- `npm run typecheck`
</verification>

<success_criteria>
- Versions and compare routes become declarative adapters over centralized preview-aware decisions.
- Route regression tests still prove unchanged public behavior across all five target surfaces.
- The repo contains a short architecture note that makes route-policy boundaries explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-03-SUMMARY.md`
</output>

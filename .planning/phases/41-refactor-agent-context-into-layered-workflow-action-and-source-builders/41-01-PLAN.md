---
phase: 41-refactor-agent-context-into-layered-workflow-action-and-source-builders
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/context/**
  - src/lib/agent/context-builder.ts
  - src/lib/agent/context-builder.test.ts
  - src/lib/agent/agent-loop.ts
  - src/lib/agent/__tests__/streaming-prompt-regression.test.ts
  - src/lib/agent/tools/rewrite-resume-full.ts
  - src/lib/agent/tools/pipeline.test.ts
  - src/lib/agent/tools/rewrite-section.test.ts
  - src/lib/agent/tools/validate-rewrite.test.ts
  - .planning/phases/41-refactor-agent-context-into-layered-workflow-action-and-source-builders/**
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "Phase 41 is a context-architecture refactor only. It must not redesign the async runtime, `/api/agent` orchestration, billing behavior, or artifact rendering."
    - "`cvState` remains canonical truth and `agentState` remains operational context."
    - "The public compatibility seam stays `buildSystemPrompt(...)` so the brownfield agent loop and prompt-regression tests do not need broad runtime surgery."
    - "Lightweight chat must remain lighter than ATS and job-targeting rewrite flows."
    - "Rewrite flows stay schema-bound and grounded; this phase must not relax factuality guardrails."
    - "Any new context-local action taxonomy must be explicitly mapped to current runtime vocabulary (`WorkflowMode`, durable `AgentActionType`, and current `resume_review` handling) instead of silently replacing it."
  artifacts:
    - path: "src/lib/agent/context/index.ts"
      provides: "Typed layered context composition entry point"
      contains: "buildAgentContext"
    - path: "src/lib/agent/context/debug/describe-context-composition.ts"
      provides: "Inspectable context debug metadata"
      contains: "describeContextComposition"
    - path: "src/lib/agent/context-builder.ts"
      provides: "Compatibility wrapper over the new context engine"
      contains: "buildSystemPrompt"
    - path: "src/lib/agent/agent-loop.ts"
      provides: "Action-aware prompt composition seam"
      contains: "buildSystemPrompt(session, userMessage)"
  key_links:
    - from: "src/lib/agent/context-builder.ts"
      to: "src/lib/agent/context/index.ts"
      via: "compatibility wrapper delegates to the new layered engine"
      pattern: "buildAgentContext("
    - from: "src/lib/agent/agent-loop.ts"
      to: "src/lib/agent/context-builder.ts"
      via: "the active chat loop now passes `userMessage` for action-aware composition"
      pattern: "buildSystemPrompt(session, userMessage)"
    - from: "src/lib/agent/tools/rewrite-resume-full.ts"
      to: "src/lib/agent/context/"
      via: "rewrite helpers reuse shared base/workflow/source guardrails instead of drifting"
      pattern: "build"
---

<objective>
Refactor the agent context architecture into explicit layered builders for base rules, workflow rules, action contracts, source selection, output contracts, and inspectable debug metadata without widening the brownfield runtime scope.
</objective>

<execution_context>
@C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fabio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-refactor-agent-context-into-layered-workflow-action-and-source-builders/41-CONTEXT.md
@.planning/phases/41-refactor-agent-context-into-layered-workflow-action-and-source-builders/41-RESEARCH.md
@.planning/phases/41-refactor-agent-context-into-layered-workflow-action-and-source-builders/41-VALIDATION.md
@AGENTS.md
@CLAUDE.md
@src/lib/agent/context-builder.ts
@src/lib/agent/agent-loop.ts
@src/lib/agent/action-classification.ts
@src/lib/jobs/source-of-truth.ts
@src/lib/agent/tools/rewrite-resume-full.ts
@src/types/agent.ts
@src/types/jobs.ts
</context>

<threat_model>
<trust_boundaries>
- Agent loop -> system prompt builder
- Deterministic rewrite pipeline -> shared context guardrails
- Source-of-truth helpers -> context source selection and debug metadata
</trust_boundaries>
<stride_register>
<threat>
  <threat_id>T-41-01</threat_id>
  <category>Tampering</category>
  <component>Prompt composition</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Preserve factuality and prompt-injection guardrails as a shared base layer and prove them in regression tests.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-41-02</threat_id>
  <category>Information Disclosure</category>
  <component>Context debug metadata</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep debug metadata structural and inspectable without dumping raw prompt bodies or sensitive source content.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-41-03</threat_id>
  <category>Repudiation</category>
  <component>Source-of-truth selection</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Record which source blocks and snapshot source were selected so future debugging can explain prompt behavior deterministically.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the typed layered context module and compatibility wrapper</name>
  <files>src/lib/agent/context/**, src/lib/agent/context-builder.ts, src/lib/agent/context-builder.test.ts</files>
  <read_first>src/lib/agent/context-builder.ts, src/types/agent.ts, src/types/jobs.ts, src/lib/jobs/source-of-truth.ts, .planning/phases/41-refactor-agent-context-into-layered-workflow-action-and-source-builders/41-RESEARCH.md</read_first>
  <action>Create `src/lib/agent/context/` with explicit base, workflow, action, source, schema, debug, and type modules plus one `buildAgentContext(...)` entry point. Keep the public `buildSystemPrompt(...)` export in `src/lib/agent/context-builder.ts`, but make it delegate to the new engine. The new result must include inspectable structural metadata such as selected workflow, selected action, selected snapshot source, included source block keys, and schema flags. Preserve the current safety boundaries around user-provided content. If the new context engine introduces context-local action names, add an explicit mapper from current runtime vocabulary (`resume_review`, `ats_enhancement`, `job_targeting`, durable action types) rather than silently renaming active contracts.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/context-builder.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/agent/context/index.ts` contains `buildAgentContext`
    - `src/lib/agent/context/debug/describe-context-composition.ts` contains `describeContextComposition`
    - `src/lib/agent/context-builder.ts` contains `buildAgentContext(`
    - `src/lib/agent/context-builder.test.ts` contains assertions about context debug metadata
  </acceptance_criteria>
  <done>The repo has one typed layered context engine behind the existing prompt-builder seam.</done>
</task>

<task type="auto">
  <name>Task 2: Make the active agent loop action-aware without widening runtime scope</name>
  <files>src/lib/agent/agent-loop.ts, src/lib/agent/__tests__/streaming-prompt-regression.test.ts, src/lib/agent/context-builder.test.ts</files>
  <read_first>src/lib/agent/agent-loop.ts, src/lib/agent/__tests__/streaming-prompt-regression.test.ts, src/lib/agent/action-classification.ts</read_first>
  <action>Thread `userMessage` into the prompt-building seam so layered context composition can infer the active action contract for the current conversational turn without changing dispatch or runtime boundaries. Preserve current loop behavior, recovery paths, and caching. Update streaming prompt regression tests so the compatibility seam remains stable while the internals become action-aware.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/context-builder.test.ts src/lib/agent/__tests__/streaming-prompt-regression.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/agent/agent-loop.ts` contains `buildSystemPrompt(session, userMessage)`
    - `src/lib/agent/__tests__/streaming-prompt-regression.test.ts` remains green
    - no new route or dispatch surface is introduced
  </acceptance_criteria>
  <done>The active chat loop uses the layered engine in an action-aware way without changing the brownfield runtime architecture.</done>
</task>

<task type="auto">
  <name>Task 3: Reuse shared context guardrails in rewrite workflows and lock source-of-truth behavior</name>
  <files>src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/tools/pipeline.test.ts, src/lib/agent/tools/rewrite-section.test.ts, src/lib/agent/tools/validate-rewrite.test.ts</files>
  <read_first>src/lib/agent/tools/rewrite-resume-full.ts, src/lib/agent/tools/pipeline.test.ts, src/lib/jobs/source-of-truth.ts, src/types/agent.ts</read_first>
  <action>Move shared base/workflow/source guardrails into the new context module and reuse them from deterministic ATS and job-targeting rewrite helpers. Keep the existing section-specific rewrite behavior close to the pipeline, but stop duplicating honesty/factuality/source-of-truth guidance across files. Add or update tests so chat remains minimal, ATS/job-targeting contexts stay rich and grounded, and source selection is inspectable.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/tools/pipeline.test.ts src/lib/agent/tools/rewrite-section.test.ts src/lib/agent/tools/validate-rewrite.test.ts src/lib/agent/context-builder.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/agent/tools/rewrite-resume-full.ts` reuses shared context helpers
    - `src/lib/agent/tools/pipeline.test.ts` proves shared guardrails stay grounded
    - lightweight chat tests still prove minimal context
  </acceptance_criteria>
  <done>Shared guardrails and source-selection rules no longer drift between chat and rewrite flows.</done>
</task>

<task type="auto">
  <name>Task 4: Close the phase with focused regression proof</name>
  <files>src/lib/agent/context-builder.test.ts, src/lib/agent/__tests__/streaming-prompt-regression.test.ts, src/lib/agent/tools/pipeline.test.ts, src/lib/agent/tools/rewrite-section.test.ts, src/lib/agent/tools/validate-rewrite.test.ts</files>
  <read_first>.planning/phases/41-refactor-agent-context-into-layered-workflow-action-and-source-builders/41-VALIDATION.md</read_first>
  <action>Finish the focused regression suite so Phase 41 proves layered context composition, explicit source selection, minimal chat context, and grounded rewrite contracts together. Keep the suite fast enough for the validation quick-run command.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/context-builder.test.ts src/lib/agent/__tests__/streaming-prompt-regression.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/agent/tools/rewrite-section.test.ts src/lib/agent/tools/validate-rewrite.test.ts</verify>
  <acceptance_criteria>
    - the quick-run command exits 0
    - context tests assert on inspectable composition metadata
    - prompt regression and rewrite-path tests stay green together
  </acceptance_criteria>
  <done>Phase 41 closes with focused proof that the layered context architecture is real and brownfield-safe.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/lib/agent/context-builder.test.ts src/lib/agent/__tests__/streaming-prompt-regression.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/agent/tools/rewrite-section.test.ts src/lib/agent/tools/validate-rewrite.test.ts`
- [ ] confirm `buildSystemPrompt(...)` remains the active compatibility seam
- [ ] confirm context debug metadata stays structural and does not dump raw prompt bodies
- [ ] confirm lightweight chat remains lighter than rewrite workflows
</verification>

<success_criteria>

- All tasks completed
- Verification checks pass
- No runtime-surface widening beyond the planned context seam
- The repo ends with typed layered context composition, explicit source selection, inspectable debug metadata, and focused regression proof
</success_criteria>

<output>
After completion, create `.planning/phases/41-refactor-agent-context-into-layered-workflow-action-and-source-builders/41-01-SUMMARY.md`
</output>

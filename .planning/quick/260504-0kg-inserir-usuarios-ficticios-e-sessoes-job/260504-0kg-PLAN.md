# Quick Task 260504-0kg: Inserir usuarios ficticios e sessoes Job Targeting multi-dominio no banco

**Date:** 2026-05-04
**Mode:** quick

## Scope

Insert fictitious shadow users, profiles, and job-targeting sessions for 10 domains, with 3 fit levels per domain, using only direct database inserts/upserts.

## Tasks

1. Confirm schema
   - Files: `prisma/schema.prisma`, `src/types/cv.ts`, `src/types/agent.ts`, `src/lib/db/sessions.ts`, `src/lib/db/session-normalization.ts`, `src/lib/profile/user-profiles.ts`
   - Action: Identify real tables and fields for users, profile `cvState`, session `cvState`, `agentState`, `generatedOutput`, and current state version.
   - Verify: Use only `users`, `user_profiles`, and `sessions`; do not touch credit, billing, generation, or artifact tables.

2. Add seed script
   - File: `scripts/job-targeting/seed-shadow-users.ts`
   - Action: Build 30 fake cases with deterministic IDs and upsert them idempotently by `seedRunId` + `caseId`.
   - Verify: Script prints safe summary counts and reports zero credit reservations, resume generations, artifacts, and OpenAI calls created.

3. Execute seed only
   - Command: `npx tsx scripts/job-targeting/seed-shadow-users.ts --seed-run-id shadow-seed-multidomain-001 --count-per-domain 3`
   - Action: Run only the seed script.
   - Verify: 30 sessions exist across 10 domains, each with high/adjacent/low metadata and `workflowMode: job_targeting`.

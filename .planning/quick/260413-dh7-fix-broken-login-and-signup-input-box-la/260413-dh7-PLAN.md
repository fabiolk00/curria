# Quick Task 260413-dh7 Plan

## Goal

Fix the broken login and signup input box layout in the embedded Clerk auth forms.

## Tasks

1. Move the visible field chrome from the inner Clerk input to the shared input group wrapper in `src/components/auth/clerk-appearance.ts`.
2. Add a small regression test for the shared Clerk appearance object.
3. Verify the auth component tests still pass.

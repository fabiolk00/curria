# Quick Task Plan: Fix copy regression audit mojibake findings

## Goal

Make `npm run audit:copy-regression` pass by removing new mojibake literals from source and tests without changing product behavior.

## Tasks

1. Replace user-facing mojibake literals in UI/service files with proper Portuguese text.
2. Keep mojibake repair tests by constructing broken strings dynamically so the source audit does not flag them.
3. Re-run the copy regression audit and focused tests if needed.

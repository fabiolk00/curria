# Quick Task Summary: Fix copy regression audit mojibake findings

## Status

Validated.

## Changes

- Removed new mojibake literals from user-facing resume and file-generation copy.
- Reworked review warning mojibake repair patterns so broken strings are generated at runtime instead of stored as mojibake source text.
- Kept the mojibake regression test by constructing the broken input with Unicode escapes.
- Updated test fixtures flagged by PT-BR copy review to use accented Portuguese.

## Verification

- `npm run audit:copy-regression`
- `npm run test -- src/lib/agent/highlight/override-review-highlights.test.ts src/lib/agent/job-targeting/core-requirement-coverage.test.ts src/components/resume/resume-comparison-view.test.tsx`
- `npm run typecheck`

## Notes

- Existing `.codex/config.toml` local changes were left untouched.
- `.planning/ptbr-copy-audit.tmp.md` was regenerated while diagnosing the audit and was not part of the source fix.

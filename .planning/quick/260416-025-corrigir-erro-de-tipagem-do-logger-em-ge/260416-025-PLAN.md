# Quick Task 260416-025 Plan

## Goal

Fix the compile-time logger typing error introduced in the signed-URL fallback path of `generate-file.ts`.

## Tasks

1. Correct the structured log payload in the signed-URL fallback so serialized error fields are passed in the shape expected by `logWarn`.
2. Run focused validation around the signed-URL fallback path to confirm the fix and preserve behavior.

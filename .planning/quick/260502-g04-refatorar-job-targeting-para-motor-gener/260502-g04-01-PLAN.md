---
phase: quick-260502-g04-refatorar-job-targeting-para-motor-gener
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/job-targeting/catalog/catalog-types.ts
  - src/lib/agent/job-targeting/catalog/catalog-validator.ts
  - src/lib/agent/job-targeting/catalog/catalog-loader.ts
  - src/lib/agent/job-targeting/catalog/generic-taxonomy.json
  - src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts
autonomous: true
requirements:
  - QUICK-260502-G04
user_setup: []
must_haves:
  truths:
    - "Catalog packs validate as versioned data before runtime matching consumes them."
    - "Catalog records that affect matching require goldenCaseIds."
  artifacts:
    - path: "src/lib/agent/job-targeting/catalog/catalog-types.ts"
      provides: "Shared catalog contracts."
    - path: "src/lib/agent/job-targeting/catalog/catalog-validator.ts"
      provides: "Zod validation for versioned catalog packs and required goldenCaseIds."
    - path: "src/lib/agent/job-targeting/catalog/catalog-loader.ts"
      provides: "Validated loader for generic taxonomy and domain packs."
      exports: ["loadJobTargetingCatalog"]
    - path: "src/lib/agent/job-targeting/catalog/generic-taxonomy.json"
      provides: "Generic requirement kinds and score dimensions only."
  key_links:
    - from: "src/lib/agent/job-targeting/catalog/catalog-loader.ts"
      to: "src/lib/agent/job-targeting/catalog/catalog-validator.ts"
      via: "validated load before export"
      pattern: "validate.*Catalog"
---

<objective>
Create the catalog contract foundation for the generic Job Compatibility engine.

Purpose: implement D-03 while keeping D-02 intact: the TypeScript contract is generic and domain-specific examples belong in later catalog JSON, fixtures, and tests.
Output: catalog types, validator, loader, generic taxonomy, and validator tests.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-CONTEXT.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-RESEARCH.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-VALIDATION.md
@AGENTS.md
@CLAUDE.md

<interfaces>
Use exactly these catalog filenames:

```text
src/lib/agent/job-targeting/catalog/catalog-types.ts
src/lib/agent/job-targeting/catalog/catalog-loader.ts
src/lib/agent/job-targeting/catalog/catalog-validator.ts
src/lib/agent/job-targeting/catalog/generic-taxonomy.json
src/lib/agent/job-targeting/catalog/domain-packs/*.json
```

The loader export consumed by later plans is `loadJobTargetingCatalog`.
</interfaces>
</context>

<decision_coverage>
| Decision | Coverage | Tasks |
|---|---|---|
| D-02 Generic core without domain hardcodes | Full | Task 1 |
| D-03 Catalog contract and goldenCaseIds | Full | Tasks 1, 2 |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define catalog types, validator, and generic taxonomy</name>
  <files>src/lib/agent/job-targeting/catalog/catalog-types.ts, src/lib/agent/job-targeting/catalog/catalog-validator.ts, src/lib/agent/job-targeting/catalog/generic-taxonomy.json, src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts</files>
  <behavior>
    - Validator accepts packs only when `id`, `version`, `domain`, `terms`, `categories`, `antiEquivalences`, and `goldenCaseIds` are present.
    - Validator rejects any catalog term, alias, category relationship, or anti-equivalence without non-empty `goldenCaseIds`.
    - `generic-taxonomy.json` contains generic requirement kinds and section weights only.
  </behavior>
  <action>Write tests first, then create zod-backed catalog contracts per D-03. Keep fields generic: term ids, aliases, categories, adjacent categories, anti-equivalences, requirement kinds, score dimensions, versions, and audit metadata. Do not include technology, vendor, stack, company, industry, or segment examples in TypeScript or `generic-taxonomy.json` per D-02.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts -x</automated>
  </verify>
  <done>Catalog schemas compile, generic taxonomy validates, and invalid missing-`goldenCaseIds` records fail closed with useful errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validated catalog loader</name>
  <files>src/lib/agent/job-targeting/catalog/catalog-loader.ts, src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts</files>
  <behavior>
    - Loader returns validated generic taxonomy and validated domain packs.
    - Loader exposes catalog ids and versions for downstream audit metadata.
    - Loader does not swallow validation failures.
  </behavior>
  <action>Implement `loadJobTargetingCatalog` and any narrowly named helper exports needed by later plans. It must read the requested catalog paths, validate through `catalog-validator.ts`, and return normalized records with version metadata. Do not create alternate catalog module names or alternate pack directories.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts -x</automated>
  </verify>
  <done>`loadJobTargetingCatalog` is available for downstream plans and returns only validated catalog data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository catalog JSON -> runtime matching | Versioned data controls compatibility decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260502-G04-01-01 | Tampering | `catalog-validator.ts` | mitigate | Enforce zod validation and required `goldenCaseIds` before data reaches the matcher. |
| T-260502-G04-01-02 | Repudiation | `catalog-loader.ts` | mitigate | Return ids and versions so assessments can persist audit metadata. |
</threat_model>

<verification>
```bash
npx vitest run src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts -x
```
</verification>

<success_criteria>
- Catalog contracts, validator, loader, generic taxonomy, and validator tests exist at the requested paths.
- Generic taxonomy contains no domain examples.
- Later plans can consume `loadJobTargetingCatalog`.
</success_criteria>

<output>
After completion, create `.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-01-SUMMARY.md`.
</output>


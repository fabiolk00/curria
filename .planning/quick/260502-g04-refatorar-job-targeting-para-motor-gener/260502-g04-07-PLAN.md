---
phase: quick-260502-g04-refatorar-job-targeting-para-motor-gener
plan: 07
type: execute
wave: 6
depends_on:
  - 260502-g04-06
files_modified:
  - src/lib/agent/job-targeting/compatibility/legacy-adapters.ts
  - src/lib/agent/job-targeting/__tests__/legacy-adapters.test.ts
  - src/lib/agent/tools/build-targeting-plan.ts
  - src/lib/agent/tools/build-targeting-plan.test.ts
autonomous: true
requirements:
  - QUICK-260502-G04
user_setup: []
must_haves:
  truths:
    - "Legacy product-facing Job Targeting structures are adapted from JobCompatibilityAssessment."
    - "`buildTargetedRewritePlan` remains callable while assessment-aware runs do not recalculate compatibility-sensitive fields."
  artifacts:
    - path: "src/lib/agent/job-targeting/compatibility/legacy-adapters.ts"
      provides: "Adapters from JobCompatibilityAssessment to existing product-facing Job Targeting structures."
    - path: "src/lib/agent/tools/build-targeting-plan.ts"
      provides: "Assessment-aware TargetingPlan handoff while preserving existing caller compatibility."
  key_links:
    - from: "src/lib/agent/tools/build-targeting-plan.ts"
      to: "src/lib/agent/job-targeting/compatibility/legacy-adapters.ts"
      via: "assessment-derived TargetingPlan inputs"
      pattern: "Assessment"
---

<objective>
Create the legacy adapter layer and connect build-targeting-plan to assessment-derived data.

Purpose: implement D-01 and D-06 at the first brownfield seam while preserving current product contracts.
Output: legacy adapters, adapter tests, and assessment-aware `buildTargetedRewritePlan` behavior.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-CONTEXT.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-RESEARCH.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-VALIDATION.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-06-SUMMARY.md
@AGENTS.md
@CLAUDE.md
@src/types/agent.ts
@src/lib/agent/tools/build-targeting-plan.ts
</context>

<decision_coverage>
| Decision | Coverage | Tasks |
|---|---|---|
| D-01 Canonical source of truth | Full | Tasks 1, 2 |
| D-06 Deterministic score job-compat-score-v1 | Full | Tasks 1, 2 |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adapters from assessment to legacy product contracts</name>
  <files>src/lib/agent/job-targeting/compatibility/legacy-adapters.ts, src/lib/agent/job-targeting/__tests__/legacy-adapters.test.ts</files>
  <behavior>
    - Adapters derive `TargetEvidence`, `TargetingPlan`, rewrite permissions, safe emphasis, core coverage, low-fit gate data, target role positioning, `JobTargetingExplanation`, recommendations, score breakdown, and validation inputs from assessment.
    - Legacy public shapes stay stable for rewrite, validation, recoverable modal, override, comparison UI, and tests.
    - Adapter output uses `job-compat-score-v1` data for display score fields.
  </behavior>
  <action>Write adapter tests first, then implement `legacy-adapters.ts` per D-01. Preserve product-facing type shapes from `src/types/agent.ts` while changing the source of truth to `JobCompatibilityAssessment`. Include narrowly named exports such as `buildTargetingPlanFromAssessment` and `buildJobTargetingExplanationFromAssessment` for pipeline use.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/job-targeting/__tests__/legacy-adapters.test.ts src/lib/agent/job-targeting/__tests__/assessment.test.ts -x</automated>
  </verify>
  <done>Legacy adapter tests prove current product shapes are assessment-derived.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make build-targeting-plan delegate for assessment-aware calls</name>
  <files>src/lib/agent/tools/build-targeting-plan.ts, src/lib/agent/tools/build-targeting-plan.test.ts</files>
  <behavior>
    - `buildTargetedRewritePlan` remains callable for existing tests and old seams.
    - Assessment-aware calls use adapter outputs for target evidence, coverage, score, claim permissions, and low-fit data.
    - New Job Targeting runs do not independently recalculate those compatibility-sensitive fields in `build-targeting-plan.ts`.
  </behavior>
  <action>Update `build-targeting-plan.ts` to accept/use assessment-derived adapter output. Preserve current metadata behavior needed by the UI, but bypass divergent compatibility classifiers, score calculators, and low-fit logic whenever assessment data is present per D-06.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/job-targeting/__tests__/legacy-adapters.test.ts src/lib/agent/tools/build-targeting-plan.test.ts -x</automated>
  </verify>
  <done>`buildTargetedRewritePlan` works for legacy callers and delegates compatibility-sensitive values for assessment-aware callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| assessment -> legacy product contracts | Server-side assessment data becomes client-visible diagnostics and rewrite constraints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260502-G04-07-01 | Tampering | `legacy-adapters.ts` | mitigate | Derive all compatibility-sensitive legacy fields from one assessment input. |
| T-260502-G04-07-02 | Repudiation | score adapter | mitigate | Preserve score version and dimension metadata in display score output. |
</threat_model>

<verification>
```bash
npx vitest run src/lib/agent/job-targeting/__tests__/legacy-adapters.test.ts src/lib/agent/tools/build-targeting-plan.test.ts -x
```
</verification>

<success_criteria>
- Legacy product contracts remain stable.
- Adapter outputs come from `JobCompatibilityAssessment`.
- `build-targeting-plan.ts` no longer owns a parallel compatibility decision for assessment-aware runs.
</success_criteria>

<output>
After completion, create `.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-07-SUMMARY.md`.
</output>


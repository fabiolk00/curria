---
phase: quick-260502-g04-refatorar-job-targeting-para-motor-gener
plan: 03
subsystem: job-targeting-catalog-wrappers
status: completed
completed: 2026-05-02
requirements:
  - QUICK-260502-G04
key-files:
  created:
    - src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts
  modified:
    - src/lib/agent/job-targeting/domain-equivalents.ts
    - src/lib/agent/job-targeting/skill-adjacency.ts
decisions:
  - Legacy compatibility imports remain synchronous while reading validated catalog JSON as their concrete data source.
  - Named catalog terms require same-term evidence; category relationships stay conservative so legacy safety tests do not promote partial aliases or adjacent tools into direct claims.
  - Runtime hardcode guard scans compatibility core files plus the legacy compatibility wrappers and excludes catalog JSON, golden fixtures, and test files.
---

# Quick Task 260502-g04 Plan 03 Summary

Catalog-backed legacy compatibility wrappers plus a runtime hardcode guard.

## Work Completed

- Replaced hardcoded equivalent-term rules in `domain-equivalents.ts` with a validated catalog index built from the generic taxonomy and domain pack JSON.
- Preserved existing legacy exports: `RequirementSubSignal`, `DomainEquivalentRule`, `DomainResumeEvidenceEntry`, `DomainEquivalentMatch`, `DOMAIN_EQUIVALENT_RULES`, `decomposeRequirementSignal`, and `findDomainEquivalentMatch`.
- Replaced hardcoded skill adjacency rules in `skill-adjacency.ts` with catalog-derived adjacency rules while preserving `SKILL_ADJACENCY_RULES` and `findSkillAdjacencyRule`.
- Added `hardcode-guard.test.ts` to fail if fixture-specific concrete examples appear in runtime compatibility sources outside allowed data/test paths.

## Deviations from Plan

- The plan command with `-x` could not execute because this repo's installed Vitest rejects `-x` as an unknown option. The same focused verification was run without `-x` as allowed by the execution rules.
- While checking legacy callers, catalog category matches were initially too broad for partial aliases. The wrappers were tightened so named requirements only become direct equivalents through same-term catalog evidence; category relationships remain conservative.
- No commits or STATE/ROADMAP updates were made because the execution rules explicitly requested direct edits only.

## Verification

- `npx vitest run src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts src/lib/agent/job-targeting/__tests__/golden-cases.test.ts -x` - rejected before test execution with `CACError: Unknown option '-x'`.
- `npx vitest run src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts src/lib/agent/job-targeting/__tests__/golden-cases.test.ts` - passed, 3 files / 13 tests.
- `npx vitest run src/lib/agent/job-targeting/target-recommendations.test.ts src/lib/agent/job-targeting/evidence-classifier.test.ts src/lib/agent/job-targeting/core-requirement-coverage.test.ts` - passed, 3 files / 40 tests.
- `npm run typecheck` - passed.

## Known Stubs

None.

## Threat Flags

None beyond the plan threat model. The wrappers now read local validated catalog data under the planned legacy-wrapper-to-catalog boundary and introduce no new network, auth, schema, or production write surface.

## Self-Check: PASSED

- Confirmed both modified wrapper files, the new hardcode guard test, and this summary exist.
- Confirmed `.planning/STATE.md` and `.planning/ROADMAP.md` were not modified.
- Stub scan found only local cache/accumulator initializers, not placeholder runtime data.
- Confirmed forbidden concrete runtime examples are absent from the two wrapper source files.

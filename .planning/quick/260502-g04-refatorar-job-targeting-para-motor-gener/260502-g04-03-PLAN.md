---
phase: quick-260502-g04-refatorar-job-targeting-para-motor-gener
plan: 03
type: execute
wave: 3
depends_on:
  - 260502-g04-02
  - 260502-g04-02.1
files_modified:
  - src/lib/agent/job-targeting/domain-equivalents.ts
  - src/lib/agent/job-targeting/skill-adjacency.ts
  - src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts
autonomous: true
requirements:
  - QUICK-260502-G04
user_setup: []
must_haves:
  truths:
    - "Legacy imports keep working while their concrete compatibility data comes from the validated catalog."
    - "Runtime compatibility source fails tests if fixture-specific tool, vendor, stack, company, industry, or segment examples appear outside allowed data/test paths."
  artifacts:
    - path: "src/lib/agent/job-targeting/domain-equivalents.ts"
      provides: "Catalog-backed legacy wrapper for existing equivalent-term callers."
    - path: "src/lib/agent/job-targeting/skill-adjacency.ts"
      provides: "Catalog-backed legacy wrapper for adjacency callers."
    - path: "src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts"
      provides: "Architecture guard excluding catalog JSON, fixtures, golden cases, and tests."
  key_links:
    - from: "src/lib/agent/job-targeting/domain-equivalents.ts"
      to: "src/lib/agent/job-targeting/catalog/catalog-loader.ts"
      via: "catalog-backed legacy wrapper"
      pattern: "loadJobTargetingCatalog"
    - from: "src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts"
      to: "src/lib/agent/job-targeting/compatibility/**/*.ts"
      via: "guard scans compatibility core before Wave 4"
      pattern: "compatibility"
---

<objective>
Move existing hardcoded compatibility data behind catalog-backed wrappers and install the architecture guard.

Purpose: enforce D-02 and prepare D-07 before the new matcher is implemented.
Output: catalog-backed `domain-equivalents.ts`, catalog-backed `skill-adjacency.ts`, and hardcode guard tests.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-CONTEXT.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-RESEARCH.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-VALIDATION.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-02-SUMMARY.md
@AGENTS.md
@CLAUDE.md
@src/lib/agent/job-targeting/domain-equivalents.ts
@src/lib/agent/job-targeting/skill-adjacency.ts
</context>

<decision_coverage>
| Decision | Coverage | Tasks |
|---|---|---|
| D-02 Generic core without domain hardcodes | Full | Tasks 1, 2 |
| D-07 Golden cases and tests | Full | Task 2 |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert legacy equivalence and adjacency wrappers to catalog data</name>
  <files>src/lib/agent/job-targeting/domain-equivalents.ts, src/lib/agent/job-targeting/skill-adjacency.ts</files>
  <behavior>
    - Existing exported function names used by current callers remain available.
    - Concrete equivalence and adjacency data is read from the validated catalog.
    - No concrete domain examples remain in these runtime wrapper files.
  </behavior>
  <action>Refactor `domain-equivalents.ts` and `skill-adjacency.ts` to delegate to `loadJobTargetingCatalog` while preserving current imports used elsewhere in the codebase. Keep these files as compatibility wrappers, not alternate sources of truth. Any concrete term examples must live in the Plan 02 catalog JSON or tests per D-02.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts src/lib/agent/job-targeting/__tests__/golden-cases.test.ts -x</automated>
  </verify>
  <done>Current callers can still import legacy wrappers, and the wrappers are catalog-backed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add runtime hardcode guard</name>
  <files>src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts</files>
  <behavior>
    - Guard fails when runtime TypeScript contains fixture-specific terms outside allowed paths.
    - Allowed paths are catalog JSON, `__fixtures__/golden-cases/*.json`, and `__tests__/*.test.ts`.
    - Guard scans compatibility core paths planned for later waves plus legacy runtime wrappers that can influence compatibility.
  </behavior>
  <action>Create `hardcode-guard.test.ts` with the concrete examples listed in CONTEXT.md. Scan `src/lib/agent/job-targeting/compatibility/**/*.ts`, `domain-equivalents.ts`, `skill-adjacency.ts`, and other legacy runtime files that can affect matching, scoring, or claim policy. Keep the allowlist explicit and path-based so catalog and fixtures remain permitted.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts src/lib/agent/job-targeting/__tests__/golden-cases.test.ts -x</automated>
  </verify>
  <done>The guard is active before Wave 4 compatibility core work and passes with concrete examples only in data/test paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| legacy wrappers -> new catalog | Old imports still influence runtime behavior but now read catalog-backed data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260502-G04-03-01 | Tampering | legacy wrappers | mitigate | Preserve wrappers as catalog readers only, with guard coverage for concrete runtime examples. |
| T-260502-G04-03-02 | Repudiation | hardcode guard | mitigate | Test records exact disallowed terms and allowed paths. |
</threat_model>

<verification>
```bash
npx vitest run src/lib/agent/job-targeting/__tests__/hardcode-guard.test.ts src/lib/agent/job-targeting/__tests__/catalog-validator.test.ts src/lib/agent/job-targeting/__tests__/golden-cases.test.ts -x
```
</verification>

<success_criteria>
- Legacy wrappers are catalog-backed and keep existing exports.
- Hardcode guard covers new compatibility core paths before those files are implemented.
- D-02 is enforceable by automated test.
</success_criteria>

<output>
After completion, create `.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-03-SUMMARY.md`.
</output>

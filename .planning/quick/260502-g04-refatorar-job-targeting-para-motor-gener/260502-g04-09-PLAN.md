---
phase: quick-260502-g04-refatorar-job-targeting-para-motor-gener
plan: 09
type: execute
wave: 8
depends_on:
  - 260502-g04-08
files_modified:
  - src/lib/agent/job-targeting-pipeline.ts
  - src/lib/agent/tools/pipeline.test.ts
  - src/lib/routes/session-comparison/decision.ts
  - src/lib/routes/session-comparison/decision.test.ts
  - src/app/api/session/[id]/job-targeting/override/route.test.ts
autonomous: true
requirements:
  - QUICK-260502-G04
user_setup: []
must_haves:
  truths:
    - "runJobTargetingPipeline persists agentState.jobCompatibilityAssessment after gap analysis."
    - "Pipeline consumers keep existing targetingPlan, jobTargetingExplanation, validation, low-fit, override, comparison, artifact, and download behavior through adapters."
    - "Displayed score and low-fit decisions use assessment-derived data."
    - "Safe structured logs include counts and versions, not full resume text or full job description."
  artifacts:
    - path: "src/lib/agent/job-targeting-pipeline.ts"
      provides: "Pipeline integration and assessment persistence."
    - path: "src/lib/routes/session-comparison/decision.ts"
      provides: "Assessment-derived score breakdown for client display."
    - path: "src/app/api/session/[id]/job-targeting/override/route.test.ts"
      provides: "Override regression coverage preserving accepted low-fit behavior."
  key_links:
    - from: "src/lib/agent/job-targeting-pipeline.ts"
      to: "src/lib/agent/job-targeting/compatibility/assessment.ts"
      via: "evaluateJobCompatibility after gap analysis"
      pattern: "evaluateJobCompatibility"
    - from: "src/lib/agent/job-targeting-pipeline.ts"
      to: "src/lib/agent/job-targeting/compatibility/legacy-adapters.ts"
      via: "assessment-derived targetingPlan, explanation, gate, validation context"
      pattern: "build.*FromAssessment"
    - from: "src/lib/routes/session-comparison/decision.ts"
      to: "src/lib/agent/job-targeting/compatibility/legacy-adapters.ts"
      via: "assessment-derived score breakdown"
      pattern: "Assessment"
---

<objective>
Wire the canonical assessment into pipeline persistence, comparison display, override behavior, and final regression proof.

Purpose: implement D-08 and complete the brownfield integration without changing the existing user-facing funnel.
Output: pipeline assessment integration, safe logs, comparison score integration, override regression coverage, focused suite proof, and typecheck.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-CONTEXT.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-RESEARCH.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-VALIDATION.md
@.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-08-SUMMARY.md
@AGENTS.md
@CLAUDE.md
@src/types/agent.ts
@src/lib/agent/job-targeting-pipeline.ts
@src/lib/routes/session-comparison/decision.ts
@src/app/api/session/[id]/job-targeting/override/route.ts

<interfaces>
Required integration shape:

```ts
const jobCompatibilityAssessment = await evaluateJobCompatibility({
  cvState: session.cvState,
  targetJobDescription,
  gapAnalysis: gapAnalysisResult,
  userId,
  sessionId,
})

const targetingPlan = buildTargetingPlanFromAssessment(jobCompatibilityAssessment)
const jobTargetingExplanation = buildJobTargetingExplanationFromAssessment(jobCompatibilityAssessment)
```
</interfaces>
</context>

<decision_coverage>
| Decision | Coverage | Tasks |
|---|---|---|
| D-01 Canonical source of truth | Full | Tasks 1, 2 |
| D-05 Structured claim safety | Full | Task 1 |
| D-06 Deterministic score job-compat-score-v1 | Full | Task 2 |
| D-07 Golden cases and tests | Full | Task 3 |
| D-08 Pipeline integration and safe logs | Full | Tasks 1, 3 |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist assessment in runJobTargetingPipeline with safe logs</name>
  <files>src/lib/agent/job-targeting-pipeline.ts, src/lib/agent/tools/pipeline.test.ts</files>
  <behavior>
    - Pipeline calls `evaluateJobCompatibility` after gap analysis with `cvState`, `targetJobDescription`, `gapAnalysis`, `userId`, and `sessionId`.
    - `agentState.jobCompatibilityAssessment` is persisted with assessment-derived `targetingPlan` and `jobTargetingExplanation`.
    - Logs cover started, catalog loaded, requirements extracted, evidence classified, claim policy built, score calculated, and completed using counts and versions only.
  </behavior>
  <action>Replace the enriched compatibility seam with `evaluateJobCompatibility` plus adapter outputs per D-08. Keep current target role and base plan metadata only where needed for UI continuity. Extend pipeline tests for assessment persistence, assessment-derived score/gate, final artifact preservation for valid jobs, validation blocks preserving previous optimized state, and safe structured logs.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/tools/pipeline.test.ts -x</automated>
  </verify>
  <done>Pipeline persists assessment and adapted legacy fields, and safe log assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use assessment-derived score in comparison and preserve override behavior</name>
  <files>src/lib/routes/session-comparison/decision.ts, src/lib/routes/session-comparison/decision.test.ts, src/app/api/session/[id]/job-targeting/override/route.test.ts</files>
  <behavior>
    - Comparison score backfill uses assessment-derived score data when available.
    - Comparison does not call an independent plan score calculator for new Job Targeting runs.
    - Accepted low-fit override suppresses only the blocking action and preserves assessment/audit persistence.
  </behavior>
  <action>Update `session-comparison/decision.ts` to prefer assessment-derived score breakdowns and avoid a parallel score path for new runs. Extend comparison and override tests for assessment-derived score, accepted low-fit override, skip flags, and persisted audit metadata.</action>
  <verify>
    <automated>npx vitest run src/lib/routes/session-comparison/decision.test.ts src/app/api/session/[id]/job-targeting/override/route.test.ts -x</automated>
  </verify>
  <done>Comparison and override regressions prove score and blocking behavior remain assessment-consistent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run final focused proof</name>
  <files>src/lib/agent/job-targeting-pipeline.ts, src/lib/agent/tools/pipeline.test.ts, src/lib/routes/session-comparison/decision.ts, src/lib/routes/session-comparison/decision.test.ts, src/app/api/session/[id]/job-targeting/override/route.test.ts</files>
  <behavior>
    - Full focused suite passes across catalog, compatibility core, adapters, wrappers, pipeline, comparison, and override.
    - Typecheck passes after all integration changes.
  </behavior>
  <action>Run the final regression commands. Fix only failures caused by this integration scope, preserving RESEARCH.md and VALIDATION.md path contracts.</action>
  <verify>
    <automated>npx vitest run src/lib/agent/job-targeting/__tests__ src/lib/agent/tools/build-targeting-plan.test.ts src/lib/agent/tools/validate-rewrite.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/routes/session-comparison/decision.test.ts src/app/api/session/[id]/job-targeting/override/route.test.ts -x</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Focused Vitest suite and typecheck pass, proving the complete quick task outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| agentState persistence -> API/UI consumers | Operational assessment data is stored server-side and adapted into client-visible diagnostics. |
| pipeline logs -> observability systems | Logs may leave process boundaries and must not contain resume/job content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260502-G04-09-01 | Information Disclosure | pipeline structured logs | mitigate | Log only ids, counts, reason enums, score version, catalog versions, and timing. |
| T-260502-G04-09-02 | Repudiation | persisted assessment | mitigate | Persist audit metadata with score version, catalog versions, requirement counts, and match metadata. |
| T-260502-G04-09-03 | Elevation of Privilege | override route | accept | No new route or auth surface is introduced; existing session ownership and override auth tests remain in the focused suite. |
</threat_model>

<verification>
```bash
npx vitest run src/lib/agent/job-targeting/__tests__ src/lib/agent/tools/build-targeting-plan.test.ts src/lib/agent/tools/validate-rewrite.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/routes/session-comparison/decision.test.ts src/app/api/session/[id]/job-targeting/override/route.test.ts -x
npm run typecheck
```
</verification>

<success_criteria>
- `runJobTargetingPipeline` persists `agentState.jobCompatibilityAssessment`.
- Current legacy product behavior remains available through adapters: generated resume, saved versions, validation modal, low-fit override, score card, comparison response, and artifact download.
- New Job Targeting runs use assessment-derived score, gate, claim policy, validation, and comparison display.
- Focused Vitest suite and `npm run typecheck` pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260502-g04-refatorar-job-targeting-para-motor-gener/260502-g04-09-SUMMARY.md`.
</output>


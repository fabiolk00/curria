# Quick Task 260412-o24 Summary

Implemented the first coherent slice of resume-generation billing:

- Added `ResumeGenerationType`, `resume_generations`, and `credit_consumptions` plus the `consume_credit_for_generation` SQL function.
- Introduced billable generation orchestration so `generate_file` now creates auditable generation records, supports idempotency keys, and consumes credits only after successful artifact creation.
- Switched `/api/agent` session creation to be free and removed the old session message-cap billing behavior.
- Updated dashboard generation calls and tests so credits are no longer decremented on new session creation.

## Verification

- `npm run typecheck`
- `npx vitest run src/app/api/session/[id]/generate/route.test.ts src/app/api/agent/route.test.ts src/components/dashboard/resume-workspace.test.tsx`

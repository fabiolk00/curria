# Quick Task 260504-1fi Plan

## Goal

Run a guarded 10-case seeded multi-domain rewrite validation with real LLM calls, without enabling source-of-truth, artifact generation, or internal credit consumption.

## Scope

- Confirm the fixed seeded input JSONL exists.
- Run `scripts/job-targeting/run-shadow-batch.ts` with rewrite validation enabled, limit 10, concurrency 1, cache enabled, provider retry/circuit guards, and explicit cost cap.
- Generate the divergence report for the 10-case output.
- Verify no `resume_generations` or `credit_reservations` rows were created for shadow sessions.
- Report cost, provider, validation, and security counters.

## Guardrails

- Do not run more than 10 cases.
- Do not run without `--confirm-llm-cost`.
- Do not run without `--max-estimated-cost-usd 0.50`.
- Do not generate artifacts or consume internal credits.
- Do not enable source-of-truth.
- Do not commit `.local`.

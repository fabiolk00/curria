# Quick Task 260504-0sl: Exportar seed shadow multidominio e rodar compatibility sem LLM

**Date:** 2026-05-04
**Mode:** quick

## Scope

Export the 30 database-seeded shadow job-targeting sessions to JSONL, run compatibility only with the existing shadow runner, and generate a local divergence report.

## Tasks

1. Add seeded export script
   - File: `scripts/job-targeting/export-seeded-shadow-cases.ts`
   - Action: Read `sessions` filtered by `agent_state.source = shadow_seed` and `agent_state.seedRunId`, then write runner-compatible JSONL.
   - Verify: Output contains the 30 cases, 10 domains, and anonymized/test metadata.

2. Run compatibility without LLM
   - Commands:
     - `npx tsx scripts/job-targeting/export-seeded-shadow-cases.ts --seed-run-id shadow-seed-multidomain-001 --output .local/job-targeting-shadow-cases/seeded-30-diverse-cases.jsonl`
     - `npx tsx scripts/job-targeting/run-shadow-batch.ts --input .local/job-targeting-shadow-cases/seeded-30-diverse-cases.jsonl --output .local/job-targeting-shadow-results/seeded-30-diverse-compatibility.jsonl --limit 30 --concurrency 2 --persist`
     - `npx tsx scripts/job-targeting/analyze-shadow-divergence.ts .local/job-targeting-shadow-results/seeded-30-diverse-compatibility.jsonl --output-dir .local/job-targeting-shadow-results/seeded-30-diverse-compatibility`
   - Verify: No `--include-rewrite-validation`, no `--use-real-gap-analysis`, OpenAI calls and estimated cost remain zero.

3. Summarize results
   - Action: Read the exported JSONL and report JSON for counts and issue summary.
   - Verify: Final response includes requested metrics.

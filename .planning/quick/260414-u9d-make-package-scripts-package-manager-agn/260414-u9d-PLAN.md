# Quick Task 260414-u9d: Make package scripts package-manager-agnostic so lint works when CI invokes npm scripts without pnpm on PATH

## Goal

Ensure repo scripts run correctly from `npm run ...` in CI by removing hard dependencies on the `pnpm` binary inside `package.json` scripts.

## Tasks

1. Replace recursive `pnpm` script chaining with direct command execution in `package.json`.
2. Verify the lint command through `npm run lint`, matching the CI workflow.
3. Record the quick-task artifacts and state update.

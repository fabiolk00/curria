---
task_id: 260428-upq
plan_id: 260428-upq-01
phase: 260428-upq
plan: "01"
mode: quick-full
type: execute
title: Smart Generation Canonical Route And Start Lock
autonomous: true
wave: 1
depends_on: []
files_modified:
  - src/components/resume/user-data-page.tsx
  - src/components/resume/user-data-page.test.tsx
  - src/app/api/profile/smart-generation/route.ts
  - src/app/api/profile/smart-generation/route.test.ts
  - src/lib/routes/smart-generation/decision.ts
  - src/lib/routes/smart-generation/session-bootstrap.ts
  - src/lib/agent/job-targeting-start-lock.ts
  - src/lib/agent/job-targeting-start-lock.test.ts
  - src/app/api/profile/ats-enhancement/route.ts
  - src/app/api/profile/ats-enhancement/route.test.ts
  - tests/e2e/profile-setup.spec.ts
locked_decisions:
  - id: U-03
    decision: "Smart Generation is the canonical guided resume generation entrypoint for ATS and job targeting."
resolved_research_decisions:
  - id: R-02
    decision: "The old /api/profile/ats-enhancement route stays as a compatibility wrapper around Smart Generation."
must_haves:
  truths:
    - "ATS and job-target guided starts both submit through POST /api/profile/smart-generation."
    - "Duplicate ATS and job-target guided starts are idempotent server-side."
    - "Start-lock logs contain ids and hashes only, never raw resume JSON/text or raw target job text."
    - "The old ATS route is compatibility-only and no normal product caller posts to it."
  artifacts:
    - path: "src/lib/agent/job-targeting-start-lock.ts"
      provides: "workflow-aware safe-hash start lock with preserved job-targeting compatibility exports"
    - path: "src/lib/routes/smart-generation/decision.ts"
      provides: "single orchestration path for ATS and job-targeting guided generation"
    - path: "src/app/api/profile/ats-enhancement/route.ts"
      provides: "compatibility wrapper that delegates to Smart Generation"
    - path: "src/components/resume/user-data-page.tsx"
      provides: "guided generation UI posting both modes to Smart Generation"
  key_links:
    - from: "src/components/resume/user-data-page.tsx"
      to: "src/app/api/profile/smart-generation/route.ts"
      via: "fetch('/api/profile/smart-generation') for ATS and job-target modes"
    - from: "src/lib/routes/smart-generation/decision.ts"
      to: "src/lib/agent/job-targeting-start-lock.ts"
      via: "workflow-aware lock acquire/mark running/mark completed/mark failed"
    - from: "src/app/api/profile/ats-enhancement/route.ts"
      to: "src/app/api/profile/smart-generation/route.ts"
      via: "compatibility delegation, not duplicated ATS orchestration"
---

<objective>
Make `/api/profile/smart-generation` the canonical guided generation start for ATS enhancement and job targeting.

Purpose: remove the split frontend/API start path while preserving backwards compatibility for any caller still using the old ATS endpoint.
Output: workflow-aware start locks, Smart Generation orchestration for both guided modes, an old ATS compatibility wrapper, and tests proving no raw PII/job text enters lock logs.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-RESEARCH.md
@src/components/resume/user-data-page.tsx
@src/app/api/profile/smart-generation/route.ts
@src/lib/routes/smart-generation/decision.ts
@src/lib/routes/smart-generation/session-bootstrap.ts
@src/lib/agent/job-targeting-start-lock.ts
@src/app/api/profile/ats-enhancement/route.ts

<decisions>
- Implement U-03 exactly: Smart Generation is canonical for ATS and job targeting.
- Implement R-02 exactly: `/api/profile/ats-enhancement` remains temporarily as a wrapper, not as a duplicate orchestration route.
- Preserve the existing job-targeting duplicate response shape: `already_running` and `already_completed` with message/session behavior.
- Do not log raw `cvState`, raw resume text, request bodies, or raw `targetJobDescription` from the start-lock path.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize the durable start lock without leaking raw inputs</name>
  <files>src/lib/agent/job-targeting-start-lock.ts, src/lib/agent/job-targeting-start-lock.test.ts</files>
  <behavior>
    - `buildSmartGenerationStartLockFingerprint` produces stable hashes for ATS and job-targeting workflows.
    - Job-targeting compatibility exports keep their current key prefix and duplicate semantics.
    - ATS uses a distinct key prefix and CV hash; it does not require or include target-job text.
    - Lock logging includes user/session ids, backend, statuses, and hashes only.
    - Tests spy on `logInfo`, `logWarn`, and `logError` and fail if raw sample resume text, raw CV JSON fields, or raw target job text appears in logged arguments.
  </behavior>
  <action>
    Refactor `src/lib/agent/job-targeting-start-lock.ts` in place into a workflow-aware Smart Generation lock while preserving the existing job-targeting exports as compatibility wrappers. Add exports named `buildSmartGenerationStartLockFingerprint`, `tryAcquireSmartGenerationStartLockDurable`, `markSmartGenerationStartLockRunningSessionDurable`, `markSmartGenerationStartLockCompletedDurable`, and `markSmartGenerationStartLockFailedDurable`. Use the existing normalized CV hash. For `job_targeting`, preserve the current key prefix and target-job hash behavior. For `ats_enhancement`, use a distinct key prefix such as `ats-enhancement-start` and omit target text entirely. Keep Redis/memory fallback semantics and production fail-closed behavior.

    Add privacy-focused tests with sentinel raw values such as a fake resume name, phone, company, and target-job sentence. The test must serialize all structured logger calls and assert those raw sentinel values are absent while hash/status/id fields are present.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agent/job-targeting-start-lock.test.ts</automated>
  </verify>
  <done>Workflow-aware lock helpers exist, job-targeting wrappers still work, ATS locking is available, and tests prove raw resume/job text is not logged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route both guided workflows through Smart Generation and wrap old ATS</name>
  <files>src/app/api/profile/smart-generation/route.ts, src/app/api/profile/smart-generation/route.test.ts, src/lib/routes/smart-generation/decision.ts, src/lib/routes/smart-generation/session-bootstrap.ts, src/app/api/profile/ats-enhancement/route.ts, src/app/api/profile/ats-enhancement/route.test.ts</files>
  <behavior>
    - Smart Generation acquires, marks running, marks completed, and marks failed through the workflow-aware lock for both `ats_enhancement` and `job_targeting`.
    - Smart Generation still performs auth, trusted mutation validation, readiness, quota, CV validation, pipeline execution, handoff validation, and PDF artifact dispatch in the existing order unless existing code already rejects earlier.
    - ATS duplicate starts return normalized lock statuses without creating a second session or running a second pipeline.
    - `/api/profile/ats-enhancement` delegates to Smart Generation and does not duplicate auth/readiness/session/pipeline/artifact orchestration.
  </behavior>
  <action>
    Update Smart Generation decision/session-bootstrap code to use the new workflow-aware lock around session creation and pipeline dispatch for both modes. Keep route handlers thin and preserve zod validation and structured logging. Add route tests for insufficient credits, ATS duplicate start, job-target duplicate start, lock failure mark behavior, and PDF-only response shape.

    Replace the implementation of `src/app/api/profile/ats-enhancement/route.ts` with a compatibility wrapper that forwards/delegates the request to Smart Generation. The wrapper may normalize the legacy request shape, but it must not duplicate the ATS pipeline. Update its route tests to prove delegation and cross-origin/trusted mutation behavior are preserved.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/profile/smart-generation/route.test.ts src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/job-targeting-start-lock.test.ts</automated>
  </verify>
  <done>Smart Generation is the only implementation path for guided ATS/job-target starts; the old ATS endpoint is a compatibility wrapper only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Switch profile setup callers and guard against old ATS route usage</name>
  <files>src/components/resume/user-data-page.tsx, src/components/resume/user-data-page.test.tsx, tests/e2e/profile-setup.spec.ts</files>
  <behavior>
    - ATS mode in profile setup posts sanitized CV data to `/api/profile/smart-generation`.
    - Job-target mode continues to post target job description to `/api/profile/smart-generation`.
    - Profile setup never calls `/api/profile/ats-enhancement`.
    - E2E route mocks intercept Smart Generation for ATS and fail if the old ATS route is called.
  </behavior>
  <action>
    Update `handleEnhancementSubmit` in `src/components/resume/user-data-page.tsx` so both modes POST to `/api/profile/smart-generation`; include `targetJobDescription` only for job targeting. Preserve local duplicate-click guards and existing validation/recoverable-validation UI behavior.

    Update component tests to assert ATS mode calls `/api/profile/smart-generation` and does not call `/api/profile/ats-enhancement`. Update `tests/e2e/profile-setup.spec.ts` route mocks so ATS happy path uses Smart Generation and any old ATS route call fails the test.
  </action>
  <verify>
    <automated>npx vitest run src/components/resume/user-data-page.test.tsx</automated>
    <automated>npm run test:e2e -- --project=chromium tests/e2e/profile-setup.spec.ts</automated>
    <automated>powershell -NoProfile -Command "$hits = @(rg -n --fixed-strings '/api/profile/ats-enhancement' src --glob '!src/app/api/profile/ats-enhancement/route.ts' --glob '!src/app/api/profile/ats-enhancement/route.test.ts' --glob '!**/*.test.ts' --glob '!**/*.test.tsx' --glob '!**/*.md' 2>$null); if ($hits.Count -gt 0) { $hits; exit 1 }"</automated>
  </verify>
  <done>Profile setup uses Smart Generation for both modes and the static guard catches any non-wrapper caller of the old ATS route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser -> `/api/profile/smart-generation` | Authenticated mutation with CV/job inputs and credit-sensitive generation side effects. |
| Smart Generation -> start lock logs | PII-bearing request data is reduced to hashes/status metadata before logging. |
| legacy caller -> `/api/profile/ats-enhancement` | Compatibility route must preserve existing trust checks while delegating implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260428-01-01 | Tampering | Smart Generation route | mitigate | Preserve trusted mutation validation and zod body validation before orchestration. |
| T-260428-01-02 | Repudiation | Start-lock logs | mitigate | Log ids, statuses, and hashes only; add raw-log privacy tests with sentinel values. |
| T-260428-01-03 | Information disclosure | Start-lock logging | mitigate | Never log raw CV/request body/target-job text; tests serialize logger args to prove absence. |
| T-260428-01-04 | Denial of service / billing abuse | Duplicate starts | mitigate | Acquire workflow-aware durable lock before session/pipeline creation for both workflows. |
</threat_model>

<verification>
Run the task-level commands above, then the plan-level gate:

```bash
npx vitest run src/app/api/profile/smart-generation/route.test.ts src/app/api/profile/ats-enhancement/route.test.ts src/lib/agent/job-targeting-start-lock.test.ts src/components/resume/user-data-page.test.tsx
npm run test:e2e -- --project=chromium tests/e2e/profile-setup.spec.ts
powershell -NoProfile -Command "$hits = @(rg -n --fixed-strings '/api/profile/ats-enhancement' src --glob '!src/app/api/profile/ats-enhancement/route.ts' --glob '!src/app/api/profile/ats-enhancement/route.test.ts' --glob '!**/*.test.ts' --glob '!**/*.test.tsx' --glob '!**/*.md' 2>$null); if ($hits.Count -gt 0) { $hits; exit 1 }"
```
</verification>

<success_criteria>
- `/api/profile/smart-generation` is the only active guided-generation implementation route.
- ATS and job-targeting starts are idempotent server-side through safe hashes.
- Raw resume/job text is absent from start-lock logs by automated test.
- The old ATS route exists only as a wrapper and no product caller posts to it.
</success_criteria>

<output>
After implementation, create `.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-01-SUMMARY.md`.
</output>

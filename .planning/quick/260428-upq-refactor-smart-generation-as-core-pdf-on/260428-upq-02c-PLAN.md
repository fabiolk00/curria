---
task_id: 260428-upq
plan_id: 260428-upq-02c
phase: 260428-upq
plan: "02c"
mode: quick-full
type: execute
title: True Chat Gate And Normalized Grep Proof
autonomous: true
wave: 2
depends_on:
  - 260428-upq-02a
  - 260428-upq-02b
  - 260428-upq-04b
files_modified:
  - src/app/(auth)/chat/page.test.tsx
  - src/app/api/agent/route.test.ts
  - src/app/api/session/[id]/messages/route.test.ts
  - src/app/api/session/[id]/ai-chat-snapshot/route.test.ts
locked_decisions:
  - id: U-04
    decision: "AI chat is no longer the primary product surface; keep chat entitlement only on true chat surfaces."
resolved_research_decisions:
  - id: R-01
    decision: "/chat remains a true Pro chat and deep-link surface, not the normal product entrypoint."
must_haves:
  truths:
    - "True chat surfaces remain gated for no-chat users."
    - "The allowed true chat surfaces are `/chat`, `/api/agent`, `/api/session/[id]/messages`, and `/api/session/[id]/ai-chat-snapshot`."
    - "Automated grep allows AI-chat entitlement only on true chat and billing helper files."
    - "The grep guard normalizes Windows backslashes before comparing paths."
  artifacts:
    - path: "src/app/(auth)/chat/page.test.tsx"
      provides: "proof that `/chat` remains a gated true chat surface"
    - path: "src/app/api/agent/route.test.ts"
      provides: "proof that `/api/agent` remains gated"
    - path: "src/app/api/session/[id]/messages/route.test.ts"
      provides: "proof that session messages remain gated"
    - path: "src/app/api/session/[id]/ai-chat-snapshot/route.test.ts"
      provides: "proof that chat snapshot remains gated"
  key_links:
    - from: "true chat tests"
      to: "getAiChatAccess"
      via: "explicit denial assertions for `/chat` and true chat APIs"
    - from: "normalized grep"
      to: "true chat source allowlist"
      via: "backslash-to-slash path normalization before StartsWith comparison"
---

<objective>
Prove true chat remains Pro-gated after non-chat surfaces are ungated, and enforce the entitlement boundary with normalized grep.

Purpose: implement the second half of U-04 and R-01 by preventing both accidental chat exposure and accidental reintroduction of chat gates on non-chat surfaces.
Output: denied-access tests for true chat surfaces plus a Windows-safe grep allowlist.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-RESEARCH.md
@src/lib/billing/ai-chat-access.ts
@src/lib/billing/ai-chat-access.server.ts
@src/app/(auth)/chat/page.tsx
@src/app/api/agent/route.ts
@src/lib/agent/request-orchestrator.ts
@src/app/api/session/[id]/messages/route.ts
@src/app/api/session/[id]/ai-chat-snapshot/route.ts

<decisions>
- Implement U-04 exactly: only true chat surfaces keep AI-chat entitlement.
- Implement R-01 exactly: `/chat` remains a true Pro chat and deep-link surface.
- Do not remove source gates from `/chat`, `/api/agent`, `/api/session/[id]/messages`, or `/api/session/[id]/ai-chat-snapshot`.
- This proof runs after Plan 02a, Plan 02b, and Plan 04b because those plans remove non-chat gates or touch an allowed true-chat source file.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove true chat surfaces stay gated and grep boundary is normalized</name>
  <files>src/app/(auth)/chat/page.test.tsx, src/app/api/agent/route.test.ts, src/app/api/session/[id]/messages/route.test.ts, src/app/api/session/[id]/ai-chat-snapshot/route.test.ts</files>
  <behavior>
    - `/chat` denies or upgrades no-chat users through the existing AI-chat access gate.
    - `/api/agent` denies no-chat users.
    - `/api/session/[id]/messages` denies no-chat users.
    - `/api/session/[id]/ai-chat-snapshot` denies no-chat users.
    - A normalized grep fails if `getAiChatAccess` or `AiChatAccessCard` appears outside the allowed true-chat and billing helper files.
  </behavior>
  <action>
    Add or update true-chat tests per U-04 and R-01. Mock denied AI-chat access and assert the expected denial or upgrade response for `/chat`, `/api/agent`, `/api/session/[id]/messages`, and `/api/session/[id]/ai-chat-snapshot`. Keep `getAiChatAccess` in the true chat route/API surfaces and billing helper modules only.

    Use the normalized grep command in verification. It must convert backslashes to slashes before allowlist comparison so the proof works on Windows and CI. The proof must also assert the four true chat source paths still contain a chat gate.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(auth)/chat/page.test.tsx" src/app/api/agent/route.test.ts "src/app/api/session/[id]/messages/route.test.ts" "src/app/api/session/[id]/ai-chat-snapshot/route.test.ts"</automated>
    <automated>powershell -NoProfile -Command "$allowed = @('src/app/(auth)/chat/page.tsx','src/lib/agent/request-orchestrator.ts','src/app/api/session/[id]/messages/route.ts','src/app/api/session/[id]/ai-chat-snapshot/route.ts','src/lib/billing/ai-chat-access.ts','src/lib/billing/ai-chat-access.server.ts'); $hits = @(rg -n 'getAiChatAccess|AiChatAccessCard' src/app src/components src/lib --glob '!**/*.test.ts' --glob '!**/*.test.tsx' 2>$null | ForEach-Object { $_ -replace '\\','/' }); $bad = $hits | Where-Object { $line = $_; -not ($allowed | Where-Object { $line.StartsWith($_ + ':') }) }; if ($bad) { $bad; exit 1 }; foreach ($path in $allowed[0..3]) { if (-not (Select-String -LiteralPath $path -Pattern 'getAiChatAccess|AiChatAccessCard' -Quiet)) { Write-Error ('Missing true chat gate: ' + $path); exit 1 } }"</automated>
  </verify>
  <done>Only true chat surfaces retain AI-chat entitlement, and tests plus normalized grep prove the boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| no-chat user -> true chat surfaces | User must still be blocked from Pro chat/deep-link/API surfaces. |
| codebase entitlement references -> release gate | Static allowlist prevents entitlement checks from returning to non-chat surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260428-02c-01 | Elevation of privilege | `/chat` and true chat APIs | mitigate | Keep `getAiChatAccess` gates and add denied-access tests for each true chat surface. |
| T-260428-02c-02 | Denial of service / product lockout | non-chat surfaces | mitigate | Normalized grep fails if chat entitlement appears outside the allowlist. |
</threat_model>

<verification>
Run the task-level commands above.
</verification>

<success_criteria>
- `/chat`, `/api/agent`, `/api/session/[id]/messages`, and `/api/session/[id]/ai-chat-snapshot` deny no-chat users.
- `getAiChatAccess` and `AiChatAccessCard` are absent outside true chat and billing helper allowlist files.
- The grep proof normalizes path separators before comparison.
</success_criteria>

<output>
After implementation, create `.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-02c-SUMMARY.md`.
</output>

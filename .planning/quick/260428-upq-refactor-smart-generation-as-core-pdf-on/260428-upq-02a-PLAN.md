---
task_id: 260428-upq
plan_id: 260428-upq-02a
phase: 260428-upq
plan: "02a"
mode: quick-full
type: execute
title: Profile Setup And App Layout Non-Chat Ungating
autonomous: true
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/profile-setup/page.tsx
  - src/app/(auth)/profile-setup/page.test.tsx
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/layout.test.tsx
locked_decisions:
  - id: U-04
    decision: "AI chat is no longer the primary product surface; keep chat entitlement only on true chat surfaces."
resolved_research_decisions:
  - id: R-01
    decision: "/chat remains a true Pro chat and deep-link surface, not the normal product entrypoint."
must_haves:
  truths:
    - "Profile setup renders for authenticated users without AI-chat entitlement."
    - "The authenticated app layout renders for no-chat users without loading chat entitlement."
    - "Profile setup still uses existing billing/credit data needed by guided generation."
  artifacts:
    - path: "src/app/(auth)/profile-setup/page.tsx"
      provides: "guided generation page without chat entitlement gate"
    - path: "src/app/(auth)/layout.tsx"
      provides: "authenticated layout without normal app-shell chat entitlement"
  key_links:
    - from: "src/app/(auth)/profile-setup/page.tsx"
      to: "src/lib/asaas/quota.ts or existing billing loader"
      via: "billing/credit data, not getAiChatAccess"
    - from: "src/app/(auth)/layout.tsx"
      to: "src/components/dashboard/sidebar.tsx"
      via: "layout props without AI-chat access"
---

<objective>
Remove AI-chat entitlement from profile setup and the authenticated app shell while preserving auth redirects and guided-generation billing/credit data.

Purpose: implement U-04 on the core guided-generation entry surface without weakening true chat gates handled by Plan 02c.
Output: profile setup and layout tests proving no-chat users can reach non-chat product surfaces.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-RESEARCH.md
@src/lib/billing/ai-chat-access.ts
@src/lib/billing/ai-chat-access.server.ts
@src/app/(auth)/profile-setup/page.tsx
@src/app/(auth)/layout.tsx

<decisions>
- Implement U-04 exactly: profile setup and normal app layout are not true chat surfaces.
- Implement R-01 exactly: this plan must not delete or weaken `/chat`; Plan 02c verifies true chat remains gated.
- Do not alter credit consumption, billing grants, or ownership rules.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove chat entitlement from profile setup</name>
  <files>src/app/(auth)/profile-setup/page.tsx, src/app/(auth)/profile-setup/page.test.tsx</files>
  <behavior>
    - Profile setup does not import or call `getAiChatAccess`.
    - Profile setup still has the billing/credit information it needs through existing non-chat billing data.
    - Tests fail if `getAiChatAccess` is called while rendering profile setup.
    - A non-Pro/no-chat authenticated user can reach profile setup.
  </behavior>
  <action>
    Remove `getAiChatAccess` from `src/app/(auth)/profile-setup/page.tsx` per U-04. Derive `activeRecurringPlan` from existing billing data instead, preferably `loadOptionalBillingInfo(appUser.id, 'profile_setup')`, not from the AI-chat access contract. Keep auth redirects, profile setup data loading, and guided-generation billing/credit behavior otherwise unchanged.

    Add or update profile setup tests so a denied/no-chat entitlement mock does not block rendering and so any `getAiChatAccess` call fails the test.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(auth)/profile-setup/page.test.tsx"</automated>
  </verify>
  <done>Profile setup no longer depends on AI-chat entitlement and remains available to authenticated no-chat users.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove chat entitlement from the authenticated layout</name>
  <files>src/app/(auth)/layout.tsx, src/app/(auth)/layout.test.tsx</files>
  <behavior>
    - Auth layout does not import or call `getAiChatAccess`.
    - Sidebar/layout props do not require AI-chat access as a normal app-shell concern.
    - Tests prove a no-chat authenticated user can render the authenticated layout.
  </behavior>
  <action>
    Remove `getAiChatAccess` from `src/app/(auth)/layout.tsx` per U-04 and stop passing chat access as a normal sidebar concern. Preserve authentication behavior and existing layout structure. If the layout was only loading chat access for sidebar upsells, remove that dependency instead of replacing it with another chat-specific entitlement call.

    Add or update layout tests so a no-chat user can render the authenticated shell and so any `getAiChatAccess` call fails the test.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(auth)/layout.test.tsx"</automated>
  </verify>
  <done>The authenticated app shell no longer loads AI-chat entitlement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated user -> profile setup | Authenticated users reach guided generation without Pro chat entitlement. |
| authenticated user -> app layout | The shell renders without exposing true chat features by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260428-02a-01 | Denial of service / product lockout | `profile-setup/page.tsx` | mitigate | Remove chat entitlement dependency and add no-chat render coverage. |
| T-260428-02a-02 | Elevation of privilege | app layout | accept | Layout ungating does not itself grant chat API access; Plan 02c verifies true chat gates. |
</threat_model>

<verification>
Run the task-level commands above, then:

```bash
npx vitest run "src/app/(auth)/profile-setup/page.test.tsx" "src/app/(auth)/layout.test.tsx"
```
</verification>

<success_criteria>
- Profile setup does not import or call `getAiChatAccess`.
- The authenticated layout does not import or call `getAiChatAccess`.
- Tests prove non-chat product surfaces render for no-chat authenticated users.
</success_criteria>

<output>
After implementation, create `.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-02a-SUMMARY.md`.
</output>

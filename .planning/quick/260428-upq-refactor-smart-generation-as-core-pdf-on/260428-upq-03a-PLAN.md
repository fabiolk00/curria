---
task_id: 260428-upq
plan_id: 260428-upq-03a
phase: 260428-upq
plan: "03a"
mode: quick-full
type: execute
title: Guided Generation Navigation
autonomous: true
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/dashboard/page.tsx
  - src/app/(auth)/dashboard/page.test.tsx
  - src/lib/routes/app.ts
  - src/lib/routes/app.test.ts
  - src/components/dashboard/sidebar.tsx
  - src/components/dashboard/sidebar.test.tsx
  - src/lib/dashboard/welcome-guide.ts
locked_decisions:
  - id: U-03
    decision: "Smart Generation is the canonical guided resume generation entrypoint for ATS and job targeting."
  - id: U-04
    decision: "AI chat is no longer the primary product surface; keep chat entitlement only on true chat surfaces."
resolved_research_decisions:
  - id: R-01
    decision: "/chat remains a true Pro chat and deep-link surface, but normal navigation must point to guided generation."
must_haves:
  truths:
    - "`/dashboard` without a session query redirects to `/profile-setup`."
    - "Legacy dashboard canonicalization points to profile setup, not chat."
    - "Sidebar primary action starts guided generation/profile setup, not a new chat."
    - "Welcome guide points users at profile setup and guided generation, not chat/session steps."
    - "`/chat/:id` compatibility remains for true chat deep links."
  artifacts:
    - path: "src/app/(auth)/dashboard/page.tsx"
      provides: "default authenticated dashboard redirect to guided generation"
    - path: "src/lib/routes/app.ts"
      provides: "canonical app route mapping"
    - path: "src/components/dashboard/sidebar.tsx"
      provides: "primary app CTA for guided generation"
    - path: "src/lib/dashboard/welcome-guide.ts"
      provides: "guided-generation welcome steps"
  key_links:
    - from: "src/app/(auth)/dashboard/page.tsx"
      to: "src/lib/routes/app.ts"
      via: "`PROFILE_SETUP_PATH` default route"
    - from: "src/components/dashboard/sidebar.tsx"
      to: "src/lib/routes/app.ts"
      via: "guided generation CTA link/push"
---

<objective>
Move default authenticated navigation to Smart Generation/profile setup while preserving true chat deep-link compatibility.

Purpose: implement U-03 and U-04 in dashboard routing, sidebar actions, and welcome guidance.
Output: dashboard, route, sidebar, and welcome tests proving guided-generation-first navigation.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-RESEARCH.md
@src/app/(auth)/dashboard/page.tsx
@src/lib/routes/app.ts
@src/components/dashboard/sidebar.tsx
@src/lib/dashboard/welcome-guide.ts

<decisions>
- Implement U-03: normal dashboard entry and primary CTA should lead to profile setup/guided generation.
- Implement U-04 without deleting `/chat`: remove chat from default navigation and primary CTA only.
- Implement R-01: preserve `/chat/:id` compatibility for true chat deep links.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Point dashboard defaults and route canonicalization at profile setup</name>
  <files>src/app/(auth)/dashboard/page.tsx, src/app/(auth)/dashboard/page.test.tsx, src/lib/routes/app.ts, src/lib/routes/app.test.ts</files>
  <behavior>
    - `/dashboard` without a `session` query redirects to `PROFILE_SETUP_PATH`.
    - `/dashboard?session=<id>` still routes to the appropriate resume comparison/preview path instead of losing the session.
    - `canonicalizeAppPath('/dashboard')` returns `/profile-setup`.
    - `/chat/:id` compatibility remains for true chat deep links.
  </behavior>
  <action>
    Update `src/app/(auth)/dashboard/page.tsx` per U-03 so default redirect uses `PROFILE_SETUP_PATH`. If a `session` query is present, preserve historical preview behavior by routing to `buildResumeComparisonPath(sessionId)` or the existing comparison path helper.

    Update `src/lib/routes/app.ts` so dashboard canonicalization returns `/profile-setup` while preserving `/chat/:id` compatibility for true chat deep links per R-01. Add `src/lib/routes/app.test.ts` if missing.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(auth)/dashboard/page.test.tsx" src/lib/routes/app.test.ts</automated>
  </verify>
  <done>Dashboard defaults and canonical app routes point to guided generation while true chat deep links remain compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace sidebar and welcome chat CTAs with guided generation</name>
  <files>src/components/dashboard/sidebar.tsx, src/components/dashboard/sidebar.test.tsx, src/lib/dashboard/welcome-guide.ts</files>
  <behavior>
    - Sidebar primary CTA links or pushes to `PROFILE_SETUP_PATH`.
    - Sidebar does not expose a Pro-gated "Nova conversa" primary button.
    - Sidebar keeps "Perfil" and "Curriculos" visible.
    - Welcome guide points users at profile setup and guided generation, not chat/session steps.
  </behavior>
  <action>
    Update `src/components/dashboard/sidebar.tsx` per U-03 and U-04: remove `AI_CHAT_UPGRADE_URL`, `NEW_CONVERSATION_EVENT`, and the Pro-gated "Nova conversa" primary button from the normal sidebar. Replace it with a non-gated "Novo curriculo" or "Gerar curriculo" action to `PROFILE_SETUP_PATH`. Keep "Perfil" and "Curriculos" visible. Remove or relabel sessions so it is not a primary chat nav item.

    Update `src/lib/dashboard/welcome-guide.ts` to remove chat/session steps from the default guide and point users at profile setup/guided generation.
  </action>
  <verify>
    <automated>npx vitest run src/components/dashboard/sidebar.test.tsx</automated>
    <automated>powershell -NoProfile -Command "$bad = @(rg -n 'Nova conversa|Chat com IA' src/components/dashboard/sidebar.tsx src/lib/dashboard/welcome-guide.ts 2>$null); if ($bad.Count -gt 0) { $bad; exit 1 }"</automated>
  </verify>
  <done>Primary app navigation and welcome guidance lead to guided generation, not chat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated nav -> app route | Redirects must not expose unauthenticated or incorrect session views. |
| normal navigation -> true chat deep links | Chat remains reachable by direct true-chat links but not primary navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260428-03a-01 | Information disclosure | dashboard session redirect | mitigate | Preserve session-specific route helper behavior instead of exposing arbitrary data. |
| T-260428-03a-02 | Denial of service | dashboard default route | accept | Redirect target is an authenticated product page; no sensitive mutation occurs. |
| T-260428-03a-03 | Spoofing / misrepresentation | sidebar/welcome copy | mitigate | Remove chat primary CTA language and add copy grep for navigation files. |
</threat_model>

<verification>
Run the task-level commands above, then:

```bash
npx vitest run "src/app/(auth)/dashboard/page.test.tsx" src/lib/routes/app.test.ts src/components/dashboard/sidebar.test.tsx
powershell -NoProfile -Command "$bad = @(rg -n 'Nova conversa|Chat com IA' src/components/dashboard/sidebar.tsx src/lib/dashboard/welcome-guide.ts 2>$null); if ($bad.Count -gt 0) { $bad; exit 1 }"
```
</verification>

<success_criteria>
- Default authenticated dashboard navigation points to guided generation, not chat.
- Sidebar and welcome guide use guided-generation CTAs.
- `/chat/:id` compatibility remains available for true chat deep links.
</success_criteria>

<output>
After implementation, create `.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-03a-SUMMARY.md`.
</output>

---
task_id: 260428-upq
plan_id: 260428-upq-03b
phase: 260428-upq
plan: "03b"
mode: quick-full
type: execute
title: Pricing And Landing PDF-Only Copy
autonomous: true
wave: 1
depends_on: []
files_modified:
  - src/lib/plans.ts
  - src/lib/pricing/plan-comparison.ts
  - src/components/landing/pricing-comparison-table.tsx
  - src/components/landing/pricing-comparison-table.test.tsx
  - src/components/landing/pricing-section.test.tsx
  - src/components/landing/o-que-e-ats-page.tsx
locked_decisions:
  - id: U-01
    decision: "PDF-only active product behavior: do not generate DOCX files and do not keep DOCX generation/import logic active."
  - id: U-03
    decision: "Smart Generation is the canonical guided resume generation entrypoint for ATS and job targeting."
  - id: U-04
    decision: "AI chat is no longer the primary product surface; keep chat entitlement only on true chat surfaces."
must_haves:
  truths:
    - "Plan feature lists advertise PDF output only."
    - "Pricing comparison has no `chatIA` property or `Chat com IA` row."
    - "Landing ATS copy does not tell users to upload or receive DOCX."
    - "Pricing and landing tests assert the absence of DOCX and chat-as-main-feature copy."
  artifacts:
    - path: "src/lib/plans.ts"
      provides: "plan feature source of truth with PDF-only copy"
    - path: "src/lib/pricing/plan-comparison.ts"
      provides: "pricing comparison model without chat row"
    - path: "src/components/landing/pricing-comparison-table.tsx"
      provides: "pricing table without chat feature row"
    - path: "src/components/landing/o-que-e-ats-page.tsx"
      provides: "ATS landing copy with PDF-only claims"
  key_links:
    - from: "src/lib/plans.ts"
      to: "pricing components"
      via: "plan feature source of truth"
    - from: "src/lib/pricing/plan-comparison.ts"
      to: "src/components/landing/pricing-comparison-table.tsx"
      via: "comparison rows without chatIA"
---

<objective>
Rewrite pricing and landing copy around PDF-only guided generation and remove chat as a main buying feature.

Purpose: implement U-01, U-03, and U-04 in user-facing product claims.
Output: pricing and landing source/tests that advertise PDF-only guided generation without DOCX or chat feature rows.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-RESEARCH.md
@src/lib/plans.ts
@src/lib/pricing/plan-comparison.ts
@src/components/landing/pricing-comparison-table.tsx
@src/components/landing/o-que-e-ats-page.tsx

<decisions>
- Implement U-01 in visible copy: product-facing copy must say PDF only and must not advertise DOCX.
- Implement U-03: pricing and landing copy should present guided Smart Generation as the core flow.
- Implement U-04: chat is not a normal pricing-table feature row or main buying claim.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove DOCX and chat feature claims from pricing sources</name>
  <files>src/lib/plans.ts, src/lib/pricing/plan-comparison.ts, src/components/landing/pricing-comparison-table.tsx, src/components/landing/pricing-comparison-table.test.tsx, src/components/landing/pricing-section.test.tsx</files>
  <behavior>
    - Plan feature lists mention PDF output only.
    - Pricing comparison no longer has a `chatIA` property.
    - Pricing comparison no longer renders a "Chat com IA" row.
    - Tests assert absence of "DOCX", "docx", and "Chat com IA" in pricing surfaces.
  </behavior>
  <action>
    Update pricing source of truth in `src/lib/plans.ts` per U-01 and U-04: remove DOCX claims and remove chat from main plan feature lists. Use PDF-only phrasing such as "arquivos PDF" or "curriculos em PDF" without introducing a new pricing concept. Update `src/lib/pricing/plan-comparison.ts` and `src/components/landing/pricing-comparison-table.tsx` to remove the `chatIA` comparison property and row entirely rather than marking it as a normal buying feature.

    Update pricing tests to assert the new PDF-only copy and absence of chat as a main plan feature.
  </action>
  <verify>
    <automated>npx vitest run src/components/landing/pricing-comparison-table.test.tsx src/components/landing/pricing-section.test.tsx</automated>
  </verify>
  <done>Pricing sources and tests advertise PDF-only guided generation without a chat feature row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make ATS landing copy PDF-only</name>
  <files>src/components/landing/o-que-e-ats-page.tsx, src/components/landing/pricing-comparison-table.test.tsx, src/components/landing/pricing-section.test.tsx</files>
  <behavior>
    - ATS landing copy no longer tells users to upload DOCX.
    - ATS landing copy no longer tells users they will receive DOCX.
    - Copy grep fails on "DOCX", "docx", and "Chat com IA" in pricing and landing surfaces.
  </behavior>
  <action>
    Update visible ATS landing copy in `src/components/landing/o-que-e-ats-page.tsx` per U-01 to say PDF only. Keep the page focused on ATS resume outcomes and guided generation, not chat. Extend existing landing/pricing tests where appropriate so DOCX and main chat-feature copy cannot return unnoticed.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$bad = @(rg -n 'Chat com IA|DOCX|docx' src/lib/plans.ts src/lib/pricing src/components/landing 2>$null); if ($bad.Count -gt 0) { $bad; exit 1 }"</automated>
  </verify>
  <done>Product-facing pricing and landing copy is PDF-only and does not sell chat as a main feature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| pricing/product copy -> user purchase expectations | Copy must not advertise removed DOCX behavior or primary chat access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260428-03b-01 | Spoofing / misrepresentation | pricing and landing copy | mitigate | Remove DOCX/chat main-feature claims and add grep/copy tests. |
</threat_model>

<verification>
Run the task-level commands above, then:

```bash
npx vitest run src/components/landing/pricing-comparison-table.test.tsx src/components/landing/pricing-section.test.tsx
powershell -NoProfile -Command "$bad = @(rg -n 'Chat com IA|DOCX|docx' src/lib/plans.ts src/lib/pricing src/components/landing 2>$null); if ($bad.Count -gt 0) { $bad; exit 1 }"
```
</verification>

<success_criteria>
- Pricing source of truth and comparison rows advertise PDF-only guided generation.
- Chat is removed as a main pricing-table feature.
- ATS landing copy has no DOCX claims.
</success_criteria>

<output>
After implementation, create `.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-03b-SUMMARY.md`.
</output>

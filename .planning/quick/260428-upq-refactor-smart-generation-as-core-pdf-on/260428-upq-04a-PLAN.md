---
task_id: 260428-upq
plan_id: 260428-upq-04a
phase: 260428-upq
plan: "04a"
mode: quick-full
type: execute
title: Generate File PDF-Only Artifact Renderer
autonomous: true
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/tools/generate-file.ts
  - src/lib/agent/tools/generate-file.test.ts
  - src/context/preview-panel-context.tsx
  - src/components/dashboard/preview-panel.test.tsx
locked_decisions:
  - id: U-01
    decision: "PDF-only active product behavior: do not generate DOCX files and do not keep DOCX generation/import logic active."
  - id: U-02
    decision: "Preserve nullable historical DB/type fields unless a safe migration exists; no destructive schema migration in this task."
resolved_research_decisions:
  - id: R-03
    decision: "Nullable DOCX persistence fields stay for historical compatibility; new active behavior is PDF-only with no migration."
must_haves:
  truths:
    - "`generate_file` can only render, upload, sign, and return PDF output."
    - "No `generate_file` code path calls DOCX rendering or DOCX upload helpers."
    - "Public output remains compatible with `docxUrl: null`."
    - "Preview context exposes active PDF preview/download behavior."
    - "Nullable historical DOCX fields are not removed."
  artifacts:
    - path: "src/lib/agent/tools/generate-file.ts"
      provides: "PDF-only artifact generation with compatibility `docxUrl: null` response"
    - path: "src/context/preview-panel-context.tsx"
      provides: "PDF-only active preview/download state"
  key_links:
    - from: "src/lib/agent/tools/generate-file.ts"
      to: "Supabase Storage"
      via: "PDF upload/signing only"
    - from: "src/context/preview-panel-context.tsx"
      to: "generatedOutput"
      via: "PDF path/URL active preview state"
---

<objective>
Make `generate_file` strictly PDF-only while preserving nullable DOCX compatibility in response/state shape.

Purpose: implement U-01 without violating U-02/R-03 historical data compatibility.
Output: PDF-only generation and preview tests with no active DOCX rendering path.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-RESEARCH.md
@src/lib/agent/tools/generate-file.ts
@src/context/preview-panel-context.tsx

<decisions>
- Implement U-01 exactly for generation: active artifact rendering must be PDF-only.
- Implement U-02 and R-03 exactly: do not remove Prisma fields, SQL columns, DB mappers, or compatibility DTO fields for historical DOCX data.
- Do not edit `prisma/schema.prisma` or migrations in this plan.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove DOCX rendering and upload from generate_file</name>
  <files>src/lib/agent/tools/generate-file.ts, src/lib/agent/tools/generate-file.test.ts</files>
  <behavior>
    - Successful `generate_file` creates/uploads/signs only a PDF.
    - No code path calls `generateDOCX`, `buildDocxDocument`, or a DOCX upload helper.
    - Public output shape remains compatible with `docxUrl: null`.
    - `generatedOutput` may retain nullable `docxPath` only for historical compatibility, but new writes pass `undefined` or null for DOCX.
  </behavior>
  <action>
    In `src/lib/agent/tools/generate-file.ts`, remove `docx` imports, `generateDOCX`, `buildDocxDocument`, DOCX helper functions, and `generateFileDeps.generateDOCX` per U-01. Keep compatibility response fields only as nullable values, with active calls returning `docxUrl: null` and no `docxPath` write. Simplify signed URL helpers so active behavior requires only `pdfPath`.

    Update tests so a successful generation proves no DOCX renderer/upload dependency exists and `docxUrl` is null. Do not remove historical DB/type fields per U-02/R-03.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agent/tools/generate-file.test.ts</automated>
  </verify>
  <done>`generate_file` is active PDF-only behavior with nullable DOCX compatibility retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep preview state focused on active PDF artifacts</name>
  <files>src/context/preview-panel-context.tsx, src/components/dashboard/preview-panel.test.tsx</files>
  <behavior>
    - Preview context/tests expose active PDF preview/download behavior.
    - Preview code does not require a DOCX URL for newly generated output.
    - Historical nullable DOCX fields remain tolerated where the existing type shape requires them.
  </behavior>
  <action>
    Update preview-panel context/tests only as needed to make active preview behavior PDF-only per U-01. Preserve nullable `docxPath`/`docxUrl` compatibility where existing state types require it, but do not present or require DOCX for new generated artifacts.
  </action>
  <verify>
    <automated>npx vitest run src/components/dashboard/preview-panel.test.tsx</automated>
  </verify>
  <done>Preview behavior uses PDF as the active artifact and tolerates nullable historical DOCX fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| agent tool -> storage | Generated artifact bytes are uploaded and signed for user access. |
| generated state -> preview UI | Artifact metadata drives client preview/download behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260428-04a-01 | Information disclosure | generated artifacts | mitigate | Generate/sign PDF only; preserve existing owner/file-access protections outside this plan. |
| T-260428-04a-02 | Denial of service | historical rows | mitigate | Keep nullable compatibility fields and avoid destructive migration. |
</threat_model>

<verification>
Run the task-level commands above, then:

```bash
npx vitest run src/lib/agent/tools/generate-file.test.ts src/components/dashboard/preview-panel.test.tsx
```
</verification>

<success_criteria>
- Active `generate_file` rendering/upload/signing is PDF-only.
- New generated output returns `docxUrl: null` without writing active DOCX paths.
- Preview tests cover active PDF behavior.
</success_criteria>

<output>
After implementation, create `.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-04a-SUMMARY.md`.
</output>

---
task_id: 260428-upq
plan_id: 260428-upq-04b
phase: 260428-upq
plan: "04b"
mode: quick-full
type: execute
title: PDF-Only Import Boundary
autonomous: true
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/tools/parse-file.ts
  - src/lib/agent/tools/parse-file.test.ts
  - src/lib/agent/tools/schemas.ts
  - src/lib/agent/tools/index.ts
  - src/lib/agent/tools/index.test.ts
  - src/lib/agent/request-orchestrator.ts
  - src/lib/agent/request-orchestrator.test.ts
  - src/lib/agent/agent-intents.ts
  - src/components/dashboard/chat-interface.tsx
  - src/components/dashboard/chat-interface.test.tsx
locked_decisions:
  - id: U-01
    decision: "PDF-only active product behavior: do not generate DOCX files and do not keep DOCX generation/import logic active."
  - id: U-04
    decision: "AI chat is no longer the primary product surface; keep chat entitlement only on true chat surfaces."
must_haves:
  truths:
    - "`parse_file` rejects DOCX MIME input with a PDF-only validation/tool failure."
    - "Tool schemas and descriptions do not advertise DOCX MIME types or DOCX filenames."
    - "Agent request upload allowlists accept PDF only."
    - "Chat upload UI accepts PDF only even though chat remains a true-chat surface."
    - "True-chat entitlement in `request-orchestrator.ts` remains intact for Plan 02c proof."
  artifacts:
    - path: "src/lib/agent/tools/parse-file.ts"
      provides: "PDF-only active file parsing behavior"
    - path: "src/lib/agent/tools/schemas.ts"
      provides: "tool schema without DOCX input advertising"
    - path: "src/lib/agent/request-orchestrator.ts"
      provides: "PDF-only upload allowlist with preserved chat entitlement"
    - path: "src/components/dashboard/chat-interface.tsx"
      provides: "PDF-only upload accept/checks"
  key_links:
    - from: "src/components/dashboard/chat-interface.tsx"
      to: "src/lib/agent/tools/parse-file.ts"
      via: "PDF-only upload accept/checks"
    - from: "src/lib/agent/request-orchestrator.ts"
      to: "src/lib/billing/ai-chat-access.server.ts"
      via: "preserved true-chat gate"
---

<objective>
Remove active DOCX import boundaries from parser, tool schemas, agent upload orchestration, and chat upload UI.

Purpose: implement U-01 while preserving U-04 true-chat entitlement in the request orchestrator.
Output: parser/tool/orchestrator/UI tests proving PDF-only import behavior and DOCX rejection.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-RESEARCH.md
@src/lib/agent/tools/parse-file.ts
@src/lib/agent/tools/schemas.ts
@src/lib/agent/tools/index.ts
@src/lib/agent/request-orchestrator.ts
@src/lib/agent/agent-intents.ts
@src/components/dashboard/chat-interface.tsx

<decisions>
- Implement U-01 exactly for active import behavior: PDF is the only accepted upload/import format.
- Preserve U-04 exactly: if `request-orchestrator.ts` contains the true-chat entitlement gate, keep it in place while changing upload allowlists.
- Do not remove nullable historical DB/type fields; Plan 04c verifies compatibility fields remain.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make parse_file reject DOCX and accept PDF only</name>
  <files>src/lib/agent/tools/parse-file.ts, src/lib/agent/tools/parse-file.test.ts</files>
  <behavior>
    - `parse_file` rejects DOCX MIME input with a PDF-only validation/tool failure.
    - The dynamic `mammoth` import is gone.
    - Existing PDF parse behavior remains covered.
  </behavior>
  <action>
    In `src/lib/agent/tools/parse-file.ts`, remove DOCX parsing and the dynamic `mammoth` import per U-01. For DOCX MIME, return a validation/tool failure stating the active product accepts PDF only. Update tests to prove PDF success paths still work and DOCX input fails with the PDF-only message.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agent/tools/parse-file.test.ts</automated>
  </verify>
  <done>`parse_file` is PDF-only active import behavior and rejects DOCX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove DOCX from tool schemas, intents, and upload orchestration</name>
  <files>src/lib/agent/tools/schemas.ts, src/lib/agent/tools/index.ts, src/lib/agent/tools/index.test.ts, src/lib/agent/request-orchestrator.ts, src/lib/agent/request-orchestrator.test.ts, src/lib/agent/agent-intents.ts</files>
  <behavior>
    - Tool schemas/descriptions no longer list DOCX MIME types or DOCX filenames.
    - Agent request upload allowlists accept PDF only.
    - Request orchestrator still keeps true-chat entitlement gating for Plan 02c.
    - Tests prove DOCX upload/import is rejected at tool and orchestrator boundaries.
  </behavior>
  <action>
    Remove DOCX MIME and filename references from `src/lib/agent/tools/schemas.ts`, `src/lib/agent/tools/index.ts` descriptions/schema enum, `src/lib/agent/agent-intents.ts` if present, and `src/lib/agent/request-orchestrator.ts` upload allowlists per U-01. While editing `request-orchestrator.ts`, preserve the true-chat `getAiChatAccess` gate per U-04.

    Update tool/index and request-orchestrator tests so DOCX upload/import is rejected and PDF remains accepted.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agent/tools/index.test.ts src/lib/agent/request-orchestrator.test.ts</automated>
  </verify>
  <done>Tool advertising and upload orchestration are PDF-only while true-chat gating remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make chat upload UI PDF-only</name>
  <files>src/components/dashboard/chat-interface.tsx, src/components/dashboard/chat-interface.test.tsx</files>
  <behavior>
    - Chat upload UI accepts PDF only.
    - `.docx` is removed from file input accept attributes.
    - DOCX filename/type checks and DOCX copy are removed or replaced with PDF-only messaging.
    - Tests prove DOCX upload is rejected and PDF upload remains allowed.
  </behavior>
  <action>
    Update `src/components/dashboard/chat-interface.tsx` per U-01 to remove DOCX copy, DOCX filename/type checks, and `.docx` from file input accept attributes. Keep PDF upload behavior unchanged. Chat remains a true-chat surface; this task changes upload formats only.
  </action>
  <verify>
    <automated>npx vitest run src/components/dashboard/chat-interface.test.tsx</automated>
  </verify>
  <done>Chat upload UI accepts PDF only and rejects DOCX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| uploaded file -> parser/orchestrator | Untrusted user files cross into parsing and agent context. |
| chat UI -> upload boundary | Client file selection must align with server-side PDF-only validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260428-04b-01 | Tampering | upload/parser boundary | mitigate | Reject DOCX MIME/extensions and keep PDF validation tests. |
| T-260428-04b-02 | Elevation of privilege | request orchestrator | mitigate | Preserve true-chat entitlement gate while editing upload allowlists. |
</threat_model>

<verification>
Run the task-level commands above, then:

```bash
npx vitest run src/lib/agent/tools/parse-file.test.ts src/lib/agent/tools/index.test.ts src/lib/agent/request-orchestrator.test.ts src/components/dashboard/chat-interface.test.tsx
```
</verification>

<success_criteria>
- Active parser/tool/orchestrator/chat upload behavior accepts PDF only.
- DOCX upload/import is rejected at server and UI boundaries.
- `request-orchestrator.ts` keeps its true-chat entitlement gate.
</success_criteria>

<output>
After implementation, create `.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-04b-SUMMARY.md`.
</output>

---
task_id: 260428-upq
plan_id: 260428-upq-02b
phase: 260428-upq
plan: "02b"
mode: quick-full
type: execute
title: Session History API And File Access Ownership Coverage
autonomous: true
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/dashboard/sessions/page.tsx
  - src/app/(auth)/dashboard/sessions/page.test.tsx
  - src/app/api/session/route.ts
  - src/app/api/session/route.test.ts
  - src/app/api/session/[id]/route.test.ts
  - src/app/api/session/[id]/compare/route.test.ts
  - src/app/api/session/[id]/versions/route.test.ts
  - src/app/api/file/[sessionId]/route.test.ts
locked_decisions:
  - id: U-04
    decision: "AI chat is no longer the primary product surface; keep chat entitlement only on true chat surfaces."
resolved_research_decisions:
  - id: R-01
    decision: "/chat remains a true Pro chat and deep-link surface, not the normal product entrypoint."
must_haves:
  truths:
    - "Session history is visible to authenticated users without AI-chat entitlement."
    - "GET /api/session returns owned sessions to authenticated users without AI-chat entitlement."
    - "Preview/comparison/version routes depend on authentication and ownership, not chat entitlement."
    - "File access depends on authentication and ownership, not chat entitlement."
    - "POST /api/session remains blocked to prevent credit bypass."
  artifacts:
    - path: "src/app/(auth)/dashboard/sessions/page.tsx"
      provides: "history page without chat upsell gate"
    - path: "src/app/api/session/route.ts"
      provides: "owned session list without chat entitlement gate and blocked POST"
    - path: "src/app/api/file/[sessionId]/route.test.ts"
      provides: "file ownership regression coverage"
  key_links:
    - from: "src/app/api/session/route.ts"
      to: "src/lib/db/sessions"
      via: "getUserSessions(appUser.id) after auth"
    - from: "preview/comparison/version route tests"
      to: "session ownership checks"
      via: "auth-owned access without getAiChatAccess"
    - from: "src/app/api/file/[sessionId]/route.test.ts"
      to: "artifact file route"
      via: "owner allowed and non-owner denied assertions"
---

<objective>
Keep session history, preview/comparison/version data, and file downloads governed by authentication and ownership instead of AI-chat entitlement.

Purpose: implement U-04 on non-chat data access while preserving billing safety through the blocked session-creation endpoint.
Output: history/API/file tests proving no-chat owners retain access and non-owners remain denied.
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-RESEARCH.md
@src/lib/billing/ai-chat-access.ts
@src/lib/billing/ai-chat-access.server.ts
@src/app/(auth)/dashboard/sessions/page.tsx
@src/app/api/session/route.ts
@src/app/api/session/[id]/route.ts
@src/app/api/session/[id]/compare/route.ts
@src/app/api/session/[id]/versions/route.ts
@src/app/api/file/[sessionId]/route.ts

<decisions>
- Implement U-04 exactly: history, preview/comparison/version, and file access are non-chat surfaces.
- Keep R-01 intact: true chat remains gated and is verified in Plan 02c.
- Do not unblock `POST /api/session`; it remains blocked as credit-bypass protection.
- Preserve existing auth, ownership checks, structured logs, and error handling.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ungate session history page from AI-chat entitlement</name>
  <files>src/app/(auth)/dashboard/sessions/page.tsx, src/app/(auth)/dashboard/sessions/page.test.tsx</files>
  <behavior>
    - Dashboard session history is visible to authenticated users without AI-chat entitlement.
    - The page does not block with `AiChatAccessCard`.
    - Visible copy uses history/session language without chat upsell language.
  </behavior>
  <action>
    Update `src/app/(auth)/dashboard/sessions/page.tsx` per U-04 so it no longer blocks the page with `AiChatAccessCard`; this is history access, not a chat entitlement gate. If visible copy says "chat", change it to history/session language without upsell language. Add or update tests that mock denied AI-chat access and still render the history page.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(auth)/dashboard/sessions/page.test.tsx"</automated>
  </verify>
  <done>Session history is available to authenticated no-chat users without a chat upsell gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep session list API auth-owned and POST blocked</name>
  <files>src/app/api/session/route.ts, src/app/api/session/route.test.ts</files>
  <behavior>
    - GET `/api/session` returns owned sessions to authenticated users without AI-chat entitlement.
    - Unauthorized users still receive 401.
    - POST `/api/session` still returns the existing blocked response to prevent credit bypass.
    - Structured logs and catch/log behavior remain.
  </behavior>
  <action>
    Update `src/app/api/session/route.ts` GET per U-04 to require only authenticated app-user ownership before returning `db.getUserSessions(appUser.id)`. Remove AI-chat entitlement from GET only. Keep structured logs, unauthorized 401, catch/log behavior, and POST blocked exactly as a credit-bypass protection. Add tests for no-chat owner GET success, unauthenticated denial, and blocked POST.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/session/route.test.ts</automated>
  </verify>
  <done>Session listing is auth-owned without AI-chat entitlement, and POST remains blocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add preview, comparison, version, and file ownership coverage</name>
  <files>src/app/api/session/[id]/route.test.ts, src/app/api/session/[id]/compare/route.test.ts, src/app/api/session/[id]/versions/route.test.ts, src/app/api/file/[sessionId]/route.test.ts</files>
  <behavior>
    - `/api/session/[id]` remains auth/ownership-gated and not AI-chat-gated.
    - `/api/session/[id]/compare` remains auth/ownership-gated and not AI-chat-gated.
    - `/api/session/[id]/versions` remains auth/ownership-gated and not AI-chat-gated.
    - `/api/file/[sessionId]` allows authenticated owners without AI-chat entitlement and denies non-owners.
  </behavior>
  <action>
    Add preview/comparison/version route coverage proving `/api/session/[id]`, `/api/session/[id]/compare`, and `/api/session/[id]/versions` depend on auth/ownership rather than AI-chat entitlement per U-04. Add file-access route coverage proving file access depends on auth/ownership rather than AI-chat entitlement. Do not change preview/file-access source unless these tests expose an actual chat gate.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/session/[id]/route.test.ts" "src/app/api/session/[id]/compare/route.test.ts" "src/app/api/session/[id]/versions/route.test.ts" "src/app/api/file/[sessionId]/route.test.ts"</automated>
  </verify>
  <done>Preview/comparison/version/file access tests prove non-chat ownership coverage without AI-chat entitlement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated user -> session history/API | User reaches owned non-chat session data without Pro chat entitlement. |
| user -> file/session data | Ownership checks still protect stored sessions and artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260428-02b-01 | Information disclosure | `/api/file/[sessionId]` | mitigate | Preserve owner checks and add no-chat owner/non-owner regression coverage. |
| T-260428-02b-02 | Information disclosure | preview/comparison/version routes | mitigate | Add auth/ownership route tests proving no-chat owner access and non-owner denial. |
| T-260428-02b-03 | Tampering / billing bypass | `POST /api/session` | mitigate | Keep POST blocked and explicitly test it remains blocked. |
| T-260428-02b-04 | Denial of service / product lockout | history page and GET `/api/session` | mitigate | Remove chat entitlement calls and guard with no-chat access tests. |
</threat_model>

<verification>
Run the task-level commands above, then:

```bash
npx vitest run "src/app/(auth)/dashboard/sessions/page.test.tsx" src/app/api/session/route.test.ts "src/app/api/session/[id]/route.test.ts" "src/app/api/session/[id]/compare/route.test.ts" "src/app/api/session/[id]/versions/route.test.ts" "src/app/api/file/[sessionId]/route.test.ts"
```
</verification>

<success_criteria>
- Session history and GET `/api/session` do not depend on AI-chat entitlement.
- Preview/comparison/version and file access are protected by auth/ownership.
- `POST /api/session` remains blocked.
</success_criteria>

<output>
After implementation, create `.planning/quick/260428-upq-refactor-smart-generation-as-core-pdf-on/260428-upq-02b-SUMMARY.md`.
</output>

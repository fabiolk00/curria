# Quick Task 260415-v7b Plan

## Goal

Align ATS enhancement completion telemetry with the final validation state after smart recovery.

## Tasks

1. Update `src/lib/agent/ats-enhancement-pipeline.ts` so `agent.ats_enhancement.completed` reports the final issue count and preserves recovery context separately.
2. Extend `src/lib/agent/tools/pipeline.test.ts` to cover the recovered-success logging payload.
3. Run the focused ATS/job-targeting pipeline tests.

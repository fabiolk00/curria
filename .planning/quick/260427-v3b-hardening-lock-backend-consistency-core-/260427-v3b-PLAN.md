# Quick Task 260427-v3b: Hardening lock backend consistency, core display cleanup, override N+1 reduction, and override observability

## Plan

1. Keep the acquired job-targeting start lock backend consistent through running/completed/failed marking and cover Redis/memory cases with tests.
2. Add human-facing core requirement display signals for non-technical vacancies while preserving full debug unsupported signals.
3. Reduce override route persistence churn by batching/coalescing usage/session writes where locally safe and clarify override generation source in logs.
4. Ensure override review issues still render "Pontos para revisar" when no inline ranges are found.

## Verification

- Run focused Vitest suites for start lock, core requirement coverage, override route, review highlights, and comparison UI.
- Run `npm run typecheck`.
